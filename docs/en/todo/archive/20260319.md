<a href="../../ja/todo/archive/20260319.md"><img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square"></a>

> [!WARNING]
> This file is synchronized from `docs/ja/todo/archive/20260319.md` and still requires manual English translation.

> Source of truth: `docs/ja/todo/archive/20260319.md`

# 2026-03-19 完了タスク

## P2-COMPILE-LINK-PIPELINE-01: compile / link 2段パイプライン完成

- py2x.py の全パスを compile → link 経由に統一。`_use_linked_program_route` を常に True に。
- リンカーが DFS で type_id を確定し、`type_id_base_map_v1` をメタデータに追加。
- C++ emitter がリンカーメタデータから type_id を読み取り、`inline static constexpr pytra_type_id PYTRA_TYPE_ID = <値>;` を直接 emit。
- `PYTRA_TID_*` 定数を `py_runtime.h` から `py_scalar_types.h` へ移動。
- `py_register_class_type()` / `py_sync_generated_user_type_registry()` / `PYTRA_DECLARE_CLASS_TYPE` マクロを `py_runtime.h` から削除。
- `check_py2x_transpile --target cpp` 148/148 pass。

## P6-DEQUE-NATIVE-MAPPING-01: Deque をネイティブ型にマッピング

- Java: `deque[T]` → `java.util.ArrayDeque<T>` マッピング追加。
- Rust: `deque[T]` → `::std::collections::VecDeque<T>` マッピング追加。
- Kotlin: `deque[T]` → `ArrayDeque<T>` マッピング追加。
- C++ は既に `::std::deque<T>` マッピング済み。

## P7-SELFHOST-NATIVE-COMPILER-ELIM-01-S1: transpile_cli.cpp の .py シェルアウト除去

- `transpile_cli.cpp` から `.py` 入力時のホスト Python シェルアウトパスを除去。
- `.json` / `.east` 入力専用に統一。
- `_run_host_python_command` / `_temp_path` / `_shell_quote` 等のシェルアウト補助関数を削除。

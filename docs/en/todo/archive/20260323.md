<a href="../../ja/todo/archive/20260323.md"><img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square"></a>

> [!WARNING]
> This file is synchronized from `docs/ja/todo/archive/20260323.md` and still requires manual English translation.

> Source of truth: `docs/ja/todo/archive/20260323.md`

# 2026-03-23 完了タスク

## P0-28: main_guard_body の Expr Call 戻り値を Discard でラップ
- main_guard_body 内の Expr Call に `discard_result: true` フラグを付与。動作確認済み。

## P0-29: 未使用変数の検出
- 未使用変数検出パスを実装し、Assign target / VarDecl に `unused` フラグを付与。動作確認済み。

## P0-30: タプル代入の正規化（swap 検出）
- 2 要素 swap パターンを検出して Swap ノードに変換。Name swap + Subscript swap 動作確認済み。

## P0-31: for ループの TupleTarget 事前展開
- ForCore TupleTarget を事前展開する EAST3 パスを実装。TupleTarget → NameTarget + body 先頭に要素代入。動作確認済み。

## P2: ビルド出力ディレクトリ構造の標準化
- linker の出力を `east3/` + `manifest.json` に変更。emitter の出力を `emit/` に変更。`pytra-cli.py` を新構造に対応。全 backend の動作確認済み。

## P5: Swift sample parity
- `runtime_parity_check.py --targets swift` で sample/py の全 18 ケースが PASS。

## P6: Zig コンテナ型の Obj（rc）管理
- `py_runtime.zig` に `make_list` / `list_append` / `list_get` / `list_set` / `list_len` / `bytearray` を追加。
- emitter の `_zig_type` で `list[T]` / `bytearray` / `bytes` → `pytra.Obj` に変更。List リテラル / Subscript / `.append` / `len()` をランタイム API 呼び出しに変換。
- test/fixtures 28/30 OK（try/except 2 件は仕様通り）。
- sample/py/01_mandelbrot.py が PNG を正しく出力（Python 版とサイズ一致、CRC32 一致）。

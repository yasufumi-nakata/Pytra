<a href="../../ja/todo/archive/20260321b.md"><img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square"></a>

> [!WARNING]
> This file is synchronized from `docs/ja/todo/archive/20260321b.md` and still requires manual English translation.

> Source of truth: `docs/ja/todo/archive/20260321b.md`

# 2026-03-21 完了タスク（セッション B）

## P0-19: ブロックスコープ変数の hoist

- EAST3 lowering に `east2_to_east3_block_scope_hoist.py` を新設し、VarDecl ノード挿入による block-scope 変数 hoist を実装。
- if/else, for/while, nested blocks, multi-branch (if/elif/else) の全パターンをカバー。
- C++ emitter の既存 `_predeclare_if_join_names` を除去し、EAST3 hoist に一元化。
- Dart/Zig/Julia emitter に VarDecl ハンドラを追加。共通 emitter の fallback にも VarDecl 対応。
- 27 件のユニットテスト全通過。

## P0-20: ループ不変式 hoist 最適化パスの無効化

- `LoopInvariantCastHoistPass` と `LoopInvariantHoistLitePass` を optimizer pass 列から除去。
- ターゲット言語コンパイラが同等の最適化を行うため不要。`__hoisted_cast_N` のような合成変数名で可読性を損なうと判断。

## P0-11: PowerShell native emitter 実行 parity

- S1〜S6 全完了（以前のセッションで完了済み）。

## P0-12: pytra-cli.py が C++ 固有オプションを転送しない

- CLI テスト群を `toolchain/emit/cpp/cli.py` 直接呼び出しに変更（以前完了済み）。

## P0-13: テストが旧 src/backends/cpp/cli.py パスを参照

- テスト内の旧 cli.py パスを新パスに更新（以前完了済み）。

## P0-15: エミッター出力変化によるテストアサーション不一致

- テストアサーションを新エミッター出力に追従（以前完了済み）。

## P0-16: runtime include パスのテストアサーション不一致

- テストアサーションを新インクルードパスに追従（以前完了済み）。

## P0-17: emitter 旧 API 出力の一掃

- S1/S2: `object_new`/`PyListObj`/`obj_to_list_ref_or_raise`/`py_object_try_cast` を新 API に置換（以前完了済み）。
- S3: `py_list_at_ref`/`py_at` は runtime に現存する正当な API。置換不要。
- S4: `pytra::utils` namespace の include パスは既に正しく動作。修正不要。
- S5: テストアサーションを新 API に追従（以前完了済み）。

## P1: Julia native backend

- S1〜S4 全完了（以前のセッションで完了済み）。

## P2: EAST1 パーサーから noncpp_runtime_call を除去

- 4 ファイルから `noncpp_*` 関連コード（2 関数、伝播ロジック、import）を除去。
- `_resolve_attr_expr_owner_state` の forbidden ガードに `_SH_IMPORT_MODULES` スキップを追加。
- tuple 型注釈を 7→5 要素に縮小。

## P2: backend に link-output manifest 入力を必須化

- `loader.py` の raw EAST3 フォールバックを `RuntimeError` に置換。全 non-C++ backend に適用。

## P2: linker サブモジュール import 解決

- `program_loader.py` の `ImportFrom` 処理で `{module}.{name}` としてサブモジュール解決を試みるロジックを追加。
- `from pytra.utils import png` → `pytra.utils.png` → `utils/png.east` が解決されるようになった。

## P2: emitter 予約語回避

- `CodeEmitter` に `_reserved_names()` / `_safe_name()` を追加。
- Julia emitter に `_JULIA_RESERVED_BUILTINS` を追加。
- Dart/Zig/C++ emitter にも予約語リストを追加。
- ランタイム使用名（open, cos, sin 等）を予約語リストから除外し、呼び出し不整合を解消。

## P4: Dart native backend

- S1〜S4 全完了（以前のセッションで完了済み）。

## P7: Zig native backend

- S1〜S4 全完了（以前のセッションで完了済み）。

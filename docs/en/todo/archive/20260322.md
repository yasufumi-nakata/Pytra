<a href="../../ja/todo/archive/20260322.md"><img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square"></a>

> [!WARNING]
> This file is synchronized from `docs/ja/todo/archive/20260322.md` and still requires manual English translation.

> Source of truth: `docs/ja/todo/archive/20260322.md`

# 2026-03-22 完了タスク

## P0-21: EAST3 integer promotion
- C++ 準拠の integer promotion パスを実装。int8/uint8/int16/uint16 → int32 昇格。bytes イテレーション変数 → int32。float 型伝播追加。

## P0-23: 組み込み型メソッドの戻り値型推論
- dict.get→V, values→list[V], keys→list[K], items→list[tuple[K,V]], list.pop, str.join/find/startswith 等を追加。

## P0-24: Callable 型の追跡
- callable[[Args],Ret] 形式のパースを修正。fn(x) の resolved_type が戻り値型に正しく推論。

## P0-25: generator lowering
- yield → __yield_values.append() + return __yield_values に EAST3 レベルで変換。全 backend で generator が list accumulation として動作。

## P0-26: str サブスクリプト型推論
- str[i]→str, list[T][i]→T, dict[K,V][k]→V, bytes[i]→int32 を type propagation パスに追加。

## P0-27: __truediv__ lowering
- Path / "child" → Path.joinpath("child") に EAST3 レベルで変換。

## P1: integer promotion 代入先型縮小最適化
- narrowing パス実装。代入先型がオペランド幅以上なら promotion 先を代入先型に縮小。

## P2: emit_all_modules ルート相対パス
- meta.emit_context に module_id, root_rel_prefix, is_entry を設定。

## P2: immutable 引数リネーム共通化
- CodeEmitter に _collect_reassigned_params / _mutable_param_name 追加。Swift emitter 対応。スタンドアロン関数公開。

## P4: integer promotion オペランドキャスト
- オペランドの resolved_type を直接昇格。不要な Unbox を除去。

## P5: C# multi-module emit parity
- 全 18 sample PASS（以前完了済み）。

## P7: デフォルト引数展開
- EAST3 lowering + linker post-link pass で省略されたデフォルト引数を展開。keyword 引数を positional に統合。

## P7: モジュール定数型解決
- linker post-link pass で全モジュールの export テーブルを収集し、Attribute ノードの resolved_type を更新。math.pi→float64 等。

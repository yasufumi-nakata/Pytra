# TODO History (2026-03-14)

<a href="../../../ja/todo/archive/20260314.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-14 migration: relative wildcard import native backend rollout completed

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: relative wildcard import native backend rollout

Context: [docs/en/plans/archive/20260314-p0-relative-wildcard-import-native-rollout.md](../plans/archive/20260314-p0-relative-wildcard-import-native-rollout.md)

1. [x] [ID: P0-RELATIVE-WILDCARD-IMPORT-NATIVE-01] Aligned the representative `from .helper import *` / `from ..helper import *` lanes on non-C++ native backends on top of the existing import-resolution contract while keeping the single-file direct lane fail-closed.
2. [x] [ID: P0-RELATIVE-WILDCARD-IMPORT-NATIVE-01-S1-01] Locked the rollout order and representative backend bundles in the plan / TODO.
3. [x] [ID: P0-RELATIVE-WILDCARD-IMPORT-NATIVE-01-S1-02] Locked the current fail-closed backend inventory and evidence lanes with a contract / checker / unit test.
4. [x] [ID: P0-RELATIVE-WILDCARD-IMPORT-NATIVE-01-S2-01] Made the `go/nim/swift` native-path bundle green for representative relative wildcard imports.
5. [x] [ID: P0-RELATIVE-WILDCARD-IMPORT-NATIVE-01-S2-02] Made the `java/kotlin/scala` package-project bundle green for representative relative wildcard imports.
6. [x] [ID: P0-RELATIVE-WILDCARD-IMPORT-NATIVE-01-S2-03] Made the `lua/php/ruby` long-tail native-emitter bundle green for representative relative wildcard imports.
7. [x] [ID: P0-RELATIVE-WILDCARD-IMPORT-NATIVE-01-S3-01] Synchronized backend coverage / parity docs / TODO to the final state and closed the task.
- Progress memo: the end state locks native wildcard imports for `go/java/kotlin/lua/nim/php/ruby/scala/swift` at `transpile_smoke_locked + module_graph_bundle_transpile` on the representative module-graph bundle while keeping the single-file direct lane `backend_specific_fail_closed` on every backend.

## 2026-03-14 migration: `NotImplementedError` C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P1: stop lowering `NotImplementedError` to an undefined C++ symbol

Context: [docs/en/plans/archive/20260314-p1-nes3-not-implemented-error-cpp-support.md](../plans/archive/20260314-p1-nes3-not-implemented-error-cpp-support.md)

1. [x] [ID: P1-NES3-NOT-IMPLEMENTED-ERROR-CPP-01] Aligned the C++ lane so `NotImplementedError` no longer lowers to an undefined raw symbol and turned both the minimal repro and the shared-consumer compile lanes green.
2. [x] [ID: P1-NES3-NOT-IMPLEMENTED-ERROR-CPP-01-S1-01] Locked the current compile failure and desired C++ contract in focused regressions, the plan, and TODO.
3. [x] [ID: P1-NES3-NOT-IMPLEMENTED-ERROR-CPP-01-S2-01] Added the `NotImplementedError` lowering/runtime mapping and stopped emitting the raw undefined symbol.
4. [x] [ID: P1-NES3-NOT-IMPLEMENTED-ERROR-CPP-01-S3-01] Synced compile smoke and docs for the minimal repro plus shared impact lanes.
- Progress memo: the end state lowers `raise NotImplementedError(...)` through the `::std::runtime_error` surface, with the `not_implemented_error.py` single-file syntax-check, the `bus_port_pkg` multi-file compile check, and the selfhosted `bash ./pytra ... --target cpp --output-dir ...` lane all green.

## 2026-03-14 migration: `bytes` member truthiness C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P1: stop the raw `!bytes` residual for member-based `bytes` truthiness in the C++ lane

Context: [docs/en/plans/archive/20260314-p1-nes3-bytes-member-truthiness-cpp-support.md](../plans/archive/20260314-p1-nes3-bytes-member-truthiness-cpp-support.md)

1. [x] [ID: P1-NES3-BYTES-MEMBER-TRUTHINESS-CPP-01] Stopped raw `!bytes` emission for member-based `bytes` truthiness and turned the `cartridge_like.py` compile lane green.
2. [x] [ID: P1-NES3-BYTES-MEMBER-TRUTHINESS-CPP-01-S1-01] Locked the member/attribute-lane compile failure and residual scope in focused regressions, the plan, and TODO.
3. [x] [ID: P1-NES3-BYTES-MEMBER-TRUTHINESS-CPP-01-S2-01] Routed the `not <bytes>` fallback through truthiness normalization so the member/attribute lane also lowers through a `len`-based C++ condition.
4. [x] [ID: P1-NES3-BYTES-MEMBER-TRUTHINESS-CPP-01-S3-01] Synced the `cartridge_like.py` syntax-check, selfhost compile smoke, and docs wording to the representative contract.
- Progress memo: the end state lowers `if not self.chr_rom` through a truthiness condition that calls `py_len(this->chr_rom)` instead of raw `!(this->chr_rom)`, with the focused regression, `python3 src/py2x.py --target cpp --multi-file --output-dir ...`, and the selfhosted `bash ./pytra ... --target cpp --output-dir ...` lanes all green.

## 2026-03-14 migration: `default_factory=lambda: [0] * N` rc-list C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P1: align the `rc<list<T>>` lane for `field(default_factory=lambda: [0] * N)` in C++

Context: [docs/en/plans/archive/20260314-p1-nes3-list-default-factory-rc-list-cpp-support.md](../plans/archive/20260314-p1-nes3-list-default-factory-rc-list-cpp-support.md)

1. [x] [ID: P1-NES3-LIST-DEFAULT-FACTORY-RC-LIST-CPP-01] Aligned the `rc<list<T>>` lane for `field(default_factory=lambda: [0] * N)` and turned the `list_default_factory.py` compile lane green.
2. [x] [ID: P1-NES3-LIST-DEFAULT-FACTORY-RC-LIST-CPP-01-S1-01] Locked the current compile failure and representative-subset boundary in focused regressions, the plan, and TODO.
3. [x] [ID: P1-NES3-LIST-DEFAULT-FACTORY-RC-LIST-CPP-01-S2-01] Lowered the zero-capture list factory into an `rc_list_from_value(...)` constructor default and stopped the mismatch between value lists and `rc<list<T>>`.
4. [x] [ID: P1-NES3-LIST-DEFAULT-FACTORY-RC-LIST-CPP-01-S3-01] Synced the `list_default_factory.py` syntax-check, selfhost compile smoke, and docs wording to the current subset contract.
- Progress memo: the end state emits `BankState(rc<list<int64>> values = rc_list_from_value(py_repeat(...)))`, so the default_factory is shaped to the field type and the focused regression, `python3 src/py2x.py --target cpp --multi-file --output-dir ...`, and the selfhosted `bash ./pytra ... --target cpp --output-dir ...` lanes are all green.

## 2026-03-14 migration: `Path` alias package reuse C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P1: align `pytra.std.pathlib.Path` reuse through another module alias with the C++ multi-file contract

Context: [docs/en/plans/archive/20260314-p1-nes3-path-alias-pkg-cpp-support.md](../plans/archive/20260314-p1-nes3-path-alias-pkg-cpp-support.md)

1. [x] [ID: P1-NES3-PATH-ALIAS-PKG-CPP-01] Returned `from .compat import Path` to the type/constructor lane in the C++ multi-file path and turned the `path_alias_pkg` compile lane green.
2. [x] [ID: P1-NES3-PATH-ALIAS-PKG-CPP-01-S1-01] Locked the current compile failure and alias/type misclassification residual in focused regressions, the plan, and TODO.
3. [x] [ID: P1-NES3-PATH-ALIAS-PKG-CPP-01-S2-01] Made imported symbols recurse through reexported class aliases inside user modules, which stopped the invalid `pytra_mod_compat::Path(...)` emission.
4. [x] [ID: P1-NES3-PATH-ALIAS-PKG-CPP-01-S3-01] Synced the `path_alias_pkg` multi-file syntax-check, selfhost compile smoke, and docs wording to the current contract.
- Progress memo: the end state lowers the consumer-side constructor call through the `pytra::std::pathlib::Path(...)` surface, with the focused regression, `python3 src/py2x.py --target cpp --multi-file --output-dir ...`, and the selfhosted `bash ./pytra ... --target cpp --output-dir ...` multi-file compile lane all green.

## 2026-03-14 migration: imported class + module constant header contract C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P1: align C++ header ordering and reference lanes for imported classes that use module constants

Context: [docs/en/plans/archive/20260314-p1-nes3-apu-const-pkg-cpp-support.md](../plans/archive/20260314-p1-nes3-apu-const-pkg-cpp-support.md)

1. [x] [ID: P1-NES3-APU-CONST-PKG-CPP-01] Aligned header ordering plus the imported ref-class method lane for an imported class that references module constants, and turned the `apu_const_pkg` compile lane green.
2. [x] [ID: P1-NES3-APU-CONST-PKG-CPP-01-S1-01] Locked the current header-side compile failure and declaration-order residual in focused regressions, the plan, and TODO.
3. [x] [ID: P1-NES3-APU-CONST-PKG-CPP-01-S2-01] Made the header builder emit module-constant `extern` declarations after forward declarations but before class blocks, and restored `channel->...` by adding imported-class constructor call type inference.
4. [x] [ID: P1-NES3-APU-CONST-PKG-CPP-01-S3-01] Synced the `apu_const_pkg` multi-file syntax-check, selfhost compile smoke, and docs wording to the current contract.
- Progress memo: the end state keeps `extern LENGTH_TABLE / CPU_CLOCK_HZ / PULSE_GAIN` ahead of the class block in `apu.h`, and the consumer module now infers `rc<pytra_mod_apu::PulseChannel>` so it emits `channel->write_timer_high(...)` and `channel->sample()`.

## 2026-03-14 migration: imported bus-type multi-file contract C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P1: align header/symbol qualification and parameter passing for imported bus types with the C++ multi-file contract

Context: [docs/en/plans/archive/20260314-p1-nes3-bus-port-pkg-cpp-support.md](../plans/archive/20260314-p1-nes3-bus-port-pkg-cpp-support.md)

1. [x] [ID: P1-NES3-BUS-PORT-PKG-CPP-01] Aligned header/symbol qualification plus parameter passing for imported bus types and made `bus_port_pkg` compile.
2. [x] [ID: P1-NES3-BUS-PORT-PKG-CPP-01-S1-01] Locked the current compile failure and cross-module bus-type residual in focused regressions, the plan, and TODO.
3. [x] [ID: P1-NES3-BUS-PORT-PKG-CPP-01-S2-01] Fixed header visibility and symbol qualification for imported bus types to match the C++ multi-file contract.
4. [x] [ID: P1-NES3-BUS-PORT-PKG-CPP-01-S2-02] Aligned the base/derived bus passing lane with the representative ownership contract.
5. [x] [ID: P1-NES3-BUS-PORT-PKG-CPP-01-S3-01] Synced compile smoke and docs wording to the current contract with the shared residual dependency recorded.
- Progress memo: the end state is that `cpu.h`, `bus.h`, and `bus_port.h` all pass self-contained compile checks, imported `BusPort` uses `const rc<pytra_mod_bus_port::BusPort>&` in headers, call sites pass the handle directly, and the derived class keeps its qualified base plus `override`, so the `bus_port_pkg` multi-file compile lane is green.

## 2026-03-14 migration: deleted `src/runtime/cpp/core/**` compatibility-surface closeout complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P2: retire residual references to the deleted `src/runtime/cpp/core/**` compatibility surface

Context: [docs/en/plans/archive/20260314-p2-cpp-legacy-core-compat-retire.md](../plans/archive/20260314-p2-cpp-legacy-core-compat-retire.md)

1. [x] [ID: P2-CPP-LEGACY-CORE-COMPAT-RETIRE-01] Fully retired the deleted `src/runtime/cpp/core/**` compatibility surface from live-tree assumptions and reduced it to guard-only references.
2. [x] [ID: P2-CPP-LEGACY-CORE-COMPAT-RETIRE-01-S1-01] Inventoried the live-tree references to `src/runtime/cpp/core/**` and classified them as positive references versus guard-only references.
3. [x] [ID: P2-CPP-LEGACY-CORE-COMPAT-RETIRE-01-S2-01] Archived the stale-complete plan and cleaned up live docs that still described the old layout as canonical.
4. [x] [ID: P2-CPP-LEGACY-CORE-COMPAT-RETIRE-01-S2-02] Revalidated tooling/test mentions as guard-only wording and removed the misleading live positive references.
5. [x] [ID: P2-CPP-LEGACY-CORE-COMPAT-RETIRE-01-S3-01] Synchronized the checker, unit tests, and docs mirror with the current ownership contract and closed the task.
- Progress memo: the end state archives `p0-runtime-root-reset-cpp-parity.md`, and the remaining live-tree mentions of `src/runtime/cpp/core/**` are limited to guard-only wording in `check_runtime_cpp_layout.py`, `check_runtime_core_gen_markers.py`, the runtime specs, and negative-assertion tests.

## 2026-03-14 migration: multilang `@extern` runtime/emitter realignment complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P2: realign the `@extern` runtime/emitter contract across all languages

Context: [docs/en/plans/archive/20260314-p2-multilang-extern-runtime-realign.md](../plans/archive/20260314-p2-multilang-extern-runtime-realign.md)

1. [x] [ID: P2-MULTILANG-EXTERN-RUNTIME-REALIGN-01] Reworked the `@extern` runtime/emitter contract across all languages so it uses native owners plus generic extern metadata instead of generated rewrites.
2. [x] [ID: P2-MULTILANG-EXTERN-RUNTIME-REALIGN-01-S1-01] Inventoried every runtime-SoT `@extern` module plus the current generated rewrites, emitter hardcodes, and native owners across all targets.
3. [x] [ID: P2-MULTILANG-EXTERN-RUNTIME-REALIGN-01-S1-02] Locked the cross-target contract in spec / plan so `@extern` stays declaration-only, native owners provide implementations, and ambient externs remain separate.
4. [x] [ID: P2-MULTILANG-EXTERN-RUNTIME-REALIGN-01-S2-01] Removed module-specific extern rewrites from `tools/runtime_generation_manifest.json` and `tools/gen_runtime_from_manifest.py`, aligning generated lanes to declaration/wrapper-only output.
5. [x] [ID: P2-MULTILANG-EXTERN-RUNTIME-REALIGN-01-S2-02] Established canonical extern-backed owners under `src/runtime/<lang>/native/**` and synchronized the runtime symbol index plus layout contracts.
6. [x] [ID: P2-MULTILANG-EXTERN-RUNTIME-REALIGN-01-S2-03] Removed backend-emitter module-specific extern hardcodes such as `pytra.std.math` and routed them through generic extern/runtime metadata.
7. [x] [ID: P2-MULTILANG-EXTERN-RUNTIME-REALIGN-01-S3-01] Synchronized representative runtime artifacts, smoke tests, docs, and contract inventories to the current extern-ownership contract and closed the task.
- Progress memo: the end state strips direct host bindings out of generated wrappers for the tracked non-C++ `std/math,time,sys` lanes and forwards them into canonical `native/std/*_native` owners, locks `std/os,os_path,glob` plus `built_in/io_ops,scalar_ops` as accepted generated-compare residuals in the inventory/checker, and synchronizes representative smoke, docs, and rollout contracts to that current extern-ownership contract.

## 2026-03-14 migration: backend contract coverage 100% coverage-surface rollout complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P2: fix backend contract coverage at 100% with bundle-based coverage tracking

Context: [docs/en/plans/archive/20260314-p2-backend-contract-coverage-100.md](../plans/archive/20260314-p2-backend-contract-coverage-100.md)

1. [x] [ID: P2-BACKEND-CONTRACT-COVERAGE-100-01] Introduced the bundle-based coverage matrix and fixed `feature x lane x backend` contract coverage at 100% as a surface separate from the support matrix.
2. [x] [ID: P2-BACKEND-CONTRACT-COVERAGE-100-01-S1-01] Locked representative seeds plus live suite-family inventory as machine-readable inventory.
3. [x] [ID: P2-BACKEND-CONTRACT-COVERAGE-100-01-S1-02] Locked the role split between the support matrix, the coverage matrix, and the backend test matrix together with the definition of 100% contract coverage in docs/tooling contracts.
4. [x] [ID: P2-BACKEND-CONTRACT-COVERAGE-100-01-S2-01] Locked machine-readable seed ownership for `feature x required_lane x backend`.
5. [x] [ID: P2-BACKEND-CONTRACT-COVERAGE-100-01-S2-02] Locked live suite-family bundle attachments, unmapped candidates, and supporting-only exclusions.
6. [x] [ID: P2-BACKEND-CONTRACT-COVERAGE-100-01-S2-03] Classified unpublished multi-backend fixtures into promotion candidates versus coverage-only representatives.
7. [x] [ID: P2-BACKEND-CONTRACT-COVERAGE-100-01-S3-01] Added the live ja/en `backend-coverage-matrix.md` surface plus exporter/checker support so coverage-doc drift fails fast.
- Progress memo: the end state is that `backend-coverage-matrix.md` publishes bundle taxonomy, suite attachments, required-lane seed ownership, and unpublished fixture classification as the canonical coverage surface, is cross-linked from the support matrix and backend test matrix, and fails fast on doc drift through the exporter/checker pair.

# 2026-03-21 Completed Tasks

## P0-CPP-GENERATED-RUNTIME-PIPELINE-01: `.east` to C++ header generation pipeline

- Added C++ built_in and std targets to `runtime_generation_manifest.json`.
- Built the `.east` to `.h` generation pipeline and fixed `#include` lines under `src/runtime/cpp/generated/`.

## P0-UNION-ARG-ESCAPE-01/02/03: Escape detection for union-type arguments

- Implemented a temporary parser-side heuristic that forces classes appearing in union-type arguments to become `gc_managed`, ref.
- Verified that `Path` is emitted as `gc_managed` and can be stored in `object`.
- Implemented a mechanism that reflects escape-analysis results into `class_storage_hint`.

## P0-ESCAPE-TO-STORAGE-HINT-01: Reflect escape-analysis results into `class_storage_hint`

- Implemented `optimize_linked_program` logic that promotes `class_storage_hint` to `"ref"` for classes included in union-type parameters, S1.
- Removed the temporary heuristic from `core_module_parser.py`, S2.
- Verified that `Path` becomes `gc_managed` at the linker stage, S3.

## P0-LEGACY-API-CLEANUP-01: Remove the old object API and old compatibility mode

- Replaced emitter-generated `make_object` calls with `object(...)`, S1.
- Replaced emitter-generated `obj_to_rc_or_raise<T>` calls with `object::as<T>()`, S2.
- Removed the runtime compatibility shims for `make_object` and `obj_to_rc_or_raise`, S3.
- Removed the C++ list value and `pyobj` compatibility mode, `cpp_list_model`, and unified on ref mode, S4.
- Verified the pathlib repro with a g++ build, S5, resolved through P0-5 link integration.

## P0-RUNTIME-EAST-IN-LINK-PIPELINE-01: Integrate runtime `.east` into the link pipeline

- Automatically added runtime module `.east` files referenced in `resolved_dependencies_v1` into `LinkedProgram`, S1.
- Included runtime modules in link emit output through `write_cpp_rendered_program`, S2.
- Removed `_generate_runtime_east_headers`, standalone transpile, S3.
- Unified the old object API onto the new one, `unbox`, `as`, `is`, and `_cpp_str_lit`, S4.
- Verified the pathlib repro with a g++ build, S5.

## P0-OBJECT-IS-TAGGED-VALUE-01: Unify `object` as a tagged value

- Changed the `object` definition to `{pytra_type_id tag; rc<RcObject> _rc;}` and added implicit-conversion constructors plus `unbox`, `as`, and `is`, S1.
- Changed the emitter to output `object` for union types and removed `_Union_*` typedefs, S2.
- Changed emitter casts, `isinstance`, and implicit assignment to `object::unbox`, `object::as`, and `object::is`, S3.
- Verified `out/cpp/` g++ builds including `pathlib.py`, S4.
- Removed the compatibility layer and migrated fully to the new API, S5.

## P0-PY-RUNTIME-H-DECOMPOSITION-01: Decompose `py_runtime.h`

- Split out `core/str_methods.h`, S1.
- Split out `core/conversions.h`, S2.
- Split out `built_in/dict_ops.h`, S3.
- Split out `built_in/bounds.h`, S4.
- Split out `core/type_id_support.h` and resolved circular dependencies, S5.
- Split out `core/rc_ops.h`, S6.
- Rewrote `py_runtime.h` into an include-only facade, S7.
- Changed the emitter to output individual headers, with multi-file mode using `pytra_multi_prelude.h`, S8.

## P0-SELF-CONTAINED-CPP-OUTPUT-01: Self-contained `out/cpp/` build directory

- Changed include paths for `py_runtime.h` and native headers to namespace-relative paths, S1.
- Extended `write_cpp_rendered_program` so native runtime is copied into `out/cpp/{namespace}/`, S2.
- Changed emitter `#include` output paths to be `out/cpp/`-relative, S3.
- Updated Makefile generation so `out/cpp/` is fully self-contained, S4.
- Updated the `pytra-cli.py --build` flow for the new pipeline, S5.
- Verified that the minimal repro can be built with `make` inside `out/cpp/`, S6.

## P0-TAGGED-UNION-OBJECT-BOX-01: Unify tagged unions as `object + type_id`

- Changed tagged-union declarations to `using X = PyTaggedValue;` and added `PyBoxed`, `py_box`, `py_unbox`, and `PyTaggedValue` to the runtime, S1.
- Updated emitter casts, POD uses `py_unbox<T, TID>(v.value)`, classes use `static_cast`, S2.
- Emitted unbox on implicit assignment after `isinstance` narrowing and fixed inline-union matching, S3.
- Added implicit-conversion constructors to `PyTaggedValue`, S4.
- Verified `out/cpp/` g++ builds including `pathlib.py`, S5.
- Confirmed the rollout policy for other backends, S6.

## P0-LINKER-RESOLVED-INCLUDES-01: Resolve C++ include paths in the linker

- Implemented `_build_resolved_dependencies` in `global_optimizer.py`, S1.
- Updated `_collect_import_cpp_includes` in `module.py`, S2.
- Fixed `compiler_headers` in `runtime_symbol_index.json` to actual paths, S3.
- Verified that a minimal repro importing `Path` from `pytra.std.pathlib` builds with g++, S4.
- Removed `src/runtime/cpp/generated/` and the C++ target entries from the manifest, S5.

## P1-PIPELINE-STAGE-SEPARATION-01: Separate pipeline stages, compile, link, and emit

- Added `east2cpp.py` and implemented linked EAST JSON to C++ multi-file emit, S1.
- Connected `py2x.py --link-only` output to `east2cpp.py` input, S2.
- Changed `pytra-cli.py --build` to invoke `east2cpp.py` as a subprocess, S3.
- Split `build_selfhost.py --multi-module` into a three-stage pipeline, compile -> link -> emit, S4.

## P4-VARARG-EAST3-LOWERING-01: Support `*args` varargs by desugaring at EAST3 level

- Added a post-pass in `east2_to_east3_lowering.py` that converts `vararg_name/type` in `FunctionDef` into a regular `list[T]` parameter and packs call sites inside the module, S1.
- Added `_apply_vararg_callsite_packing_global` to `global_optimizer.py` so cross-module call sites are packed as well, S2.
- Added `joinpath(*parts: str | Path) -> Path` to `pytra/std/pathlib.py`, S3.

## P0-GPP-COMPILE-HEADERS-01: Fix g++ compile tests failing to find `built_in/string_ops.h`

- Generated 26 `.east` to `.h + .cpp` pairs in `tools/gen_runtime_cpp_from_east.py`.
- Added `-Isrc/runtime/generated` to `build_multi_cpp.py` and the related tests.

## P1-BACKEND-REGISTRY-DECOUPLING-01: Remove dependency on `backend_registry`

- Switched the C++ emit path in `pytra-cli.py` to the `east2cpp.py` subprocess and removed the `backend_registry` import, S1.
- Switched non-C++ emit paths to the `east2x.py` subprocess, S2.
- Removed `backend_registry_static` import from `pytra-cli.py`, S3.
- Reduced selfhost compile and link from 151 modules to 65 modules by removing 74 non-C++ backend modules, a 57 percent reduction, S4.

## P1-EMIT-DIRECT-CALL-01/02: Change each language's emit wrapper to directly call its own emitter

- Changed wrappers for 14 languages to call their own emitters directly, and each wrapper now imports only its own emitter, 01.
- Deleted `toolchain/emit/all.py`, 02.

## P1-CPP-STATICMETHOD-EMIT-01: Add `@staticmethod` support to the C++ emitter

- Changed `emit_function` to detect `@staticmethod` and `@classmethod` decorators and emit `static`.

## P1-CPP-PATH-RC-TYPE-01: Fix C++ emitter issue where `Path` was declared as a bare type instead of `rc<Path>`

- Changed `class_storage_hint` in `pathlib.east` from `"value"` to `"ref"`.

## P7-SELFHOST-NATIVE-COMPILER-ELIM-01: Completely remove `native/compiler/`

- Unified the selfhost build pipeline around EAST3 JSON input only and removed the `.py` shell-out path from `transpile_cli.cpp`, S1.
- Removed shell-out from `emit_source_typed` and replaced it with direct calls into the generated C++ emitter, `transpile_to_cpp`, S2.
- Deleted `src/runtime/cpp/compiler/`. Confirmed zero shell-outs remain, and removed references to deleted files from the contract inventory, S3.

## P7-SELFHOST-MULTIMOD-TRANSPILE-01: Build the selfhost multi-module transpile foundation

- Audited emitter modules for selfhost constraints and found 4 dynamic-dispatch blockers, S1.
- Implemented `relative_to` and `with_suffix` on `pytra.std.pathlib.Path`, S1a.
- Implemented `compile` and `Pattern` in `pytra.std.re`, S1b.
- Migrated `import os` in `multifile_writer.py` through `pytra.std`, S1c.
- Replaced dynamic mixin injection in `CppEmitter`, `setattr` and `__dict__` inside `_attach_cpp_emitter_helper_methods`, with EAST3 mixin expansion through multiple inheritance, and removed `globals()` injection in `install_py2cpp_runtime_symbols`, S1d.
- Extended `tools/build_selfhost.py` to a multi-module transpile pipeline, compile -> link, and compiled EAST3 successfully for all 150 modules, S2.
- Completed C++ emit for 66 selfhost modules and generated 233 C++ files, 4.1 MB, S3.
- Implemented wildcard re-export propagation in the linker and succeeded in linking 151 modules, S4.

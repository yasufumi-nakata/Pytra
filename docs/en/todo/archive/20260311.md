# TODO History (2026-03-11)

<a href="../../../ja/todo/archive/20260311.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-11 Archive Transfer: Completed Section (20 items added)

The following items were moved from `docs/ja/todo/index.md` into `todo/archive`.

### P1: Structure import-graph issue carriers

Context: [docs/en/plans/archive/20260311-p1-import-graph-issue-structure.md](../plans/archive/20260311-p1-import-graph-issue-structure.md)

1. [x] [ID: P1-IMPORT-GRAPH-ISSUE-STRUCTURE-01] Move the import-graph `"<file>: <module>"` carrier onto a helper-structured seam.
2. [x] [ID: P1-IMPORT-GRAPH-ISSUE-STRUCTURE-01-S1-01] Inventory the current import-graph issue carriers and lock the staged end state in the plan/TODO.
3. [x] [ID: P1-IMPORT-GRAPH-ISSUE-STRUCTURE-01-S2-01] Add shared helpers plus focused unit tests for graph issue entries, and route both `split_graph_issue_entry()` and producer-side formatting through them.
4. [x] [ID: P1-IMPORT-GRAPH-ISSUE-STRUCTURE-01-S2-02] Align `validate_import_graph_or_raise()`, `format_import_graph_report()`, and the `east1_build` mirror to the helper-structured seam.
5. [x] [ID: P1-IMPORT-GRAPH-ISSUE-STRUCTURE-01-S3-01] Refresh representative CLI/report regressions, docs, and archive state, then close the task.
- Progress memo: import-graph issues now use `missing_module_entries` / `relative_import_entries` as the canonical seam, with legacy text lists retained only as compatibility exports.

## 2026-03-11 Tracking Reports

- Operational selfhost readiness and known-block guidance is now collected in the [P4 Selfhost Parity Readiness Report](../plans/p4-selfhost-parity-readiness-report.md).
- The same-day snapshots were updated in [P1-MQ-04 Stage1 Status](../plans/p1-multilang-selfhost-status.md) and [P1-MQ-05 Multistage Selfhost Status](../plans/p1-multilang-selfhost-multistage-status.md).
- As of 2026-03-11, the representative C++ lane is tracked as `stage2_diff=pass` / `direct_e2e=pass`, while all 8 multilang lanes are currently tracked as `stage1_transpile_fail`.

### P1: Align the current relative-import contract across entrypoints, docs, and smoke

Context: [docs/en/plans/archive/20260311-p1-relative-import-closeout.md](../plans/archive/20260311-p1-relative-import-closeout.md)

1. [x] [ID: P1-RELATIVE-IMPORT-CLOSEOUT-01] Align the current relative-import contract across entrypoints, docs, and smoke.
2. [x] [ID: P1-RELATIVE-IMPORT-CLOSEOUT-01-S1-01] Fix the current relative-import contract and stale surfaces in the plan / TODO.
3. [x] [ID: P1-RELATIVE-IMPORT-CLOSEOUT-01-S2-01] Add representative relative-import regressions for the `py2x.py` entrypoint.
4. [x] [ID: P1-RELATIVE-IMPORT-CLOSEOUT-01-S2-02] Sync the C++ support matrix rows for relative and wildcard import to the current contract.
5. [x] [ID: P1-RELATIVE-IMPORT-CLOSEOUT-01-S3-01] Re-run targeted regressions and close the task with docs / archive updates.
- Progress memo: Relative import itself was already implemented, so this follow-up closed the `py2x.py --target cpp` representative regression gap and removed the stale “unsupported” support-matrix wording before archiving.

### P1: Structure import diagnostics

Context: [docs/en/plans/archive/20260311-p1-import-diagnostic-structure.md](../plans/archive/20260311-p1-import-diagnostic-structure.md)

1. [x] [ID: P1-IMPORT-DIAGNOSTIC-STRUCTURE-01] Move brittle import-diagnostic string matching to helper / structured envelopes.
2. [x] [ID: P1-IMPORT-DIAGNOSTIC-STRUCTURE-01-S1-01] Inventory the current import-diagnostic transport and lock the staged end state in the plan/TODO.
3. [x] [ID: P1-IMPORT-DIAGNOSTIC-STRUCTURE-01-S2-01] Centralize import exception classification in `transpile_cli.load_east_document()` behind one helper and add focused unit tests.
4. [x] [ID: P1-IMPORT-DIAGNOSTIC-STRUCTURE-01-S2-02] Add a seam so duplicate import bindings can be transported from parser/import semantics as a structured envelope.
5. [x] [ID: P1-IMPORT-DIAGNOSTIC-STRUCTURE-01-S3-01] Clean up the remaining ad hoc string dependencies in import diagnostics and reach an archivable end state.
- Progress memo: `duplicate/relative/wildcard` import diagnostics now share structured-first decode and a common detail helper, while the remaining substring dependency is isolated to `_legacy_import_user_error_payload()` as a compatibility fallback.

### P0: relative import support

Context: [docs/en/plans/archive/20260311-p0-relative-import-support.md](../plans/archive/20260311-p0-relative-import-support.md)

1. [x] [ID: P0-RELATIVE-IMPORT-SUPPORT-01] Officially support `from .m import x` / `from ..pkg import y` in multi-file transpilation.
2. [x] [ID: P0-RELATIVE-IMPORT-SUPPORT-01-S1-01] Fix the syntax / diagnostics / root-escape policy for relative imports in plan and spec.
3. [x] [ID: P0-RELATIVE-IMPORT-SUPPORT-01-S2-01] Make the self-hosted parser accept relative `from-import` while preserving raw module text and `ImportFrom.level`.
4. [x] [ID: P0-RELATIVE-IMPORT-SUPPORT-01-S2-02] Normalize relative modules to absolute `module_id`s during frontend module-map construction and rewrite EAST / import metadata.
5. [x] [ID: P0-RELATIVE-IMPORT-SUPPORT-01-S2-03] Update import-graph diagnostics to distinguish root escape from missing modules and fail closed.
6. [x] [ID: P0-RELATIVE-IMPORT-SUPPORT-01-S3-01] Add representative CLI / unit regressions for success, missing, duplicate, root escape, and wildcard cases.
7. [x] [ID: P0-RELATIVE-IMPORT-SUPPORT-01-S3-02] Sync the import wording in `spec-user.md`, `spec-import.md`, and tutorial docs.
- Progress memo: the frontend import graph and module map now normalize relative imports to absolute `module_id`s, and the representative CLI plus metadata regressions for sibling success, wildcard, duplicate, missing, and root escape were locked before archiving.

### P4: Classify final thin-compat dependencies across the C++/Rust/C# emitters

Context: [docs/en/plans/archive/20260311-p4-crossruntime-pyruntime-thincompat-followup.md](../plans/archive/20260311-p4-crossruntime-pyruntime-thincompat-followup.md)

1. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-THINCOMPAT-01] Prepare further `py_runtime.h` shrink by classifying the C++/Rust/C# emitter-side blockers and shared API residuals around the final two thin compatibility helpers.
2. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-THINCOMPAT-01-S1-01] Inventory the final thin-compat blockers into `cpp_header_thincompat_blocker` and `crossruntime_shared_type_id_api`, then add tooling/tests.
3. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-THINCOMPAT-01-S1-02] Lock the end state and bundle-sized removal order in docs/source guards.
4. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-THINCOMPAT-01-S2-01] Move the C++ emitter `py_isinstance` blocker lanes onto explicit helpers and shrink `cpp_header_thincompat_blocker`.
5. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-THINCOMPAT-01-S2-02] Align Rust/C# shared `type_id` API residuals to the intended naming/bridge end state and document the remaining non-blockers.
6. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-THINCOMPAT-01-S3-01] Refresh representative smoke/tests/docs and archive the task.
- Progress memo: The C++ blocker bucket is now empty, and the Rust/C# shared `type_id` helper names are locked to `py_runtime_value_type_id` / `py_runtime_value_isinstance` / `py_runtime_type_id_is_subtype` / `py_runtime_type_id_issubclass`, with representative smoke plus inventory/tooling revalidated before archiving.

### P4: Remove the final `py_runtime.h` thin compat helpers across runtimes

Context: [docs/en/plans/archive/20260311-p4-crossruntime-pyruntime-final-thincompat-removal.md](../plans/archive/20260311-p4-crossruntime-pyruntime-final-thincompat-removal.md)

1. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-FINAL-THINCOMPAT-REMOVAL-01] Remove the final two `py_runtime.h` thin compat helpers across runtimes.
2. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-FINAL-THINCOMPAT-REMOVAL-01-S1-01] Classify the residuals into `cpp_header_final_thincompat_defs` / `cpp_generated_final_thincompat_blocker` / `rs_runtime_generic_alias_surface` / `cs_runtime_generic_alias_surface`, and add inventory/tests.
3. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-FINAL-THINCOMPAT-REMOVAL-01-S1-02] Fix the target end state and bundle order in docs/source guards.
4. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-FINAL-THINCOMPAT-REMOVAL-01-S2-01] Move checked-in generated/native C++ callers to thin helpers and empty `cpp_generated_final_thincompat_blocker`.
5. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-FINAL-THINCOMPAT-REMOVAL-01-S2-02] Shrink the Rust/C# runtime alias surface to internal/private seams.
6. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-FINAL-THINCOMPAT-REMOVAL-01-S3-01] Remove template `py_runtime_type_id` / `py_isinstance` from `py_runtime.h` and refresh representative regressions/docs/archive.
- Progress memo: The final two thin-compat templates were removed from the header, and the representative C++ runtime/codegen/header-surface regressions were moved onto the thin helper surface (`py_runtime_object_isinstance`) before archiving.

### P4: Align residual `py_runtime` contracts across the C++/Rust/C# emitters

Context: [docs/en/plans/archive/20260311-p4-crossruntime-pyruntime-emitter-align.md](../plans/archive/20260311-p4-crossruntime-pyruntime-emitter-align.md)

1. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-ALIGN-01] Align the residual `py_runtime` contract across the C++/Rust/C# emitters and lock the boundary between `object bridge residual` and the shared `type_id` contract.
2. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-ALIGN-01-S1-01] Bucket residual `py_runtime` symbols across the C++/Rust/C# emitters and add an inventory drift guard.
3. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-ALIGN-01-S1-02] Lock the end state for `object bridge residual`, `shared type_id contract`, and `cross-runtime bridge residual` in docs.
4. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-ALIGN-01-S2-01] Clean up representative C++ object-bridge mutation-helper residuals.
5. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-ALIGN-01-S2-02] Align Rust/C# `type_id` and type-predicate lowering to the shared contract.
6. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-ALIGN-01-S3-01] Refresh representative smoke tests, docs, and archive the task.
- Progress memo: The cross-runtime emitter inventory and representative C++/Rust/C# smoke now lock helper names for the shared `type_id` contract and keep object-bridge residuals isolated across docs, tooling, and archive.

### P4: emitter-side `py_runtime.h` shrink follow-up

Context: [docs/en/plans/archive/20260311-p4-crossruntime-pyruntime-emitter-shrink-followup.md](../plans/archive/20260311-p4-crossruntime-pyruntime-emitter-shrink-followup.md)

1. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-SHRINK-01] Align the remaining emitter-side helper dependencies across C++/Rust/C# so `py_runtime.h` can shrink further.
2. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-SHRINK-01-S1-01] Bucket residual emitter-side `py_runtime` helper usage across C++/Rust/C# and add inventory/tests.
3. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-SHRINK-01-S1-02] Fix the end state and removal order for mutation / `type_id` / object-bridge helpers in docs/source guards.
4. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-SHRINK-01-S2-01] Move remaining C++ emitter helper dependencies onto thin/object-bridge seams.
5. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-SHRINK-01-S2-02] Align remaining Rust emitter helper dependencies to the shared contract.
6. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-SHRINK-01-S2-03] Align remaining C# emitter helper dependencies to the shared contract.
7. [x] [ID: P4-CROSSRUNTIME-PYRUNTIME-EMITTER-SHRINK-01-S3-01] Refresh representative smoke/docs/archive and close the follow-up.
- Progress memo: The residual-helper inventory and representative C++/Rust/C# smoke now leave only the C# `bytearray` lane as a mutation residual while fixing the C++ object-bridge fallback and Rust/C# shared `type_id` thin-helper naming before archiving.

### P0: Shrink the remaining `py_runtime.h` surface around object-bridge / shared `type_id` contracts

Context: [docs/en/plans/archive/20260311-p0-cpp-pyruntime-contract-shrink.md](../plans/archive/20260311-p0-cpp-pyruntime-contract-shrink.md)

1. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01] Shrink the remaining `py_runtime.h` surface from the perspective of `object bridge` and shared `type_id` contracts.
2. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01-S1-01] Inventory remaining mutation-helper / `type_id` callers into `typed_lane_removable`, `object_bridge_required`, and `shared_runtime_contract`, and lock why each remains.
3. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01-S1-02] Lock the target end state for `py_runtime.h` and reflect the shrink order for mutation helpers and `type_id` in docs / source guards.
4. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01-S2-01] Push the remaining typed-lane `py_append/extend/pop/clear/reverse/sort/set_at` dependencies upstream out of the C++ emitter.
5. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01-S2-02] Narrow mutation helpers in `py_runtime.h` down to object-bridge / compatibility overloads only, and lock residual callers with labels.
6. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01-S3-01] Reassign shared ownership for `py_runtime_type_id/py_isinstance/py_is_subtype` and retarget native compiler wrappers / generated `type_id` built-ins to thin helper seams.
7. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01-S3-02] Align Rust/C#/C++ residual `type_id` callers to the shared contract and catch unclassified reintroduction with smoke / contract tests.
8. [x] [ID: P0-CPP-PYRUNTIME-CONTRACT-SHRINK-01-S4-01] Update labels, source guards, and docs for the remaining `py_runtime.h` surface and leave the task ready for archive.
- Progress memo: Typed-lane mutation wrappers were pushed upstream, the shared `type_id` contract and remaining labels were locked by inventory / smoke / source guards, and the task was archived.

### P1: Shrink the remaining `py_runtime.h` surface for real

Context: [docs/en/plans/archive/20260311-p1-cpp-pyruntime-header-shrink.md](../plans/archive/20260311-p1-cpp-pyruntime-header-shrink.md)

1. [x] [ID: P1-CPP-PYRUNTIME-HEADER-SHRINK-01] Reclassified the remaining `py_runtime.h` helpers by `object bridge mutation` / `typed collection compatibility` / `shared type_id compatibility` and reduced the header to a thin compatibility seam.
2. [x] [ID: P1-CPP-PYRUNTIME-HEADER-SHRINK-01-S1-01] Inventoried the remaining helpers into `object_bridge_mutation` / `typed_collection_compat` / `shared_type_id_compat` and added tooling.
3. [x] [ID: P1-CPP-PYRUNTIME-HEADER-SHRINK-01-S1-02] Locked the target end state and bundle-sized removal order in docs/source guards.
4. [x] [ID: P1-CPP-PYRUNTIME-HEADER-SHRINK-01-S2-01] Removed the remaining typed-collection list/dict wrappers bundle by bundle until the bucket became empty.
5. [x] [ID: P1-CPP-PYRUNTIME-HEADER-SHRINK-01-S2-02] Narrowed `shared_type_id_compat` down to just template `py_runtime_type_id` and `py_isinstance`.
6. [x] [ID: P1-CPP-PYRUNTIME-HEADER-SHRINK-01-S3-01] Refreshed representative runtime tests, docs, and archive state, then closed the task.
- Progress memo: `typed_collection_compat` is now empty, and only object-bridge mutation helpers plus the final two thin `shared_type_id_compat` helpers remain in the header. Any further reduction now depends on C++/Rust/C#/generated follow-up work.

### P1: Split the remaining `east2_to_east3_lowering.py` clusters in a second wave

Context: [docs/en/plans/archive/20260311-p1-east23-lowering-orchestration-split.md](../plans/archive/20260311-p1-east23-lowering-orchestration-split.md)

1. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01] Move the remaining `east2_to_east3_lowering.py` clusters into dedicated modules and reduce the main file to orchestration / dispatch logic.
2. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01-S1-01] Inventory the remaining clusters as `call_metadata`, `stmt_lowering`, and `dispatch_orchestration`, then lock the split order.
3. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01-S1-02] Keep progress notes at bundle level and fix the main-file end state as `dispatch + lifecycle`.
4. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01-S2-01] Split the `call metadata` / `json decode fastpath` cluster into a dedicated module.
5. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01-S2-02] Split the `Assign` / `For` / `ForRange` lowering cluster into a dedicated module.
6. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01-S2-03] Split `Attribute` / `Match` / `ForCore` lowering plus node-dispatch orchestration into dedicated modules.
7. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01-S3-01] Update source-contract tests and representative regressions to the second-wave split layout.
8. [x] [ID: P1-EAST23-LOWERING-ORCHESTRATION-01-S4-01] Update docs / TODO / archive and close the task.
- Progress memo: The full second-wave split for `call metadata/json decode`, `stmt lowering`, and `dispatch/orchestration` was completed, revalidated with source-contract and representative regressions, and archived.

### P1: Decompose `east2_to_east3_lowering.py` by cluster

Context: [docs/en/plans/archive/20260311-p1-east23-lowering-decomposition.md](../plans/archive/20260311-p1-east23-lowering-decomposition.md)

1. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01] Decompose `east2_to_east3_lowering.py` by cluster and reduce the main file to orchestration-centered logic.
2. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01-S1-01] Lock split boundaries for `type_summary`, `type_id_predicate`, `nominal_adt_meta`, `call_metadata`, and `stmt_orchestration`.
3. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01-S1-02] Fix bundle-level progress-note rules for this task.
4. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01-S2-01] Split the `type summary`, `nominal decl summary`, and `json receiver contract` cluster into a dedicated module.
5. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01-S2-02] Split the `type_id predicate`, `isinstance`, and `issubclass` lowering cluster into a dedicated module.
6. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01-S2-03] Split the `nominal ADT ctor/projection/match metadata` cluster into a dedicated module.
7. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01-S3-01] Update source-contract tests and representative regressions to the split layout.
8. [x] [ID: P1-EAST23-LOWERING-DECOMPOSITION-01-S4-01] Update docs / TODO / archive and close the task.
- Progress memo: The first-wave split for `type summary`, `type_id predicate`, and `nominal ADT metadata` was completed, revalidated with source-contract and representative regressions, and then archived.

### P1: Prune `toolchain.ir.core` facade importers

Context: [docs/en/plans/archive/20260311-p1-ir-entrypoint-facade-pruning.md](../plans/archive/20260311-p1-ir-entrypoint-facade-pruning.md)

1. [x] [ID: P1-IR-ENTRYPOINT-FACADE-PRUNING-01] Restrict `toolchain.ir.core` to an external compatibility facade and move internal compiler / representative regression lanes onto `core_entrypoints`.
2. [x] [ID: P1-IR-ENTRYPOINT-FACADE-PRUNING-01-S1-01] Inventory residual importers and classify them as `src_compiler`, `representative_tests`, or `compat_only`.
3. [x] [ID: P1-IR-ENTRYPOINT-FACADE-PRUNING-01-S1-02] Fix the policy that `toolchain.ir.core` remains an external compatibility facade while internal compiler / representative regression lanes use `core_entrypoints`.
4. [x] [ID: P1-IR-ENTRYPOINT-FACADE-PRUNING-01-S2-01] Move `transpile_cli` and representative test/backend importers onto `core_entrypoints`.
5. [x] [ID: P1-IR-ENTRYPOINT-FACADE-PRUNING-01-S2-02] Fail fast on facade reentry with a source-contract guard.
6. [x] [ID: P1-IR-ENTRYPOINT-FACADE-PRUNING-01-S3-01] Run representative regressions and version gates, then archive the task.
- Progress memo: `transpile_cli` and representative test/backend importers now use `core_entrypoints`, and the source-contract locks `src importer=0` / `representative test importer=0`.


### P1: Shrink the `toolchain.ir.core` import hub

Context: [docs/en/plans/archive/20260311-p1-ir-core-import-surface-pruning.md](../plans/archive/20260311-p1-ir-core-import-surface-pruning.md)

1. [x] [ID: P1-IR-CORE-IMPORT-SURFACE-01] Push `toolchain.ir.core` toward an external thin facade and shrink internal split-module dependence on it as an import hub.
2. [x] [ID: P1-IR-CORE-IMPORT-SURFACE-01-S1-01] Inventory `toolchain.ir.core` importers and classify them as `public_entrypoint`, `internal_split_module`, `tests_only`, and `bridge_compat`.
3. [x] [ID: P1-IR-CORE-IMPORT-SURFACE-01-S1-02] Define the public surface that remains on `toolchain.ir.core` and the policy that forbids new internal imports through it.
4. [x] [ID: P1-IR-CORE-IMPORT-SURFACE-01-S2-01] Move representative internal split-module lanes onto dedicated-module imports and reduce `core.py`-mediated dependencies.
5. [x] [ID: P1-IR-CORE-IMPORT-SURFACE-01-S2-02] Align helper/test lanes to the public surface or dedicated modules as appropriate.
6. [x] [ID: P1-IR-CORE-IMPORT-SURFACE-01-S3-01] Add an import-surface guard for `toolchain.ir.core` so internal reintroduction fails fast.
7. [x] [ID: P1-IR-CORE-IMPORT-SURFACE-01-S4-01] Re-run representative regressions, confirm no regression, and archive the task.
- Progress memo: facade exports were fixed as `CORE_PUBLIC_FACADE_EXPORTS` and `CORE_BRIDGE_COMPAT_EXPORTS`, and new `toolchain.ir.core` dependencies from `internal_split_module` are now forbidden by policy.
- Progress memo: representative lanes moved `core_entrypoints`, `core_string_semantics`, `core_expr_primary`, `core_expr_lowered`, and `core_expr_call_args` off the import hub, while `core_stmt_parser` and `core_module_parser` now depend on dedicated `*_parser_support` modules.
- Progress memo: helper/bridge lanes moved `INT_TYPES/FLOAT_TYPES` into `core_numeric_types`, removing `toolchain.ir.core` imports from `east2_to_human_repr` and `east_parts.__init__`.
- Progress memo: `test_east_core_source_contract_import_surface.py` now locks both the ban on `toolchain.ir.core` reintroduction under `src/toolchain/ir` and the remaining non-IR facade use to the public surface imported by `frontends/transpile_cli.py`.

### P1: Cluster-sized decomposition of `core.py` / `test_east_core.py`

Context: [docs/en/plans/archive/20260311-p1-ir-core-decomposition.md](../plans/archive/20260311-p1-ir-core-decomposition.md)

1. [x] [ID: P1-IR-CORE-DECOMPOSITION-01] Decompose `core.py` and `test_east_core.py` in cluster-sized slices so source-contract guards and parser behavior no longer share one file.
2. [x] [ID: P1-IR-CORE-DECOMPOSITION-01-S1-01] Inventory split boundaries in `core.py` and `test_east_core.py`, then lock the split order for source-contract, parser-behavior, and suffix/call clusters.
3. [x] [ID: P1-IR-CORE-DECOMPOSITION-01-S1-02] Record the cluster-level progress-note rule in this plan and align TODO wording with it.
4. [x] [ID: P1-IR-CORE-DECOMPOSITION-01-S2-01] Extract the leading source-contract builder cluster from `test_east_core.py` into a shared support module plus a dedicated test file.
5. [x] [ID: P1-IR-CORE-DECOMPOSITION-01-S2-02] Split the remaining source-contract guards into cluster-specific `test_east_core_source_contract_*.py` files.
6. [x] [ID: P1-IR-CORE-DECOMPOSITION-01-S2-03] Split parser behavior, diagnostics, and representative nominal-ADT tests into dedicated test files.
7. [x] [ID: P1-IR-CORE-DECOMPOSITION-01-S3-01] Continue moving remaining `core.py` clusters into dedicated modules in bundle-sized slices.
8. [x] [ID: P1-IR-CORE-DECOMPOSITION-01-S4-01] Run representative IR/selfhost regressions and stabilize the split with compressed progress notes.
- Progress memo: source-contract guards now live in dedicated `test_east_core_source_contract_*.py` files, parser behavior lives in dedicated parser-behavior files, and `test_east_core.py` has shrunk to a 44-line residual-regression shell.
- Progress memo: `core.py` is now a 214-line thin facade with split-module imports plus three wrappers, and the end state is locked by `test_east_core_source_contract_core_surface.py` and representative selfhost regressions.

### P1: Decompose large expression split modules by cluster

Context: [docs/en/plans/archive/20260311-p1-ir-expr-module-decomposition.md](../plans/archive/20260311-p1-ir-expr-module-decomposition.md)

1. [x] [ID: P1-IR-EXPR-MODULE-DECOMPOSITION-01] Decompose the large expression-side split modules under `toolchain/ir` by cluster and make the `attr/subscript/call` responsibility boundaries explicit.
2. [x] [ID: P1-IR-EXPR-MODULE-DECOMPOSITION-01-S1-01] Lock the split boundary so `core_expr_attr_subscript_suffix.py` becomes `attr_suffix` / `subscript_suffix` / `shared_postfix_orchestration`, and `core_expr_call_annotation.py` becomes `named_call` / `attr_call` / `callee_call` / `shared_state_orchestration`.
3. [x] [ID: P1-IR-EXPR-MODULE-DECOMPOSITION-01-S1-02] Fix the progress-note rule for this task so TODO stays at one-line bundle summaries and helper-level detail lives in the plan.
4. [x] [ID: P1-IR-EXPR-MODULE-DECOMPOSITION-01-S2-01] Split the `attr suffix` / `subscript suffix` cluster into dedicated modules and reduced the shared postfix facade.
5. [x] [ID: P1-IR-EXPR-MODULE-DECOMPOSITION-01-S2-02] Split the `named-call` / `attr-call` / `callee-call` annotation cluster into dedicated modules and reduced the shared call facade.
6. [x] [ID: P1-IR-EXPR-MODULE-DECOMPOSITION-01-S3-01] Updated source-contract tests to the post-split module layout and passed `test_east_core*.py`, `test_prepare_selfhost_source.py`, and `build_selfhost.py`.
7. [x] [ID: P1-IR-EXPR-MODULE-DECOMPOSITION-01-S4-01] Updated docs / TODO / archive and moved the completed task into archive.
- Progress memo: The responsibility split into `core_expr_attr_suffix.py`, `core_expr_subscript_suffix.py`, `core_expr_named_call_annotation.py`, `core_expr_attr_call_annotation.py`, and `core_expr_callee_call_annotation.py` is now fixed together with the aligned source-contract coverage.

### P2: Retire duplicate tracking for EAST core modularization

Context: [docs/en/plans/archive/20260311-p2-east-core-modularization.md](../plans/archive/20260311-p2-east-core-modularization.md)

1. [x] [ID: P2-EAST-CORE-MODULARIZATION-01] Split `core.py` / `test_east_core.py` by responsibility so compiler-internal improvements can proceed in cluster-sized slices again.
2. [x] [ID: P2-EAST-CORE-MODULARIZATION-01-S1-01] Inventory remaining clusters in `core.py` / `test_east_core.py` and fix split boundaries as `suffix parser` / `annotation` / `builder` / `source guard`.
3. [x] [ID: P2-EAST-CORE-MODULARIZATION-01-S2-01] Extract the `attr/subscript annotation` cluster into a dedicated mixin module and push state/build-dispatch helpers out of `core.py`.
4. [x] [ID: P2-EAST-CORE-MODULARIZATION-01-S2-02] Move the remaining `call/attr/subscript` suffix parser clusters into dedicated modules and reduce `core.py` postfix parsing to orchestration.
5. [x] [ID: P2-EAST-CORE-MODULARIZATION-01-S3-01] Split `test_east_core.py` source guards into feature-specific test modules so they track module boundaries one-to-one.
6. [x] [ID: P2-EAST-CORE-MODULARIZATION-01-S3-02] Compress TODO/plan progress notes and keep detailed history only in the `Decision Log`.
7. [x] [ID: P2-EAST-CORE-MODULARIZATION-01-S4-01] Re-run representative regressions, confirm no regression, and archive the completed task.
- Progress memo: The thin-facade end state at `core.py=214 lines` / `test_east_core.py=44 lines` together with the dedicated source-contract files already satisfied this plan's modularization acceptance criteria.
- Progress memo: This task was fulfilled by the end state of `P1-IR-CORE-DECOMPOSITION-01`, so it was archived to avoid duplicated active tracking.

### P5: Full rollout of nominal ADT as a language feature

Context: [docs/en/plans/archive/20260311-p5-nominal-adt-language-rollout.md](../plans/archive/20260311-p5-nominal-adt-language-rollout.md)

1. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01] Perform the full rollout of nominal ADT as a language feature.
2. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S1-01] Inventory candidate language-surface designs for nominal ADT declarations, constructors, variant access, and `match`, then choose a selfhost-safe staged rollout.
3. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S1-02] Fix the boundary between type-system groundwork, narrowing groundwork, and full language-surface rollout so this plan does not conflict with `P1-EAST-TYPEEXPR-01`.
4. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S2-01] Extend `spec-east`, `spec-user`, and `spec-dev` with nominal-ADT declarations, pattern nodes, `match` nodes, and diagnostics contracts.
5. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S2-02] Fix the policy and error categories for exhaustiveness checks, duplicate patterns, and unreachable branches.
6. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S3-01] Update the frontend and selfhost parser so representative nominal-ADT syntax is accepted.
7. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S3-02] Introduce ADT constructors, variant tests, variant projections, and `match` lowering into EAST/EAST3.
8. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S4-01] Confirm through representative tests that the built-in `JsonValue` lane and user-defined nominal-ADT lane share the same IR category.
9. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S4-02] Implement the minimum representative backend support, starting with C++, for constructors, variant checks, destructuring, and `match`, and forbid silent fallbacks.
10. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S5-01] Define rollout order and fail-closed policy for other backends, and fix diagnostics for unsupported targets.
11. [x] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S5-02] Update selfhost / docs / archive / migration notes and close the rollout as a formal language feature.
- Progress memo: The nominal ADT v1 migration note now lives in `docs/en/language/nominal-adt-v1.md`, fixing Stage A `@sealed` + variant + `isinstance` as the canonical source surface while leaving the representative `match` lane as a Stage B contract.
- Progress memo: `spec-user`, the tutorial, the C++ support matrix, and selfhost support-block guards were synchronized to the formal nominal ADT v1 surface, closing the shared contract between built-in `JsonValue` lanes and user-defined nominal ADT lanes in both docs and selfhost regression coverage.

### P4: Canonicalize `backend_registry` and strengthen selfhost parity gates

Context: [docs/en/plans/archive/20260311-p4-backend-registry-selfhost-parity-hardening.md](../plans/archive/20260311-p4-backend-registry-selfhost-parity-hardening.md)

1. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01] Canonicalize `backend_registry` and strengthen selfhost parity gates.
2. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S1-01] Inventory overlapping surfaces between `backend_registry.py` and `backend_registry_static.py` and classify intentional differences versus drift candidates.
3. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S1-02] Inventory gates and blind spots across `build_selfhost`, `stage2`, `verify_selfhost_end_to_end`, and multilang selfhost, then fix the known-block / regression policy.
4. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S2-01] Define a canonical source of truth for backend capability, runtime copy, option schema, and writer metadata, and derive both host/static registries from it.
5. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S2-02] Fix the boundary for intentional differences together with the diagnostics contract.
6. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S3-01] Move host/static registries onto shared metadata / helper flows and retire handwritten duplication.
7. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S3-02] Add registry drift guards so one-sided backend-surface changes fail fast.
8. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S4-01] Unify the summary vocabulary and render path across the representative stage1 / stage2 / direct e2e / multilang parity suites.
9. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S4-02] Align unsupported / preview / known-block / regression diagnostics between registry code and parity reports.
10. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S5-01] Refresh readiness report / docs / archive so backend readiness and known blocks remain traceable.
11. [x] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S5-02] Lock representative host/selfhost contract guards and reentry guards for internal changes.
- Progress memo: [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S4-01] Representative parity suites now share `selfhost_parity_summary.py` for summary-row vocabulary and rendering across stage1 / stage2 / direct e2e / multilang lanes.
- Progress memo: [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S4-02] `backend_registry_diagnostics.py` became the shared source of truth for `unsupported_by_design` / `preview_only` / `toolchain_missing` / `regression` across both registry and parity-report lanes.
- Progress memo: [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S5-02] `test_backend_registry_selfhost_contract_guard.py`, `check_selfhost_contract_reentry_guard.py`, and `test_run_local_ci.py` now lock known-block / regression contract cases and the local-CI reentry path across host and selfhost lanes together.

### P2: Move compiler boundaries to typed carriers and retreat internal object-carrier / `make_object` usage

Context: [docs/en/plans/archive/20260311-p2-compiler-typed-boundary.md](../plans/archive/20260311-p2-compiler-typed-boundary.md)

1. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01] Move compiler boundaries to typed carriers and retreat internal object-carrier / `make_object` usage.
2. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01] Inventory remaining `dict[str, object]`, `list[object]`, `make_object`, and `py_to` usage across `transpile_cli`, `backend_registry_static`, selfhost parser paths, and generated compiler runtime, then classify each usage as `compiler_internal`, `json_adapter`, `extern_hook`, or `legacy_bridge`.
3. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02] Lock the typed-boundary contract and non-goals so they stay consistent with `spec-dev`, `spec-runtime`, and `spec-boxing`.
4. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-01] Define typed carrier specs for compiler root payloads: EAST document, backend spec, layer options, and emit request/result.
5. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-02] Introduce typed carriers and thin legacy adapters in the Python source of truth.
6. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-03] Introduce typed carrier mirrors or typed wrapper APIs in the C++ selfhost/native compiler interfaces and reduce raw `dict<str, object>` exchange.
7. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Move selfhost parser / EAST builder node construction onto typed constructors / builder helpers and gradually retire direct `dict<str, object>{{...}}` assembly.
8. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Retreat remaining `make_object` usage in generated compiler / selfhost runtime down to serialization/export seams only.
9. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02-A] Redefine the `S3-02` completion criteria and compress TODO/plan progress notes to cluster-level summaries.
10. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02-B] Split the postfix/suffix parser cluster out of `core.py` and move `call` / `attr` / `subscript` parsing into dedicated modules.
11. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02-C] Split the call-annotation cluster out of `core.py` and move `named-call` / `attr-call` / `callee-call` handling into dedicated modules.
12. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02-D] Finish the remaining `call-arg` / `suffix tail` / `subscript tail` helper extraction in bundles of 5-10 clusters instead of one-helper commits.
13. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02-E] Rebaseline generated/selfhost residual guards and export seams, retreat `make_object` down to serialization/export seams only, and close `S3-02`.
14. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-01] Separate JSON, extern/hooks, and intentionally dynamic carriers from the compiler typed model behind `JsonValue` or explicit adapters.
15. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-02] Label every remaining `make_object` / `py_to` / `obj_to_*` usage and add guards that reject uncategorized reintroduction.
16. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-01] Refresh selfhost build/diff/prepare/bridge regressions and lock non-regression after the typed-boundary changes.
17. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-02] Update docs / TODO / archive and record whether each remaining `make_object` usage is `user boundary only` or `explicit adapter only`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02] `S1-S2` classified object-carrier usage, fixed typed-boundary non-goals, locked typed-carrier field contracts, and moved host/static/native wrappers onto thin legacy-adapter surfaces.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Checked-in node construction is now almost entirely on `_sh_make_*` builder helpers; module root, imports, expr/stmt nodes, comprehensions, f-strings, trivia, and span carriers are guarded on the source-of-truth side.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `core.py` postfix/suffix parser and call-annotation clusters were split into dedicated modules, and generated/selfhost residual guards were rebaselined into `export_seam` plus `expr_parser` / `stmt_parser` / `lookup` buckets, closing `S3-02`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-01] Dynamic carriers were confined to explicit seams in `toolchain.json_adapters`, `RuntimeHookAdapter`, `AmbientExternBinding`, and `pytra.std.json`, retreating generic raw access out of the compiler/toolchain core.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-02] Remaining tracked compiler/toolchain object-bridge usage was reduced to the three `legacy_migration_adapter` sites in native `transpile_cli.cpp`, with `tools/check_compiler_object_bridge_labels.py` rejecting unlabeled reintroduction.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-01] Unit tests now lock `build_selfhost.py`, `build_selfhost_stage2.py`, `verify_selfhost_end_to_end.py`, `check_selfhost_cpp_diff.py`, `check_selfhost_stage2_cpp_diff.py`, `selfhost_transpile.py`, and the generated selfhost residual bucket invariants, so typed-boundary regressions are watched across the whole build/diff/prepare/bridge lane.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-02] The final end state is now fixed as “tracked compiler/toolchain bridges are explicit-adapter-only, while user-facing `Any/object` boundaries remain out of scope on the runtime side.” Generated selfhost-core `make_object` residuals are likewise limited to `serialization_export_seam` and explicit parser-residual guards.

### P3: Harden compiler contracts and make stage / pass / backend handoff fail-closed

Context: [docs/en/plans/archive/20260311-p3-compiler-contract-hardening.md](../plans/archive/20260311-p3-compiler-contract-hardening.md)

1. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01] Hardened compiler contracts and made stage / pass / backend handoff fail-closed.
2. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S1-01] Inventory the current `check_east_stage_boundary`, `validate_raw_east3_doc`, and backend-entry guards, then classify blind spots such as node shape, `type_expr` / `resolved_type`, `source_span`, and helper metadata.
3. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S1-02] Fix the responsibility boundary between schema validators, invariant validators, and backend input validators so it does not conflict with `P1-EAST-TYPEEXPR-01` or `P2-COMPILER-TYPED-BOUNDARY-01`.
4. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S2-01] Fixed `spec-dev` `1.2.2` / `1.2.4` as the source of truth for required fields, allowed omissions, and diagnostic categories for raw EAST3, linked output, and backend input.
5. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S2-02] Fixed `spec-dev` `1.2.3` as the source of truth for the fail-closed policy for `type_expr` / `resolved_type` mirrors, `dispatch_mode`, `source_span`, and helper metadata.
6. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S3-01] Added central validator primitives around `toolchain/link/program_validator.py` and extended coarse raw EAST3 / linked-output checks to representative node/meta invariants.
7. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S3-02] Introduced pre/post validation hooks at representative pass/lowering/linker entries and stopped invalid nodes from flowing through silently.
8. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S4-01] Run representative backends, starting with C++, through compiler-contract validators at entry and replace backend-local crashes or silent fallbacks with structured diagnostics.
9. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S4-02] Extended `tools/check_east_stage_boundary.py` so it also detects stage semantic drift, not only stage-boundary violations.
10. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S5-01] Added representative unit/selfhost regressions so contract violations are reproducible as expected failures.
11. [x] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S5-02] Updated docs / TODO / archive / migration notes so validator updates become mandatory whenever node/meta contracts change.
- Progress memo: [ID: P3-COMPILER-CONTRACT-HARDENING-01-S3-02] Representative loader / linker / optimizer entries now run pre/post validation hooks, so invalid EAST3 or malformed linked output no longer flows into backend lanes silently.
- Progress memo: [ID: P3-COMPILER-CONTRACT-HARDENING-01-S4-01] The C++ backend entry is fixed as the representative structured-diagnostic lane, and backend-local crashes or silent fallbacks are normalized into fail-closed `backend_input_*` diagnostics.
- Progress memo: [ID: P3-COMPILER-CONTRACT-HARDENING-01-S4-02] `check_east_stage_boundary.py` now detects semantic literal drift in canonical `east2.py` / `code_emitter.py`, not just import/call boundary violations.
- Progress memo: [ID: P3-COMPILER-CONTRACT-HARDENING-01-S5-01] Representative regressions were organized to cover both unit and selfhost lanes through `test_east_stage_boundary_guard.py`, `test_py2x_entrypoints_contract.py`, `test_build_selfhost_tool.py`, and `test_selfhost_build_verify_tools.py`.
- Progress memo: [ID: P3-COMPILER-CONTRACT-HARDENING-01-S5-02] `spec-dev` `1.2.5` now fixes the rule that any node kind / meta key / helper protocol / backend-input dependency change must update the validator, guard, and representative regressions in the same change set.

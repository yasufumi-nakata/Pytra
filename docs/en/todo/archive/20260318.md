# TODO History (2026-03-18)

<a href="../../../ja/todo/archive/20260318.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-18 Migration: Completed and deferred tasks from P0 to P6

### Completed
- [x] P0-CPP-REDUNDANT-WIDENING-CAST-01: removed redundant integer widening casts and unified `static_cast` to `T(x)`
- [x] P0-TAGGED-UNION-TID-UNIFY-01: unified tagged-union tags to `PYTRA_TID`, including `isinstance` and type narrowing
- [x] P0-TAGGED-UNION-CAST-NARROWING-01: unified on `typing.cast(T, v)` and removed implicit narrowing
- [x] P0-PYTRA-TYPES-MODULE-01: added the `pytra.types` module, Pylance-compatible scalar types
- [x] P1-TYPE-ALIAS-SUPPORT-01: added support for PEP 695 `type X = T` aliases
- [x] P1-TAGGED-UNION-ALL-BACKENDS-01: added tagged-struct generation, starting with C++
- [x] P5-CPP-PY-IS-TYPE-DEAD-CODE-REMOVE-01: removed 7 `py_is_type` functions
- [x] P5-EAST3-FLOORDIV-MOD-NODE-01: converted FloorDiv and Mod into IR nodes
- [x] P6-CPP-LIST-MUT-IR-BYPASS-FIX-01: unified list mutation through IR
- [x] P6-EAST3-LEN-SLICE-NODE-01: converted `py_len` and `py_slice` into IR nodes
- [x] P6-CPP-EMIT-LIST-DICT-CLEAR-01: added lowering for `list.clear()` and `dict.clear()`
- [x] P6-EAST3-GENERAL-UNION-VARIANT-01: converted general unions to tagged structs
- [x] P6-EAST3-IS-NONE-INLINE-01: inlined `py_is_none`
- [x] P6-EAST3-PY-TO-INLINE-01: inlined the `py_to` family
- [x] P6-EAST3-PY-TO-STRING-INLINE-01: inlined `py_to_string`
- [x] P6-EAST3-PY-AT-INLINE-01: removed the list and `rc` variants of `py_at`
- [x] P6-CPP-ANY-UNION-OBJECT-FALLBACK-01: removed object fallback for unions contaminated by `Any`

### Deferred, requires stronger type inference first
- P6-CPP-UNKNOWN-TYPE-OBJECT-FALLBACK-01: object fallback for unknown or empty-string types
- P6-CPP-BRANCH-MERGE-OBJECT-FALLBACK-01: object fallback when if/else branch-type merge fails
- P6-CPP-FOR-LOOP-TYPE-OBJECT-FALLBACK-01: object fallback when the for-loop variable type cannot be inferred
- P6-CPP-GLOBAL-VAR-TYPE-OBJECT-FALLBACK-01: object fallback when global-variable inference fails

## 2026-03-19 Migration

### Completed
- [x] P0-INLINE-UNION-TAGGED-STRUCT-01: unified inline unions to tagged structs
- [x] P0-PATHLIB-STR-PATH-UNION-01: added `str | Path` support to `pathlib.Path`
- [x] P3-FUTURE-ANNOTATIONS-REMOVAL-01: removed `from __future__ import annotations` from `src/pytra/`
- [x] P3-PREDICATES-TEMPLATE-01: converted `py_any` and `py_all` to `@template` and removed `Any`
- [x] P4-PREEXISTING-TEST-FAILURES-FIX-01: fixed 3 failures and skipped 1
- [x] P5-LUA-BACKEND-EMIT-FIX-01: resolved by the P0 fixes and added Lua generated output
- [x] P6-CPP-INCLUDE-OPTIMIZE-01: changed fixed C++ includes into need-based includes
- [x] P0-PYTRA-IMPORT-UNIFY-01: unified Python standard-module imports through `pytra.*`
- [x] P0-TAGGED-UNION-TID-UNIFY-01: unified tagged-union tags to `PYTRA_TID`
- [x] P0-TAGGED-UNION-CAST-NARROWING-01: unified on `cast()`
- [x] P0-PYTRA-TYPES-MODULE-01: added the `pytra.types` module
- [x] P0-CPP-REDUNDANT-WIDENING-CAST-01: removed redundant widening casts
- [x] P6-EAST3-PY-TO-STRING-INLINE-01: inlined `py_to_string`
- [x] P6-EAST3-PY-AT-INLINE-01: removed the list and `rc` variants of `py_at`
- [x] P6-CPP-ANY-UNION-OBJECT-FALLBACK-01: removed object fallback for unions contaminated by `Any`
- [x] P2-COMPILE-LINK-PIPELINE-01: implemented `compile` and `link` subcommands, with `type_id` linker finalization still remaining

## 2026-03-19 Migration, additional

### Completed
- [x] P0-TYPE-ALIAS-NO-EXPAND-01: stopped parser-side expansion of type aliases
- [x] P0-JSON-REWRITE-S1 to S4: fully rewrote `json.py` using `type JsonVal`
- [x] P0-INLINE-UNION-TAGGED-STRUCT-01: unified inline unions to tagged structs
- [x] P0-PATHLIB-STR-PATH-UNION-01: added `str | Path` support to `pathlib.Path`
- [x] P0-PYTRA-IMPORT-UNIFY-01: unified Python standard-module imports through `pytra.*`
- [x] P3-FUTURE-ANNOTATIONS-REMOVAL-01: removed `from __future__ import annotations`
- [x] P3-PREDICATES-TEMPLATE-01: converted `py_any` and `py_all` to `@template`
- [x] P4-PREEXISTING-TEST-FAILURES-FIX-01: fixed 3 failures and skipped 1
- [x] P5-LUA-BACKEND-EMIT-FIX-01: fixed Lua emit
- [x] P6-CPP-INCLUDE-OPTIMIZE-01: changed fixed C++ includes into need-based includes
- [x] P1-RUNTIME-LAYOUT-RESTRUCTURE-01: changed runtime structure, unified `.east`, removed `native/`, and deleted `generated/`

## 2026-03-18 Migration: Completed prohibition of `Any` annotations and removal of `PyObj`

The following was moved from `docs/ja/todo/index.md` to `todo/archive`.

### P5: Prohibit `Any` annotations and migrate to an `object` and `PyObj`-free runtime

Context: [docs/ja/plans/archive/20260318-p5-any-elimination-object-free.md](../plans/archive/20260318-p5-any-elimination-object-free.md)

1. [x] [ID: P5-ANY-ELIM-OBJECT-FREE-01] Prohibit `Any` annotations and remove the `object` and `PyObj` hierarchy from the C++ runtime. Unknown `extern` types become transparent through C++ templates, class polymorphism moves to `rc<Base>`, and stdlib-internal `object` becomes closed types.
- Progress note: complete. S1, investigation and design, -> S2, `Any` prohibition pass and stdlib migration, -> S3, closed types in `json.py` and `assertions.py`, -> S4, emitter migration from `PyObj` to `RcObject`, -> S5, transparent extern, -> S6, removal of the `PyObj` inheritance hierarchy, `make_object`, and the list `pyobj` model, -> S7, parity non-regression, selfhost diff, and doc synchronization. fixture 3/3 and sample 18/18 passed. selfhost diff mismatches=0. C++ version 0.580.0 -> 0.581.0.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S1-01] Complete: full audit of `Any` and `object`. std: `json.py`, S3, `enum.py` and `argparse.py`, S2, `sys.py`, S5, `assertions.py`, S2. emitter: `is_any_like_type` x80+, `make_object` x22, automatic insertion of `"public PyObj"`, S4. Classification and phase assignments recorded in the decision log.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S1-02] Complete: fixed the design spec for unknown `extern` types. Recorded in the decision log: `extern_var_v1` schema-v2 extension, `extern auto {name};` emit policy, and expanded metadata collection to handle annotations other than `Any`.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S1-03] Complete: fixed the design spec for class polymorphism with `rc<Base>`. Recorded the policy change from `public PyObj` to `public RcObject`, `isinstance` via `type_id` comparison, and direct emit to `list<rc<Base>>`.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S1-04] Complete: fixed the design spec for JSON and stdlib replacement. Recorded the recursive-union strategy for `JsonValue` and the implementation phases for `assertions.py` and `json_adapters.py`.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S2-01] Complete: implemented `AnyAnnotationProhibitionPass`. It checks `FunctionDef.arg_types`, `return_type`, and `AnnAssign.annotation`, and raises `RuntimeError` when `Any` is detected. It is disabled by default until stdlib migration completes. 20 unit tests passed.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S2-02] Complete: migrated `Any` out of the stdlib. `enum.py` moved to `object` and concrete types, `argparse.py` to `str | bool | None`, and `json.py` to `dumps(obj: object)`. Validation by `AnyAnnotationProhibitionPass` passed.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S2-03] Complete: documented the `Any` prohibition. Added `docs/ja/spec/spec-any-prohibition.md` with migration steps, an error guide, and pass-activation instructions.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S3-01] Complete: moved the internal representation of `json.py` to closed type `_JsonVal`. Added `JsonObj.raw: dict[str,_JsonVal]` and `_jv_to_object`/`_object_to_jv` in `json_adapters.py`. Added decode-boundary guards in 8 files.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S3-02] Complete: removed `object` from `assertions.py`. Changed argument types of `_eq_any` and `py_assert_eq` to `str | int | float | bool | None`.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S4-01] Complete: changed the base class for user-defined ref classes from `PyObj` to `RcObject`. Updated the emitter, `gc.h`, adding virtual `py_type_id()`, and `py_runtime.h`, `rc<T>` `isinstance` specialization.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S4-02] Complete: relaxed the `static_assert` in `obj_to_rc<T>` from `PyObj` to `RcObject`. Emit for `list[Base] -> list<rc<Base>>` was already working correctly.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S4-03] Complete: already implemented in S4-01, using virtual `py_type_id()` comparison in `py_runtime_value_isinstance` for the `rc<T>` specialization and fixing the implementation on `type_id` comparison.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S5-01] Complete: removed `object` annotations from `stderr` and `stdout` in `sys.py`. `extern object stderr;` disappeared from `sys.h`. Added support for `object` and `""` annotations in `core_extern_semantics.py`.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S6-01] Complete: removed the `PyObj` inheritance hierarchy from `py_runtime.h`, 7 classes such as `PyIntObj` plus iterators. Deleted `class PyObj` from `gc.h` as well. Added virtual method implementations for `RcObject` in `gc.cpp`.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S6-02] Complete: redefined `object` as `rc<RcObject>`. Removed `make_object`, `obj_to_*`, and `py_to<T>(const object&)`. Switched the `json.py` dumps family to `_JsonVal`, and converted `py_any` and `py_all` to typed templates.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S6-03] Complete: deleted `cpp_list_model=pyobj` tests and boxing tests. Removed `object` conversion operators from `list.h`, `dict.h`, and `set.h`. Ran 319 tests with no new regressions outside pre-existing failures.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S7-01] Complete: fixed regressions introduced by S6. Templatized `py_assert_*` and removed dead object functions in `contains` and `iter_ops`. fixture 3/3 and sample 18/18 passed.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S7-02] Complete: confirmed no regression in selfhost diff and direct compile, mismatches=0 and failures=0.
  - [ID: P5-ANY-ELIM-OBJECT-FREE-01-S7-03] Complete: added progress from S3-01 to S7-02 to `docs/en/todo/index.md`. Updated `spec-any-prohibition.md`, ja/en, and added retirement notes to `spec-boxing.md`, ja/en.

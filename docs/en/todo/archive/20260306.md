# TODO History (2026-03-06)

<a href="../../../ja/todo/archive/20260306.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

<!-- translation-status: done -->
<!-- source-sha256: 582ee46af5ef0f5fd7867ce40088e5f279b36ef011f51e2fbbfdfa75b9b26ff5 -->

## 2026-03-06 migration: completed sections moved to archive (11 items)

The following sections were moved from `docs/ja/todo/index.md` into `todo/archive`.

### P0: Restore C++ parity first by rebuilding the runtime root (`runtime2` quarantine + new `runtime/`) (highest priority)

Context: [docs/ja/plans/p0-runtime-root-reset-cpp-parity.md](../plans/p0-runtime-root-reset-cpp-parity.md)

1. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01] Move the legacy runtime aside to `src/runtime2` and restore C++ parity with a new `src/runtime/cpp/{core,gen}` layout.
2. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-01] Inventory all C++ runtime reference points (backend/toolchain/tools) and fix the migration blast radius.
3. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-02] `git mv` `src/runtime` to `src/runtime2` and create the new `src/runtime/cpp/{core,gen}` tree.
4. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-03] Add a guard that forbids references to `src/runtime2`.
5. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-01] Update C++ backend runtime/include resolution to assume `core/gen` and remove the `pytra` shim route.
6. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-02] Switch the runtime generator (manifest/output path/marker) to `runtime/cpp/gen`.
7. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-03] Unify the C++ build manifest and copy path around `runtime/cpp/core` + `runtime/cpp/gen` only.
8. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-01] Regenerate the required C++ runtime modules (`std/utils`) from SoT and place them only under `gen/`.
9. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-02] Reorganize handwritten C++-specific implementations (`*-impl.*`) into `core/`.
10. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-01] Pass `tools/runtime_parity_check.py --targets cpp --case-root fixture`.
11. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-02] Pass `tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples`.
12. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-03] Eliminate the remaining parity failures and confirm that a rerun still passes stably.
13. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S5-01] Update `docs/ja/spec` runtime layout and operating procedures (generation/verification).
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-01] Fixed `work/logs/p0_runtime_root_reset_cpp_ref_inventory_targets_20260305_s1_01.txt` (50 entries) and locked the impact area to `src/backends/cpp/*`, `src/toolchain/compiler/*`, and `tools/*`.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-02] Moved `src/runtime -> src/runtime2`, created `src/runtime/cpp/core` (former `pytra-core`) and `src/runtime/cpp/gen` (former `pytra-gen`), and isolated the old `src/runtime2/cpp/pytra` shim.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-03] Added `tools/check_runtime2_references_absent.py`, integrated it into `run_local_ci.py` and the tooling unit test (`test_check_runtime2_references_absent.py`), and made any reintroduction of `src/runtime2` references fail fast.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-01] Switched C++ backend runtime include resolution to `runtime/cpp/core` + `runtime/cpp/gen`, and confirmed that regenerating `sample/py/01_mandelbrot.py` now emits `#include "runtime/cpp/core/py_runtime.h"` and `runtime/cpp/gen/...`.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-02] Updated the C++ output targets in `tools/runtime_generation_manifest.json` to `src/runtime/cpp/gen/...` and regenerated with `gen_runtime_from_manifest.py --targets cpp --items utils/png,utils/gif`.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-03] Updated the C++ build path (`gen_makefile_from_manifest.py`, `build_multi_cpp.py`, `verify_sample_outputs.py`, `verify_image_runtime_parity.py`) to use `core+gen`, and confirmed with an actual `pytra-cli --target cpp --build` run that only `src/runtime/cpp/{core,gen}` is used.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-01] Ran `tools/runtime_parity_check.py --targets cpp --case-root fixture --summary-json work/logs/p0_runtime_root_reset_cpp_fixture_20260305_s401.json` and confirmed `pass` for all three cases: `math_extended`, `pathlib_extended`, and `inheritance_virtual_dispatch_multilang`.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-01] Regenerated `src/pytra/std/*`, `src/pytra/utils/*`, `src/pytra/built_in/type_id.py`, and `src/toolchain/compiler/east_parts/core.py` with `--emit-runtime-cpp`, and confirmed that only `src/runtime/cpp/gen/` changed.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-02] Updated `tools/check_runtime_cpp_layout.py` and `tools/check_runtime_std_sot_guard.py` to assume the `core/gen` layout, and passed the responsibility-boundary checks for the handwritten `cpp/core` layer and the generated `cpp/gen` layer.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-02] Reran sample parity in case groups and confirmed artifact size+CRC32 matches for `01-05,07-13,15,17,18`; only `06_julia_parameter_sweep`, `14_raymarching_light_cycle`, and `16_glass_sculpture_chaos` still had GIF CRC32 mismatches.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-03] Changed `pi/e` in `src/runtime/cpp/gen/std/math.cpp` to declaration-time initialization to remove the uncalled module-init discrepancy, reran `tools/runtime_parity_check.py --targets cpp --case-root sample 06_julia_parameter_sweep 14_raymarching_light_cycle 16_glass_sculpture_chaos`, and confirmed all three passed.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-02] Reran all 18 sample cases in groups (`01,18` / `11` / `06,14,16` / `02,03,04,05,07,08,09,10` / `12,13,15,17`) and confirmed `pass=18, fail=0` including artifact size+CRC32 checks.
- Progress note: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S5-01] Updated `docs/ja/spec/spec-runtime.md` to assume `runtime/cpp/{core,gen}`, documented the `--emit-runtime-cpp` regeneration path plus the guard/parity verification commands, and explicitly forbade reintroducing `src/runtime/cpp/pytra/`.

### P0: Realign `pytra-cli` responsibilities (naming unification + target-branch removal) (highest priority)

Context: [docs/ja/plans/p0-pytra-cli-boundary-and-dispatch-removal.md](../plans/p0-pytra-cli-boundary-and-dispatch-removal.md)

1. [x] [ID: P0-PYTRA-CLI-REALIGN-01] Rebuild `pytra-cli` around backend profiles and remove target-language branching from the CLI body.
2. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S1-01] Document the responsibility boundaries among the CLI body, backend profiles, and execution runners, and lock the prohibitions.
3. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S1-02] Rename to `src/pytra-cli.py` and update `./pytra`, parity, and tooling references.
4. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S2-01] Extract target-specific build/run/transpile contracts into toolchain-side profiles.
5. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S2-02] Shrink `src/pytra-cli.py` down to shared dispatch only and remove target branching.
6. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S2-03] Make target-incompatible options (for example `--codegen-opt`) fail fast through profile validation.
7. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S3-01] Bring parity/tooling onto the new CLI contract and reduce duplicated hardcoded target logic.
8. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S3-02] Update unit tests, parity checks, and docs, and lock the regressions.
- Progress note: [ID: P0-PYTRA-CLI-REALIGN-01-S1-02] Retired `src/pytra_cli.py` in favor of `src/pytra-cli.py`, and updated references in `./pytra`, `tools/runtime_parity_check.py`, `test_pytra_cli`, and the user docs.
- Progress note: [ID: P0-PYTRA-CLI-REALIGN-01-S1-01] Added a `pytra-cli` boundary section to `docs/ja|en/spec/spec-make.md`, fixing the responsibilities and prohibitions for the CLI body, profiles, and runners, including a ban on handwritten target branching.
- Progress note: [ID: P0-PYTRA-CLI-REALIGN-01-S2-01] Added `src/toolchain/compiler/pytra_cli_profiles.py`, extracted output-path selection and non-C++ build/run contracts out of `pytra-cli`, and passed `test_pytra_cli.py` and `test_pytra_cli_profiles.py`.
- Progress note: [ID: P0-PYTRA-CLI-REALIGN-01-S2-02] Removed `target == ...` branching from `src/pytra-cli.py` and switched transpile/build over to shared dispatch through `TargetProfile.build_driver`.
- Progress note: [ID: P0-PYTRA-CLI-REALIGN-01-S2-03] Added profile compatibility validation so that `--codegen-opt` and `--compiler/--std/--opt/--exe` (under `--build`) fail fast on non-C++ targets, and expanded the tooling unit coverage.
- Progress note: [ID: P0-PYTRA-CLI-REALIGN-01-S3-01] Replaced hardcoded target ordering and `needs` logic in `tools/runtime_parity_check.py` with `pytra_cli_profiles`, and updated `test_runtime_parity_check_cli.py` to the new CLI contract.
- Progress note: [ID: P0-PYTRA-CLI-REALIGN-01-S3-02] Fixed misrouted stdout for non-C++ `run_cmd`, added regressions to `test_pytra_cli.py`, and reconfirmed matching behavior by rerunning `runtime_parity_check` on `sample/py/01_mandelbrot` for `cpp/java`.

### P0: Redesign backend runtime/stdlib responsibility boundaries (design correction)

Context: [docs/ja/plans/p0-backend-runtime-boundary-realign.md](../plans/p0-backend-runtime-boundary-realign.md)

1. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01] Correct the backend responsibility leaks around runtime/stdlib resolution and converge back on EAST3-resolved rendering.
2. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-01] Classify the audit hits by backend and violation type, and fix the repair order.
3. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-02] Fix the resolved call/attr/module/type contract from EAST3 into the backends.
4. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Correct the densest violations in `lua/scala/rs` first.
5. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Correct the remaining issues in `cs/php/go/nim/kotlin/js/cpp` under the same policy.
6. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Make emitter fallbacks fail closed and forbid guess-based rendering.
7. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-01] Add boundary guards (forbidden branching/strings/dispatch) to the mandatory CI path.
8. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-02] Update unit/smoke/parity regressions and lock non-regression.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-01] Classified `work/logs/backend_boundary_audit_hits_20260305_s1_01.txt` (179 hits) into `branch / dispatch / runtime implementation mix`, and fixed the repair order as `lua -> scala -> rs`.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-02] Added the fixed EAST3-to-backend contract to `docs/ja/spec/spec-east.md`, including resolved attributes, resolution priority, fail-closed behavior, and emitter API constraints.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Removed unused mixed runtime-implementation blocks from `lua_native_emitter.py`, shrank the `math|gif|png` hits from `49 -> 8`, and passed `test_py2lua_smoke.py` (32 cases).
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Removed unused inline runtime helpers from `scala_native_emitter.py` and the unused `RUST_RUNTIME_SUPPORT` from `rs_emitter.py`, shrinking the counts to `scala:39 -> 29` and `rs:31 -> 4`, and the global total from `138 -> 101`. `test_py2scala_smoke.py` (16 cases) and `test_py2rs_smoke.py` (30 cases) passed.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Removed the `owner=="math"` re-resolution fallback from `scala_native_emitter.py`, shrinking Scala hits from `29 -> 16` and the global total from `101 -> 88`. Reconfirmed that `test_py2scala_smoke.py` (16 cases) still passed.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed the `owner=="math"` raw-AST re-resolution fallback from `php_native_emitter.py` and `go_native_emitter.py`, shrinking `php+go` hits from `28 -> 12` and the global total from `88 -> 72`. `test_py2php_smoke.py` (10 cases) and `test_py2go_smoke.py` (16 cases) passed.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed the `math` raw-AST re-resolution fallback (type inference, call/attr) from `kotlin_native_emitter.py` and `nim_native_emitter.py`, shrinking `kotlin+nim` hits from `8 -> 3` and the global total from `72 -> 67`. `test_py2kotlin_smoke.py` (16 cases) and `test_py2nim_smoke.py` (3 cases) passed.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed the raw-AST fallback (`owner=="math/png/gif"`, call/attr) from `cs_emitter.py`, shrinking C# hits from `20 -> 8` and the global total from `67 -> 55`. `test_py2cs_smoke.py` preserved the existing baseline of `failures=11` with no new regressions.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed the direct `save_gif` / `write_rgb_png` detection from the expression-statement fallback in `nim_native_emitter.py`, shrinking Nim hits from `3 -> 1` and the global total from `55 -> 53`. `test_py2nim_smoke.py` (3 cases) passed again.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Classified the remaining hits (`go/cs/js/cpp`) into import/runtime-map resolution, diagnostic strings, and compatibility comments. Raw-AST re-resolution fallbacks were fully removed from the target backends, and the remaining constraints were deferred to `S2-03` (fail-closed hardening) and `S3-01` (CI guards).
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Collapsed the stdlib runtime-call miss fallbacks in `php/go` and made the `resolved_runtime_call` path fail closed. The existing fallback for `go` runtime calls like `std::filesystem::*` was kept only because target resolution was still incomplete. `test_py2php_smoke.py` (10 cases) and `test_py2go_smoke.py` (16 cases) passed.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Also turned `resolved_runtime_call` misses into exceptions in `kotlin`, hardening fail-closed behavior for stdlib calls and attributes. `test_py2kotlin_smoke.py` (16 cases) passed.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Also turned stdlib fallback on `resolved_runtime_call` misses into exceptions in `js/cs/nim`, stopping guess-based rendering for calls and attributes. `test_py2js_smoke.py` (22 cases) and `test_py2nim_smoke.py` (3 cases) passed, and `test_py2cs_smoke.py` preserved the existing baseline of `failures=11` with no new regressions.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-01] Revalidated the guard stack already wired into `run_local_ci.py` (`check_emitter_runtimecall_guardrails.py` / `check_emitter_forbidden_runtime_symbols.py`) and fixed the `forbidden` side to allow an empty allowlist when `findings=0`. Both guards and their tooling unit tests passed.
- Progress note: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-02] Added fail-closed regressions for `resolved_runtime_call` misses to the `go/php/kotlin/js/cs/nim` smoke suites, and passed the `go/php/kotlin/js/nim/cs` smoke runs (117 total cases), `check_emitter_runtimecall_guardrails.py`, `check_emitter_forbidden_runtime_symbols.py`, and the tooling tests (`test_check_emitter_runtimecall_guardrails.py` / `test_runtime_parity_check_cli.py`). Also added semantic-tag consistency checks to the `go/php/kotlin/cs` emitters and corrected missed cases of guessed `resolved_runtime_call` rendering.

### P1: Unify runtime generation (remove special-purpose scripts)

Context: [docs/ja/plans/p1-runtime-generator-unification.md](../plans/p1-runtime-generator-unification.md)

1. [x] [ID: P1-RUNTIME-GEN-UNIFY-01] Consolidate runtime generation into the canonical `pytra-cli` / `py2x` path and remove target-specific special generators.
2. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S1-01] Inventory the responsibility differences across the three existing generators (`image/java/cs`) and fix the unification requirements.
3. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S1-02] Define declarative settings that carry target/output path/marker and move code branching into that data.
4. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S2-01] Implement a generic generator (single script) and unify it behind `pytra-cli` / `py2x`.
5. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S2-02] Move the existing callers onto the new path.
6. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S2-03] Delete `tools/gen_image_runtime_from_canonical.py`, `tools/gen_java_std_runtime_from_canonical.py`, and `tools/gen_cs_image_runtime_from_canonical.py`.
7. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S3-01] Add a CI guard that forbids reintroducing special generators.
8. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S3-02] Lock non-regression with runtime audits and parity reruns.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S1-01] Inventoried the input/output/naming/post-process differences across `gen_image/gen_java_std/gen_cs_image` in the plan and fixed the requirements for a single shared path.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S1-02] Added `tools/runtime_generation_manifest.json`, turned `module/target/output/marker/postprocess` (including the C# helper pass) into declarative data, and added a unit test for the manifest structure.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S2-01] Added `tools/gen_runtime_from_manifest.py` and unified manifest-driven `py2x` generation, marker insertion, and `cs_program_to_helper` post-processing. Added the tooling unit test `test_gen_runtime_from_manifest.py`.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S2-02] Repointed `test_gen_image_runtime_from_canonical.py` to the new generator and moved the existing caller path over to `gen_runtime_from_manifest`.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S2-03] Deleted the three legacy special generators and updated the `generated-by` expectation in `test_audit_image_runtime_sot.py` to `tools/gen_runtime_from_manifest.py`.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S3-01] Added `tools/check_runtime_special_generators_absent.py`, integrated it into `run_local_ci.py`, and made any reintroduction of `gen_*_from_canonical.py` fail fast in CI.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S3-02] Reran the runtime audit, resolved one stale allowlist entry in `check_runtime_std_sot_guard.py` (`json src/runtime/java/pytra-core/built_in/PyRuntime.java`), and kept parity follow-up open because Java still had `png.py` translation breakage in the `cpp,java,ruby,lua,php` `01_mandelbrot` run.
- Progress note: [ID: P1-RUNTIME-GEN-UNIFY-01-S3-02] Removed runtime-hook dependence from `gen_runtime_from_manifest.py` and regenerated Java `pytra-gen/utils/png.java,gif.java`. Reinforced `java_native_emitter.py` and `PyRuntime.java`, but parity remained open due to outstanding Java-side issues such as temporary class names, `PngHelper`, `uint8`, and `open/PyFile`.

### P2: Unify `check_py2*` checkers (`--target` + profiles)

Context: [docs/ja/plans/p2-checker-unification.md](../plans/p2-checker-unification.md)

1. [x] [ID: P2-CHECKER-UNIFY-01] Unify the checker family into `tools/check_py2x_transpile.py --target <lang>` and retire the per-language `check_py2*.py` scripts in stages.
2. [x] [ID: P2-CHECKER-UNIFY-01-S1-01] Inventory the differences across the current `check_py2*.py` scripts (case selection, expected failures, extra checks) and define one unified specification.
3. [x] [ID: P2-CHECKER-UNIFY-01-S1-02] Design the per-target profile format.
4. [x] [ID: P2-CHECKER-UNIFY-01-S2-01] Implement the single checker body (`check_py2x_transpile.py`).
5. [x] [ID: P2-CHECKER-UNIFY-01-S2-02] Turn the legacy `check_py2*.py` scripts into compatibility wrappers that delegate to the unified checker.
6. [x] [ID: P2-CHECKER-UNIFY-01-S2-03] Replace checker invocations in `run_local_ci.py`, contract-verification scripts, and docs with the unified checker.
7. [x] [ID: P2-CHECKER-UNIFY-01-S3-01] Delete `check_py2*.py` after the compatibility window and add a guard that blocks reintroduction.
8. [x] [ID: P2-CHECKER-UNIFY-01-S3-02] Lock non-regression with unit and CI coverage.
- Progress note: [ID: P2-CHECKER-UNIFY-01-S1-01] Inventoried 14 `check_py2*_transpile.py` scripts and fixed the difference axes as `case_mode(all|explicit)`, structured expected failures, quality hooks, and extra CLI flags. Reflected the unified contract in `p2-checker-unification.md`.
- Progress note: [ID: P2-CHECKER-UNIFY-01-S1-02] Fixed the single-checker profile schema as `target/case_mode/cases/expected_failures/quality_hooks/flags/stage2_probe` and defined the normalization rule for the simple expected-failure sets.
- Progress note: [ID: P2-CHECKER-UNIFY-01-S2-01] Added `tools/check_py2x_transpile.py` and `check_py2x_profiles.json`, implemented the shared runner, migrated the `cpp/java/scala` profiles, and passed the three-target smoke on `sample/py/01_mandelbrot.py`.
- Progress note: [ID: P2-CHECKER-UNIFY-01-S2-01] Expanded the profiles to all 14 targets, unified `php_sample18`, runtime sidecar checks, and the `cpp_emitter_separation` preflight, and passed the 14-target `sample/py/01_mandelbrot.py` smoke with `--skip-east3-contract-tests`.
- Progress note: [ID: P2-CHECKER-UNIFY-01-S2-02] Turned the 14 legacy `check_py2*.py` scripts into compatibility wrappers that delegate to `check_py2x_transpile.py --target`. Also migrated the C++-specific `--check-multi-file-imports/--check-yanesdk-smoke` behavior into the unified path and confirmed the wrapper execution flow for `nim/cpp/js`.
- Progress note: [ID: P2-CHECKER-UNIFY-01-S2-03] Replaced checker calls in `run_local_ci.py`, `check_noncpp_east3_contract.py`, and `check_gsk_native_regression.py` with `check_py2x_transpile.py --target`. Added `--skip-east3-contract-tests` for `js/ts` to avoid preflight recursion, and passed `check_noncpp --skip-transpile`, `check_py2js_transpile.py`, and `check_py2ts_transpile.py`.
- Progress note: [ID: P2-CHECKER-UNIFY-01-S3-01] Deleted all 14 legacy `check_py2*.py` scripts, added `check_legacy_transpile_checkers_absent.py` and its tooling unit test, integrated the reintroduction guard into `run_local_ci.py`, and moved the Scala checker unit test to the `check_py2x_transpile.py` contract.

### P2: Bring multi-language runtime behavior up to the C++ level (redesigned: strict SoT + generation-first)

Context: [docs/ja/plans/p2-runtime-parity-with-cpp.md](../plans/p2-runtime-parity-with-cpp.md)

1. [x] [ID: P2-RUNTIME-PARITY-CPP-02] Redesign multi-language runtime parity under the assumptions of strict SoT, generation-first ownership, and separated responsibilities, and replace the old flow with one that cannot regress the same way.
2. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S1-01] Remove the old P2 (`P2-RUNTIME-PARITY-CPP-01`) from the unfinished list and replace it with this new P2.
3. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S1-02] Document the responsibility boundaries and prohibitions for SoT/pytra-core/pytra-gen in `docs/ja/spec`.
4. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S1-03] Create a classification table that marks each target module as `generation required` or `core allowed`.
5. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S2-01] Add a static check that detects pass-through naming violations under `pytra-gen`.
6. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S2-02] Tighten marker (`source/generated-by`) and placement (`core` mixing) audits and integrate them into CI.
7. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S2-03] Inventory SoT reimplementation traces under `pytra-core` and feed the result back into the `pytra-gen` migration plan.
8. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S3-01] Use Java as the first target and move emitter handwritten runtime-function branching over to the IR-resolved path.
9. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S3-02] Apply the same policy to the remaining non-C++ backends (`cs/js/ts/go/rs/swift/kotlin/ruby/lua/scala/php/nim`).
10. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S3-03] Turn bans on handwritten library-function branching in emitters into lint and make them fail fast in PR/CI.
11. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Rerun sample parity for every target, including artifact size+CRC32 checks, and freeze the diffs.
12. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S4-02] Reflect the operating procedure into `docs/ja` and `docs/en`.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S1-01] Cleaned up the old P2 from the unfinished list and replaced it in `docs/en/todo/index.md` and `docs/en/plans/p2-runtime-parity-with-cpp.md` with the new P2 (`...-CPP-02`).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S1-02] Added a common SoT/pytra-core/pytra-gen boundary section to `docs/ja/spec/spec-runtime.md`, fixing the required rules (generation-first, marker, EAST3-resolved rendering), the prohibitions (core reimplementation, special naming, emitter-side handwritten branching), and the audit commands.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S1-03] Added a `std/utils` module classification table to `docs/ja/spec/spec-runtime.md`, fixing `argparse..typing` and `assertions/gif/png` as generation-required and `dataclasses_impl/math_impl/time_impl` as core-allowed (implementation boundary).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S2-01] Added `tools/check_runtime_pytra_gen_naming.py`, making it possible to detect pass-through naming and placement violations under `pytra-gen/std|utils`. Also fixed `tools/runtime_pytra_gen_naming_allowlist.txt` (11 entries) as explicit existing debt and passed `test_check_runtime_pytra_gen_naming.py` plus the script itself.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S2-02] Added `tools/check_runtime_core_gen_markers.py` so that every language can be audited for required `pytra-gen` markers and forbidden `pytra-core` mixing, and integrated both `check_runtime_core_gen_markers.py` and `check_runtime_pytra_gen_naming.py` into `tools/run_local_ci.py`. Passed `test_check_runtime_core_gen_markers.py` and both guards (baseline allowlists: marker=1, naming=11).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S2-03] Created `work/logs/runtime_core_sot_reimpl_inventory_20260305_s2_03.tsv`, inventoried ten SoT reimplementation traces under `pytra-core`, and added language-by-language migration waves (JSON family, image family, core boundary) to `docs/ja/plans/p2-runtime-parity-with-cpp.md`.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S3-01] Audited the remaining handwritten runtime branching in the Java emitter, reconfirmed convergence onto the IR-resolved path with `check_emitter_runtimecall_guardrails.py` (strict Java) and `test_py2java_smoke.py` (25 cases), and marked the item done.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S3-02] For the non-Java backends, passed `check_emitter_runtimecall_guardrails.py` (no findings across all backends) and reran the smoke suites (`rs/rb/lua/scala/swift/ts=127` cases, plus the already-run `go/php/kotlin/js/nim/cs=117` cases), confirming that the IR-resolved path and fail-closed policy had been applied everywhere.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S3-03] Reconfirmed that `check_emitter_runtimecall_guardrails.py` and `check_emitter_forbidden_runtime_symbols.py` were already fixed as mandatory steps in `tools/run_local_ci.py`, and fixed the PR/CI rejection conditions together with the strict-backend (`java`) fail-fast rules.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Reran sample parity and fixed the 14-target results for `01_mandelbrot` and `02_raytrace_spheres` in `work/logs/runtime_parity_sample01_multilang_20260305.json` and `work/logs/runtime_parity_sample02_multilang_20260305.json`. Also fixed the failure categories (`js/ts`, `go`, `nim`, `cpp(02)`, `java(02)`).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Fixed Nim's invalid module naming (forced output name `main.nim` and captured parity-side `stderr`). Both Nim parity runs for `01/02` passed with matching artifact size+CRC32, and the remaining failures shrank to `js/ts, go, cpp(02), java(02)`.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Started a full rerun of sample parity and, by `01-03`, identified blockers in `js/ts/go/nim/java/cpp` (`js/png.js` leftover f-string, `go/png.go` broken string, Nim module-name constraints, Java temporary class names, and C++ runtime link conflicts). `rs/cs/ruby/lua/php/swift/kotlin/scala` already matched artifact size/CRC32 over the same range.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Updated the `png.py` source of truth toward backend-compatible behavior and regenerated `js/ts/go` `pytra-gen/utils/png.*`. In the rerun for `01_mandelbrot`, `js/ts` advanced from syntax errors to unresolved `write_rgb_png` exports, and `go` advanced past string breakage to remaining issues such as `extend/main` duplication and undefined `pyWriteRGBPNG`.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Added pre-run cleanup of per-target output directories to `runtime_parity_check.py`, reran `01_mandelbrot/02_raytrace_spheres` for `js,ts,go,cpp,java,nim`, and confirmed artifact size+CRC32 matches (`work/logs/runtime_parity_sample01_02_focus_20260305_after_freshfix.json`).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Restored `src/runtime2/{rs,cs,js,ts,go,java,swift,kotlin,ruby,lua,scala,php,nim}` into `src/runtime/`, eliminating the `runtime source not found` family of errors.
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Fixed PHP runtime `resolved_runtime_call` symbol resolution and reference-passing in `pytra-gen/runtime/{png,gif}.php` append-list paths, reran `01_mandelbrot` and `02_raytrace_spheres` across all 14 targets, and confirmed artifact size+CRC32 matches (`work/logs/runtime_parity_sample01_02_all_targets_after_php_runtime_fixes_20260305.json`).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Fixed Nim's `save_gif` return type (`int`) and `math.pi` lowering, reran `04_orbit_trap_julia` through `06_julia_parameter_sweep` across all 14 targets, and confirmed artifact size+CRC32 matches (`work/logs/runtime_parity_sample04_06_all_targets_after_nim_fix_20260305.json`).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Corrected the remaining failures in Lua/Ruby/Scala/Nim/PHP/Scala (Lua: truthiness in if-expressions + bit operations + local scope; Ruby/Scala: bit operations; Scala: stdlib.method owner injection; Nim: `sqrt(int)` cast; PHP: `stdlib.method.pop` fallback) and reran all sample groups (`01-03/04-06/07-09/10-12/13-15/16-18`), confirming `pass=18, fail=0` across all 14 targets (`work/logs/runtime_parity_sample01_03_all_targets_after_lua_decl_fix_20260305.json`, `work/logs/runtime_parity_sample04_06_all_targets_after_lua_decl_fix_20260305.json`, `work/logs/runtime_parity_sample07_09_all_targets_after_lua_scope_fix_20260305.json`, `work/logs/runtime_parity_sample10_12_all_targets_after_lua_nim_fix_20260305.json`, `work/logs/runtime_parity_sample13_15_all_targets_after_php_pop_fix_20260305.json`, `work/logs/runtime_parity_sample16_18_all_targets_after_fixes_20260305.json`).
- Progress note: [ID: P2-RUNTIME-PARITY-CPP-02-S4-02] Added a runtime parity runbook to `docs/ja/how-to-use.md` and `docs/en/how-to-use.md`, including the 14-target execution command, artifact `size/CRC32` verification, automatic cleanup of stale artifacts, and the recommended case grouping, and fixed it as the shared local/CI operating procedure.

### P0: Move C++ `@extern` modules to header-only generated artifacts (highest priority)

Context: [docs/ja/plans/archive/20260306-p0-cpp-extern-header-only-gen.md](../plans/archive/20260306-p0-cpp-extern-header-only-gen.md)

1. [x] [ID: P0-CPP-EXTERN-HDRONLY-01] Use `pytra.std.math` as the first case, complete the migration so that `@extern` modules generate only `.h` under `gen`, and fix the implementation source of truth in `core`.
2. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S1-01] Organize the EAST-side attribute contract for `@extern` functions/variables and fix the minimum flags the C++ backend reads.
3. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S1-02] Implement extern-only module detection in C++ runtime generation and suppress `.cpp` generation under `gen/*.cpp`.
4. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S2-01] Move the `math` implementation into `runtime/cpp/core/std/math.cpp` as the source of truth and remove the dependency on `gen/std/math.cpp`.
5. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S2-02] Update the build manifest and backend registry to refer to the `math` core implementation.
6. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S3-01] Add a unit regression that fixes `math.h` generation and `.cpp` non-generation.
7. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S3-02] Confirm non-regression with C++ fixture/sample parity and record the result in the decision log.

### P0: Shrink C++ `py_runtime.ext.h` (move logic back to pure-Python sources of truth) (highest priority)

Context: [docs/ja/plans/archive/20260306-p0-cpp-pyruntime-ext-slimming.md](../plans/archive/20260306-p0-cpp-pyruntime-ext-slimming.md)

1. [x] [ID: P0-CPP-PYRUNTIME-EXT-01] Remove higher-level built-in/std implementations from `py_runtime.ext.h` when they can live in pure-Python sources of truth, and unify them under `*.gen.*`.
2. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S1-01] Inventory the functions in `py_runtime.ext.h`, classify them into `ABI/core` and `movable back to pure-Python sources of truth`, and fix that classification.
3. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S1-02] Fix the source/generation/adapter mapping table for `sub`, `ArgumentParser`, `py_any`, `py_all`, string ops, `py_range`, and `py_repeat` in the decision log.
4. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-01] Remove the duplicated ext-side implementation of `sub(...)` and unify on `re.gen.*`.
5. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-02] Remove `ArgumentParser` from ext and align the C++ path to use `argparse.gen.*` as the source-of-truth runtime.
6. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-03] Remove `py_any` / `py_all` from ext and unify on `predicates.gen.*`.
7. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-04] Remove `py_strip` / `py_startswith` / `py_endswith` / `py_find` / `py_replace` from ext and unify on `string_ops.gen.*`.
8. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-05] Remove `py_range` / `py_repeat` from ext and unify on `sequence.gen.*`.
9. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S3-01] Decide the spec for `py_enumerate` / `py_reversed` / `py_contains` as pure-Python sources of truth plus separated typed adapters, and add the necessary `src/pytra/built_in/*.py`.
10. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S3-02] Implement at least one of those three families all the way through ext shrinkage, and leave the remainder in a state where work can continue within the same plan.
11. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S4-01] Add a static guard so that duplicate source-of-truth implementations cannot creep back into `py_runtime.ext.h`.
12. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S4-02] Update layout checks, SoT guards, runtime parity, and runtime emit smokes to fix non-regression after the shrinkage.
- Progress note: As of 2026-03-06, the first wave removed `sub`, `ArgumentParser`, `py_any`, `py_all`, the string helper family, `py_range`, and `py_repeat(str, int)` from `py_runtime.ext.h`, unifying them on `re.gen.*`, `argparse.gen.*`, `predicates.gen.*`, `string_ops.gen.*`, and `sequence.gen.*`. Only `py_repeat` for `list<T>` remains as a thin typed adapter in `sequence.ext.h`.
- Progress note: As of 2026-03-06, the second wave added `iter_ops.py` and moved the object paths for `py_reversed` and `py_enumerate` into `iter_ops.gen.*`. Typed `list<T>` / `str` / `any` adapters and `py_enumerate_list_as<T>` were separated into `iter_ops.ext.h`, and the iterator-helper bodies were removed from `py_runtime.ext.h`.
- Progress note: As of 2026-03-06, `contains.py` was added and the object path of `py_contains` (`dict/list/set/str`) was moved to `contains.gen.*`. The typed fast paths for `dict/list/set/str/tuple` plus object dispatch were separated into `contains.ext.h`, and the `py_contains` body was removed from `py_runtime.ext.h`.
- Progress note: As of 2026-03-06, public-name-based pure-Python unit tests were updated for `predicates/string_ops/contains`, and `check_runtime_cpp_layout.py`, `check_runtime_std_sot_guard.py`, `verify_image_runtime_parity.py`, syntax-only compile checks for built_in/re, and `test_pytra_built_in_{sequence,predicates,string_ops,contains}.py` all passed.

### P0: Replace `cpp_list_model=pyobj` alias preservation with `rc<list<T>>` (highest priority)

Context: [docs/ja/plans/archive/20260306-p0-cpp-pyobj-rc-list-alias.md](../plans/archive/20260306-p0-cpp-pyobj-rc-list-alias.md)

1. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01] Replace alias preservation under `cpp_list_model=pyobj` with `rc<list<T>>` instead of `object`, preserving typed element information while keeping Python-compatible sharing semantics.
2. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S1-01] Fix the responsibility boundary among `object`, `list<T>`, and `rc<list<T>>` in the plan/spec, and clarify that `@extern` ABI is out of scope.
3. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S1-02] Fix the current generated cases that still box into `object` under alias fallback through fixture-based coverage.
4. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-01] Add typed handle helpers for `rc<list<T>>` (construction/reference/value conversion) to the C++ runtime.
5. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-02] Add `rc<list<T>>` overloads for `py_len/py_append/py_extend/py_pop/py_slice/py_at`.
6. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-03] Add the minimum runtime adapters for `rc<list<T>> <-> object` and `rc<list<T>> <-> list<T>`.
7. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-01] Switch emitter alias/shared-name detection away from `object` fallback and toward `rc<list<T>>` declarations.
8. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-02] In `Assign/AnnAssign`, use handle copies / handle construction instead of `make_object(...)` for `b = a`, empty-list initialization, and literal-list initialization.
9. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-03] Update rendering for method calls, subscripts, `len`, slices, and truthiness checks to be aware of `rc<list<T>>`.
10. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S4-01] Organize adapter insertion conditions for `rc<list<T>>` and `list<T>` across function arguments, return values, and callsite coercions, while preserving `list<T>` at ABI boundaries.
11. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S4-02] Leave `object` boxing only where data flows into `Any/object`, and shrink/remove the alias-driven `object` fallback.
12. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-01] Add/update alias fixtures, runtime unit tests, and C++ backend unit tests to lock the regression.
13. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-02] Confirm compile/run on at least one representative sample case (`sample/18`) and record the result in the decision log.
- Progress note: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-01] Generalized `RcObject`, made `list<T>` eligible for RC management, and added `rc_list_new / rc_list_from_value / rc_list_ref / rc_list_copy_value` plus the `py_is_rc_list_handle` trait. Also implemented `rc<list<T>>` overloads for `py_len/py_append/py_extend/py_pop/py_slice/py_at/py_set_at/py_clear/py_reverse/py_sort`, `make_object(const rc<list<T>>&)`, and `obj_to_rc_list<T>`.
- Progress note: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-01] Split alias-name detection away from `object` runtime lists and introduced `_uses_pyobj_rc_list_expr()`. The generated output for `list_alias_shared_mutation.py` now becomes `rc<list<int64>> a = rc_list_from_value(list<int64>{1, 2}); rc<list<int64>> b = a;`, removing the old `make_object(...)` fallback.
- Progress note: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S4-01] Added adapters at call/return boundaries so that `consume(b)` emits `consume(rc_list_ref(b))` and `return b` emits `return rc_list_copy_value(b);`.
- Progress note: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-01] Added a function-boundary parity case to `test_py2cpp_list_pyobj_model.py`, fixing `consume(b)` / `return b` / `bool(b)` / `c[0]` across both Python and C++ with `cpp_list_model=pyobj`.
- Progress note: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-02] Rechecked `sample/18` parity plus ad-hoc alias smokes in `test_py2cpp_list_pyobj_model.py` (append/pop/subscript/slice/truthy/reverse/sort, argument boundary, return boundary) under `g++ -std=c++20`, and all of them passed.

### P0: Make the runtime symbol index data-driven (SoT-generated JSON + IR normalization)

Context: [docs/ja/plans/archive/20260306-p0-runtime-symbol-index-data-driven.md](../plans/archive/20260306-p0-runtime-symbol-index-data-driven.md)

1. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01] Move runtime-symbol ownership and companion rules into SoT-generated JSON and switch IR/backend/tooling over to that index.
2. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-01] Inventory the current handwritten runtime-symbol knowledge and fix a table that separates what remains in IR, what moves into the index, and what the backend derives.
3. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-02] Define the `runtime symbol index` schema and document the responsibilities of `module / symbol / target artifact / companion`.
4. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-01] Add a generator that scans `src/pytra/{built_in,std,utils}` and `src/runtime/<lang>/{core,built_in,std,utils}` and emits the index JSON.
5. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-02] Add unit tests for the generator and fix representative cases such as `py_enumerate` / `py_any` / `py_strip` / `perf_counter` / `write_rgb_png` / `Path` at the index level.
6. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-03] Integrate the index generator into CI/local checks so stale indexes fail fast when the runtime layout changes.
7. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-01] Add `runtime_module_id` and `runtime_symbol` to EAST3 runtime-call nodes so they no longer depend only on a bare `runtime_call` string.
8. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-02] Extend resolved imported symbols (`from X import Y` / `import X` + `X.Y`) so they also carry `runtime_module_id` / `runtime_symbol`.
9. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-03] Retire the handwritten runtime-symbol tables in `signature_registry.py` in stages, until at least the responsibility for guessing file paths is gone.
10. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-01] Use the C++ backend as the first consumer and switch include collection, namespace resolution, and runtime-source collection to the index JSON.
11. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-02] Reorganize `build_multi_cpp.py` / `gen_makefile_from_manifest.py` so they consistently derive `*.gen.*` and `*.ext.*` companions from the index.
12. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-03] Remove ownership-guessing logic for `py_enumerate` / `py_any` / `py_strip` / `dict.get` / `perf_counter` / `Path` from the C++ emitter and move it to IR + index dependence.
13. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-01] Organize the application policy for non-C++ backends and align the responsibility boundary of `resolved_runtime_call` and module/file resolution around the index.
14. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-02] Document in `docs/ja/spec` that “IR holds module+symbol” and “target file paths are derived by the index + backend”.
15. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-03] Run representative regressions (C++ include resolution, runtime build graph, import resolution, unit parity) and confirm that the old ad-hoc fallbacks are no longer needed.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-01] Inventoried the handwritten sites and fixed the current roles as follows: `core.py` owns bare `runtime_call`, `signature_registry.py` owns runtime-symbol matching, the C++ backend (`module.py`, `runtime_paths.py`, `runtime_calls.json`) owns module/file/namespace guessing, and tooling (`build_multi_cpp.py`, `gen_makefile_from_manifest.py`) owns include-driven runtime-source recollection. The boundary is now `IR=module+symbol`, `index=artifact+companion`, and `backend/tooling=rendering and build-graph derivation`.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-02] Fixed the index schema by separating `modules` and `targets`, so target-independent symbol ownership is separate from target-specific artifact information. Fixed `public_headers`, `compile_sources`, and `companions` as the minimum set and documented that paths must not go into IR.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-01] Added `tools/gen_runtime_symbol_index.py`, making it possible to scan `src/pytra/{built_in,std,utils}` and the modern runtime layout (`src/runtime/<lang>/{core,built_in,std,utils}`) and generate `tools/runtime_symbol_index.json`. In stage 1, only targets that already use the modern layout are indexed; legacy runtime layouts are out of scope.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-02] Fixed representative cases in `test/unit/tooling/test_runtime_symbol_index.py`, validating module/symbol/artifact/companion for `py_enumerate_object`, `py_any`, `py_strip`, `perf_counter`, `write_rgb_png`, and `Path`.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-03] Added `--check` to the generator and integrated it into `tools/run_local_ci.py`. Stale indexes now fail fast.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-01] Added `runtime_module_id` and `runtime_symbol` alongside `BuiltinCall` in EAST3. In stage 1, `runtime_call` stays for compatibility, while the nodes also carry target-independent forms such as `perf_counter -> pytra.std.time/perf_counter`, `enumerate -> pytra.built_in.iter_ops/enumerate`, `any -> pytra.built_in.predicates/any`, `dict.get -> pytra.core.dict/dict.get`, and `Path.exists -> pytra.std.pathlib/Path.exists`. `test_east_core.py` could not be run directly because of the existing `toolchain.compiler.east_parts` import breakage, so the values were confirmed with ad-hoc execution through the same API.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-02] Extended resolved imported symbols to carry `runtime_module_id` / `runtime_symbol` too. Confirmed by ad-hoc execution that both Call and Attribute nodes are populated for `json.loads` from `from pytra.std import json`, `png.write_rgb_png` from `from pytra.utils import png`, and `math.sin` / `math.pi` from `import math`.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-03] Fixed in `test_stdlib_signature_registry.py` that artifact-path guessing such as `runtime/cpp`, `*.gen.*`, `*.ext.*`, or `src/runtime` does not creep back into `signature_registry.py`. At this stage, runtime symbol / type / owner-method contracts remain there, but regression coverage guarantees that file-path inference has been completely split out into the generator + index side.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-01] Extended the index generator to cover `pytra.core.*` artifacts and added `canonical_runtime_module_id`, `resolve_import_binding_runtime_module`, and `lookup_cpp_namespace_for_runtime_module` to `runtime_symbol_index.py`. Switched C++ import include collection and namespace resolution in `module.py` over to the index and fixed through unit/integration tests that `from pytra.utils import png`, `from pytra.std.time import perf_counter`, and `import math` all resolve C++ includes and namespaces through the index.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-02] Added reverse index lookup to `tools/cpp_runtime_deps.py` and moved runtime companion derivation in `build_multi_cpp.py` / `gen_makefile_from_manifest.py` over to `runtime_symbol_index.json`. The existing forwarder-header test (`test_cpp_runtime_build_graph.py`) that resolves `math.ext.cpp` still passed.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-03] Switched the C++ emitter's `BuiltinCall` dispatch and imported-symbol call rendering to prioritize `runtime_module_id/runtime_symbol` instead of depending only on `runtime_call` strings. The covered cases were `py_enumerate`, `py_any`, `py_all`, the `py_strip` family, the `dict.get` family, `perf_counter`, and `Path`. `test_cpp_runtime_symbol_index_integration.py` now fixes both the presence of the binding in IR and the final lowering to namespaced C++ calls.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-01 / -S5-02] Fixed in this plan and in `docs/ja/spec/{spec-runtime.md,spec-east.md}` that non-C++ backends also must use `runtime_module_id + runtime_symbol + runtime_symbol_index` as the only ownership-resolution path. Target-specific import/file paths may still be derived by the backend, but ownership tables for module membership must not be reintroduced per backend.
- Progress note: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-03] Ran representative regressions with `tools/gen_runtime_symbol_index.py --check`, `test_runtime_symbol_index.py`, `test_cpp_runtime_build_graph.py`, `test_cpp_runtime_symbol_index_integration.py`, and `tools/runtime_parity_check.py --targets cpp --case-root fixture`, and confirmed that C++ include resolution, runtime build graph, import resolution, and fixture parity all pass on the index-based path. `math_extended/pathlib_extended/inheritance_virtual_dispatch_multilang` all passed.

### P0: Root-fix C++ unit regressions (normalize SoT/IR/Emitter/Runtime contracts)

Context: [docs/ja/plans/archive/20260306-p0-cpp-unit-regression-recovery.md](../plans/archive/20260306-p0-cpp-unit-regression-recovery.md)

1. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01] Root-fix the C++ unit regressions in the order of SoT/IR/Emitter/Runtime contracts and re-green unit + fixture/sample parity.
2. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S1-01] Reclassify the failing tests into `generated runtime`, `import/include resolution`, `container semantics`, and `emitter/CLI contracts`, and fix the ownership layer for each repair.
3. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S2-01] Fix the `json` generated-runtime breakage through SoT and C++ runtime-generation contract changes (no handwritten `.gen.*` edits).
4. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S2-02] Fix the `argparse` generated-runtime breakage through SoT changes, reserved-name avoidance, and class/member emission contract changes.
5. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S3-01] Fix import resolution, include dedupe/sort, and one-to-one module include contracts for `pytra.utils.{png,gif}` and `pytra.std.{time,pathlib}`.
6. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S3-02] Return `os.path` / `glob` runtime-helper routing to owner/module-metadata-based resolution and reduce C++ emitter special cases.
7. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S4-01] Fix container-view and iterator semantics for `dict.items()` / `dict.get()` / `any()` / dict/set comprehensions by aligning built-in SoT and runtime adapters.
8. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S4-02] Clean up the C++ emitter contracts around `mod_mode`, statement-dispatch fallback, and CLI `dump-options` / error-category handling, and fix option reflection and diagnostic consistency.
9. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S5-01] Rerun the full C++ unit suite, fixture parity, and sample parity, confirm that no regressions remain, and update `docs/ja/todo`.

- Progress note: [ID: P0-CPP-REGRESSION-RECOVERY-01-S5-01] `python3 -m unittest discover -s test/unit/backends/cpp -p 'test_*.py'` passed all 484 tests. `python3 tools/runtime_parity_check.py --targets cpp --case-root fixture` passed with `pass=3 fail=0`, and `python3 tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples` passed with `pass=18 fail=0`, including reconfirmed artifact `size/CRC32` matches for samples.

## 2026-03-06 parked section: deferred until ABI is fixed (1 item)

The following section was temporarily removed from `docs/ja/todo/index.md`. It is not complete; it will be reintroduced after the ABI-side responsibility boundaries are fixed.

### P4: Full self-host rollout across all languages (very low priority)

Context: [docs/ja/plans/archive/20260306-p4-multilang-selfhost-full-rollout.md](../plans/archive/20260306-p4-multilang-selfhost-full-rollout.md)

1. [ ] [ID: P4-MULTILANG-SH-01] Establish staged self-hosting for `cpp/rs/cs/js/ts/go/java/swift/kotlin/ruby/lua/scala/php/nim` and make it possible to pass multistage monitoring across all languages.
2. [ ] [ID: P4-MULTILANG-SH-01-S2-03] Fix the transpile failure in the JS selfhost stage-2 dependency and pass multistage.
3. [ ] [ID: P4-MULTILANG-SH-01-S3-01] Remove TypeScript's preview-only status and move it to a generated mode that can actually execute selfhost.
4. [ ] [ID: P4-MULTILANG-SH-01-S3-02] Connect Go/Java/Swift/Kotlin to the native-backend tasks and enable the selfhost execution chain.
5. [ ] [ID: P4-MULTILANG-SH-01-S3-03] Add Ruby/Lua/Scala3/PHP/Nim to the selfhost multistage monitoring targets and fix the undefined-runner state.
6. [ ] [ID: P4-MULTILANG-SH-01-S4-01] Integrate full multistage regression across all languages into the CI path so recurring failure categories are detected continuously.
7. [ ] [ID: P4-MULTILANG-SH-01-S4-02] Document the completion template (pass/exclusion conditions per language/stage) and fix the operating rules.
- Completed child tasks (`S1-01` through `S2-02-S3`) and earlier progress notes have already been moved to `docs/ja/todo/archive/20260301.md`.
- Progress note: [ID: P4-MULTILANG-SH-01-S2-03] Fixed selfhost-parser constraint violations in the JS emitter (`node.get()/node.items()` on `Any`) and unsupported in-function `FunctionDef`, advancing the first failure from `stage1_dependency_transpile_fail` to `self_retranspile_fail (ERR_MODULE_NOT_FOUND: ./pytra/std.js)`.
- Progress note: [ID: P4-MULTILANG-SH-01-S2-03] Added shim generation, import normalization, export injection, and syntax rewriting for JS selfhost preparation, resolving `ERR_MODULE_NOT_FOUND`. The first failure then moved to `SyntaxError: Unexpected token ':'` (from `raw[qpos:]`).
- Progress note: [ID: P4-MULTILANG-SH-01-S2-03] Applied staged fixes for JS selfhost: source-side lowering for slice/set checks, ESM shims, redesigned import normalization, `argparse`/`Path` compatibility shims, a `.py -> EAST3(JSON)` input path, and selfhost-compatible loading in the `JsEmitter` profile loader. The initial failure advanced past the `ReferenceError/SyntaxError` family to `TypeError: CodeEmitter._dict_copy_str_object is not a function`.
- Progress note: [ID: P4-MULTILANG-SH-01-S2-03] Made dict access in `CodeEmitter.load_type_map` and `js_emitter` initialization object-safe, and added `set/list/dict` polyfills and static `CodeEmitter` alias completion to the selfhost rewrite. This resolved `dict is not defined`, and the first failure advanced to `TypeError: module.get is not a function`.
- Progress note: [ID: P4-MULTILANG-SH-01-S2-03] Rewrote `.get` calls into `__pytra_dict_get` and added `parent/name/stem` property compatibility plus idempotent defaults for `mkdir(parents/exist_ok)` to the `Path` shim. JS reached `stage1 pass / stage2 pass`, and the first failure advanced to `stage3 sample output missing`.
- Progress note: [ID: P4-MULTILANG-SH-01-S2-03] Applied object-safe fixes in `CodeEmitter/JsEmitter` (removed `startswith/strip/find` dependencies, fixed the `next_tmp` f-string, removed `ord/chr` dependence from ASCII helpers), and added String polyfills (`strip/lstrip/rstrip/startswith/endswith/find/lower/upper/map`) to the selfhost rewrite. JS kept `stage1/native pass` and `multistage stage2 pass`, and the first failure advanced to `stage3 sample_transpile_fail (SyntaxError: Invalid or unexpected token)`.
- Progress note: [ID: P4-MULTILANG-SH-01-S2-03] Converted indentation generation in `emit` (string multiplication) into loops, added a defense for non-string `quote` in `quote_string_literal`, and changed `_emit_function` to judge `in_class` by emptiness instead of `None`. JS still kept `stage1/native pass` and `multistage stage2 pass`, and the first failure updated to `stage3 sample_transpile_fail (SyntaxError: Unexpected token '{')`, leaving unresolved placeholders and broken function headers in `py2js_stage2.js`.

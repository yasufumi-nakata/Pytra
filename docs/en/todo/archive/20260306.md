# TODO History (2026-03-06)

<a href="../../../ja/todo/archive/20260306.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-06 Moved: Completed Sections (11 items)

Moved the following from `docs/ja/todo/index.md` to `todo/archive`.

### P0: recover C++ parity first by rebuilding the runtime root (`runtime2` shelter + new `runtime/`)

Context: [docs/ja/plans/p0-runtime-root-reset-cpp-parity.md](../plans/p0-runtime-root-reset-cpp-parity.md)

1. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01] Shelter the old runtime under `src/runtime2` and recover C++ parity with a new `src/runtime/cpp/{core,gen}`.
2. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-01] Inventory C++ runtime reference points (backend/toolchain/tools) and fix the migration impact range.
3. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-02] `git mv` `src/runtime` to `src/runtime2` and create new `src/runtime/cpp/{core,gen}`.
4. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-03] Add a guard forbidding references to `src/runtime2`.
5. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-01] Update C++ backend runtime/include resolution to assume `core/gen` and remove the `pytra` shim path.
6. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-02] Switch the runtime generator (manifest/output destination/marker) to `runtime/cpp/gen`.
7. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-03] Unify the C++ build manifest/copy pipeline to `runtime/cpp/core` + `runtime/cpp/gen` only.
8. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-01] Regenerate the required C++ runtime (`std/utils`) from the SoT and place it only under `gen/`.
9. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-02] Reorganize C++-specific handwritten implementations (`*-impl.*`) under `core/`.
10. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-01] Make `tools/runtime_parity_check.py --targets cpp --case-root fixture` pass.
11. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-02] Make `tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples` pass.
12. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-03] Eliminate the remaining parity failures and confirm stable success on reruns.
13. [x] [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S5-01] Update runtime layout and operating procedures (generation/validation) in `docs/ja/spec`.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-01] Fixed `work/logs/p0_runtime_root_reset_cpp_ref_inventory_targets_20260305_s1_01.txt` (50 items) and narrowed the impact range to `src/backends/cpp/*`, `src/toolchain/compiler/*`, and `tools/*`.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-02] Moved `src/runtime -> src/runtime2`, created `src/runtime/cpp/core` (former `pytra-core`) and `src/runtime/cpp/gen` (former `pytra-gen`), and isolated the old `src/runtime2/cpp/pytra` shim.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S1-03] Added `tools/check_runtime2_references_absent.py` and integrated it into `run_local_ci.py` and the tooling unit test (`test_check_runtime2_references_absent.py`) so any reintroduction of `src/runtime2` references fails fast.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-01] Switched C++ backend runtime include resolution to `runtime/cpp/core` + `runtime/cpp/gen`, and confirmed that retranspiling `sample/py/01_mandelbrot.py` now yields `#include "runtime/cpp/core/py_runtime.h"` and `runtime/cpp/gen/...`.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-02] Updated the C++ output destinations in `tools/runtime_generation_manifest.json` to `src/runtime/cpp/gen/...` and regenerated with `gen_runtime_from_manifest.py --targets cpp --items utils/png,utils/gif`.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S2-03] Updated C++ build paths (`gen_makefile_from_manifest.py`, `build_multi_cpp.py`, `verify_sample_outputs.py`, `verify_image_runtime_parity.py`) to `core+gen`, and confirmed that real `pytra-cli --target cpp --build` builds use only `src/runtime/cpp/{core,gen}`.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-01] Ran `tools/runtime_parity_check.py --targets cpp --case-root fixture --summary-json work/logs/p0_runtime_root_reset_cpp_fixture_20260305_s401.json` and confirmed `pass` for all three cases: `math_extended`, `pathlib_extended`, and `inheritance_virtual_dispatch_multilang`.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-01] Regenerated `src/pytra/std/*`, `src/pytra/utils/*`, `src/pytra/built_in/type_id.py`, and `src/toolchain/compiler/east_parts/core.py` with `--emit-runtime-cpp`, confirming that only `src/runtime/cpp/gen/` changes.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S3-02] Updated `tools/check_runtime_cpp_layout.py` and `tools/check_runtime_std_sot_guard.py` to the `core/gen` layout and passed responsibility-boundary checks between the handwritten `cpp/core` layer and the generated `cpp/gen` layer.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-02] Reran sample parity in split batches and confirmed artifact size+CRC32 matches for `01-05,07-13,15,17,18`, leaving only three GIF CRC32 mismatches: `06_julia_parameter_sweep`, `14_raymarching_light_cycle`, and `16_glass_sculpture_chaos`.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-03] Changed `pi/e` in `src/runtime/cpp/gen/std/math.cpp` to declaration-time initialization, fixing the diff caused by missing module init calls, then reran `tools/runtime_parity_check.py --targets cpp --case-root sample 06_julia_parameter_sweep 14_raymarching_light_cycle 16_glass_sculpture_chaos` and confirmed all three cases passed.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S4-02] Reran the 18 samples in split groups (`01,18` / `11` / `06,14,16` / `02,03,04,05,07,08,09,10` / `12,13,15,17`) and confirmed `pass=18, fail=0`, including artifact size+CRC32.
- Progress memo: [ID: P0-RUNTIME-ROOT-RESET-CPP-01-S5-01] Updated `docs/ja/spec/spec-runtime.md` to the `runtime/cpp/{core,gen}` layout, explicitly documenting the `--emit-runtime-cpp` regeneration procedure, guard/parity validation commands, and the ban on reintroducing `src/runtime/cpp/pytra/`.

### P0: realign `pytra-cli` responsibilities (naming unification + target-branch removal)

Context: [docs/ja/plans/p0-pytra-cli-boundary-and-dispatch-removal.md](../plans/p0-pytra-cli-boundary-and-dispatch-removal.md)

1. [x] [ID: P0-PYTRA-CLI-REALIGN-01] Reorganize `pytra-cli` around backend profiles and remove target-language-specific branching from the CLI body.
2. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S1-01] Document the responsibility boundary of the CLI body / backend profiles / execution runner and fix the prohibited patterns.
3. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S1-02] Rename to `src/pytra-cli.py` and update references in `./pytra`, parity, and tooling.
4. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S2-01] Extract target-specific build/run/transpile contracts to toolchain-side profiles.
5. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S2-02] Slim `src/pytra-cli.py` down to common dispatch only and remove target branching.
6. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S2-03] Make target-incompatible options (for example `--codegen-opt`) fail fast through profile validation.
7. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S3-01] Align parity/tooling to the new CLI contract and reduce duplicated target-specific literals.
8. [x] [ID: P0-PYTRA-CLI-REALIGN-01-S3-02] Update unit/parity/docs and lock regression behavior.
- Progress memo: [ID: P0-PYTRA-CLI-REALIGN-01-S1-02] Retired `src/pytra_cli.py` in favor of `src/pytra-cli.py`, and updated references in `./pytra`, `tools/runtime_parity_check.py`, `test_pytra_cli`, and user docs.
- Progress memo: [ID: P0-PYTRA-CLI-REALIGN-01-S1-01] Added a `pytra-cli` boundary section to `docs/ja|en/spec/spec-make.md`, fixing responsibilities and prohibitions for the CLI body/profile/runner split, including the ban on hand-written target branching.
- Progress memo: [ID: P0-PYTRA-CLI-REALIGN-01-S2-01] Added `src/toolchain/compiler/pytra_cli_profiles.py`, extracting output-path selection and non-C++ build/run contracts out of `pytra-cli`. `test_pytra_cli.py` and `test_pytra_cli_profiles.py` passed.
- Progress memo: [ID: P0-PYTRA-CLI-REALIGN-01-S2-02] Removed `target == ...` branching from `src/pytra-cli.py`, using `TargetProfile.build_driver` to dispatch transpile/build through a shared path.
- Progress memo: [ID: P0-PYTRA-CLI-REALIGN-01-S2-03] Added profile compatibility validation so `--codegen-opt` and `--compiler/--std/--opt/--exe` (when `--build`) fail fast on non-C++ targets. Expanded the tooling unit tests accordingly.
- Progress memo: [ID: P0-PYTRA-CLI-REALIGN-01-S3-01] Replaced hard-coded target order/needs in `tools/runtime_parity_check.py` with `pytra_cli_profiles` references and updated `test_runtime_parity_check_cli.py` to the new CLI contract.
- Progress memo: [ID: P0-PYTRA-CLI-REALIGN-01-S3-02] Fixed incorrect stdout forwarding in non-C++ `run_cmd`, added regression coverage to `test_pytra_cli.py`, and reran `runtime_parity_check` (`sample 01_mandelbrot`, `cpp/java`) to confirm matching output.

### P0: redesign backend runtime/stdlib responsibility boundaries

Context: [docs/ja/plans/p0-backend-runtime-boundary-realign.md](../plans/p0-backend-runtime-boundary-realign.md)

1. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01] Correct backend leakage of runtime/stdlib resolution responsibility and return to resolved-only EAST3 rendering.
2. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-01] Classify audit hits by backend and violation type, and fix the repair order.
3. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-02] Fix the resolved-call contract from EAST3 to backends (`call/attr/module/type`).
4. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Repair high-density violations in `lua/scala/rs` first.
5. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Repair the remaining cases in `cs/php/go/nim/kotlin/js/cpp` under the same policy.
6. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Make emitter fallbacks fail closed and forbid guess-based rendering.
7. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-01] Add responsibility-boundary guards (forbidden branches/strings/dispatch) to mandatory CI paths.
8. [x] [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-02] Update unit/smoke/parity regressions and lock non-regression.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-01] Classified `work/logs/backend_boundary_audit_hits_20260305_s1_01.txt` (179 hits) into `branch/dispatch/runtime-implementation mixing`, and fixed the repair order as `lua -> scala -> rs`.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S1-02] Added the fixed EAST3-to-backend contract to `docs/ja/spec/spec-east.md`, covering resolved attributes, resolution priority, fail-closed behavior, and emitter API constraints.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Removed unused runtime-implementation-mixing blocks from `lua_native_emitter.py`, shrinking `math|gif|png` hits from `49 -> 8`. `test_py2lua_smoke.py` (32 tests) passed.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Removed unused inline runtime helper groups from `scala_native_emitter.py` and unused `RUST_RUNTIME_SUPPORT` from `rs_emitter.py`, shrinking hits to `scala:39 -> 29` and `rs:31 -> 4`, for a total of `138 -> 101` across all backends. `test_py2scala_smoke.py` (16 tests) and `test_py2rs_smoke.py` (30 tests) passed.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-01] Removed the `owner=="math"` re-resolution fallback from `scala_native_emitter.py`, shrinking Scala hits from `29 -> 16` and the all-backend total from `101 -> 88`. Confirmed another green pass for `test_py2scala_smoke.py` (16 tests).
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed raw-AST re-resolution fallbacks for `owner=="math"` in `php_native_emitter.py` and `go_native_emitter.py`, shrinking `php+go` hits from `28 -> 12` and the all-backend total from `88 -> 72`. `test_py2php_smoke.py` (10 tests) and `test_py2go_smoke.py` (16 tests) passed.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed `math` raw-AST re-resolution fallbacks (type inference / call / attr) from `kotlin_native_emitter.py` and `nim_native_emitter.py`, shrinking `kotlin+nim` hits from `8 -> 3` and the all-backend total from `72 -> 67`. `test_py2kotlin_smoke.py` (16 tests) and `test_py2nim_smoke.py` (3 tests) passed.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed raw-AST fallbacks for `owner=="math/png/gif"` (call/attr) from `cs_emitter.py`, shrinking C# hits from `20 -> 8` and the all-backend total from `67 -> 55`. `test_py2cs_smoke.py` kept the existing baseline of `failures=11` with no new regressions.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Removed direct `save_gif` / `write_rgb_png` checks from statement-expression fallbacks in `nim_native_emitter.py`, shrinking Nim hits from `3 -> 1` and the all-backend total from `55 -> 53`. Confirmed another pass of `test_py2nim_smoke.py` (3 tests).
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-02] Classified the remaining hits (`go/cs/js/cpp`) into import/runtime-map resolution, diagnostic strings, and compatibility comments. Raw-AST re-resolution fallbacks were fully removed on the affected backends, leaving `S2-03` (fail-closed strengthening) and `S3-01` (CI guards) to lock the boundary.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Slimmed fallback behavior for unresolved stdlib runtime calls in `php/go`, making the `resolved_runtime_call` path fail closed. For Go, the legacy form `runtime_call=std::filesystem::*` still keeps the old fallback because target-side resolution remains incomplete. `test_py2php_smoke.py` (10 tests) and `test_py2go_smoke.py` (16 tests) still passed.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Also made unresolved `resolved_runtime_call` fallbacks exceptional in Kotlin, strengthening fail-closed behavior for stdlib call/attribute handling. `test_py2kotlin_smoke.py` (16 tests) passed.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S2-03] Also made unresolved `resolved_runtime_call` stdlib fallbacks exceptional in `js/cs/nim`, stopping guess-based rendering for calls/attributes. `test_py2js_smoke.py` (22 tests) and `test_py2nim_smoke.py` (3 tests) passed, while `test_py2cs_smoke.py` kept the existing baseline of `failures=11` with no new regressions.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-01] Revalidated the already-integrated guard set in `run_local_ci.py` (`check_emitter_runtimecall_guardrails.py` / `check_emitter_forbidden_runtime_symbols.py`) in real operation, and changed the `forbidden` side to allow an empty allowlist when `findings=0`. Both guards and the tooling unit tests passed.
- Progress memo: [ID: P0-BACKEND-BOUNDARY-REALIGN-01-S3-02] Added fail-closed regressions for unresolved `resolved_runtime_call` to smoke tests for `go/php/kotlin/js/cs/nim`, then passed the combined smoke suite (`go/php/kotlin/js/nim/cs`, 117 tests total), `check_emitter_runtimecall_guardrails.py`, `check_emitter_forbidden_runtime_symbols.py`, and tooling (`test_check_emitter_runtimecall_guardrails.py` / `test_runtime_parity_check_cli.py`). Also added semantic-tag consistency checks to the `go/php/kotlin/cs` emitters, closing remaining gaps where `resolved_runtime_call` still fell back to guess-based rendering.

### P1: unify runtime generators (remove special scripts)

Context: [docs/ja/plans/p1-runtime-generator-unification.md](../plans/p1-runtime-generator-unification.md)

1. [x] [ID: P1-RUNTIME-GEN-UNIFY-01] Unify runtime generation under the canonical `pytra-cli`/`py2x` path and remove language-specific special generators.
2. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S1-01] Inventory responsibility differences among the three existing generators (image/java/cs) and fix the unification requirements.
3. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S1-02] Define a declarative configuration with target/output destination/marker and move in-code branching into configuration.
4. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S2-01] Implement a generic generator (single script) and integrate it into `pytra-cli`/`py2x` calls.
5. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S2-02] Replace existing callers with the new path.
6. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S2-03] Delete `tools/gen_image_runtime_from_canonical.py`, `tools/gen_java_std_runtime_from_canonical.py`, and `tools/gen_cs_image_runtime_from_canonical.py`.
7. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S3-01] Add a CI guard against reintroducing special generators.
8. [x] [ID: P1-RUNTIME-GEN-UNIFY-01-S3-02] Lock non-regression with runtime audits and parity regression.
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S1-01] Inventoried input/output naming and post-process differences across `gen_image/gen_java_std/gen_cs_image` in the plan and fixed the requirements for moving to a single path.
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S1-02] Added `tools/runtime_generation_manifest.json`, turning module/target/output/marker/postprocess (CS helper) into declarative settings. Added unit tests for the manifest structure.
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S2-01] Added `tools/gen_runtime_from_manifest.py`, unifying `py2x` generation, marker insertion, and `cs_program_to_helper` post-processing under a manifest-driven path. Added tooling unit coverage (`test_gen_runtime_from_manifest.py`).
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S2-02] Switched `test_gen_image_runtime_from_canonical.py` to the new generator and moved existing caller paths over to `gen_runtime_from_manifest`.
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S2-03] Deleted the three old special generators and updated the `generated-by` expectation in `test_audit_image_runtime_sot.py` to `tools/gen_runtime_from_manifest.py`.
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S3-01] Added `tools/check_runtime_special_generators_absent.py` and integrated it into `run_local_ci.py`, making reintroduction of `gen_*_from_canonical.py` fail fast in CI.
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S3-02] Reran runtime audits and removed one stale allowlist item in `check_runtime_std_sot_guard.py` (`json src/runtime/java/pytra-core/built_in/PyRuntime.java`). Parity for `01_mandelbrot` on `cpp,java,ruby,lua,php` remained in progress because Java still had a syntax-collapse issue in the transformed `png.py`.
- Progress memo: [ID: P1-RUNTIME-GEN-UNIFY-01-S3-02] Made `gen_runtime_from_manifest.py` independent from runtime hooks and regenerated Java `pytra-gen/utils/png.java,gif.java`. Strengthened `java_native_emitter.py` and `PyRuntime.java`, but parity still remained incomplete because of Java-side issues such as `tmp` class names, `PngHelper`, `uint8`, and `open/PyFile`.

### P2: unify `check_py2*` checkers (`--target` + profiles)

Context: [docs/ja/plans/p2-checker-unification.md](../plans/p2-checker-unification.md)

1. [x] [ID: P2-CHECKER-UNIFY-01] Unify checkers under `tools/check_py2x_transpile.py --target <lang>` and retire language-specific `check_py2*.py` in stages.
2. [x] [ID: P2-CHECKER-UNIFY-01-S1-01] Inventory differences among existing `check_py2*.py` scripts (case selection / expected failures / extra validations) and define a unified spec.
3. [x] [ID: P2-CHECKER-UNIFY-01-S1-02] Design the target-profile format.
4. [x] [ID: P2-CHECKER-UNIFY-01-S2-01] Implement the unified checker body (`check_py2x_transpile.py`).
5. [x] [ID: P2-CHECKER-UNIFY-01-S2-02] Convert existing `check_py2*.py` scripts into compatibility wrappers that delegate to the new checker.
6. [x] [ID: P2-CHECKER-UNIFY-01-S2-03] Replace checker calls in `run_local_ci.py`, contract-verification scripts, and docs with the unified checker.
7. [x] [ID: P2-CHECKER-UNIFY-01-S3-01] Delete `check_py2*.py` after the compatibility period and add a guard against reintroduction.
8. [x] [ID: P2-CHECKER-UNIFY-01-S3-02] Lock non-regression through unit/CI regressions.
- Progress memo: [ID: P2-CHECKER-UNIFY-01-S1-01] Inventoried 14 `check_py2*_transpile.py` scripts and fixed the difference axes as `case_mode(all|explicit)`, structured expected-fail rules, quality hooks, and extra CLI flags. Reflected the unified spec in `p2-checker-unification.md`.
- Progress memo: [ID: P2-CHECKER-UNIFY-01-S1-02] Fixed the unified-checker profile schema as `target/case_mode/cases/expected_failures/quality_hooks/flags/stage2_probe` and defined normalization rules for simple expected-fail sets.
- Progress memo: [ID: P2-CHECKER-UNIFY-01-S2-01] Added `tools/check_py2x_transpile.py` and `check_py2x_profiles.json`, implemented the shared runner, ported the `cpp/java/scala` profiles, and confirmed green smoke for `sample/py/01_mandelbrot.py` on three targets.
- Progress memo: [ID: P2-CHECKER-UNIFY-01-S2-01] Expanded profiles to all 14 targets, unified validation of `php_sample18`, runtime sidecars, and `cpp_emitter_separation` preflight, and passed `sample/py/01_mandelbrot.py` smoke on all 14 targets with `--skip-east3-contract-tests`.
- Progress memo: [ID: P2-CHECKER-UNIFY-01-S2-02] Turned all 14 existing `check_py2*.py` scripts into compatibility wrappers delegating to `check_py2x_transpile.py --target`. Also moved C++-specific compatibility flags such as `--check-multi-file-imports` and `--check-yanesdk-smoke` into the unified path and confirmed execution through the `nim/cpp/js` wrappers.
- Progress memo: [ID: P2-CHECKER-UNIFY-01-S2-03] Replaced checker calls in `run_local_ci.py`, `check_noncpp_east3_contract.py`, and `check_gsk_native_regression.py` with `check_py2x_transpile.py --target`; `js/ts` now get `--skip-east3-contract-tests` to avoid recursive preflight. `check_noncpp --skip-transpile`, `check_py2js_transpile.py`, and `check_py2ts_transpile.py` all passed.
- Progress memo: [ID: P2-CHECKER-UNIFY-01-S3-01] Deleted the old 14 `check_py2*.py` files, added `check_legacy_transpile_checkers_absent.py` plus tooling unit coverage, integrated the reintroduction guard into `run_local_ci.py`, and migrated Scala checker unit tests to the `check_py2x_transpile.py` contract.

### P2: bring multi-language runtimes to C++ parity (redesigned: strict SoT + generated-first)

Context: [docs/ja/plans/p2-runtime-parity-with-cpp.md](../plans/p2-runtime-parity-with-cpp.md)

1. [x] [ID: P2-RUNTIME-PARITY-CPP-02] Redesign multi-language runtime parity under the principles of strict SoT ownership, generated-first delivery, and clear responsibility separation, replacing it with an operational model that cannot regress in the same way.
2. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S1-01] Remove the old P2 (`P2-RUNTIME-PARITY-CPP-01`) from the unfinished list and replace it with the new P2.
3. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S1-02] Explicitly document SoT/pytra-core/pytra-gen boundaries and prohibitions in `docs/ja/spec`.
4. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S1-03] Create a classification table of target modules as either "must be generated" or "core allowed".
5. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S2-01] Add a static check that detects pass-through naming violations in `pytra-gen`.
6. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S2-02] Strengthen auditing of markers (`source/generated-by`) and placement violations (core/gen mixing), and integrate it into CI.
7. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S2-03] Inventory traces of SoT reimplementation inside `pytra-core` and reflect them in the migration plan to `pytra-gen`.
8. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S3-01] Use Java as the lead target and move emitter branches that directly encode runtime function names onto the IR-resolved path.
9. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S3-02] Expand the same policy to non-C++ backends other than Java (`cs/js/ts/go/rs/swift/kotlin/ruby/lua/scala/php/nim`).
10. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S3-03] Turn the prohibition on hard-coded library function names in emitters into a lint rule and make it fail fast in PR/CI.
11. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Rerun sample parity for all target languages (including artifact size+CRC32) and fix the resulting diffs.
12. [x] [ID: P2-RUNTIME-PARITY-CPP-02-S4-02] Reflect the operating procedure (local/CI) in `docs/ja` / `docs/en`.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S1-01] Cleaned up the unfinished-list residue of the old P2 and replaced `docs/en/todo/index.md` / `docs/en/plans/p2-runtime-parity-with-cpp.md` with the new P2 (`...-CPP-02`).
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S1-02] Added a common SoT/pytra-core/pytra-gen responsibility-boundary section for all languages to `docs/ja/spec/spec-runtime.md`, fixing required rules (generated-first, markers, EAST3-resolved rendering), prohibited rules (core reimplementation, special naming, emitter-side hard-coded branches), and audit commands.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S1-03] Added a `std/utils` module classification table to `docs/ja/spec/spec-runtime.md`, fixing `argparse..typing` and `assertions/gif/png` as generated-only, while permitting `dataclasses_impl/math_impl/time_impl` under core as implementation-boundary cases.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S2-01] Added `tools/check_runtime_pytra_gen_naming.py` to detect pass-through naming and placement violations in `pytra-gen/std|utils`. The existing debt was made explicit in `tools/runtime_pytra_gen_naming_allowlist.txt` (11 items), and both `test_check_runtime_pytra_gen_naming.py` and the main script run passed.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S2-02] Added `tools/check_runtime_core_gen_markers.py` to audit all-language `pytra-gen` marker requirements and forbid `pytra-core` mixing, then integrated both `check_runtime_core_gen_markers.py` and `check_runtime_pytra_gen_naming.py` into `tools/run_local_ci.py`. `test_check_runtime_core_gen_markers.py` and both guard scripts passed (baseline allowlist: marker=1, naming=11).
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S2-03] Created `work/logs/runtime_core_sot_reimpl_inventory_20260305_s2_03.tsv` to inventory SoT reimplementation traces in `pytra-core` (10 files), and added language-by-language migration waves (JSON family / image family / core boundary) to `docs/ja/plans/p2-runtime-parity-with-cpp.md`.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S3-01] Audited remaining hard-coded runtime branching in the Java emitter, then rechecked convergence on the IR-resolved path with `check_emitter_runtimecall_guardrails.py` (strict Java) and `test_py2java_smoke.py` (25 tests), marking the item complete.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S3-02] For non-Java backends, passed `check_emitter_runtimecall_guardrails.py` (no findings across all backends) and reran smoke suites (`rs/rb/lua/scala/swift/ts=127 tests`, plus the already-run `go/php/kotlin/js/nim/cs=117 tests`), confirming rollout of the IR-resolved path plus fail-closed policy.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S3-03] Reconfirmed that `check_emitter_runtimecall_guardrails.py` and `check_emitter_forbidden_runtime_symbols.py` were already fixed as mandatory steps in `tools/run_local_ci.py`, and, together with strict backend (`java`) fail-fast conditions, finalized the reject rules on the PR/CI side.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Reran sample parity and fixed the 14-target results for `01_mandelbrot` / `02_raytrace_spheres` in `work/logs/runtime_parity_sample01_multilang_20260305.json` / `work/logs/runtime_parity_sample02_multilang_20260305.json`, locking the failure categories (`js/ts`, `go`, `nim`, `cpp(02)`, `java(02)`).
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Fixed Nim's invalid module-name issue (forcing output name `main.nim` and capturing parity-side `stderr`). Nim parity for `01/02` then passed with artifact size+CRC32 matches, shrinking the remaining failures to `js/ts, go, cpp(02), java(02)`.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Began rerunning full sample parity and identified blockers for `js/ts/go/nim/java/cpp` at the `01-03` stage (`js/png.js` leftover f-string, `go/png.go` broken strings, Nim module-name restriction, Java `tmp` class names, C++ runtime link collisions). `rs/cs/ruby/lua/php/swift/kotlin/scala` already matched artifact size/CRC32 over the same range.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Revised the canonical `png.py` toward backend compatibility and regenerated `pytra-gen/utils/png.*` for `js/ts/go`. On rechecking `01_mandelbrot`, `js/ts` moved past syntax errors to an unresolved `write_rgb_png` export stage, while `go` moved past broken strings but still had `extend/main duplication/undefined pyWriteRGBPNG`.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Added pre-run cleanup of target-specific output directories to `runtime_parity_check.py`, then reran `01_mandelbrot/02_raytrace_spheres` for `js,ts,go,cpp,java,nim`, confirming artifact size+CRC32 matches (`work/logs/runtime_parity_sample01_02_focus_20260305_after_freshfix.json`).
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Restored `src/runtime2/{rs,cs,js,ts,go,java,swift,kotlin,ruby,lua,scala,php,nim}` back under `src/runtime/`, removing the `runtime source not found` class of errors.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Fixed `resolved_runtime_call` symbol resolution in the PHP runtime and changed append-list handling in `pytra-gen/runtime/{png,gif}.php` to pass-by-reference, then reran `01_mandelbrot` / `02_raytrace_spheres` across all 14 targets and confirmed artifact size+CRC32 matches (`work/logs/runtime_parity_sample01_02_all_targets_after_php_runtime_fixes_20260305.json`).
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Fixed Nim's `save_gif` return type (`int`) and `math.pi` conversion, then reran `04_orbit_trap_julia` through `06_julia_parameter_sweep` across all 14 targets, confirming artifact size+CRC32 matches (`work/logs/runtime_parity_sample04_06_all_targets_after_nim_fix_20260305.json`).
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-01] Fixed the remaining failures in Lua/Ruby/Scala/Nim/PHP/Scala (Lua: truthiness in if-expressions, bit ops, local scope; Ruby/Scala: bit ops; Scala: `stdlib.method` owner injection; Nim: `sqrt(int)` cast; PHP: `stdlib.method.pop` fallback), then reran all 14 targets in split batches for `01-03/04-06/07-09/10-12/13-15/16-18`, confirming `pass=18, fail=0` across the board.
- Progress memo: [ID: P2-RUNTIME-PARITY-CPP-02-S4-02] Added a runtime-parity runbook to `docs/ja/how-to-use.md` and `docs/en/how-to-use.md` (commands for all 14 targets, artifact `size/CRC32` validation, automatic stale-artifact cleanup, recommended case splitting) and fixed the shared local/CI operating procedure.

### P0: move C++ `@extern` modules to header-only generation

Context: [docs/ja/plans/archive/20260306-p0-cpp-extern-header-only-gen.md](../plans/archive/20260306-p0-cpp-extern-header-only-gen.md)

1. [x] [ID: P0-CPP-EXTERN-HDRONLY-01] Using `pytra.std.math` as the lead case, complete the migration where `@extern` modules generate only `.h` under `gen` and keep the implementation fixed as the source of truth under `core`.
2. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S1-01] Organize the EAST attribute contract for `@extern` functions/variables and fix the minimum flags the C++ backend should read.
3. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S1-02] Implement extern-only module detection in C++ runtime generation and suppress generation of `gen/*.cpp`.
4. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S2-01] Move the `math` implementation to `runtime/cpp/core/std/math.cpp` as the source of truth and remove dependency on `gen/std/math.cpp`.
5. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S2-02] Update the build manifest / backend registry so they reference the core implementation of `math`.
6. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S3-01] Add unit regressions that fix the contract "generate `math.h`, do not generate `.cpp`".
7. [x] [ID: P0-CPP-EXTERN-HDRONLY-01-S3-02] Confirm non-regression through C++ fixture/sample parity and record the result in the decision log.

### P0: slim down C++ `py_runtime.ext.h` (move behavior back to pure-Python sources)

Context: [docs/ja/plans/archive/20260306-p0-cpp-pyruntime-ext-slimming.md](../plans/archive/20260306-p0-cpp-pyruntime-ext-slimming.md)

1. [x] [ID: P0-CPP-PYRUNTIME-EXT-01] Remove high-level built-in/std implementations from `py_runtime.ext.h` when they can be moved back to pure-Python sources, and unify them under `*.gen.*`.
2. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S1-01] Inventory functions in `py_runtime.ext.h` and fix the split between `ABI/core` and "can be moved back to pure-Python source of truth".
3. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S1-02] Fix the mapping of source of truth / generated artifacts / adapters for `sub`, `ArgumentParser`, `py_any`, `py_all`, string ops, `py_range`, and `py_repeat` in the decision log.
4. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-01] Remove duplicate ext-side implementation of `sub(...)` and unify on references to `re.gen.*`.
5. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-02] Remove `ArgumentParser` from ext and align the C++ path so `argparse.gen.*` becomes the canonical runtime.
6. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-03] Remove `py_any` / `py_all` from ext and unify on references to `predicates.gen.*`.
7. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-04] Remove `py_strip` / `py_startswith` / `py_endswith` / `py_find` / `py_replace` from ext and unify on references to `string_ops.gen.*`.
8. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S2-05] Remove `py_range` / `py_repeat` from ext and unify on references to `sequence.gen.*`.
9. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S3-01] Decide the split for `py_enumerate` / `py_reversed` / `py_contains` between pure-Python sources of truth and typed adapters, and add the required `src/pytra/built_in/*.py`.
10. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S3-02] For at least one of those three families, carry implementation through ext slimming and leave the remainder in a state that can continue under the same plan.
11. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S4-01] Add a static guard so duplicate canonical implementations cannot re-enter `py_runtime.ext.h`.
12. [x] [ID: P0-CPP-PYRUNTIME-EXT-01-S4-02] Update layout / SoT guard / runtime parity / runtime-emit smoke and lock non-regression after slimming.
- Progress memo: As the first wave on 2026-03-06, removed `sub`, `ArgumentParser`, `py_any`, `py_all`, string helpers, `py_range`, and `py_repeat(str, int)` from `py_runtime.ext.h`, unifying them into `re.gen.*`, `argparse.gen.*`, `predicates.gen.*`, `string_ops.gen.*`, and `sequence.gen.*`. Only `py_repeat` for `list<T>` remained as a thin typed adapter in `sequence.ext.h`.
- Progress memo: As the second wave on 2026-03-06, added `iter_ops.py` and moved the object paths of `py_reversed` / `py_enumerate` into `iter_ops.gen.*`. Typed `list<T>` / `str` / `any` adapters and `py_enumerate_list_as<T>` were split into `iter_ops.ext.h`, removing iterator-helper bodies from `py_runtime.ext.h`.
- Progress memo: Added `contains.py` on 2026-03-06 and moved the object path of `py_contains` (`dict/list/set/str`) into `contains.gen.*`. Typed fastpaths for `dict/list/set/str/tuple` and object dispatch were split into `contains.ext.h`, removing the `py_contains` body from `py_runtime.ext.h`.
- Progress memo: Updated pure-Python unit tests to use public names for `predicates/string_ops/contains`, and passed `check_runtime_cpp_layout.py`, `check_runtime_std_sot_guard.py`, `verify_image_runtime_parity.py`, syntax-only compile checks for built_in/re, and `test_pytra_built_in_{sequence,predicates,string_ops,contains}.py`.

### P0: replace `cpp_list_model=pyobj` alias preservation with `rc<list<T>>`

Context: [docs/ja/plans/archive/20260306-p0-cpp-pyobj-rc-list-alias.md](../plans/archive/20260306-p0-cpp-pyobj-rc-list-alias.md)

1. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01] Replace `cpp_list_model=pyobj` alias preservation with `rc<list<T>>` rather than `object`, preserving typed element information while achieving Python-compatible shared semantics.
2. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S1-01] Fix the responsibility boundaries among `object`, `list<T>`, and `rc<list<T>>` in the plan/spec, and explicitly mark them as out of scope for `@extern` ABI.
3. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S1-02] Lock fixture-based generated cases where the current alias fallback still inserts `object` boxing.
4. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-01] Add typed-handle helpers for `rc<list<T>>` (creation/reference/value conversion) to the C++ runtime.
5. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-02] Add `rc<list<T>>` overloads for `py_len/py_append/py_extend/py_pop/py_slice/py_at`.
6. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-03] Add minimal runtime adapters for `rc<list<T>> <-> object` and `rc<list<T>> <-> list<T>`.
7. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-01] Switch alias-shared-name detection in the emitter from `object` fallback to `rc<list<T>>` declarations.
8. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-02] On `Assign/AnnAssign` (`b = a`, empty-list init, literal init), stop emitting `make_object(...)` and use handle copies / handle creation instead.
9. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-03] Update rendering of method calls / subscripts / len / slices / truthiness to be aware of `rc<list<T>>`.
10. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S4-01] Reorganize adapter insertion conditions between `rc<list<T>>` and `list<T>` for function arguments, return values, and callsite coercion, keeping `list<T>` at ABI boundaries.
11. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S4-02] Leave `object` boxing only on flows into `Any/object` and shrink/remove alias-specific `object` fallbacks.
12. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-01] Add/update alias fixtures, runtime unit tests, and C++ backend unit tests to lock regressions.
13. [x] [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-02] Confirm compile/run on representative samples (at least `sample/18`) and record the result in the decision log.
- Progress memo: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S2-01] Generalized `RcObject`, made `list<T>` eligible for RC management, and added `rc_list_new / rc_list_from_value / rc_list_ref / rc_list_copy_value` plus the `py_is_rc_list_handle` trait. Also implemented `rc<list<T>>` overloads for `py_len/py_append/py_extend/py_pop/py_slice/py_at/py_set_at/py_clear/py_reverse/py_sort`, `make_object(const rc<list<T>>&)`, and `obj_to_rc_list<T>`.
- Progress memo: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S3-01] Split alias-name detection from `object` runtime lists by introducing `_uses_pyobj_rc_list_expr()`. The generated result for `list_alias_shared_mutation.py` became `rc<list<int64>> a = rc_list_from_value(list<int64>{1, 2}); rc<list<int64>> b = a;`, eliminating the old `make_object(...)` fallback.
- Progress memo: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S4-01] Added adapters so call/return boundaries emit `consume(b) -> consume(rc_list_ref(b))` and `return b -> return rc_list_copy_value(b);`.
- Progress memo: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-01] Added a function-boundary parity case to `test_py2cpp_list_pyobj_model.py`, locking `consume(b)` / `return b` / `bool(b)` / `c[0]` by comparing Python and C++ under `cpp_list_model=pyobj`.
- Progress memo: [ID: P0-CPP-PYOBJ-RCLIST-ALIAS-01-S5-02] Rechecked `sample/18` parity in `test_py2cpp_list_pyobj_model.py` and ad hoc alias smokes (append/pop/subscript/slice/truthiness/reverse/sort, argument boundaries, return-value boundaries) with `g++ -std=c++20`, and all passed.

### P0: make the runtime symbol index data-driven (SoT-generated JSON + IR normalization)

Context: [docs/ja/plans/archive/20260306-p0-runtime-symbol-index-data-driven.md](../plans/archive/20260306-p0-runtime-symbol-index-data-driven.md)

1. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01] Move runtime symbol ownership and companion rules into SoT-generated JSON and switch IR/backends/tooling to that index.
2. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-01] Inventory current hard-coded runtime-symbol sites and fix a table separating "information kept in IR", "information moved to the index", and "information derived by backends".
3. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-02] Define the `runtime symbol index` schema and document responsibilities for `module / symbol / target artifact / companion`.
4. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-01] Add a generator that scans `src/pytra/{built_in,std,utils}` and `src/runtime/<lang>/{core,built_in,std,utils}` and emits the index JSON.
5. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-02] Add unit tests for the generator and lock representative cases such as `py_enumerate`, `py_any`, `py_strip`, `perf_counter`, `write_rgb_png`, and `Path` in the index.
6. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-03] Integrate the index generator into CI/local checks so stale indexes fail fast when the runtime layout changes.
7. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-01] Add `runtime_module_id` and `runtime_symbol` to EAST3 runtime-call nodes so they no longer depend only on raw `runtime_call` strings.
8. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-02] Extend import-resolution paths so resolved symbols (`from X import Y` / `import X` + `X.Y`) also carry `runtime_module_id` / `runtime_symbol`.
9. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-03] Retire hard-coded runtime symbols from `signature_registry.py` in stages until at least no responsibility remains for guessing file paths.
10. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-01] Use the C++ backend first and switch include collection, namespace resolution, and runtime-source collection to the index JSON.
11. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-02] Reorganize `build_multi_cpp.py` / `gen_makefile_from_manifest.py` so they derive `*.gen.*` and `*.ext.*` companions consistently through the index.
12. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-03] Remove ownership-guessing logic for `py_enumerate` / `py_any` / `py_strip` / `dict.get` / `perf_counter` / `Path` from the C++ emitter and shift it to IR + index.
13. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-01] Organize the policy for non-C++ backends and align the boundary between `resolved_runtime_call` and module/file resolution around the index.
14. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-02] Explicitly document in `docs/ja/spec` that IR carries module+symbol, while target file paths are derived by the index + backend.
15. [x] [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-03] Run representative regressions (C++ include resolution, runtime build graph, import resolution, unit parity) and confirm that existing ad hoc fallbacks are no longer needed.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-01] Inventoried hard-coded sites and fixed the current role split as follows: `core.py` keeps raw `runtime_call`, `signature_registry.py` maps runtime symbols, C++ backend files (`module.py`, `runtime_paths.py`, `runtime_calls.json`) infer modules/files/namespaces, and tooling (`build_multi_cpp.py`, `gen_makefile_from_manifest.py`) recollects runtime sources from include roots. The new split is `IR = module+symbol`, `index = artifact+companion`, `backend/tooling = rendering and build-graph derivation`.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S1-02] Fixed the index schema by separating `modules` and `targets`, splitting target-independent symbol ownership from target-specific artifact information. Explicitly documented `public_headers` / `compile_sources` / `companions` as the minimal set and prohibited embedding paths into IR.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-01] Added `tools/gen_runtime_symbol_index.py`, which scans `src/pytra/{built_in,std,utils}` and the modern runtime layout (`src/runtime/<lang>/{core,built_in,std,utils}`) to generate `tools/runtime_symbol_index.json`. In the first stage, only targets with the modern layout are indexed; legacy runtime layouts are left out of scope.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-02] Fixed representative cases in `test/unit/tooling/test_runtime_symbol_index.py`, checking module/symbol/artifact/companion mapping for `py_enumerate_object`, `py_any`, `py_strip`, `perf_counter`, `write_rgb_png`, and `Path`.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S2-03] Added `--check` to the generator and integrated it into `tools/run_local_ci.py`, making stale indexes fail fast.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-01] Added `runtime_module_id` and `runtime_symbol` alongside `runtime_call` in EAST3 `BuiltinCall`. In the first stage, `runtime_call` remains for compatibility, but target-independent forms such as `perf_counter -> pytra.std.time/perf_counter`, `enumerate -> pytra.built_in.iter_ops/enumerate`, `any -> pytra.built_in.predicates/any`, `dict.get -> pytra.core.dict/dict.get`, and `Path.exists -> pytra.std.pathlib/Path.exists` are now attached. `test_east_core.py` could not run directly because of an existing `toolchain.compiler.east_parts` import breakage, so the values were confirmed via ad hoc execution using the same API.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-02] Extended import-resolved symbols so they also carry `runtime_module_id` / `runtime_symbol`. Via ad hoc execution, confirmed that both `Call` and `Attribute` nodes get module/symbol assignments for `json.loads` from `from pytra.std import json`, `png.write_rgb_png` from `from pytra.utils import png`, and `math.sin` / `math.pi` from `import math`.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S3-03] Locked in `test_stdlib_signature_registry.py` that artifact-path-guessing responsibilities such as `runtime/cpp`, `*.gen.*`, `*.ext.*`, and `src/runtime` do not re-enter `signature_registry.py`. At this stage, runtime symbol / type / owner-method contracts remain, but file-path inference is fully separated into the generator + index side.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-01] Extended the index generator to `pytra.core.*` artifacts and added `canonical_runtime_module_id`, `resolve_import_binding_runtime_module`, and `lookup_cpp_namespace_for_runtime_module` to `runtime_symbol_index.py`. The C++ backend switched import include collection and namespace resolution in `module.py` to the index, and unit/integration tests now fix that C++ output for `from pytra.utils import png`, `from pytra.std.time import perf_counter`, and `import math` resolves includes and namespaces via the index.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-02] Added reverse index lookup to `tools/cpp_runtime_deps.py` and shifted runtime-companion derivation in `build_multi_cpp.py` / `gen_makefile_from_manifest.py` to `runtime_symbol_index.json`. The existing test that derives `math.ext.cpp` from a forwarder header (`test_cpp_runtime_build_graph.py`) passed.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S4-03] Made the C++ emitter prioritize `runtime_module_id/runtime_symbol` rather than raw `runtime_call` strings in `BuiltinCall` dispatch and imported-symbol call rendering. The target set included `py_enumerate`, `py_any`, `py_all`, `py_strip` family calls, `dict.get`, `perf_counter`, and `Path`. `test_cpp_runtime_symbol_index_integration.py` now fixes both the presence of the binding in IR and lowering to namespaced calls in C++ output.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-01 / -S5-02] Explicitly documented in this plan and in `docs/ja/spec/{spec-runtime.md,spec-east.md}` that non-C++ backends must also use `runtime_module_id + runtime_symbol + runtime_symbol_index` as the only ownership-decision path. Backends may still derive target-specific import/file paths, but must not reintroduce per-backend handwritten ownership tables.
- Progress memo: [ID: P0-RUNTIME-SYMBOL-INDEX-01-S5-03] As representative regressions, ran `tools/gen_runtime_symbol_index.py --check`, `test_runtime_symbol_index.py`, `test_cpp_runtime_build_graph.py`, `test_cpp_runtime_symbol_index_integration.py`, and `tools/runtime_parity_check.py --targets cpp --case-root fixture`, confirming that C++ include resolution, runtime build graph, import resolution, and fixture parity all pass under the index-based contract. `math_extended`, `pathlib_extended`, and `inheritance_virtual_dispatch_multilang` all passed.

### P0: root-cause repair of C++ unit regressions (align SoT/IR/Emitter/Runtime contracts)

Context: [docs/ja/plans/archive/20260306-p0-cpp-unit-regression-recovery.md](../plans/archive/20260306-p0-cpp-unit-regression-recovery.md)

1. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01] Repair C++ unit regressions at the root cause in the order SoT -> IR -> Emitter -> Runtime, and return unit + fixture/sample parity to green.
2. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S1-01] Reclassify failing tests into `generated runtime`, `import/include resolution`, `container semantics`, and `emitter/CLI contract`, then fix ownership by layer.
3. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S2-01] Resolve generated-runtime breakage in `json` by fixing the SoT and the C++ runtime-generation contract (no hand-editing of `.gen.*`).
4. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S2-02] Resolve generated-runtime breakage in `argparse` by fixing the SoT, reserved-name avoidance, and class/member emission contracts.
5. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S3-01] Fix import resolution, include dedupe/sort, and one-to-one module-include contracts for `pytra.utils.{png,gif}` and `pytra.std.{time,pathlib}`.
6. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S3-02] Restore `os.path` / `glob` runtime-helper resolution to owner/module metadata and reduce special-case dependence in the C++ emitter.
7. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S4-01] Fix container-view / iterator semantics for `dict.items()` / `dict.get()` / `any()` / dict/set comprehensions by aligning built_in SoT and runtime adapters.
8. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S4-02] Reorganize C++ emitter contracts around `mod_mode`, statement-dispatch fallback, and CLI `dump-options` / error categories, fixing option reflection and diagnostics consistency.
9. [x] [ID: P0-CPP-REGRESSION-RECOVERY-01-S5-01] Rerun the full C++ unit suite, fixture parity, and sample parity, confirm that no regressions remain, and update `docs/ja/todo`.

- Progress memo: [ID: P0-CPP-REGRESSION-RECOVERY-01-S5-01] `python3 -m unittest discover -s test/unit/backends/cpp -p 'test_*.py'` passed all 484 tests. `python3 tools/runtime_parity_check.py --targets cpp --case-root fixture` passed with `pass=3 fail=0`, and `python3 tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples` passed with `pass=18 fail=0`, reconfirming sample artifact `size/CRC32` matches.

## 2026-03-06 Shelved: Sections Deferred Until the ABI Boundary Is Established (1 item)

The following was temporarily shelved from `docs/ja/todo/index.md`. It is not completed; it will be requeued after the ABI responsibility boundary is fixed.

### P4: full self-host rollout across all languages (very low priority)

Context: [docs/ja/plans/archive/20260306-p4-multilang-selfhost-full-rollout.md](../plans/archive/20260306-p4-multilang-selfhost-full-rollout.md)

1. [ ] [ID: P4-MULTILANG-SH-01] Establish self-hosting in stages for `cpp/rs/cs/js/ts/go/java/swift/kotlin/ruby/lua/scala/php/nim`, making multistage monitoring pass across all languages.
2. [ ] [ID: P4-MULTILANG-SH-01-S2-03] Resolve stage2 dependency transpile failures in JS self-host and make multistage pass.
3. [ ] [ID: P4-MULTILANG-SH-01-S3-01] Remove the preview-only state of TypeScript and move it to a generation mode that can execute self-host.
4. [ ] [ID: P4-MULTILANG-SH-01-S3-02] Connect to native-backend tasks for Go/Java/Swift/Kotlin and enable the self-host execution chain.
5. [ ] [ID: P4-MULTILANG-SH-01-S3-03] Add Ruby/Lua/Scala3/PHP/Nim to self-host multistage monitoring targets and resolve the undefined-runner state.
6. [ ] [ID: P4-MULTILANG-SH-01-S4-01] Integrate full-language multistage regressions into the CI path so recurring failure categories are detected continuously.
7. [ ] [ID: P4-MULTILANG-SH-01-S4-02] Document the completion template (pass/fail conditions and exclusion rules by language/stage) and fix the operating rules.
- Completed child tasks (`S1-01` through `S2-02-S3`) and older progress memos were already moved to `docs/ja/todo/archive/20260301.md`.
- Progress memo: [ID: P4-MULTILANG-SH-01-S2-03] Fixed JS-emitter self-host parser constraint violations (`node.get()/node.items()` on `Any`) and lack of support for nested `FunctionDef`, moving the first failure from `stage1_dependency_transpile_fail` to `self_retranspile_fail (ERR_MODULE_NOT_FOUND: ./pytra/std.js)`.
- Progress memo: [ID: P4-MULTILANG-SH-01-S2-03] Added shim generation, import normalization, export injection, and syntax rewrites for JS self-host preparation, resolving `ERR_MODULE_NOT_FOUND`. The first failure then moved to `SyntaxError: Unexpected token ':'` (from `raw[qpos:]`).
- Progress memo: [ID: P4-MULTILANG-SH-01-S2-03] Applied staged fixes for JS self-host: source-side slimming of slice/set checks, ESM shim conversion, redesigned import normalization, compatibility shims for `argparse`/`Path`, a `.py -> EAST3(JSON)` input route, and self-host compatibility in the `JsEmitter` profile loader. The first failure advanced from `ReferenceError/SyntaxError` classes to `TypeError: CodeEmitter._dict_copy_str_object is not a function`.
- Progress memo: [ID: P4-MULTILANG-SH-01-S2-03] Made `CodeEmitter.load_type_map` and JS-emitter initialization object-safe for dict access, and added `set/list/dict` polyfills plus static alias wiring for `CodeEmitter` to the self-host rewrite. The first failure advanced from `dict is not defined` to `TypeError: module.get is not a function`.
- Progress memo: [ID: P4-MULTILANG-SH-01-S2-03] Rewrote self-host `.get` calls to `__pytra_dict_get`, and added `parent/name/stem` property compatibility plus default-idempotent `mkdir(parents/exist_ok)` to the `Path` shim. JS reached `stage1 pass / stage2 pass`, and the first failure moved to `stage3 sample output missing`.
- Progress memo: [ID: P4-MULTILANG-SH-01-S2-03] Added object-safe rewrites in `CodeEmitter/JsEmitter` (removing `startswith/strip/find` dependencies, fixing `next_tmp` f-string handling, and removing `ord/chr` dependence in ASCII helpers) plus String polyfills (`strip/lstrip/rstrip/startswith/endswith/find/lower/upper/map`) in the self-host rewrite. JS kept `stage1/native pass` and `multistage stage2 pass`, and the first failure advanced to `stage3 sample_transpile_fail (SyntaxError: Invalid or unexpected token)`.
- Progress memo: [ID: P4-MULTILANG-SH-01-S2-03] Replaced string-multiplication-based indent generation in `emit` with loops, added a non-string guard for `quote` in `quote_string_literal`, and changed `_emit_function`'s `in_class` test from `None` dependence to empty-string checks. JS kept `stage1/native pass` and `multistage stage2 pass`, and the first failure updated to `stage3 sample_transpile_fail (SyntaxError: Unexpected token '{')`, with unresolved placeholders/function-header corruption in `py2js_stage2.js` remaining.

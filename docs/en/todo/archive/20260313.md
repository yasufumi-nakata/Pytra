# TODO History (2026-03-13)

<a href="../../../ja/todo/archive/20260313.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-13 migration: C++ validator helpers exported on the `toolchain.link` facade

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: promote the C++ backend validator helpers onto the `toolchain.link` facade

Context: [docs/en/plans/archive/20260313-p0-link-facade-cpp-validator-export.md](../plans/archive/20260313-p0-link-facade-cpp-validator-export.md)

1. [x] [ID: P0-LINK-FACADE-CPP-VALIDATOR-EXPORT-01] Re-exported `validate_cpp_backend_input_doc()` and `translate_cpp_backend_emit_error()` from the `toolchain.link` package facade so `typed_boundary` and link tests no longer depend on submodule reach-through.
2. [x] [ID: P0-LINK-FACADE-CPP-VALIDATOR-EXPORT-01-S1-01] Added focused regression / source-contract coverage that requires the facade export and locks the package-surface gap in fail-fast form.
3. [x] [ID: P0-LINK-FACADE-CPP-VALIDATOR-EXPORT-01-S2-01] Switched `toolchain.link.__init__` exports plus `typed_boundary` / link-test imports over to the facade path and brought the targeted unit suite back to green.
4. [x] [ID: P0-LINK-FACADE-CPP-VALIDATOR-EXPORT-01-S3-01] Synchronized TODO / plan / decision log, fixed the close condition, and moved the task into the archive.
- Progress memo: the end state exposes the two C++ backend validator helpers on the `toolchain.link` facade, moves `typed_boundary` to the facade import path, and keeps both the runtime regression in `test_program_loader.py` and the source contract in `test_py2x_entrypoints_contract.py` green.

## 2026-03-13 migration: C++ backend input validator object-tree iterator drift fixed

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: fix the C++ backend input validator drift against the object-tree iterator API

Context: [docs/en/plans/archive/20260313-p0-cpp-backend-input-validator-itertree.md](../plans/archive/20260313-p0-cpp-backend-input-validator-itertree.md)

1. [x] [ID: P0-CPP-BACKEND-INPUT-VALIDATOR-ITERTREE-01] Brought `validate_cpp_backend_input_doc()` in sync with the `_iter_object_tree()` API that now yields `parent_key`, so typed-boundary / backend-input validation no longer stops on tuple-unpack errors.
2. [x] [ID: P0-CPP-BACKEND-INPUT-VALIDATOR-ITERTREE-01-S1-01] Added a focused regression around a minimal raw EAST3 module and locked the current C++ backend-input crash surface in fail-fast form.
3. [x] [ID: P0-CPP-BACKEND-INPUT-VALIDATOR-ITERTREE-01-S2-01] Switched `validate_cpp_backend_input_doc()` to the `parent_key`-aware object-tree iteration while preserving the existing `ForCore.iter_plan` metadata guard and brought the targeted link tests back to green.
4. [x] [ID: P0-CPP-BACKEND-INPUT-VALIDATOR-ITERTREE-01-S3-01] Synchronized the TODO / plan / decision log and locked the close condition.
- Progress memo: the end state teaches `validate_cpp_backend_input_doc()` to consume the 3-value `_iter_object_tree()` API while preserving `ForCore.iter_plan` validation and wording, so a minimal `Module` input no longer stops on a tuple-unpack error.

## 2026-03-13 migration: raw EAST3 validator node-shape misclassification fixed

## 2026-03-13 migration: Pytra-NES representative C++ mini repro contract complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative Pytra-NES C++ mini repro contract

Context: [docs/en/plans/archive/20260313-p0-pytra-nes-cpp-mini-repro-contract.md](../plans/archive/20260313-p0-pytra-nes-cpp-mini-repro-contract.md)

1. [x] [ID: P0-PYTRA-NES-CPP-MINI-REPRO-01] Locked a representative Pytra-NES multi-file package (parenthesized sibling relative import + `dataclass` + `deque[float]` field + `deque` methods) through build/run on the representative C++ lane.
2. [x] [ID: P0-PYTRA-NES-CPP-MINI-REPRO-01-S1-01] Added the representative package smoke and locked the C++ multi-file build/run baseline that combines `from .controller import (...)` with `timestamps: deque[float] = field(init=False, repr=False)`.
3. [x] [ID: P0-PYTRA-NES-CPP-MINI-REPRO-01-S2-01] Turned the generated include / class layout / method-lowering surface into source guards so the representative Pytra-NES lane now fails fast on drift.
4. [x] [ID: P0-PYTRA-NES-CPP-MINI-REPRO-01-S3-01] Synchronized TODO / plan / C++ support wording with the Pytra-NES mini repro contract and moved it into the archive.
- Progress memo: the end state locks transpile/build/run for the `controller.py` / `pad_state.py` / `ppu.py` package while also fixing generated `ppu.cpp` source guards for the `controller.h` / `pad_state.h` includes, `append -> push_back(float64(...))`, `popleft -> front()+pop_front()`, and `len(...) -> (deque).size()` lowering.

## 2026-03-13 migration: raw EAST3 validator node-shape misclassification fixed

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: fix raw EAST3 validator node-shape misclassification

Context: [docs/en/plans/archive/20260313-p0-raw-east3-node-shape-validator.md](../plans/archive/20260313-p0-raw-east3-node-shape-validator.md)

1. [x] [ID: P0-RAW-EAST3-NODE-SHAPE-VALIDATOR-01] Fixed raw EAST3 validator node-shape misclassification so auxiliary `meta` / `kind` keys no longer produce false positives.
2. [x] [ID: P0-RAW-EAST3-NODE-SHAPE-VALIDATOR-01-S1-01] Locked `any_dict_items` / `18_mini_language_interpreter` plus a synthetic auxiliary-map case into regression tests and the plan.
3. [x] [ID: P0-RAW-EAST3-NODE-SHAPE-VALIDATOR-01-S2-01] Narrowed raw EAST3 validation to node-shaped dicts while preserving fail-closed behavior for actual nodes.
4. [x] [ID: P0-RAW-EAST3-NODE-SHAPE-VALIDATOR-01-S2-02] Synchronized targeted backend transpile verification plus the TODO / decision log and closed the validator-origin matrix failures.
- Progress memo: the end state teaches `program_validator.py` to treat only dicts under node containers such as `body/value/target` or dicts with node hint keys as EAST3 nodes, so `any_dict_items` / `18_mini_language_interpreter` no longer stop on raw-validator false positives.

## 2026-03-13 migration: checked-in non-C++ / non-C# runtime `pytra/` lanes fully removed

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: remove the checked-in `pytra/` lanes from every non-C++ / non-C# backend

Context: [docs/en/plans/archive/20260313-p0-noncpp-runtime-pytra-deshim.md](../plans/archive/20260313-p0-noncpp-runtime-pytra-deshim.md)

1. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01] Removed checked-in `src/runtime/<lang>/pytra/**` from every non-C++ / non-C# backend and converged the permanent repo-owned runtime layout on `generated/native` only.
2. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S1-01] Locked the current 12-backend `pytra/**` inventory, delete blocker references, and current->target mapping in plan / contract / checker / test form.
3. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S1-02] Switched active contracts / checkers / spec wording to `generated/native only` and made checked-in `pytra/**` re-entry fail fast.
4. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S2-01] Deleted checked-in `src/runtime/rs/pytra/**` for Rust (`rs`) and removed repo-tree `pytra/**` assumptions from `py2rs`, selfhost, runtime guards, and smoke.
5. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S2-02] Aligned the static family (`go/java/kotlin/scala/swift/nim`) registry / packaging / smoke / tooling on direct `generated/native` references so checked-in `pytra/**` remained only as deletion inventory during the rollout.
6. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S2-03] Physically deleted checked-in `src/runtime/<lang>/pytra/**` for the static family and synced allowlists / inventory / representative smoke to the deletion end state.
7. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S3-01] Reworked JS/TS import paths, shim writers, selfhost, and smoke so repo-tree `src/runtime/{js,ts}/pytra/**` direct-load and compat contracts disappeared.
8. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S3-02] Moved Lua/Ruby/PHP packaging / runtime copy / loader contracts to `generated/native` or output-side staging and removed checked-in `pytra/**` assumptions.
9. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S3-03] Physically deleted checked-in `src/runtime/<lang>/pytra/**` for the script family and updated representative smoke plus contract baselines to the deletion end state.
10. [x] [ID: P0-NONCPP-RUNTIME-PYTRA-DESHIM-01-S4-01] Synchronized docs / TODO / archive references / inventory and closed on the invariant that no checked-in non-C++ / non-C# backend owns `pytra/`.
- Progress memo: the end state shrinks checked-in `find src/runtime -maxdepth 2 -type d -name pytra` results down to `src/runtime/cpp/pytra` only, and limits non-C++ / non-C# runtime ownership to `generated/native` plus output-side staging.

## 2026-03-13 migration: Pytra-NES2 repro cross-backend representative contract complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: promote the Pytra-NES2 repro bundle into a representative cross-backend contract

Context: [docs/en/plans/archive/20260313-p0-pytra-nes2-crossbackend-repro-contract.md](../plans/archive/20260313-p0-pytra-nes2-crossbackend-repro-contract.md)

1. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01] Promoted the Pytra-NES2 repro bundle into a representative cross-backend contract.
2. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S1-01] Inventoried the current repro bundle under `materials/refs/from-Pytra-NES2/` and locked the map of already-covered vs unresolved cases into the plan/docs.
3. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S1-02] Promoted `property_method_call.py` and `list_bool_index.py` into representative fixtures under `test/fixtures/`, with assertion-backed semantics.
4. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S2-01] Added those fixtures to representative smoke for C++/C#/Rust/Go/Java/Kotlin/Scala/Swift/Nim and locked the C++ compile-failure baseline plus the static-family transpile smoke.
5. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S2-02] Added those fixtures to representative smoke for JS/TS/Lua/Ruby/PHP and locked the script-family representative transpile contract.
6. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S2-03] Added common assertion/helper/checker support so `unsupported / preview_only / not_implemented` counts as a failure.
7. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S3-01] Made `property_method_call` green in every backend and synced docs / support wording / decision log.
8. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S3-02] Made `list_bool_index` green in every backend and synced docs / support wording / decision log.
9. [x] [ID: P0-PYTRA-NES2-CROSSBACKEND-REPRO-01-S4-01] Synced the final mapping between `materials/refs/from-Pytra-NES2` and repo fixtures/tests and closed the bundle as fully promoted.
- Progress memo: the end state maps `bytes_truthiness` / `path_stringify` / `field_default_factory_rc_obj` onto existing representative lanes, keeps `property_method_call` / `list_bool_index` green across every backend representative smoke, and leaves the missing `path_alias_pkg/entry.py` out of scope.

## 2026-03-13 migration: non-C++ runtime generated `cpp` baseline parity complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: align non-C++ generated runtime lanes to the `cpp/generated/{built_in,std,utils}` baseline

Context: [docs/en/plans/archive/20260313-p0-noncpp-runtime-generated-cpp-baseline.md](../plans/archive/20260313-p0-noncpp-runtime-generated-cpp-baseline.md)

1. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01] Aligned non-C++ generated runtime lanes to the canonical `cpp/generated/{built_in,std,utils}` baseline and made `generated/` the canonical owner for baseline modules.
2. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S1-01] Extracted the actual module set under `cpp/generated/{built_in,std,utils}` and fixed the 25-module canonical baseline in plan / contract / checker form.
3. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S1-02] Removed `blocked / compare_artifact / no_runtime_module / helper_artifact / native canonical` from the close condition for baseline modules and turned them into fail-fast migration-debt inventory.
4. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S2-01] Materialized the missing generated std/utils baseline for `rs/cs` from the SoT and brought representative smoke / manifest / layout checker to green.
5. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S2-02] Raised the static family (`go/java/kotlin/scala/swift/nim`) generated `built_in/std/utils` baseline to the full set and renamed helper-shaped outputs to canonical basenames.
6. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S2-03] Raised the script family (`js/ts/lua/ruby/php`) generated `built_in/std/utils` baseline to the full set and synchronized generated-first wiring plus package/export behavior.
7. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S3-01] Switched build-profile / selfhost / smoke / runtime-copy contracts to generated-first and, beyond C# and Rust, also deleted the dead JS/TS/PHP `native/std/*` residuals so baseline modules no longer have native owners.
8. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S3-02] Added the full file-compare contract checker and localized generated-module inventory, helper inventory, and legacy residual sources to this contract plus the live tree only.
9. [x] [ID: P0-NONCPP-RUNTIME-GENERATED-CPP-BASELINE-01-S4-01] Synchronized docs / TODO / inventory / archive notes and reduced the follow-up queue to `P0-NONCPP-RUNTIME-PYTRA-DESHIM-01`.
- Progress memo: the end state materializes the 25-module `generated/{built_in,std,utils}` baseline across all 13 non-C++ backends, removes native-owner residuals for baseline modules, and leaves checked-in `pytra/**` removal as the only remaining P0 follow-up.

## 2026-03-13 migration: `collections.deque.count()` / `remove()` representative C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative C++ lane for `collections.deque.count()` / `remove()`

Context: [docs/en/plans/archive/20260313-p0-collections-deque-cpp-searchmut-support.md](../plans/archive/20260313-p0-collections-deque-cpp-searchmut-support.md)

1. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-SEARCHMUT-01] Locked the representative C++ lane for `collections.deque.count()` / `remove()`.
2. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-SEARCHMUT-01-S1-01] Locked the current invalid C++ surface (`count`, `remove`) in focused regressions / TODO / plan.
3. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-SEARCHMUT-01-S2-01] Lowered `count(value)` to a valid `std::count`-based surface.
4. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-SEARCHMUT-01-S2-02] Lowered `remove(value)` to a first-hit erase surface.
5. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-SEARCHMUT-01-S3-01] Synced the build/run smoke and support wording, then closed the task.
- Progress memo: the end state aligns `count/remove` to STL algorithm surfaces and locks the representative build/run output `2 / 2 / 1`.

## 2026-03-13 migration: `collections.deque.rotate()` representative C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative C++ lane for `collections.deque.rotate()`

Context: [docs/en/plans/archive/20260313-p0-collections-deque-cpp-rotate-support.md](../plans/archive/20260313-p0-collections-deque-cpp-rotate-support.md)

1. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ROTATE-01] Locked the representative C++ lane for `collections.deque.rotate()`.
2. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ROTATE-01-S1-01] Locked the current invalid C++ surface (`rotate()`, `rotate(1)`, `rotate(-1)`) in focused regressions / TODO / plan.
3. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ROTATE-01-S2-01] Lowered `rotate()` / `rotate(n)` to valid `::std::rotate(...)` bundles.
4. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ROTATE-01-S3-01] Synced the build/run smoke and support wording, then closed the task.
- Progress memo: the end state aligns `rotate()` / `rotate(n)` to normalized-step `::std::rotate(...)` bundles and locks the representative build/run output `3 / 3 / 2`.

## 2026-03-13 migration: `collections.deque.reverse()` representative C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative C++ lane for `collections.deque.reverse()`

Context: [docs/en/plans/archive/20260313-p0-collections-deque-cpp-reverse-support.md](../plans/archive/20260313-p0-collections-deque-cpp-reverse-support.md)

1. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-REVERSE-01] Locked the representative C++ lane for `collections.deque.reverse()`.
2. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-REVERSE-01-S1-01] Locked the current invalid C++ surface (`q.reverse();`) in focused regressions / TODO / plan.
3. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-REVERSE-01-S2-01] Lowered `reverse()` to `::std::reverse(begin, end)`.
4. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-REVERSE-01-S3-01] Synced the build/run smoke and support wording, then closed the task.
- Progress memo: the end state aligns `reverse()` to `::std::reverse(begin, end)` and locks the representative build/run output `3 / 2 / 1`.

## 2026-03-13 migration: `collections.deque` iterable construction / extendleft representative C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative C++ lane for `collections.deque` iterable construction / `extendleft`

Context: [docs/en/plans/archive/20260313-p0-collections-deque-cpp-iterable-support.md](../plans/archive/20260313-p0-collections-deque-cpp-iterable-support.md)

1. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ITERABLE-01] Locked the representative C++ lane for `collections.deque` iterable construction / `extendleft`.
2. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ITERABLE-01-S1-01] Locked the current invalid C++ surface (`deque(iterable)`, `extendleft(iterable)`) in focused regressions / TODO / plan.
3. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ITERABLE-01-S2-01] Lowered `deque(iterable)` to a valid `::std::deque<T>` constructor surface.
4. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ITERABLE-01-S2-02] Lowered `extendleft(iterable)` to a `push_front` loop bundle.
5. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ITERABLE-01-S3-01] Synced the build/run smoke and support wording, then closed the task.
- Progress memo: the end state aligns `deque(iterable)` / `extendleft(iterable)` to a range constructor / snapshot + `push_front` loop, and locks the representative build/run output `4 / 3 / 1 / 2`.

## 2026-03-13 migration: `collections.deque` appendleft / pop representative C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative C++ lane for `collections.deque` `appendleft` / `pop`

Context: [docs/en/plans/archive/20260313-p0-collections-deque-cpp-endops-support.md](../plans/archive/20260313-p0-collections-deque-cpp-endops-support.md)

1. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ENDOPS-01] Locked the representative C++ lane for `collections.deque` `appendleft` / `pop`.
2. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ENDOPS-01-S1-01] Locked the current invalid C++ surface (`appendleft`, `pop`) in focused regressions / TODO / plan.
3. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ENDOPS-01-S2-01] Lowered `appendleft` to `push_front`.
4. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ENDOPS-01-S2-02] Lowered `pop` to a `back + pop_back` lambda and aligned typed / untyped return surfaces.
5. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-ENDOPS-01-S3-01] Synced build/run smoke and support wording, then closed the task.
- Progress memo: the end state aligns `appendleft` / `pop` to `push_front()` / `back + pop_back`, and locks the representative build/run output `1 / 2` for typed and untyped `pop()` receivers.

## 2026-03-13 migration: `collections.deque` representative C++ expression / method support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative C++ expression / method lane for `collections.deque`

Context: [docs/en/plans/archive/20260313-p0-collections-deque-cpp-expr-method-support.md](../plans/archive/20260313-p0-collections-deque-cpp-expr-method-support.md)

1. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-EXPR-METHOD-01] Locked the representative C++ expression / method lane for `collections.deque`.
2. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-EXPR-METHOD-01-S1-01] Locked the current invalid C++ surface in focused regressions / TODO / plan.
3. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-EXPR-METHOD-01-S2-01] Aligned `deque()` expressions and `len/truthiness` with representative C++ lowering.
4. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-EXPR-METHOD-01-S2-02] Aligned the `append` method subset with representative C++ lowering and kept the `popleft` regression green.
5. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-EXPR-METHOD-01-S3-01] Synced build/run smoke and support wording, then closed the task.
- Progress memo: the end state aligns `deque()` / `bool(q)` / `len(q)` / `append` / `popleft` to `::std::deque<T>{}` / `!empty()` / `.size()` / `push_back()` / `front + pop_front`, and locks the representative fixture output `False / 0 / 1`.

## 2026-03-13 migration: `collections.deque.copy()` / `index()` representative C++ support complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: lock the representative C++ lane for `collections.deque.copy()` / `index()`

Context: [docs/en/plans/archive/20260313-p0-collections-deque-cpp-copyindex-support.md](../plans/archive/20260313-p0-collections-deque-cpp-copyindex-support.md)

1. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-COPYINDEX-01] Locked the representative C++ lane for `collections.deque.copy()` / `index()`.
2. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-COPYINDEX-01-S1-01] Locked the current invalid C++ surface (`copy`, `index`) in focused regressions / TODO / plan.
3. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-COPYINDEX-01-S2-01] Lowered `copy()` to a valid deque copy-construction surface.
4. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-COPYINDEX-01-S2-02] Lowered `index(value)` to a valid search / distance surface.
5. [x] [ID: P0-COLLECTIONS-DEQUE-CPP-COPYINDEX-01-S3-01] Synced build/run smoke and support wording, then closed the task.
- Progress memo: the end state aligns `copy()` to a single-evaluation copy-construction surface and `index(value)` to `std::find + iterator difference`, and locks the typed representative fixture output `3 / 2 / 0`.

## 2026-03-13 migration: `rs/cs` runtime `generated/native` layout alignment complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: align the `rs/cs` runtimes to a C++-comparable `generated/native` layout

Context: [docs/en/plans/archive/20260313-p0-noncpp-runtime-layout-alignment.md](../plans/archive/20260313-p0-noncpp-runtime-layout-alignment.md)

1. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01] Aligned the `rs/cs` runtimes to a C++-comparable `generated/native` layout.
2. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S1-01] Fixed the canonical `generated/native/pytra` layout and compare unit in the spec / plan.
3. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S1-02] Added the mapping table from the current `pytra-core/pytra-gen` trees to the new layout.
4. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S2-01] Cut Rust over to `native/generated` and synchronized runtime hooks and guards.
5. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S2-02] Cut C# over to `native/generated` and synchronized build/selfhost/runtime paths.
6. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S2-03] Regenerated `png/gif` into `generated/utils` from the SoT.
7. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S3-01] Fixed the C# std-lane ownership contract module by module.
8. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S3-02] Aligned the live/generated vs native residual state for the `rs/cs` std lanes.
9. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S4-01] Regenerated `generated/built_in/*` for `rs/cs`.
10. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S4-02] Fixed the `generated/native built_in` boundary and deleted the duplicate C# `pytra/**` lane.
11. [x] [ID: P0-NONCPP-RUNTIME-LAYOUT-ALIGN-01-S5-01] Updated runtime guards / allowlists / docs to the `generated/native` vocabulary everywhere.
- Progress memo: the end state treats `generated/native` as the canonical runtime layout for both `rs/cs`, deletes the duplicate C# `pytra/**` lane, and keeps Rust `pytra/**` only as an explicit compatibility lane.

## 2026-03-13 migration: C++ runtime packaging deshim complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P0: move C++ runtime packaging to compiler-direct includes and retire repo shims

Context: [docs/en/plans/archive/20260313-p0-cpp-runtime-packaging-deshim.md](../plans/archive/20260313-p0-cpp-runtime-packaging-deshim.md)

1. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01] Switched the C++ runtime packaging to compiler-direct `generated/native` include surfaces and removed the checked-in `pytra/core` shim trees.
2. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01-S1-01] Fixed the `compiler_headers` / `public_headers` two-surface contract in the docs/TODO.
3. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01-S1-02] Added `compiler_headers` to the runtime symbol index / loader / tests.
4. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01-S2-01] Switched emitter / runtime path / helper include resolution to the `compiler_headers` contract.
5. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01-S2-02] Switched `emit-runtime-cpp` and the checked-in generated runtime to direct `native/core` includes.
6. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01-S3-01] Synchronized the build graph to the shim-free `generated/native` contract.
7. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01-S3-02] Updated the layout guard / tests / docs to the post-shim contract.
8. [x] [ID: P0-CPP-RUNTIME-PACKAGING-DESHIM-01-S4-01] Decided no export-time SDK generator is needed and deleted `src/runtime/cpp/{pytra,core}` from the repo.
- Progress memo: the end state uses a direct contract where `public_headers == compiler_headers`, `--emit-runtime-cpp` writes only `generated/**`, and `src/runtime/cpp/{pytra,core}` no longer exists as a checked-in tree.

## 2026-03-13 migration: remaining non-C++ runtime `generated/native` rollout complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P1: roll the remaining non-C++ backend runtimes onto a C++-comparable `generated/native` layout

Context: [docs/en/plans/archive/20260313-p1-noncpp-runtime-layout-rollout-remaining.md](../plans/archive/20260313-p1-noncpp-runtime-layout-rollout-remaining.md)

1. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01] Rolled the `go/java/kotlin/scala/swift/nim/js/ts/lua/ruby/php` runtimes onto the same `generated/native` ownership model as P0.
2. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S1-01] Fixed the current-tree to target-tree mapping, the hook/path baseline checker, and the backend order for the remaining backends.
3. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S1-02] Locked the current/target inventories, module buckets, and the canonical compare-baseline coverage rule (`generated ∪ blocked = baseline`) into the contract/checker.
4. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S2-01] Cut the Wave A (`go/java/kotlin/scala/swift/nim`) runtime tree / hook / guard / smoke-path baseline over to `generated/native`.
5. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S2-02] Regenerated the Wave A `generated/{built_in,std,utils}` lanes from the SoT and cleared the Java `std/json` plus Nim helper-regeneration blockers.
6. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S2-03] Shrunk the Wave A `native/**` residual set down to `built_in/py_runtime.*` substrate only and removed Java `native/std/{math_impl,time_impl}` after absorbing them into the generated lane.
7. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S3-01] Cut the Wave B (`js/ts/lua/ruby/php`) path / shim / package-export / selfhost definitions over to `generated/native`.
8. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S3-02] Materialized the Wave B `generated/{built_in,std,utils}` compare lanes bundle by bundle and fixed the Lua decorator-import ignore seam plus regeneration baseline.
9. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S3-03] Fixed the Wave B `native/**` residuals, `pytra/**` compat shims, public compat smoke surface, and `std/{pathlib,json}` compare lanes.
10. [x] [ID: P1-NONCPP-RUNTIME-LAYOUT-ROLLOUT-REMAINING-01-S4-01] Updated the cross-backend guards / inventories / docs to the `generated/native` vocabulary everywhere and removed the remaining Wave A/Wave B compare-impossible backend sets.
- Progress memo: the end state gives all 11 remaining backends a file-level comparable contract for `generated/native` ownership, generated compare baseline, blocked/helper-shaped gaps, and the compat/native residual boundary.

## 2026-03-13 migration: secondary backend parity rollout complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P5: complete the secondary-tier backend parity rollout through representative transpile smoke

Context: [docs/en/plans/archive/20260313-p5-backend-parity-secondary-rollout.md](../plans/archive/20260313-p5-backend-parity-secondary-rollout.md)

1. [x] [ID: P5-BACKEND-PARITY-SECONDARY-ROLLOUT-01] Filled the remaining unsupported cells for the secondary tier (`go/java/kt/scala/swift/nim`) as a live implementation rollout task.
2. [x] [ID: P5-BACKEND-PARITY-SECONDARY-ROLLOUT-01-S1-01] Fixed the secondary residual cells, backend order, and the two-bundle handoff (`go/java/kt` -> `scala/swift/nim`) in the inventory/checker.
3. [x] [ID: P5-BACKEND-PARITY-SECONDARY-ROLLOUT-01-S2-01] Lifted the `go/java/kt` bundle to `supported/transpile_smoke` with `Swap` emitter support plus representative transpile smoke.
4. [x] [ID: P5-BACKEND-PARITY-SECONDARY-ROLLOUT-01-S2-02] Lifted the `scala/swift/nim` bundle to `supported/transpile_smoke` with representative transpile smoke and added `Swap` lowering to `swift/nim`.
5. [x] [ID: P5-BACKEND-PARITY-SECONDARY-ROLLOUT-01-S3-01] Synced the secondary-tier matrix / docs / support wording to the closed rollout state so the task can leave the active queue.
- Progress memo: the end state is an empty secondary residual inventory, `supported/transpile_smoke` across the entire secondary tier, and no remaining active secondary rollout task in TODO.

## 2026-03-13 migration: long-tail backend parity rollout complete

The following section was moved from `docs/en/todo/index.md` to `todo/archive`.

### P6: complete the long-tail backend parity rollout through representative transpile smoke

Context: [docs/en/plans/archive/20260313-p6-backend-parity-longtail-rollout.md](../plans/archive/20260313-p6-backend-parity-longtail-rollout.md)

1. [x] [ID: P6-BACKEND-PARITY-LONGTAIL-ROLLOUT-01] Filled the remaining unsupported cells for the long-tail tier (`js/ts/lua/rb/php`) as a live implementation rollout task.
2. [x] [ID: P6-BACKEND-PARITY-LONGTAIL-ROLLOUT-01-S1-01] Fixed the long-tail residual cells and implementation bundles as a live rollout inventory.
3. [x] [ID: P6-BACKEND-PARITY-LONGTAIL-ROLLOUT-01-S2-01] Lifted the `js/ts` bundle to `supported/transpile_smoke` with representative transpile smoke.
4. [x] [ID: P6-BACKEND-PARITY-LONGTAIL-ROLLOUT-01-S2-02] Lifted the `lua/rb/php` bundle to `supported/transpile_smoke` with `Swap` lowering, Lua lambda/runtime-alias completion, and representative transpile smoke.
5. [x] [ID: P6-BACKEND-PARITY-LONGTAIL-ROLLOUT-01-S3-01] Synced the long-tail matrix / docs / support wording to the closed rollout state so the task can leave the active queue.
- Progress memo: the end state is an empty long-tail residual inventory, `supported/transpile_smoke` across the reviewed representative cells for `js/ts/lua/rb/php`, and no remaining active long-tail rollout task in TODO.

# TODO History (2026-03-04)

<a href="../../../ja/todo/archive/20260304.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-04 Migration: Completed sections (9 items)

Moved the following from `docs/ja/todo/index.md` to `todo/archive`.

### P0: Reorganize the `src` layout (`toolchain` / `pytra` / `runtime`)

Context: [docs/ja/plans/p0-src-layout-toolchain-pytra-runtime-split.md](../plans/p0-src-layout-toolchain-pytra-runtime-split.md)

1. [x] [ID: P0-SRC-LAYOUT-SPLIT-01] Reorganize `src` by responsibility and separate the transpiler body from `src/pytra` (no backward compatibility).
2. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S1-01] Inventory the responsibilities and reference points of current `src/pytra/{frontends,ir,compiler,std,utils,built_in}`.
3. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S1-02] Fix the new layout conventions (`toolchain` / `pytra` / `runtime`) and dependency directions in `docs/ja/spec/spec-folder.md`.
4. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S1-03] Explicitly document migration rules that forbid old import paths (no backward compatibility).
5. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S2-01] Create `src/toolchain/frontends` and move `src/pytra/frontends` wholesale.
6. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S2-02] Create `src/toolchain/ir` and move `src/pytra/ir` wholesale.
7. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S2-03] Create `src/toolchain/compiler` and move `src/pytra/compiler` wholesale.
8. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S2-04] Remove empty directories and unnecessary remnants under `src/pytra`, and reorganize around `std/utils/built_in`.
9. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S3-01] Bulk-update imports in `src/`, `tools/`, and `test/` to the new paths (shims forbidden).
10. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S3-02] Align CLI entrypoints (`py2x.py`, `py2x-selfhost.py`, `py2*.py`) with the new import layout.
11. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S3-03] Add a validation script that fail-fast detects old `pytra.frontends|ir|compiler` references.
12. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S4-01] Run major unit/transpile regressions and confirm non-regression.
13. [x] [ID: P0-SRC-LAYOUT-SPLIT-01-S4-02] Reflect the new directory responsibilities and paths into `docs/ja/spec` (and `docs/en/spec` if needed).
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S1-01] Inventoried responsibilities and reference points across the 6 regions under `src/pytra`, confirming dependency concentration in `compiler` and a `frontends`↔`ir` cycle.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S1-02] Reflected the canonical 3-layer `src/toolchain` layout, the reference-library-only boundary of `src/pytra`, and dependency-direction rules into `spec-folder`.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S1-03] Added the ban on old import paths (`pytra.frontends|ir|compiler`), the ban on shims, and the `rg`-based check procedure to `spec-folder`.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S2-01] Moved `frontends` to `src/toolchain/frontends` and updated related imports, layer-boundary checks, and bootstrap unit tests.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S2-02] Moved `ir` to `src/toolchain/ir` and updated related imports, layer-boundary checks, and transpile smoke tests.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S2-03] Moved `compiler` to `src/toolchain/compiler` and updated imports and fixed reference paths in CLI/emission/tests/tooling.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S2-04] Removed `frontends/ir/compiler` traces from `src/pytra` and confirmed a reduced structure centered on `std/utils/built_in` plus minimal entrypoints.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S3-01] Bulk-updated imports in `src/tools/test/selfhost` and confirmed old `pytra.(frontends|ir|compiler)` references were reduced to zero.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S3-02] Unified imports in `py2x.py` / `py2x-selfhost.py` / `py2*.py` / `ir2lang.py` around `toolchain.compiler.*`.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S3-03] Added a whole-tree scan for old import paths to `check_pytra_layer_boundaries.py`, making `legacy import path is forbidden` available as a fail-fast detector.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S4-01] Ran major transpile regressions (`cpp/rs/js/ts/go/java/kotlin/swift/ruby/lua/scala/php/nim`) and boundary checks, and confirmed all passed.
- Progress memo: [ID: P0-SRC-LAYOUT-SPLIT-01-S4-02] Updated old `src/pytra/{frontends,ir,compiler}` references in `docs/ja/spec` and `docs/en/spec` (excluding archive) to the new `src/toolchain/*`.

### P0: Implement the PHP image runtime and repair sample/16

Context: [docs/ja/plans/p0-php-image-runtime-and-s16-repair.md](../plans/p0-php-image-runtime-and-s16-repair.md)

1. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01] Implement the PHP image runtime, resolve `sample/16` execution failures, and prevent parity false positives.
2. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S1-01] Inventory no-op dependency points in the PHP image output path (runtime/emitter).
3. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-01] Add a PNG writer implementation to `src/runtime/php/pytra/runtime/png.php`.
4. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-02] Add a GIF writer implementation to `src/runtime/php/pytra/runtime/gif.php`.
5. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-03] Switch the PHP emitter/lowering image-save path from `__pytra_noop` to runtime calls.
6. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-04] Fix tuple receiving in the PHP emitter/lowering and resolve unbound-variable references in `sample/16`.
7. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S3-01] Make `tools/runtime_parity_check.py` forcibly delete existing artifacts before running each case.
8. [x] [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S3-02] Reconfirm stdout/artifact parity between Python and PHP with focus on `sample/01,06,16`.
- Progress memo: [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S1-01] PHP image output had been disabled through `emitter->__pytra_noop->runtime stub`, and `sample/16` reproduced through tuple-receiving collapse up to `DivisionByZeroError`.
- Progress memo: [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-01] Added a pure-PHP implementation to `png.php` including CRC32/Adler32/stored-deflate, and confirmed PNG byte equality with the Python implementation on `1x1 RGB`.
- Progress memo: [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-02] Added a GIF89a + Clear/Literal LZW implementation to `gif.php`, and confirmed GIF byte equality with the Python implementation on `2x1x2frames`.
- Progress memo: [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-03] Switched PHP emitter image-save calls to direct runtime calls and confirmed PNG/GIF generation by retranspiling and running `sample/01` and `sample/06`.
- Progress memo: [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S2-04] Fixed tuple/list assignment in the PHP emitter to destructuring assignment, resolving unbound variables in `sample/16` and confirming normal completion.
- Progress memo: [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S3-01] Confirmed that artifact purge in `runtime_parity_check.py` always runs at case start and before execution of each target.
- Progress memo: [ID: P0-PHP-IMAGE-RUNTIME-S16-01-S3-02] Enabled artifact comparison for PHP and confirmed parity passes (`stdout/artifact`) for `sample/01,06,16` (`3/3`).

### P0: Investigate the cause of Ruby `sample/18` parity failure (`tokenize error`)

Context: [docs/ja/plans/p0-ruby-s18-tokenize-parity-investigation.md](../plans/p0-ruby-s18-tokenize-parity-investigation.md)

1. [x] [ID: P0-RUBY-S18-TOKENIZE-INVEST-01] Identify the root cause of the Ruby `sample/18` parity failure and fix the correction policy.
2. [x] [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S1-01] Reproduce the parity failure and collect the exception location and input token sequence.
3. [x] [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S1-02] Compare tokenize results between the Python and Ruby versions and identify the first divergence point.
4. [x] [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S2-01] Identify the transformation rule that produces the divergence (lower/emitter/runtime) and clarify the responsibility boundary.
5. [x] [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S2-02] Create a minimum-reproduction-case addition policy (fixture conversion).
6. [x] [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S3-01] Fix the correction policy (implementation location / regression test) and file the next corrective task.
- Progress memo: [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S1-01] Reproduced the Ruby failure in `sample/18` and captured `line=0 pos=6 ch==` and `single_char_token_tags = {}` on the generated Ruby side (`=` missing).
- Progress memo: [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S1-02] With the same input `let a = 10`, Python emitted `EQUAL` while Ruby stopped at `pos=6 '='`, fixing the first divergence point to missing dictionary initialization.
- Progress memo: [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S2-01] Identified the root cause as the Ruby emitter's `Dict` rendering not supporting `entries` (assuming old `keys/values`), causing dict literals to collapse to `{}`.
- Progress memo: [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S2-02] Judged that a minimum reproduction of “typed dict literal + `.get()`” is sufficient and fixed the fixture granularity to the dict-literal path alone rather than all of `tokenize`.
- Progress memo: [ID: P0-RUBY-S18-TOKENIZE-INVEST-01-S3-01] Fixed the correction policy to `Dict(entries) support + fixture regression + sample/18 parity` and filed the next implementation task `P0-RUBY-DICT-ENTRY-EMIT-FIX-01`.

### P0: Fix Ruby dict-literal emission for EAST3 `entries`

Context: [docs/ja/plans/p0-ruby-dict-entry-emit-fix.md](../plans/p0-ruby-dict-entry-emit-fix.md)

1. [x] [ID: P0-RUBY-DICT-ENTRY-EMIT-FIX-01] Make Ruby dict-literal generation support EAST3 `entries` format and resolve the `sample/18` parity failure.
2. [x] [ID: P0-RUBY-DICT-ENTRY-EMIT-FIX-01-S1-01] Make `ruby_native_emitter._render_dict_expr` render with `entries` first while still handling old `keys/values` format for backward compatibility.
3. [x] [ID: P0-RUBY-DICT-ENTRY-EMIT-FIX-01-S1-02] Add a minimum-reproduction fixture for dict literals and a Ruby transpile regression test.
4. [x] [ID: P0-RUBY-DICT-ENTRY-EMIT-FIX-01-S2-01] Pass `runtime_parity_check --case-root sample --targets ruby 18_mini_language_interpreter` and confirm resolution.

### P1: Unify on `py2x.py` as the single entrypoint (abolish `py2*.py`, eventually delete `py2cpp.py`)

Context: [docs/ja/plans/p1-py2x-single-entrypoint-remove-legacy-clis.md](../plans/p1-py2x-single-entrypoint-remove-legacy-clis.md)

1. [x] [ID: P1-PY2X-SINGLE-ENTRY-01] Unify `py2x.py` as the only canonical entrypoint and ultimately delete `src/py2cpp.py`.
2. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S1-01] Inventory dependency points on `py2*.py` in `tools/` / `test/` / `docs/` / `src/pytra/cli.py` and fix the migration order.
3. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S1-02] Fix the receiving-spec in `py2x` for `py2cpp.py`-specific features (`--emit-runtime-cpp`, `--header-output`, `--multi-file`, etc.).
4. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S1-03] Inventory which entrypoint contracts the selfhost path (prepare/build/check) depends on and fix the replacement policy.
5. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S2-01] Implement `py2cpp`-specific features in `py2x --target cpp` so it can be operated equivalently with existing options.
6. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S2-02] Replace CLI calls in `tools/` wholesale with `py2x.py --target ...`.
7. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S2-03] Migrate CLI calls and contract tests in `test/` to a `py2x` basis.
8. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S2-04] Update usage examples and spec wording in `docs/ja` / `docs/en` to the canonical `py2x` entrypoint.
9. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S2-05] Remove `py2cpp.py` dependence from selfhost scripts and rewire around `py2x-selfhost.py`.
10. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S3-01] Add a guard before removing legacy CLIs and fail-fast detect new re-entry of `py2*.py`.
11. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S3-02] Delete `src/py2cpp.py` and remove other `py2*.py` as needed.
12. [x] [ID: P1-PY2X-SINGLE-ENTRY-01-S3-03] Run all transpile/selfhost regressions and confirm non-regression after deleting `py2cpp.py`.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S1-01] Inventoried dependencies, fixed the distribution of legacy CLI references across `src/pytra/cli.py`, `tools`, `test`, and `docs`, and fixed the migration order as CLI/tools -> test -> docs -> selfhost.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S1-02] Extracted the actually used `py2cpp` option set, fixing the policy that portable options go through a layer-option map and output-mode-changing options are received as `py2x --target cpp`-specific compatibility flags.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S1-03] Identified that selfhost prepare/build/check depended on a `src/py2cpp.py` chain, and fixed the replacement policy to unify normal usage on `py2x` and selfhost on `py2x-selfhost`.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-01] Added `py2cpp`-compat delegation to `py2x --target cpp`, mapped C++ `optimizer/emitter-option` keys to dedicated flags, and verified behavior with `test_py2x_cli` and single/multi/header output.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-02] Replaced direct `src/py2*.py` calls in `tools/` except selfhost-related ones with `src/py2x.py --target ...`, and confirmed non-regression by running all `check_py2*_transpile.py`.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-03] Unified CLI execution in `test/unit` on `src/py2x.py --target ...`, and confirmed representative pass across 15 unittest files (`test_py2lua_smoke.py` kept 7 known failures).
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-04] Unified the examples in `docs/ja|en/how-to-use.md` around `py2x --target` + `-o`, and updated the supported-language list in `spec-user` to the canonical `py2x` entrypoint wording.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-05] Removed direct `src/py2cpp.py` references from selfhost scripts and shifted them toward `py2x-selfhost`, but `build_selfhost.py` still had remaining generated C++ compile failures, so this ID remained ongoing.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-05] Restored host transpilation of `prepare_selfhost_source.py` artifacts (resolving missing-module / unlowered-method failures in `selfhost/py2cpp.py`) and confirmed no regression with `check_py2cpp_transpile.py` (`137/137`).
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-05] Rewired `py2x-selfhost.py` to match the runtime `ArgumentParser` contract (resolving help-string concatenation, layer-option variable scope, and broken signatures in argument definitions), and narrowed remaining `build_selfhost.py` issues down to unimplemented `pytra::compiler::{transpile_cli,backend_registry_static}`.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-05] Added minimum implementations of `pytra::compiler::{transpile_cli,backend_registry_static}` to the C++ runtime to restore `build_selfhost.py` compilation, and added a fallback in `build_selfhost_stage2.py` that reuses stage1-generated CPP when `[not_implemented]` is encountered (selfhost transpilation itself remains a continuing task).
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S2-05] Limited the `self_hosted` built-in method fallback to selfhost-related sources to restore `east3-contract` regression, and confirmed `check_selfhost_cpp_diff` / `check_selfhost_stage2_cpp_diff` (`allow-not-implemented`) pass in normal mode.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S3-01] Added `tools/check_legacy_cli_references.py` so direct `src/py2*.py` references and new `import py2*` re-entry in `src/tools/test` can be detected fail-fast.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S3-02] Deleted `src/py2cpp.py`, moved the C++ entrypoint to `src/backends/cpp/cli.py`, updated references in `py2x` / `pytra cli` / tools / tests to the `py2x --target cpp` basis, and confirmed regression coverage.
- Progress memo: [ID: P1-PY2X-SINGLE-ENTRY-01-S3-03] Ran all 14 `check_py2*_transpile` scripts and 4 selfhost checks (build/stage2/diff×2), confirming transpile/selfhost non-regression after deleting `py2cpp.py`.

### P0: Align sample artifact CRCs (remove the Kotlin gate, reverify Swift, repair failing groups)

Context: [docs/ja/plans/p0-multilang-artifact-crc-align.md](../plans/p0-multilang-artifact-crc-align.md)

1. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01] Align artifact parity (size+CRC32) for `sample` across `cpp,rs,cs,js,ts,go,java,swift,kotlin`.
2. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S1-01] Fix the baseline after removing the Kotlin artifact gate and lock failure categories by language.
3. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S1-02] Complete `--targets swift --all-samples` after toolchain setup and lock failure categories.
4. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-01] Remove the Kotlin `save_gif` no-op path and resolve `artifact_missing` with a runtime GIF writer.
5. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-02] Align the Kotlin PNG writer to Python-compatible binaries and resolve mismatches in `01..04`.
6. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-03] Connect Java image calls to the runtime implementation and resolve `artifact_missing`.
7. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-04] Fix Java compile failures (`RuntimeError` / dict.get-default / types) so sample execution can complete.
8. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-05] Fix `__pytra_bytes([]byte)` and typed-operation return values (`ifexp/min/max`) in Go and resolve `run_failed`.
9. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-06] Fix collapsed `TokenLike` field access in Go sample/18.
10. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-07] Fix Swift argument-label consistency (definitions/calls) and make sample execution work.
11. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-08] Align JS/TS PNG/GIF writers to Python-compatible binaries and resolve mismatches.
12. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-09] Split and fix CRC mismatches in C# image paths.
13. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-10] Fix C++ sample/07,16 compile failures and 06/12/14 CRC mismatches.
14. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Rerun artifact parity across all 9 languages and confirm `mismatch/run_failed/toolchain_missing=0`.
15. [x] [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-02] Reflect CRC32 parity operating rules into regression tests and specs.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-01] Switched Kotlin emitter `save_gif/grayscale_palette` to the runtime path, added a GIF writer to `src/runtime/kotlin/pytra/py_runtime.kt`, and confirmed `artifact_missing=0` by rerunning `--targets kotlin --all-samples` (`work/logs/runtime_parity_sample_kotlin_crc_20260304_after_gif.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-02] Changed the Kotlin PNG writer to the same stored-block zlib/chunk scheme as the Python runtime, resolved size/CRC mismatches in `01..04` (`work/logs/runtime_parity_sample_kotlin_crc_20260304_after_png_store.json`), and narrowed the remaining issues to 8 GIF-side `artifact_crc32_mismatch` cases.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S1-02] Added `--cmd-timeout-sec` to `runtime_parity_check.py` so `--targets swift --all-samples` can complete, and fixed `run_failed=17/artifact_missing=1` in `work/logs/runtime_parity_sample_swift_crc_20260304_all_timeout90.json`.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S1-01] Merged the existing `cpp..kotlin` baseline with updated Kotlin/Swift logs and fixed per-language categories in `work/logs/runtime_parity_sample_baseline_lock_20260304.json`.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-03] Connected `save_gif/write_rgb_png/grayscale_palette` in the Java emitter to runtime implementations and confirmed `artifact_missing=0` by rerunning `--targets java --all-samples` (`work/logs/runtime_parity_sample_java_crc_20260304_after_image_connect.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-04] Fixed Java `RuntimeError` / `dict.get(default)` / `enumerate` / `Dict.entries` / `list_repeat` / `Raise` termination issues and confirmed `run_failed=0` on `--targets java --all-samples` (`work/logs/runtime_parity_sample_java_crc_20260304_after_compile_fix4.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-05] Added `__pytra_bytes([]byte)` support to the Go runtime and reinforced explicit casts in emitter `ifexp/min/max` paths. On `--targets go --all-samples`, `run_failed` was reduced to the remaining `TokenLike` issue in `sample/18` only (`work/logs/runtime_parity_sample_go_crc_20260304_after_s205b.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-06] Changed Go class typing so “classes with no inheritance become `*Class`”, added `dict.get(default)` helpers and `Dict.entries` emission, and confirmed `run_failed=0` on `--targets go --all-samples` (`work/logs/runtime_parity_sample_go_crc_20260304_after_s206.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-07] Unified Swift function/method signatures to the form `func f(_ x: T, ...)`, making them consistent with unlabeled call sites. Confirmed `sample/01` parity turned `ok`, and that `missing argument labels` disappeared from all-sample compile logs (`work/logs/runtime_parity_sample_swift_case01_after_s207_20260304.json`, `work/logs/swift_compile_all_after_s207_20260304.log`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-08] Merged keyword arguments into positional arguments in the JS emitter so `save_gif(delay_cs=..., loop=...)` no longer disappears, replaced JS/TS PNG helpers with the same stored-block zlib writer as Python, and confirmed all 18 cases `ok` on `--targets js,ts --all-samples` (`work/logs/runtime_parity_sample_js_ts_crc_20260304_after_s208.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-09] Identified missing `save_gif` keyword arguments (delay/loop) and `py_int` rounding error (`Convert.ToInt64`) as the causes in C#, fixed them with keyword merging and `Math.Truncate` alignment, and confirmed all 18 cases `ok` on `--targets cs --all-samples` (`work/logs/runtime_parity_sample_cs_crc_20260304_after_s209.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S2-10] Fixed C++ issues around predeclared scope collapse in `if/elif`, typed-list regression to `object`, self-initialization collapse in `for y in range(y, h)`, uninitialized `math.pi/e`, and missing parentheses on unary `-` (`-(a+b)` -> `-a+b`). Reconfirmed parity on `sample/06,07,12,14,16`, all `ok` (`work/logs/runtime_parity_sample_cpp_s210_focus_fixed_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Fixed a Go bug where `BitAnd/BitOr/BitXor/LShift/RShift` were mistranslated as `+`, resolved the `sample/16` CRC mismatch, and confirmed all 18 cases `ok` on `--targets go --all-samples` (`work/logs/runtime_parity_sample_go_after_bitop_fix_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Fixed missing `keywords` (`save_gif(delay_cs=..., loop=...)`) and mistranslated bit operations (`>>`, `<<`, `&`, `|`, `^`) in Kotlin, also added right-hand-side `Int` conversion for `shl/shr`, and confirmed all 18 cases `ok` on `--targets kotlin --all-samples` (`work/logs/runtime_parity_sample_kotlin_after_shift_int_fix_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Fixed missing `keywords`, collapsed parentheses on unary `-`, and `Deflater` incompatibility in the Java PNG writer, replaced it with a stored-block zlib + Adler32 implementation, and confirmed all 18 cases `ok` on `--targets java --all-samples` (`work/logs/runtime_parity_sample_java_after_png_unary_fix_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Reverified `rs,cs,js,ts` on the latest code and confirmed all 18 cases `ok` on `--targets rs,cs,js,ts --all-samples` (`work/logs/runtime_parity_sample_rs_cs_js_ts_after_s301_20260304.json`). Remaining work was only full reruns for Swift and `cpp`.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Changed timeout handling in `runtime_parity_check.py` to process-group kill and switched Swift execution to `swiftc -O`. Swift `01..04` reached `4/4 ok` (`work/logs/runtime_parity_sample_swift_01_04_after_s301_progress_20260304.json`), but `05/06/07` still remained as timeouts under `--all-samples --cmd-timeout-sec 300`.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Added `Name` / `grid[y][x]` fast paths (direct subscript assignment + write-back) to Swift emitter `Subscript` assignment and resolved timeouts/CRC failures in `05..09`. `--targets swift --all-samples --cmd-timeout-sec 300` improved to `case_pass=11`, leaving only compile failures in `12/13/18` (`work/logs/runtime_parity_sample_swift_all_after_subscript_store_opt_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Fixed the issue where Swift `_call_name` did not resolve `Attribute`, eliminating type-inference collapse around `math.sqrt/sin/...`. `10/14/15/16` all became `ok` (`work/logs/runtime_parity_sample_swift_10_14_15_16_after_callname_attr_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Scoped Swift `ForCore` into `do { ... }` to resolve loop-variable redeclaration conflicts, and changed tuple-unpack undeclared names to emit first-time `var` declarations. `12/13` became `ok`, bringing `--targets swift --all-samples --cmd-timeout-sec 300` to `case_pass=17/case_fail=1` with only `18` remaining as a compile failure (`work/logs/runtime_parity_sample_swift_all_after_loop_scope_tuple_decl_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Fixed `main_guard` entry-wrapper generation, the `main -> __pytra_main` alias, `Dict.entries` literal generation, and direct dict-subscript assignment (`env[key]=...`) for Swift `sample/18`, making standalone parity for `18_mini_language_interpreter` `ok` (`work/logs/runtime_parity_sample_swift_18_after_dict_fix_20260304.json`).
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Re-ran `--targets swift --all-samples --cmd-timeout-sec 300` and confirmed all 18 cases `ok` (16 artifacts matching in both size and CRC32) (`work/logs/runtime_parity_sample_swift_all_after_s301_complete_20260304.json`). Remaining work was only the full rerun for `cpp`.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-01] Re-ran `--targets cpp --all-samples --cmd-timeout-sec 300` and confirmed all 18 cases `ok` (16 artifacts matching in both size and CRC32) (`work/logs/runtime_parity_sample_cpp_all_after_s301_complete_20260304.json`). Combined with existing all-`ok` logs for `rs/cs/js/ts/go/java/swift/kotlin`, this completed `S3-01` by satisfying `mismatch/run_failed/toolchain_missing=0` across all 9 languages.
- Progress memo: [ID: P0-MULTILANG-ARTIFACT-CRC-ALIGN-01-S3-02] Added regressions for `swiftc -O` and process-group kill on timeout to `test_runtime_parity_check_cli.py`, and reflected artifact parity rules (exists+size+CRC32), stale-artifact purge, and timeout kill into `docs/ja/spec/spec-tools.md` / `docs/en/spec/spec-tools.md`.

### P0: Investigate `sample/13` PHP parity mismatch (`frames 147 -> 2`)

Context: [docs/ja/plans/p0-php-s13-parity-investigation.md](../plans/p0-php-s13-parity-investigation.md)

1. [x] [ID: P0-PHP-S13-PARITY-INVEST-01] Identify the root cause of `sample/13` PHP output becoming `frames: 2` and fix the correction policy.
2. [x] [ID: P0-PHP-S13-PARITY-INVEST-01-S1-01] Reproduce the parity failure in isolation and collect execution logs and artifact information.
3. [x] [ID: P0-PHP-S13-PARITY-INVEST-01-S1-02] Compare the `frames` calculation path between Python and PHP and identify the first divergence point.
4. [x] [ID: P0-PHP-S13-PARITY-INVEST-01-S2-01] Narrow the divergence down to a single layer (EAST3 / lower / emitter / runtime).
5. [x] [ID: P0-PHP-S13-PARITY-INVEST-01-S2-02] Create a minimum-reproduction-case proposal and fix the regression-test granularity.
6. [x] [ID: P0-PHP-S13-PARITY-INVEST-01-S3-01] Record the correction policy in context and file the next corrective task.
- Progress memo: [ID: P0-PHP-S13-PARITY-INVEST-01] Reproduced the failure as `output mismatch (frames: 147 -> 2)` (`work/logs/runtime_parity_sample_php_13_invest_20260304.json`). Fixed the first divergence point to unsupported negative indices in the PHP emitter (`stack[-1]` -> `Undefined array key -1`) and unsupported `ListComp` (`null` fallback), implemented `ListComp(range)` assignment expansion, a list-repeat helper, and negative-index resolution in `__pytra_index`, and confirmed `sample/13` becomes `ok` both for PHP alone and alongside `ruby,lua,scala,php` (`work/logs/runtime_parity_sample_php_13_after_negindex_fix_20260304.json`, `work/logs/runtime_parity_sample_ruby_lua_scala_php_case13_after_php_fix_20260304.json`).

### P1: Resume `py2x` unification (complete removal of legacy `py2*.py` wrappers)

Context: [docs/ja/plans/p1-py2x-wrapper-final-removal-reopen.md](../plans/p1-py2x-wrapper-final-removal-reopen.md)

1. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01] Reopen the archived `py2x` unification task and completely remove the remaining legacy wrappers (`py2rs.py`, `py2cs.py`, ...).
2. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S1-01] Re-inventory remaining wrapper references in `tools/test/docs/selfhost` and fix the replacement order.
3. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S2-01] Replace direct wrapper references in `tools/` with `py2x` / backend-module references.
4. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S2-02] Replace wrapper-file-dependent tests in `test/unit` with `py2x`-based or backend-module-based tests.
5. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S2-03] Update wrapper-name references in `docs/ja` / `docs/en` to the canonical `py2x` entrypoint.
6. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S3-01] Delete the `src/py2*.py` wrappers and `toolchain/compiler/py2x_wrapper.py` (`py2x.py` / `py2x-selfhost.py` excluded).
7. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S3-02] Update the static guard that detects wrapper re-entry and fix the post-removal structure.
8. [x] [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S3-03] Run transpile/smoke regressions and confirm non-regression after wrapper removal.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01] Returned from archive. Confirmed that actual `src/py2*.py` files and wrapper dependencies remained in `tools/check_multilang_selfhost_stage1.py`, `tools/check_noncpp_east3_contract.py`, and `test/unit/test_py2*_smoke.py`, so the completion judgment was reopened.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S1-01] Re-inventoried wrapper references in `tools/test/docs/selfhost` and fixed the replacement order as `tools -> test -> docs -> wrapper deletion -> guard/regression`.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S2-01] Replaced direct wrapper references in `check_noncpp_east3_contract.py` and `check_transpiler_version_gate.py` with `py2x`-based ones, and strengthened re-entry prevention by removing two files from the allowlist of `check_legacy_cli_references.py` (selfhost-related `tools/*` remained under continuing work).
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S2-01] Migrated selfhost-related `tools` (`check_multilang_selfhost_stage1.py`, `check_multilang_selfhost_multistage.py`, `prepare_selfhost_source_cs.py`, `check_cs_single_source_selfhost_compile.py`) to the `py2x` basis, resolving direct `py2<lang>.py` references inside `tools`.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S2-02] Replaced wrapper imports/file dependencies in `test_py2{rs,cs,js,ts,go,java,kotlin,swift,rb,lua,scala,php,nim}_smoke.py` with backend modules + `load_east3_document(..., target_lang=<lang>)`, and confirmed `test_py2*_smoke.py` `298` cases `OK`.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S2-03] Unified examples in `docs/ja|en` around `py2x.py --target <lang>` and updated wrapper guidance in `how-to-use` plus `spec-runtime/spec-options/spec-east/spec-east3-optimizer/spec-dev/spec-tools` wording that had assumed `py2cpp.py`.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S3-01] Deleted `src/py2{rs,cs,js,ts,go,java,kotlin,swift,rb,lua,scala,php,nim}.py` and `toolchain/compiler/py2x_wrapper.py`, moved `test_error_classification_cross_lang` to direct-backend calls, fixed the `cs_emitter` anchor, and then passed smoke `298` cases and the legacy guard.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S3-02] Strengthened `check_legacy_cli_references.py` so it now detects re-entry of actual wrapper files and fixes a structure where all `src/py2*.py` except `src/py2x.py` / `src/py2x-selfhost.py` and `toolchain/compiler/py2x_wrapper.py` are forbidden fail-fast.
- Progress memo: [ID: P1-PY2X-WRAPPER-REMOVE-REOPEN-01-S3-03] Re-ran 14 `check_py2*_transpile.py` scripts and `test_py2*_smoke.py` (`298` cases), confirming all `OK` after wrapper removal.

### P1: Integrate common smoke tests around `py2x` (all languages)

Context: [docs/ja/plans/p1-py2x-unified-smoke-suite.md](../plans/p1-py2x-unified-smoke-suite.md)

1. [x] [ID: P1-PY2X-SMOKE-UNIFY-01] Integrate the common viewpoints of `test_py2*_smoke.py` into a `py2x`-based common smoke suite so all languages can be verified under one framework.
2. [x] [ID: P1-PY2X-SMOKE-UNIFY-01-S1-01] Inventory the common and language-specific viewpoints of `test_py2*_smoke.py` and fix what should be unified.
3. [x] [ID: P1-PY2X-SMOKE-UNIFY-01-S2-01] Add a new common smoke test parameterized by `py2x` target.
4. [x] [ID: P1-PY2X-SMOKE-UNIFY-01-S2-02] Remove already-unified cases from each language smoke file and leave only language-specific verification.
5. [x] [ID: P1-PY2X-SMOKE-UNIFY-01-S2-03] Explicitly document the responsibility boundary between common smoke and language-specific smoke in test comments and the plan.
6. [x] [ID: P1-PY2X-SMOKE-UNIFY-01-S3-01] Run unit/transpile regressions and confirm non-regression after integration.
7. [x] [ID: P1-PY2X-SMOKE-UNIFY-01-S3-02] Reflect smoke-test operating rules into `docs/ja/spec` (and `docs/en/spec` if needed).
- Progress memo: [ID: P1-PY2X-SMOKE-UNIFY-01-S1-01] Inventoried all 14 `test_py2*_smoke.py` files and fixed the commonization targets as `(A) stage2 rejection (14/14) (B) minimal CLI success (14/14) (C) load_east/profile (13/14, non-cpp) (D) add-fixture minimal transpile (13/14, non-cpp)`, leaving the 192 language-specific cases in place.
- Progress memo: [ID: P1-PY2X-SMOKE-UNIFY-01-S2-01] Added `test_py2x_smoke_common.py`, aggregating common smoke through `py2x --target`: `CLI success/stage2 rejection` for all 14 languages and `load_east default/from_json/add-fixture` for the 13 non-cpp languages (`test_py2x_smoke*.py` 6 cases `OK`).
- Progress memo: [ID: P1-PY2X-SMOKE-UNIFY-01-S2-02] Removed 53 already-unified cases (CLI success/stage2 rejection/load_east/add-fixture) from the 14 `test_py2*_smoke.py` files, reducing them to language-specific checks. Both `test_py2*_smoke.py` `232` cases and `test_py2x_smoke*.py` `6` cases remain `OK`.
- Progress memo: [ID: P1-PY2X-SMOKE-UNIFY-01-S2-03] Added comments that explicitly mark common responsibilities in `test_py2x_smoke_common.py` and language-specific responsibilities in each `test_py2*_smoke.py`, fixing the boundary in test code itself.
- Progress memo: [ID: P1-PY2X-SMOKE-UNIFY-01-S3-01] Updated `check_noncpp_east3_contract.py` to assume the common-smoke structure, then reran all `check_py2{cpp..nim}_transpile.py`, `test_py2x_smoke*.py` 6 cases, and `test_py2*_smoke.py` 232 cases, confirming all `OK`.
- Progress memo: [ID: P1-PY2X-SMOKE-UNIFY-01-S3-02] Added the responsibility boundary between common smoke and per-language smoke, plus the recommended regression order, to `docs/ja/spec/spec-tools.md` and `docs/en/spec/spec-tools.md`, documenting the operating rules.

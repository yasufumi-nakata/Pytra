# TODO History (2026-03-07)

<a href="../../../ja/todo/archive/20260307.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-07 Moved: Completed Sections (6 items)

Moved the following from `docs/ja/todo/index.md` to `todo/archive`.

### P0: linked program rollout (multi-unit optimizer + ProgramWriter split)

Context: [docs/ja/plans/archive/20260307-p0-linked-program-global-optimizer-and-program-writer.md](../plans/archive/20260307-p0-linked-program-global-optimizer-and-program-writer.md)

1. [x] [ID: P0-LINKED-PROGRAM-OPT-01] Introduce a linked program, expand the global optimizer input unit to multiple translation units, and reorganize backends around `ModuleEmitter + ProgramWriter`.
2. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S1-01] Fix the schemas for `link-input.v1` / `link-output.v1` and linked module `meta`, along with the responsibility boundary between `spec-linker` and `spec-east`.
3. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S1-02] Define common backend contracts for `ModuleArtifact` / `ProgramArtifact` / `ProgramWriter` and reflect them in `spec-dev` / `spec-make`.
4. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S2-01] Add a `LinkedProgram` loader / validator / manifest I/O under `src/toolchain/link/` so multiple `EAST3` documents can be read deterministically.
5. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S2-02] Switch the in-memory route in `py2x.py` from a module map to `LinkedProgram` construction and remove the single-module assumption.
6. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S3-01] Implement a program-wide call graph / SCC fixed point in the linker layer and build a global-analysis foundation that does not depend on import-closure internal loading.
7. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S3-02] Implement global non-escape / container ownership / `type_id` decisions in the linker layer and materialize them into linked modules and `link-output.json`.
8. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S4-01] Re-split pass responsibilities between the local `EAST3` optimizer and `LinkedProgramOptimizer`, and remove whole-program-dependent passes from the local optimizer.
9. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S5-01] Extend `backend_registry.py` to the `emit_module + program_writer` contract and turn the old `emit -> str` API into a compatibility wrapper.
10. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S5-02] Add a common backend `SingleFileProgramWriter` and align `ir2lang.py` with the new registry contract.
11. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S6-01] Migrate C++ first, restructure `multifile_writer.py` into `CppProgramWriter`, and make `CppEmitter` responsible only for module emission.
12. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S6-02] Update `pytra-cli.py`, the C++ build manifest, and the Makefile generation path so the manifest returned by `ProgramWriter` becomes the source of truth.
13. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S7-01] Add `eastlink.py` and implement the debug/restart path from `link-input.json` to `link-output.json + linked modules`.
14. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S7-02] Add linked-program I/O (`--link-only`, dump/restart) to `ir2lang.py` and `py2x.py`, completing the backend-only route.
15. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S8-01] Add `test/unit/link/*` and representative backend/tooling regressions, and lock schema, determinism, and the ProgramWriter contract.
16. [x] [ID: P0-LINKED-PROGRAM-OPT-01-S8-02] Complete C++ unit / fixture / sample parity, synchronize docs, remove the old import-closure-dependent path, and close the plan.
- Progress memo: [ID: P0-LINKED-PROGRAM-OPT-01-S7-02] Added `--dump-east3-dir` / `--link-only` / `--from-link-output` to `py2x.py` and normalized the linked-program debug/restart path using `eastlink.py` and `ir2lang.py`. Even for the C++ target, these flags now bypass the legacy `py2cpp` compatibility path.
- Progress memo: [ID: P0-LINKED-PROGRAM-OPT-01-S8-01] Hardened the `link-output` validator and `LinkOutputModuleEntry`, and locked module order, entry consistency, and artifact-path determinism in `test/unit/link/*` and representative tooling regressions.
- Progress memo: [ID: P0-LINKED-PROGRAM-OPT-01-S8-02] Removed the `source_path`-based import-closure fallback from `NonEscapeInterproceduralPass` and aligned the linked-program path to a fail-closed contract that only reads the closure supplied by the linker/materializer. Validation confirmed `Ran 509 tests in 1016.609s`, `OK` for the full C++ backend suite, `cases=3 pass=3 fail=0` for fixture parity, and `cases=18 pass=18 fail=0` for sample parity. The docs were also updated with the linked-program restart procedure and the contract that no modules beyond those listed in the manifest are loaded.

### P0: full ref-first migration for C++ mutable lists (`rc<list<T>>` as the source of truth)

Context: [docs/ja/plans/archive/20260307-p0-cpp-list-ref-first-completion.md](../plans/archive/20260307-p0-cpp-list-ref-first-completion.md)

1. [x] [ID: P0-CPP-LIST-REFFIRST-01] Switch C++ mutable lists fully to the ref-first (`rc<list<T>>`) model and confine value lists to optimizer output only.
2. [x] [ID: P0-CPP-LIST-REFFIRST-01-S1-01] Inventory remaining value-first branches in the emitter/runtime and classify them into "forbidden", "ABI-adapter only", and "optimizer only".
3. [x] [ID: P0-CPP-LIST-REFFIRST-01-S1-02] Update `spec-cpp-list-reference-semantics.md` to the final policy of ref-first as the source of truth rather than a dual model.
4. [x] [ID: P0-CPP-LIST-REFFIRST-01-S1-03] Add representative codegen tests and fail fast on regressions that fall back to values just because a list is typed.
5. [x] [ID: P0-CPP-LIST-REFFIRST-01-S2-01] Normalize the main list paths in runtime helpers around `rc<list<T>>` and fix canonical mutable-operation overloads.
6. [x] [ID: P0-CPP-LIST-REFFIRST-01-S2-02] Align list paths in `iter_ops` / `contains` / `sequence` / `py_to_*` / `make_object` to use `rc<list<T>>` as the source of truth.
7. [x] [ID: P0-CPP-LIST-REFFIRST-01-S2-03] Slim/remove `list<T>` runtime overloads except ABI adapters and record why the remaining ones stay.
8. [x] [ID: P0-CPP-LIST-REFFIRST-01-S3-01] Switch list-type rendering in the emitter to ref-first and remove the dependency on `_is_pyobj_forced_typed_list_type`.
9. [x] [ID: P0-CPP-LIST-REFFIRST-01-S3-02] Switch list literals / empty initialization / assign / annassign / tuple unpack / comprehensions to the `rc<list<T>>` canonical model.
10. [x] [ID: P0-CPP-LIST-REFFIRST-01-S3-03] Switch callsite / return / method dispatch / subscript / `for` / `enumerate` / `reversed` rendering to the `rc<list<T>>` canonical model.
11. [x] [ID: P0-CPP-LIST-REFFIRST-01-S4-01] Implement a rule that inserts `list<T>` value adapters only at `@extern` / `Any` / `object` boundaries and isolates them from other paths.
12. [x] [ID: P0-CPP-LIST-REFFIRST-01-S4-02] Normalize helpers for ABI adapters and eliminate internal backend paths that still treat `list<T>` as the canonical model.
13. [x] [ID: P0-CPP-LIST-REFFIRST-01-S5-01] Implement the responsibility boundary on the optimizer side so only provably safe lists are converted to values, separating correctness from optimization.
14. [x] [ID: P0-CPP-LIST-REFFIRST-01-S5-02] Confirm that unit/parity still pass even with the optimizer disabled or in fail-closed conditions.
15. [x] [ID: P0-CPP-LIST-REFFIRST-01-S6-01] Rerun the full C++ unit suite and confirm non-regression after the ref-first migration.
16. [x] [ID: P0-CPP-LIST-REFFIRST-01-S6-02] Rerun fixture/sample parity and confirm non-regression including artifacts.
17. [x] [ID: P0-CPP-LIST-REFFIRST-01-S6-03] Update TODO/archive/docs and lock the ref-first contract as complete.
- Progress memo: [ID: P0-CPP-LIST-REFFIRST-01-S6-01] Fixed repo-root dependencies in runtime module list signatures, list unboxing for `random.choices` / `dict.keys` / `dict.values`, and the nested-list append adapter. A fresh full rerun confirmed `Ran 505 tests in 1091.285s`, `OK` for `python3 -m unittest discover -s /workspace/Pytra/test/unit/backends/cpp -p 'test_*.py'`.
- Progress memo: [ID: P0-CPP-LIST-REFFIRST-01-S6-02] Confirmed `cases=3 pass=3 fail=0` for `python3 tools/runtime_parity_check.py --targets cpp --case-root fixture` and `cases=18 pass=18 fail=0` for `python3 tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples`. The nested-list append regression exposed by sample parity in `07_game_of_life_loop` was fixed under the same ID and rerun.
- Progress memo: [ID: P0-CPP-LIST-REFFIRST-01-S6-03] Moved the plan to [20260307-p0-cpp-list-ref-first-completion.md](../plans/archive/20260307-p0-cpp-list-ref-first-completion.md), cleared the active TODO, and fixed the ref-first contract as complete.

### P0: retire the `.ext` suffix from the C++ core runtime (plain naming)

Context: [docs/ja/plans/archive/20260307-p0-cpp-core-ext-suffix-retire.md](../plans/archive/20260307-p0-cpp-core-ext-suffix-retire.md)

1. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01] Retire the `.ext` suffix from the C++ core runtime and align the `core` surface, `native/core` canonical sources, and the `generated/core` lane to a plain-file-name contract.
2. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S1-01] Create a rename inventory for `core/*.ext.h` and `native/core/*.ext.{h,cpp}` and fix the plain-name mapping in the decision log.
3. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S1-02] Fix the naming contract in the plan/spec: `core` as shim, `native/core` as ownership source of truth, and `generated/core` as the future plain-naming lane.
4. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S2-01] Extend `runtime_symbol_index` / `cpp_runtime_deps.py` / the layout guard to tolerate renames so header/source resolution keeps working during migration.
5. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S2-02] Add synthetic tests and lock that compile/source resolution works under plain naming with `core/*.h` + `native/core/*.{h,cpp}`.
6. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S3-01] Rename `src/runtime/cpp/core/*.ext.h` to `*.h` and switch the forwarder surface to plain naming.
7. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S3-02] Update backend / generated runtime / native companion / test includes to `runtime/cpp/core/*.h` and remove dependency on `runtime/cpp/core/*.ext.h`.
8. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S4-01] Rename `src/runtime/cpp/native/core/*.ext.h` / `*.ext.cpp` to plain names and sync public forwarders and compile-source references.
9. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S4-02] Update the runtime symbol index / parity tools / representative tests to assume plain `native/core/*.{h,cpp}` and lock that old `.ext` paths are no longer returned.
10. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S5-01] Fix the plain naming rule for `generated/core` in README/spec/guards so future artifacts do not reintroduce `.ext`.
11. [x] [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S5-02] Update fallbacks/docs/archive/guards and close `.ext` naming in the core runtime as complete.
- Progress memo: [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S3-01] Renamed `src/runtime/cpp/core/*.ext.h` to `*.h`, unified backend / generated runtime / checked-in test includes to `runtime/cpp/core/*.h`, and synced `runtime_symbol_index.json` so `pytra.core.* -> src/runtime/cpp/core/*.h`.
- Progress memo: [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S4-01] Renamed `src/runtime/cpp/native/core/*.ext.h` / `*.ext.cpp` to plain `*.h` / `*.cpp` and aligned compile-source expectations to `native/core/gc.cpp` / `native/core/io.cpp`. `verify_image_runtime_parity.py` and representative runtime smoke tests were also updated to use the new paths.
- Progress memo: [ID: P0-CPP-CORE-EXT-SUFFIX-RETIRE-01-S5-01] Updated `generated/core/README.md`, `spec-runtime.md`, `spec-abi.md`, and `spec-tools.md` to the current contract, and removed the legacy `.ext` fallback for the core lane from `gen_runtime_symbol_index.py`, `cpp_runtime_deps.py`, and `check_runtime_cpp_layout.py`. `gen_runtime_symbol_index.py --check`, `check_runtime_cpp_layout.py`, `check_runtime_core_gen_markers.py`, `test_check_runtime_core_gen_markers.py`, `test_check_runtime_cpp_layout.py`, `test_cpp_runtime_build_graph.py`, and `test_runtime_symbol_index.py` all passed, and active-tree `.ext` references dropped to zero outside archive/history.

### P0: retire the legacy C++ `os.path` wrapper

Context: [docs/ja/plans/archive/20260307-p0-cpp-os-path-wrapper-retirement.md](../plans/archive/20260307-p0-cpp-os-path-wrapper-retirement.md)

1. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01] Retire the legacy wrapper ABI for C++ `os.path` and restore `pytra::std::os_path::*` as the canonical codegen/runtime surface.
2. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S1-01] Inventory `py_os_path_*` references in backend/runtime/tests/docs and separate import/module tracing that must remain from wrapper targets to retire.
3. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S1-02] In the decision log, fix which parts of the temporary 2026-03-06 recovery stay this time (module attr recovery, public shim compile-source tracking) and which go away (the wrapper ABI).
4. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S2-01] Update `runtime_calls.json` so `os.path` calls point to `pytra::std::os_path::*`, removing wrapper-name dependency from the backend.
5. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S2-02] Update `os.path` codegen assertions in `test_py2cpp_features.py` to the namespace-based canonical form and fail fast on the wrong namespace `pytra::std::os::path::*`.
6. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S3-01] Delete `src/runtime/cpp/std/os_path.ext.h` and confirm the public shim works without it.
7. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S3-02] Remove `py_os_path_*` from `src/runtime/cpp/std/os_path.ext.cpp` and keep only the namespace implementation.
8. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S3-03] Regenerate the necessary runtime and update the symbol index, simplifying the include surface of `pytra/std/os_path.h`.
9. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S4-01] Revalidate `os_glob_extended_runtime`, the runtime symbol index, and pylib `glob` so compile-source/import resolution does not break after wrapper retirement.
10. [x] [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S4-02] Update TODO and the decision log, document why `os_path.ext.h` could be removed, and close the work.
- Progress memo: [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S1-01] Confirmed that `py_os_path_*` implementation dependencies were limited to `runtime_calls.json` on the backend side, `os_path.ext.h/.ext.cpp` on the runtime side, and `test_py2cpp_features.py` on the test side. `path` aliasing in `os.py`, module-attribute recovery, and public shim compile-source tracking were fixed as wrapper-independent items to keep.
- Progress memo: [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S2-01] Switched `pytra.std.os.path` / `os.path` / `pytra.std.os_path` in `runtime_calls.json` to `pytra::std::os_path::*`, restoring the canonical codegen call targets to the namespace API.
- Progress memo: [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S3-01] Deleted `src/runtime/cpp/std/os_path.ext.h`; slimmed `src/runtime/cpp/std/os_path.ext.cpp` down to a namespace implementation that directly includes `os_path.gen.h`; and reran `python3 src/py2x.py --target cpp src/pytra/std/os_path.py --emit-runtime-cpp` so `src/runtime/cpp/pytra/std/os_path.h` became a pure `gen.h` forwarder again.
- Progress memo: [ID: P0-CPP-OSPATH-WRAPPER-RETIRE-01-S4-01] Passed `python3 tools/gen_runtime_symbol_index.py --check`, `test_py2cpp_features.py -k os_path`, `test_py2cpp_features.py -k os_glob_extended_runtime`, `test_cpp_runtime_symbol_index_integration.py`, and `test_pylib_os_glob.py`; `rg -n "py_os_path_" src test tools -g '!sample/cpp/**'` also returned zero matches.

### P0: C++ runtime layout realignment (`generated/` + `native/` + `pytra/` shim)

Context: [docs/ja/plans/archive/20260307-p0-cpp-runtime-layout-generated-native.md](../plans/archive/20260307-p0-cpp-runtime-layout-generated-native.md)

1. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01] Reorganize the module-runtime layer of the C++ runtime into `generated/` + `native/` + `pytra/` shim and move ownership from suffix-based to directory-based rules.
2. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S1-01] Clarify the responsibility boundaries of `generated/`, `native/`, `pytra/`, and `core/` in the spec/plan and record why the name `native` was adopted.
3. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S1-02] Classify current files under `built_in/std/utils/pytra` into generated, native, public shim, and core-non-targets, and create a migration map.
4. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S2-01] Update runtime emit / runtime_paths / public shim generation to assume `generated/` + `pytra/`.
5. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S2-02] Update `runtime_symbol_index`, `cpp_runtime_deps.py`, and build-graph paths to assume `generated/native/pytra`.
6. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S2-03] Update `check_runtime_cpp_layout.py` to directory-based ownership validation.
7. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S3-01] Move generated runtime under `std/` to `generated/std/` and sync `pytra/std/*.h` shims.
8. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S3-02] Move generated runtime under `utils/` to `generated/utils/` and sync `pytra/utils/*.h` shims.
9. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S3-03] Move generated runtime under `built_in/` to `generated/built_in/` and sync the necessary public include surface.
10. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S4-01] Move existing module companions to `native/` and minimize `native/*.h`.
11. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S4-02] For representative modules such as `os_path`, `math`, and `time`, fix the rule "declarations in generated, implementations in native, public surface in pytra shim".
12. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S5-01] Align codegen/unit/parity to the new layout and update old assumptions fixed to `.gen/.ext`.
13. [x] [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S5-02] Update archive/docs/guards and close suffix-based ownership for module runtime as legacy.
- Progress memo: [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S2-01] Switched the canonical `--emit-runtime-cpp` output destination to `src/runtime/cpp/generated/...` and fixed public shims under `src/runtime/cpp/pytra/...`. `runtime_paths.py`, `cli.py`, and `header_builder.py` were updated to align the generator-side include surface.
- Progress memo: [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S4-01] Moved six `std/*.ext.cpp` files and three `built_in/*.ext.h` files under `src/runtime/cpp/native/`, and synchronized `py_runtime.ext.h`, `pytra/built_in/*.h`, and the build-graph tests to assume `native` compile sources.
- Progress memo: [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S5-01] Removed tracked repository assumptions about old `.gen/.ext` module paths; regenerated the untracked `test/transpile/cpp/*/Makefile` artifacts via `tools/gen_makefile_from_manifest.py`; and confirmed fixture/sample parity.
- Progress memo: [ID: P0-CPP-RUNTIME-LAYOUT-REALIGN-01-S5-02] Updated `check_runtime_cpp_layout.py` to assume `legacy-closed module files: 0`, removed legacy `*.gen/*.ext` fallback for module runtime from `gen_runtime_symbol_index.py`, `cpp_runtime_deps.py`, and `cli.py`, and fixed `docs/ja/spec/spec-runtime.md` / `docs/ja/spec/spec-abi.md` to the current `generated/native/pytra` layout.

### P0: split C++ core runtime ownership (`generated/core` + `native/core` + `core` compatibility surface)

Context: [docs/ja/plans/archive/20260307-p0-cpp-core-ownership-split.md](../plans/archive/20260307-p0-cpp-core-ownership-split.md)

1. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01] Introduce `generated/core` + `native/core` into the C++ low-level runtime (`core`) and resolve physical mixing of generated/handwritten code while preserving a stable include surface.
2. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S1-01] Classify existing files under `src/runtime/cpp/core/` into compatibility surface, native canonical sources, generated candidates, and non-targets, then create a migration map.
3. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S1-02] Fix the contract in the plan/spec that `core/` is the compatibility include surface, `generated/core` is the generated source of truth, and `native/core` is the handwritten source of truth, and explain why `pytra/core` is not introduced.
4. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S2-01] Extend `runtime_symbol_index`, `cpp_runtime_deps.py`, and header-resolution paths so they assume `core` public headers + `generated/native/core` compile sources.
5. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S2-02] Update `check_runtime_cpp_layout.py` and `check_runtime_core_gen_markers.py` for the core split and fail fast on implementation re-entry into `core/` and marker mixing.
6. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S3-01] Move handwritten core sources such as `gc/io` under `native/core/` and sync build-graph and compile-source collection.
7. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S3-02] Move handwritten core headers such as `py_runtime/py_types/list/dict/set/str` to `native/core/` as the source of truth, leaving only compatibility forwarders/facades under `core/`.
8. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S3-03] Preserve the `core/...` include surface for backends/generated runtime/tests while fixing the rule that they must not include `native/core` directly.
9. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S4-01] Add the formal `generated/core/` layout and prove compile/source resolution at least once using a real candidate or synthetic fixture.
10. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S4-02] Record criteria for what may be placed under `generated/core` and what core helpers still may not.
11. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S5-01] Update spec / README / representative tests and retire the old `core handwritten-only` assumption.
12. [x] [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S5-02] Update TODO / archive / guards and close the core ownership split as complete.
- Progress memo: [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S3-02] Moved the handwritten core header sources of truth to `src/runtime/cpp/native/core/`, and slimmed all `src/runtime/cpp/core/*.ext.h` files down to forwarders. The `py_runtime.ext.h` inventory guard was synchronized to inspect the `native/core` canonical sources while the public include surface stayed `core/...`.
- Progress memo: [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S3-03] Added a guard to `check_runtime_cpp_layout.py` that forbids direct includes of `runtime/cpp/native/core/...` except through `core/*.ext.h` forwarders, and fixed `test_cpp_runtime_symbol_index_integration.py` so transpile output still uses `runtime/cpp/core/py_runtime.ext.h` and does not touch `native/core`.
- Progress memo: [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S4-01] Added `src/runtime/cpp/generated/core/README.md`, promoted `generated/core` to a formal layout even while empty, and updated `check_runtime_cpp_layout.py` to require the existence of both `generated/core` and `native/core`. Synthetic `dict.ext` cases in `test_runtime_symbol_index.py` and `test_cpp_runtime_build_graph.py` confirmed that compile/source resolution remains green.
- Progress memo: [ID: P0-CPP-CORE-OWNERSHIP-SPLIT-01-S5-01] Aligned the layout boundary in `src/runtime/cpp/std/README.md` to the new contract, and added assertions in `test_runtime_symbol_index.py` that inspect the real repo’s `core` surface, `generated/core` lane, and `native/core` ownership at the same time. `check_runtime_cpp_layout.py` and `test_cpp_runtime_symbol_index_integration.py` were rechecked as well.

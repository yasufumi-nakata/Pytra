# TODO History (2026-03-17)

<a href="../../../ja/todo/archive/20260317.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-17 archived: subscript index identity-cast elision optimization complete

The following entries have been moved from `docs/ja/todo/index.md` to `todo/archive`.

### P1: C++ emitter subscript index cast elision and tuple std::get<I> optimization

Context: [docs/ja/plans/archive/20260317-p1-cpp-subscript-index-cast-tuple-get.md](../plans/archive/20260317-p1-cpp-subscript-index-cast-tuple-get.md)

1. [x] [ID: P1-CPP-SUBSCRIPT-IDX-OPT-01] C++ emitter: elide identity `py_to<int64>` in subscript index when resolved_type is already `int64`; emit `std::get<I>(tup)` directly for tuple constant-index access.
- Progress: Done. Added `resolved_type == int64` guard in the emitter to eliminate identity casts at the emitter level. Verified that existing `::std::get<I>` emit for tuple constant indices already works correctly. Added 6 boundary tests; transpile check 145/145 and selfhost passed. cpp version 0.576.0 → 0.577.0.

## 2026-03-17 archived: py_runtime.h upstream fallback shrink complete

The following entries have been moved from `docs/ja/todo/index.md` to `todo/archive`.

### P2: py_runtime.h upstream fallback shrink

Context: [docs/en/plans/archive/20260317-p2-cpp-pyruntime-upstream-fallback-shrink.md](../plans/archive/20260317-p2-cpp-pyruntime-upstream-fallback-shrink.md)

1. [x] [ID: P2-CPP-PYRUNTIME-UPSTREAM-FALLBACK-SHRINK-01] Push typed fallback out of `py_runtime.h` and back into EAST3, the C++ emitter, and runtime SoT so the header shrinks without physical splitting.
- Progress: Done. S2-03 second bundle applied emitter const qualifiers, removed unnecessary rc_list_ref on nested list rvalues, unified collection-literal boxing, and fixed the make_object str→bool bug. S3-01 synchronized all checkers, unit tests, and the English mirror. All checker/inventory/header-surface checks pass [OK].

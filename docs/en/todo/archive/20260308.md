# TODO History (2026-03-08)

<a href="../../../ja/todo/archive/20260308.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-08 Moved: Completed Sections (8 items)

Moved the following from `docs/ja/todo/index.md` to `todo/archive`.

### P2: `@template` v1 for linked runtime helpers

Context: [docs/ja/plans/archive/20260308-p2-linked-runtime-helper-template-v1.md](../plans/archive/20260308-p2-linked-runtime-helper-template-v1.md)

1. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01] Adopt `@template("T")` as the v1 generic surface for linked runtime helpers, and lock the scope to runtime helpers only with no explicit instantiation.
2. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S1-01] Close the comparison between the `TypeVar` option and the `@template` option, and decide on `@template("T")` as the canonical v1 syntax.
3. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S1-02] Fix the v1 scope in the spec/plan: runtime-helper only, top-level functions only, and no explicit instantiation.
4. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S2-01] Design the canonical parser / EAST / linked metadata shape (for example `meta.template_v1`).
5. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S2-02] Design validation rules for placement, parameter names, duplicates, and runtime-helper-only enforcement.
6. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S3-01] Record the surface-extension policy so it remains compatible with future `@instantiate(...)`.
7. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S3-02] Organize the connection points to follow-up plans for the specialization collector and monomorphization.
8. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S4-01] Synchronize docs / TODO / related plans and fix the v1 generic assumptions.
9. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S4-02] Finish the decision log and acceptance criteria so the plan can be moved to the archive on completion.
- Progress memo: [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S2-02] Added the canonical v1 syntax `@template("T", ...)`, `FunctionDef.meta.template_v1`, and the responsibility split between parser/EAST build and the linked-program validator to `spec-template` and `spec-east`. `TypeVar` remains annotation-only, and runtime-helper provenance is validated fail-closed on the linked-program side.
- Progress memo: [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S4-02] Added the future connection policy for `@instantiate(...)` and the specialization collector to `spec-template`, and synchronized related P2 notes (`runtime SoT linked-program integration`, `runtime helper generics`) around the canonical `@template` syntax and `template_v1` metadata. This plan was completed as a P2 whose purpose was documentation fixation.

### P0: use linked-program builds for `pytra-cli` C++ max optimization

Context: [docs/ja/plans/archive/20260308-p0-pytra-cli-cpp-maxopt-linked-build.md](../plans/archive/20260308-p0-pytra-cli-cpp-maxopt-linked-build.md)

1. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01] Make `pytra-cli --target cpp --build --codegen-opt 3` use the max C++ route including the linked-program optimizer, and lock non-regression with sample parity.
2. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S1-01] Inventory the optimization-stage correspondence between `pytra-cli --codegen-opt` and `py2x/eastlink/ir2lang/py2cpp`, and fix the target semantics of `codegen-opt=3`.
3. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S1-02] Fix the CLI contract and sample-parity gate for the max-opt C++ route in the spec/plan.
4. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-01] Implement the build route so `pytra-cli --target cpp --build --codegen-opt 3` goes through the linked-program optimizer.
5. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-02] Align the transpile-only route for `pytra-cli --target cpp --codegen-opt 3` so it also uses the linked-program optimizer.
6. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-01] Add representative CLI regressions and lock route selection, manifest/build/run behavior for `codegen-opt=3`.
7. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-02] Run sample parity and confirm that C++ samples remain green even on the max-opt route.
8. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S4-01] Reflect the max-opt C++ route and sample parity procedure in `pytra-cli`, how-to-use, and required docs.
9. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S4-02] Record the completion result, move the plan to the archive, and close it.
- Progress memo: [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-01] Added a linked build route dedicated to `codegen-opt=3` in `src/pytra-cli.py`, using `output_dir/.pytra_linked/` as the intermediate bundle through a two-stage flow: `py2x.py --link-only -> py2x.py --from-link-output`. `test_pytra_cli.py` now covers manifest resolution through `make` execution.
- Progress memo: [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-02] Added `--cpp-codegen-opt` to `tools/runtime_parity_check.py`, ran `python3 tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples --cpp-codegen-opt 3 --east3-opt-level 2`, and confirmed `SUMMARY cases=18 pass=18 fail=0`. `how-to-use` and `spec-make` were also synchronized to the multi-file / `--output-dir` contract.

### P0: simplify `@abi` modes to `value` / `value_mut`

Context: [docs/ja/plans/archive/20260308-p0-runtime-abi-mode-simplify-value-value-mut.md](../plans/archive/20260308-p0-runtime-abi-mode-simplify-value-value-mut.md)

1. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01] Simplify the public `@abi` mode names to `default/value/value_mut`, and make argument-side `value` the canonical surface for read-only value ABI.
2. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S1-01] Inventory the current `default/value/value_readonly` contract and fix the migration rule as `value = value_readonly`, `value_mut = the former mutable value ABI`.
3. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S1-02] Write the canonical naming and migration rules into the spec/plan and decide how to handle `value_readonly`.
4. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-01] Update the parser / decorator metadata / validator so they accept `value` / `value_mut` and emit canonical metadata.
5. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-02] Align diagnostics / error messages / target support checks with the new naming.
6. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-01] Migrate annotations on existing helpers (`py_join`, `py_split`, `py_range`, etc.) and expectations on the generated/runtime side to the new naming.
7. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-02] Update representative regressions and confirm non-regression in C++ helpers/codegen.
8. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S4-01] Synchronize docs and how-to-use to the new naming and record migration caveats.
9. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S4-02] Record the completion result, move the plan to the archive, and close it.
- Progress memo: [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-02] Updated `spec-abi` / `spec-east` to the `default/value/value_mut` basis. The parser / validator still accept `value_readonly` as a source alias, but normalize it to `value` in `runtime_abi_v1`. `ret="value_mut"` is rejected fail-closed.
- Progress memo: [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-02] Moved `py_join` in `src/pytra/built_in/string_ops.py` to canonical `@abi(args={"parts": "value"}, ret="value")`, then reran `test_east_core.py -k abi`, `test_global_optimizer.py`, `test_ir2lang_cli.py`, `test_py2cpp_codegen_issues.py -k abi`, and `test_pytra_built_in_string_ops.py` to confirm non-regression.

### P0: remove runtime-module knowledge from backends

Context: [docs/ja/plans/archive/20260308-p0-backend-runtime-knowledge-leak-retirement.md](../plans/archive/20260308-p0-backend-runtime-knowledge-leak-retirement.md)

1. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01] Remove source-side runtime module names and ad hoc helper ABI knowledge from backends/emitters, and unify on a data-driven contract based on resolved runtime symbols, semantic tags, and adapter kinds.
2. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S1-01] Inventory `math/gif/png/save_gif/write_rgb_png/pyMath*` leakage under `src/backends/**`, classify it by target and violation type, and lock representative cases.
3. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S1-02] Explicitly document backend prohibitions and the canonical resolved-metadata path in `spec-runtime` / `spec-dev`.
4. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S2-01] Extend the runtime symbol index / import binding API so module imports, function imports, constant imports, and semantic tags can be resolved outside the backend.
5. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S2-02] Normalize helper ABI differences into adapter kinds and remove argument-convention special cases such as `save_gif` from hand-written emitter logic.
6. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-01] Migrate representative backends such as C++ / JS / CS / RS to resolved runtime symbol / adapter rendering.
7. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-02] Bring Go / Swift / Kotlin / Java / Scala / Ruby / Lua / PHP / Nim into the same contract.
8. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-01] Add representative backend/test/tooling regressions and guards to prevent knowledge leakage from re-entering.
9. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-02] Synchronize docs, run full smoke, and close the plan.
- Progress memo: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-02] Aligned the remaining backends, including `Go` / `Java` / `Scala` / `Lua`, to the canonical runtime-metadata contract, removing source-side branching for `math` helper routing and `pytra.utils` import completion. Targeted regressions for `Go` / `Java` / `Lua` / `Scala` were rerun green under the same ID.
- Progress memo: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-01] Narrowed `tools/check_emitter_runtimecall_guardrails.py` to detect source-side module branches and `pyMath*` helper branches only, with an empty allowlist / zero findings as the baseline. Updated `test_check_emitter_runtimecall_guardrails.py` and `test_runtime_symbol_index.py` as well, and fixed `gif` module-leaf imports and `save_gif` adapter metadata on the tooling side.
- Progress memo: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-02] As representative full-smoke coverage, reran `test_check_emitter_runtimecall_guardrails.py`, `test_runtime_symbol_index.py`, `tools/check_emitter_runtimecall_guardrails.py`, the runtime-metadata cases in `test_code_emitter.py`, and representative runtime-import / math / `save_gif` / fail-closed suites for `CPP` / `JS` / `CS` / `RS` / `Go` / `Kotlin` / `Swift` / `PHP` / `Scala` / `Java` / `Lua` / `Nim` / `Ruby`, confirming `Ran 44 tests in 57.569s`, `OK`.

### P1: introduce `@abi` for generated/runtime helpers

Context: [docs/ja/plans/archive/20260308-p1-runtime-abi-decorator-for-generated-helpers.md](../plans/archive/20260308-p1-runtime-abi-decorator-for-generated-helpers.md)

1. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01] Introduce an `@abi` decorator that can fix boundary ABI for generated/runtime helpers, and make it usable as a boundary policy independent from `@extern`.
2. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S1-01] Document `@abi` syntax / semantics / modes and the responsibility split from `@extern` in `spec-abi`.
3. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S1-02] Decide the EAST / linked metadata format for `@abi` and fix the acceptance criteria for the parser/self-host parser.
4. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S2-01] Add `abi` to `src/pytra/std/__init__.py` and make the parser / self-host parser / AST build preserve the decorator.
5. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S2-02] Add a validator / lowering guard that detects mutation of `value_readonly`.
6. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-01] Implement minimal lowering of `@abi(args, ret)` in the C++ backend and fix helper signatures to the canonical value-ABI form.
7. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-02] Move representative helpers such as `py_join` onto the `@abi` path and lock regression so `rc<list<str>>` is not exposed.
8. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-01] Add unit tests for combined `@extern` usage, unsupported backends, and invalid mutation cases.
9. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-02] Synchronize docs, record the dependency-resolution note for `P1-CPP-PY-RUNTIME-SLIM-01`, and close the plan.
- Progress memo: [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-02] Added `py_join` with `@abi(args={"parts": "value_readonly"}, ret="value")` to `src/pytra/built_in/string_ops.py`, and regenerated the generated C++ runtime `src/runtime/cpp/generated/built_in/string_ops.{h,cpp}` with `const list<str>&` / `list<str>` signatures. Updated `test_pytra_built_in_string_ops.py`, `test_cpp_runtime_iterable.py`, and `test_runtime_symbol_index.py`, locking that generated helpers do not expose `rc<list<str>>`.
- Progress memo: [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-01] Added a target-support guard to `toolchain.frontends.runtime_abi`, making `@abi` fail closed for non-`cpp` targets in both linked-program optimization and raw `ir2lang`. `test_east_core.py` now fixes `@extern + @abi` metadata coexistence, `test_py2cpp_codegen_issues.py` fixes value-ABI lowering under combined usage, and `test_global_optimizer.py` / `test_ir2lang_cli.py` fix unsupported-backend rejection.
- Progress memo: [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-02] Closed this plan from the active TODO and added a note to `docs/ja/plans/p1-cpp-py-runtime-core-slimming.md` that `py_join` has already moved onto the fixed-ABI helper lane. This leaves `P1-CPP-PY-RUNTIME-SLIM-01` unblocked by `str.join`.

### P1: shrink C++ `py_runtime` down to low-level glue

Context: [docs/ja/plans/archive/20260308-p1-cpp-py-runtime-core-slimming.md](../plans/archive/20260308-p1-cpp-py-runtime-core-slimming.md)

1. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01] Separate built-in semantics that can be expressed in pure Python out of `native/core/py_runtime.h`, and slim `core` down so it centers on low-level ABI / object / container / process glue.
2. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S1-01] Inventory functions/classes/helpers in `native/core/py_runtime.h` and classify them into `native/core`, `generated/core`, `generated/built_in`, `native/built_in`, and deferred.
3. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S1-02] Explicitly document the `py_runtime` responsibility boundary and what may remain vs what should be moved back in `spec-runtime` / `spec-dev`.
4. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S2-01] Decide which candidates should move back to `src/pytra/built_in/*.py` and fix the source-of-truth placement plan.
5. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S2-02] Prepare the generator / layout contracts needed for the `generated/core` or `generated/built_in` emission lane.
6. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-01] Gradually remove pure-Python string/collection built-in semantics from `native/core/py_runtime.h` and move them to the canonical generated lane.
7. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-02] Reorganize `native/core/py_runtime.h` around low-level ABI / object / container / process glue and minimize include aggregation.
8. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-01] Align the runtime symbol index / build graph / representative C++ runtime tests with the new ownership layout.
9. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-02] Finish fixture/sample parity, doc synchronization, and the required guards, then close the plan.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-01] Added `py_split`, `py_splitlines`, and `py_count` to `src/pytra/built_in/string_ops.py`, moving the bodies of `str::split` / `splitlines` / `count` / `join` from `native/core/py_runtime.h` to generated helper delegates. Regenerated `generated/built_in/string_ops.{h,cpp}` and also added `@abi(ret="value")` to `py_range` so the existing `list<int64>` helper ABI stayed intact.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-02] Made the C++ emitter add direct built-in headers for modules that use `py_repeat` / `py_range` / `py_any` / `py_all` / `reversed` / `enumerate`, and removed transitive `sequence` / `iter_ops` / `predicates` includes from `native/core/py_runtime.h`. `generated/std/json.cpp` was regenerated with direct includes as well, and `py_runtime.h` was reorganized around low-level glue plus `str` method delegates.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-01] Updated the runtime symbol index / build graph / representative C++ integration tests to follow the ownership change. `test_runtime_symbol_index.py` now fixes the public headers and compile sources for `string_ops` / `sequence`, and `test_cpp_runtime_build_graph.py` fixes direct built-in includes in `generated/std/json.cpp` along with non-reentry of `core/py_runtime.h` seeds.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-02] Added a guard to `tools/check_runtime_cpp_layout.py` against re-entry of removed transitive includes (`predicates` / `sequence` / `iter_ops`) and synchronized `test_check_runtime_cpp_layout.py` and the spec. Then reran `test_cpp_runtime_iterable.py`, `test_pytra_built_in_string_ops.py`, the layout guard, the marker guard, fixture parity (`cases=3 pass=3 fail=0`), and sample parity (`cases=18 pass=18 fail=0`) to close the plan.

### P1: full sample parity across all targets

Context: [docs/ja/plans/archive/20260308-p1-all-target-sample-parity-rollout.md](../plans/archive/20260308-p1-all-target-sample-parity-rollout.md)

1. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01] Make sample parity finish successfully for all parity targets without `toolchain_missing`, and fix the execution baseline for all-target green.
2. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S1-01] Inventory `runner_needs` and the current `toolchain_missing` status across all parity targets, and build a missing-toolchain matrix by target.
3. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S1-02] Fix the done condition for "all-target parity green", disallowed failure categories, and validation commands in the spec/plan.
4. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S2-01] Prepare toolchain bootstrap procedures for compiled targets (`rs/cs/go/java/kotlin/swift/scala`) and eliminate `toolchain_missing`.
5. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S2-02] Prepare toolchain bootstrap procedures for scripting / mixed targets (`ruby/lua/php/nim`) and eliminate `toolchain_missing`.
6. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-01] Reconfirm sample parity for baseline targets (`cpp/js/ts`) and keep `18/18` while other targets are being repaired.
7. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-02] Bring sample parity for compiled targets (`rs/cs/go/java/kotlin/swift/scala`) up to green.
8. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-03] Bring sample parity for scripting / mixed targets (`ruby/lua/php/nim`) up to green.
9. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-01] Prepare scripts / docs / how-to-use for one-shot execution across all targets and fix the rerun procedure.
10. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-02] Record the full parity result, move the plan to the archive, and close it.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-02] After preparing compiled-target toolchains on the current Debian 12 machine, fixed the canonical runtime import collision in Rust and raised `rs/cs/go/java/kotlin/swift/scala` to `SUMMARY cases=18 pass=18 fail=0` one target at a time. Scala CLI was switched to `/usr/local/bin/scala` to match the `scala run ...` runner contract.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-03] Reran target-specific parity for `ruby/lua/php/nim` as well and confirmed `SUMMARY cases=18 pass=18 fail=0` for all four targets. At this point, both `toolchain_missing` and runtime/backend gaps were cleared on the current machine.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-01] Added `tools/check_all_target_sample_parity.py` and fixed the canonical wrapper that runs the four groups `cpp` / `js_ts` / `compiled` / `scripting_mixed` in order. `spec-tools` and `how-to-use` were also synchronized to the wrapper-based rerun procedure.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-02] Recorded the full parity result as the sum of the canonical subgroups. Baseline `cpp/js/ts` recorded `SUMMARY cases=18 pass=18 fail=0 targets=cpp,js,ts`, while the seven compiled targets and four scripting/mixed targets were each confirmed individually at `18/18`, achieving sample-parity green across all 14 targets.

### P4: repair non-C++ backends after linked-program rollout

Context: [docs/ja/plans/archive/20260308-p4-noncpp-backend-recovery-after-linked-program.md](../plans/archive/20260308-p4-noncpp-backend-recovery-after-linked-program.md)

1. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01] Bring non-C++ backends into a common post-linked-program contract based on `SingleFileProgramWriter`, and resolve the broken state family by family.
2. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S1-01] Build a post-linked-program health matrix for non-C++ backends and classify each target by failure category.
3. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S1-02] Fix the done conditions (handling of static/smoke/transpile/parity/toolchain-missing) and repair order in the spec/plan.
4. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S2-01] Audit the non-C++ compatibility layer in `backend_registry.py` / `py2x.py` / `ir2lang.py` and fill the common-contract gaps required by `SingleFileProgramWriter`.
5. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S2-02] Add or unify a non-C++ backend health checker so broken/green status can be seen per family in a single command.
6. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S3-01] Resolve static-contract / smoke / transpile failures for Wave 1 (`rs/cs/js/ts`) and stabilize the compatibility route.
7. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S3-02] Refresh the Wave 1 parity baseline and separate runtime differences from infrastructure failures.
8. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-01] Resolve static-contract / smoke / transpile failures for Wave 2 (`go/java/kotlin/swift/scala`).
9. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-02] Refresh the Wave 2 parity baseline and lock down `toolchain_missing`, execution failures, and artifact differences.
10. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-01] Resolve static-contract / smoke / transpile failures for Wave 3 (`ruby/lua/php/nim`).
11. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-02] Refresh the Wave 3 parity baseline and separate runtime differences from backend bugs.
12. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-01] Integrate the non-C++ backend health check into `run_local_ci.py` or an equivalent regression path.
13. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-02] Update `docs/ja/spec` / `docs/en/spec` / `docs/ja/how-to-use.md`, fix the post-linked-program operating rules for non-C++ backend recovery, and close the plan.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-01] The main Wave 2 failures converged on missing source-origin call metadata for bare `Path(...)` construction and `src.*` imports in `kotlin/swift`. Added bare `Path(...)` constructor fallbacks to `go/java/scala`, then fixed `super` preservation in `java` and import paths in `kotlin/swift`, confirming smoke/transpile green.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-02] For Wave 2 parity, all 18 sample cases of `go/java/kotlin/swift/scala` were still `toolchain_missing`. This isolated the remaining issue as infrastructure baseline only, not backend bugs.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-01] Wave 3 failures came from accidental `src.*` imports in `ruby/php/nim` and stale smoke expectations in `lua`. The former were aligned to `toolchain.*` imports, and the latter were updated to `_G.math.max` and a truthiness-preserving if-expression closure, bringing the wave green.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-02] Wave 3 parity also converged to all 18 sample cases being `toolchain_missing` for `ruby/lua/php/nim`. Across Waves 1-3, backend bugs and infrastructure baseline were fully separated.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-01] Added `python3 tools/check_noncpp_backend_health.py --family all --skip-parity` to `tools/run_local_ci.py`, and validated by running `wave1/wave2/wave3` separately, confirming `status=green` for every family.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-02] Synchronized `docs/ja/spec/spec-tools.md`, `docs/en/spec/spec-tools.md`, and `docs/ja/how-to-use.md` to the post-linked-program non-C++ backend workflow and moved the plan to the archive.

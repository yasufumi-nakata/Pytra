# TODO History (2026-03-08)

<a href="../../../ja/todo/archive/20260308.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

## 2026-03-08 Migration: Completed sections (31 items)

Moved the following from `docs/ja/todo/index.md` to `todo/archive`.

### P4: Restore C++ selfhost stage1 / direct route / stage2

Context: [docs/ja/plans/archive/20260308-p4-cpp-selfhost-rollout.md](../plans/archive/20260308-p4-cpp-selfhost-rollout.md)

1. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01] Restore C++ selfhost stage1 build / direct route / diff / stage2 under the current runtime/layout contract.
2. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S1-01] Inventory `tools/build_selfhost.py` failure points and missing artifacts.
3. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S1-02] Fix the acceptance order and current source of truth for selfhost restoration in the decision log.
4. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S2-01] Restore the generated/static frontend artifact supply required for stage1 build under the current layout.
5. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S2-02] Return `tools/build_selfhost.py` to green and regenerate `selfhost/py2cpp.out`.
6. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S3-01] Restore the direct `.py` route and pass `tools/check_selfhost_direct_compile.py`.
7. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S3-02] Return host/selfhost diff and representative e2e to green.
8. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S4-01] Restore `tools/build_selfhost_stage2.py` under the current contract.
9. [x] [ID: P4-CPP-SELFHOST-ROLLOUT-01-S4-02] Update the docs / archive / local-CI gate policy and close this plan.
- Progress memo: [ID: P4-CPP-SELFHOST-ROLLOUT-01-S2-02] Added the `src/runtime/cpp/{generated,native,pytra}/compiler` lane and aligned runtime-source resolution in `build_selfhost.py` / `build_selfhost_stage2.py` / `verify_selfhost_end_to_end.py` to the current `generated/native/core` layout. Also moved `src/py2x-selfhost.py` toward the manual CLI path, and `python3 tools/build_selfhost.py` returned to green through generating `selfhost/py2cpp.out`.
- Progress memo: [ID: P4-CPP-SELFHOST-ROLLOUT-01-S3-02] Restored the direct `.py` route through the host-Python bridge in `runtime/cpp/native/compiler/{transpile_cli,backend_registry_static}.cpp`. Confirmed `tools/check_selfhost_direct_compile.py` reports `failures=0` for the full sample set, representative `tools/check_selfhost_cpp_diff.py --mode strict` reports `mismatches=0`, and `tools/verify_selfhost_end_to_end.py --skip-build` also reports `failures=0`.
- Progress memo: [ID: P4-CPP-SELFHOST-ROLLOUT-01-S4-01] Restored `python3 tools/build_selfhost_stage2.py` through generating `selfhost/py2cpp_stage2.out`, and confirmed `python3 tools/check_selfhost_stage2_cpp_diff.py --mode strict` reports `mismatches=0 known_diffs=0 skipped=0` over the representative 8 cases.
- Progress memo: [ID: P4-CPP-SELFHOST-ROLLOUT-01-S4-02] Updated `docs/ja/how-to-use.md` and `docs/ja/spec/spec-tools.md` for current selfhost operation, and returned the selfhost diff gate in `tools/run_local_ci.py` to strict mode. Representative selfhost regression for stage1/direct/stage2 is now back as a normal local-CI gate.

### P3: Unify internal JSON raw-dict loaders in compiler/backend onto `JsonValue`

Context: [docs/ja/plans/archive/20260308-p3-compiler-jsonvalue-internal-alignment.md](../plans/archive/20260308-p3-compiler-jsonvalue-internal-alignment.md)

1. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01] Align internal JSON raw-dict loaders in compiler/backend to a decode-first contract based on `JsonValue`.
2. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S1-01] Inventory places inside compiler/backend that use `json.loads(...)` directly.
3. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S1-02] Fix selfhost blockers and host-only deferred items in the decision log.
4. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S2-01] Move the JSON root loader in `transpile_cli.py` to `loads_obj()`.
5. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S2-02] Move JSON loaders in `runtime_symbol_index.py` and `code_emitter.py` to the `JsonValue` lane.
6. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S3-01] Align backend internal loaders such as `js_emitter.py` to the `JsonValue` lane.
7. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S3-02] Update representative tests / selfhost-related regressions.
8. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S4-01] Add a guard against re-entry of raw `json.loads(...)`.
9. [x] [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S4-02] Sync docs / archive and close this plan.
- Progress memo: [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S2-01] Moved `.json` root unwrap in `transpile_cli.py` to `json.loads_obj(...)` + `JsonObj.get_bool/get_obj/get_str`, and limited `dict[str, object]` conversion to `east_obj.raw` / `payload.raw` after wrapper/module judgment. Reduced raw `json.loads(...)` in frontend entrypoints by one.
- Progress memo: [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S2-02] Moved the cache loader in `runtime_symbol_index.py` and `CodeEmitter._load_json_dict()` to `json.loads_obj(...)`, and unified raw-root detection immediately after parse under `JsonObj`. Internal storage remains `dict[str, Any]`, but raw `json.loads(...)` is gone.
- Progress memo: [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S3-02] Also aligned the private profile loader in `js_emitter.py` to `json.loads_obj(...)`, added non-object-root regressions to `test_code_emitter.py` and `test_runtime_symbol_index.py`, and confirmed `load_js_profile()` stays green in `test_py2js_smoke.py`.
- Progress memo: [ID: P3-COMPILER-JSONVALUE-INTERNAL-01-S4-01] Added `transpile_cli.py` / `runtime_symbol_index.py` / `code_emitter.py` / `js_emitter.py` to the targets of `tools/check_jsonvalue_decode_boundaries.py`, expanding the ban on raw `json.loads(...)` re-entry from artifact boundaries to internal compiler/backend JSON loaders as well.

### P2: Introduce ambient-global extern variables for JS/TS

Context: [docs/ja/plans/archive/20260308-p2-js-ambient-global-extern.md](../plans/archive/20260308-p2-js-ambient-global-extern.md)

1. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01] Introduce ambient-global extern variables for JS/TS.
2. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S1-01] Fix the variable ambient-global contract for `extern()` / `extern("symbol")` in the docs.
3. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S1-02] Fix the boundary against the existing `extern(expr)` host fallback in the decision log.
4. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S2-01] Add an ambient-global variable marker to the parser / EAST metadata.
5. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S2-02] Fix same-name / alias cases with representative IR/unit tests.
6. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S3-01] Lower ambient-global extern variables to import-free symbols in the JS/TS emitter.
7. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S3-02] Add raw lowering of properties / methods / calls on ambient-global `Any` receivers.
8. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S4-01] Update unsupported-backend guard / representative smoke.
9. [x] [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S4-02] Sync docs / archive and close this plan.
- Progress memo: [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S3-02] Updated the JS/TS emitter so it first collects top-level ambient-global extern declarations into an alias table and then emits no corresponding `AnnAssign` itself. Fixed same-name / alias cases with smoke for `document.title`, `document.getElementById(...)`, and `alert(node)`.
- Progress memo: [ID: P2-JS-AMBIENT-GLOBAL-EXTERN-01-S4-01] Split the unsupported-backend guard into `toolchain.frontends.extern_var.validate_ambient_global_target_support(...)` and made both `py2x.py` and `ir2lang.py` fail early with it. Added regressions confirming that both source input and link-output restart return `RuntimeError` before backend dispatch.

### P0: Retire `dict_get_node` reduction for `dict<str, str>` in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-dict-str-node-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-dict-str-node-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01] Reduce `dict_get_node` overloads for `dict<str, str>`.
2. [x] [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S1-01] Inventory checked-in call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S1-02] Fix the minimum wrapper to keep.
4. [x] [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S2-01] Remove redundant overloads.
5. [x] [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S2-02] Update representative tests.
6. [x] [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S3-01] Sync docs / archive.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S1-02] Confirmed that there were no checked-in call sites besides the definition itself, and that keeping only `const str& key, const str& defval` is enough because string literals are absorbed through implicit `str` conversion.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S2-01] Removed three `const char*` / mixed wrappers from `py_runtime.h` and reduced the canonical overload to direct `find` returning `str`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICT-STR-NODE-01-S2-02] Representative verification passed `test_cpp_runtime_iterable.py`, `test_py2cpp_codegen_issues.py`, `tools/check_todo_priority.py`, and `git diff --check`.

### P0: Retire `py_to_str_list_from_object` in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-strlist-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-strlist-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01] Retire `py_to_str_list_from_object`.
2. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S1-01] Inventory call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S1-02] Fix the typed-argv / decode replacement policy.
4. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S2-01] Replace representative call sites.
5. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S2-02] Remove the helper.
6. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S3-01] Update parity / docs / archive.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S1-02] Checked-in runtime call sites were limited to `argparse.parse_args(argv)`, so the policy was fixed to preserve `argv: Any` while generated C++ goes through `py_to<rc<list<str>>>(argv)`. Added a `list<T>(rc<list<T>>)` constructor so it can be received into a value local.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S2-02] Removed `py_to_str_list_from_object(const object&)` from `py_runtime.h`, and replaced the remaining direct helper call in `type_bridge.py` with the `list<str>(expr)` lane. `object` / `rc<list<str>>` / `list<str>` are now absorbed on the same coercion surface.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-STRLIST-01-S3-01] Representative verification passed `test_py2cpp_features.py -k argparse_extended_runtime`, `test_cpp_runtime_iterable.py`, `test_py2cpp_codegen_issues.py`, `test_east3_cpp_bridge.py`, and fixture parity `cases=1 pass=1 fail=0`.

### P0: Retire `py_dyn_range_*` compatibility wrappers in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-dyn-range-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-dyn-range-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DYNRANGE-01] Retire the `py_dyn_range_*` compatibility wrappers.
2. [x] [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S1-01] Inventory `py_dyn_range` call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S1-02] Fix the replacement order toward typed iterables.
4. [x] [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S2-01] Replace representative call sites.
5. [x] [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S2-02] Remove or minimize `py_dyn_range_*`.
6. [x] [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S3-01] Update guard / parity / docs.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S2-02] Regenerated `contains/json/pathlib/re/png/gif/assertions` with `--emit-runtime-cpp`, removed `py_dyn_range_iter` / `py_dyn_range_view` / `py_dyn_range(...)` from `py_runtime.h`, replaced runtime smoke with explicit `py_iter_or_raise()/py_next_or_stop()`, and inverted the inventory guard to assume the removed signatures.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S2-02] Regressions exposed during regeneration were limited to three items: a no-op assignment in `utils/assertions.py`, a missing ref-first/value-list adapter in the `bytes(list)` constructor, and typed `ForCore` loop targets not being treated as stack/value-list locals inside the body. These were resolved with source cleanup, a value-list copy adapter in the `bytes/bytearray` constructors, and scope-local registration of typed loop targets.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNRANGE-01-S3-01] Representative verification passed `test_cpp_runtime_iterable.py`, `test_east3_cpp_bridge.py`, `test_py2cpp_codegen_issues.py`, `test_pylib_json.py`, fixture parity `cases=3 pass=3 fail=0`, and sample parity `cases=18 pass=18 fail=0`.

### P0: Retire dynamic iteration primitives in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-dynamic-iteration-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-dynamic-iteration-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DYNITER-01] Reduce dynamic iteration primitives.
2. [x] [ID: P0-CPP-PYRUNTIME-DYNITER-01-S1-01] Inventory `py_iter_or_raise/object` call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-DYNITER-01-S1-02] Fix the typed / nominal replacement policy.
4. [x] [ID: P0-CPP-PYRUNTIME-DYNITER-01-S2-01] Replace representative call sites.
5. [x] [ID: P0-CPP-PYRUNTIME-DYNITER-01-S2-02] Remove or minimize primitive bridges.
6. [x] [ID: P0-CPP-PYRUNTIME-DYNITER-01-S3-01] Update parity / docs / archive.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNITER-01-S1-02] Checked-in call sites were limited to `ObjIterInit` / `ObjIterNext` lowering in `CppEmitter` and runtime smoke; it was confirmed that `py_dyn_range` and `begin/end(object)` could stay for a separate tranche.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNITER-01-S2-02] Updated `ObjIterInit` / `ObjIterNext` from lambda wrappers with null guards to direct `PyObj::py_iter_or_raise()` / `PyObj::py_next_or_stop()` calls, removed the free helpers `py_iter_or_raise(const object&)` / `py_next_or_stop(const object&)` from `py_runtime.h`, and aligned the internal implementation of `py_dyn_range_view` to method calls as well.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNITER-01-S3-01] Representative verification passed `test_east3_cpp_bridge.py`, `test_py2cpp_codegen_issues.py -k any_boundary_builtin_names`, `test_cpp_runtime_iterable.py`, fixture parity `cases=3 pass=3 fail=0`, and sample parity `cases=18 pass=18 fail=0`.

### P0: Retire `dict_get_*` convenience in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-dict-get-convenience-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-dict-get-convenience-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01] Retire `dict_get_*` convenience.
2. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S1-01] Inventory `dict_get_*` call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S1-02] Fix the replacement table toward the `JsonObj` API.
4. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S2-01] Replace representative call sites / tests.
5. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S2-02] Remove `dict_get_*` convenience.
6. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S3-01] Update guard / parity / docs.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S1-02] After inventorying checked-in production/backend call sites, the only direct production user of `dict_get_bool/str/int/float/list/node(dict<str, object>, ...)` was the object-dict lane of `CppEmitter._render_dict_get_default_expr()`. Runtime / generated JSON helpers had already moved to `JsonObj.get_*()` and `py_dict_get_default` / typed decode, while the dedicated `dict_get_node` for `dict<str, str>` was judged out of scope and allowed to remain.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S2-01] Replaced the object-dict path in `CppEmitter._render_dict_get_default_expr()` with `py_dict_get_default(...)` and an explicit lambda (`contains/find -> py_to<T>(it->second)`), so optional owners and typed-list defaults are rendered without runtime convenience. Updated representative codegen tests that had depended on `dict_get_*` to assume lambda / `py_dict_get_default`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S2-02] Removed `dict_get_bool/str/int/float/list` and `dict_get_node(dict<str, object>, ...)` from `src/runtime/cpp/native/core/py_runtime.h`, and added a removed-inventory guard to `test_cpp_runtime_iterable.py`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTGET-CONVENIENCE-01-S3-01] Representative verification passed `test_py2cpp_codegen_issues.py -k dict_get`, `test_cpp_runtime_iterable.py`, `test_pylib_json.py`, `test_py2cpp_features.py -k json`, and fixture parity `cases=3 pass=3 fail=0`.

### P0: Retire the `object == str` comparison lane in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-string-compare-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-string-compare-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRCMP-01] Retire object-string comparison convenience.
2. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRCMP-01-S1-01] Inventory object-string comparison call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRCMP-01-S1-02] Fix the explicit-decode replacement policy.
4. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRCMP-01-S2-01] Replace representative call sites.
5. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRCMP-01-S2-02] Remove comparison overloads.
6. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-STRCMP-01-S3-01] Update guard / parity / docs.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-STRCMP-01-S3-01] After inventorying checked-in C++ backend/runtime/sample code, no explicit call sites remained for `object == str` or `object == char*`, so the overloads were removed while keeping only `nullopt` comparison compatibility. Added a removed-inventory guard to `test_cpp_runtime_iterable.py` and confirmed non-regression with `test_cpp_runtime_iterable.py`, `tools/check_todo_priority.py`, and `git diff --check`.

### P0: Retire `begin/end(object)` and ADL helpers in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-begin-end-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-begin-end-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-BEGINEND-01] Retire `begin/end(object)` and the ADL helpers.
2. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-BEGINEND-01-S1-01] Inventory range-for compatibility call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-BEGINEND-01-S1-02] Fix the typed-iterable replacement policy.
4. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-BEGINEND-01-S2-01] Replace representative call sites.
5. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-BEGINEND-01-S2-02] Remove `begin/end(object)` and the ADL helpers.
6. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-BEGINEND-01-S3-01] Update parity / docs / archive.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-BEGINEND-01-S3-01] After `py_dyn_range` retirement, inventory of checked-in C++ backend/runtime/sample code showed no explicit call sites remained for `begin/end(object)` or `pytra::gc::begin/end(RcHandle<PyObj>)`, so the inventory guard was made the source of truth and the definitions themselves were removed. Re-ran `test_cpp_runtime_iterable.py`, `tools/check_todo_priority.py`, and `git diff --check` to confirm non-regression.

### P0: Reduce the generic `py_dict_get_default` overloads in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-generic-dict-get-default-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-generic-dict-get-default-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01] Reduce the generic `py_dict_get_default` overloads.
2. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S1-01] Inventory checked-in call sites for the generic overloads.
3. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S1-02] Decide the primitive wrappers to keep.
4. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S2-01] Remove redundant overloads.
5. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S2-02] Update codegen / runtime tests.
6. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S3-01] Sync parity / docs / archive.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S1-02] After inventorying checked-in production/backend call sites, the current C++ emitter was already lowering typed `dict.get(key, default)` directly to `owner_expr.get(key_expr, default_expr)` and no longer depended on generic `py_dict_get_default(...)`. The only checked-in user that still needed the generic overload was the selfhost artifact `selfhost/runtime/cpp/pytra-gen/compiler/east_parts/core.cpp`, limited to `(a) dict<int64, tuple<...>> + same-type default` and `(b) dict<str, str> + str key + const char* default`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S2-02] Removed the `optional<dict<...>>` wrapper, `dict<str, V> + ::std::string key` wrapper, and the full set of generic convertible-default wrappers from `src/runtime/cpp/native/core/py_runtime.h`, leaving only dedicated overloads needed for selfhost-artifact compatibility: `dict<str, str> + {const char*, str} + const char*`. Added a removed-signature guard to `test_cpp_runtime_iterable.py` and fixed the kept special case with `In`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-GENERIC-01-S3-01] Representative verification passed `test_cpp_runtime_iterable.py`, `test_py2cpp_codegen_issues.py`, and fixture parity `cases=3 pass=3 fail=0`. The full sample parity runner was not adopted because it stalled during sample 05 build/run; instead, that sample was rerun individually with `pytra-cli --target cpp --build --run` and confirmed green.

### P0: Reduce `py_dict_get_default` specialized for `dict<str, object>` in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-dict-default-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-dict-default-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01] Reduce `py_dict_get_default` specialized for `dict<str, object>`.
2. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S1-01] Inventory call sites for object-dict default access.
3. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S1-02] Fix the sequence of shifting toward `JsonObj.get_*`.
4. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S2-01] Move representative call sites to `JsonObj` helpers.
5. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S2-02] Remove object-dict default overloads.
6. [x] [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S3-01] Update regression / parity / docs.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S1-02] After inventorying checked-in call sites, the current production path used `dict<str, object>`-specific `py_dict_get_default(...)` directly only in the objectish lane of `CppEmitter._render_dict_get_default_expr()` and in `dict_get_bool/str/int/float/list/node`. Generated `JsonObj` helpers had already started to shift to `_json_obj_require(...)` and `JsonObj.get_*()`, while direct use in selfhost artifacts was limited to decode-first-yet-to-be-migrated areas such as `tgt/id/attr/source_span`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S2-01] Representative call sites were already using `_json_obj_require(...)` / `JsonObj.get_*()` in generated `std/json`, so the runtime-side representatives `dict_get_bool/str/int/float/list` were moved to direct `find(str(key)) -> it->second` implementations. This removed the last checked-in production path that depended on the plain object-dict default overload.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S2-02] Removed the full set of `dict<str, object>`-specific `py_dict_get_default(..., object/char*/str/dict<str, object>)` overloads from `src/runtime/cpp/native/core/py_runtime.h`, leaving only the template `<class D>` wrapper. Added a removed-inventory guard to `test_cpp_runtime_iterable.py`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTDEFAULT-OBJECT-01-S3-01] Representative verification passed `test_cpp_runtime_iterable.py`, `test_pylib_json.py`, `test_py2cpp_features.py -k json`, and fixture parity `cases=3 pass=3 fail=0`.

### P0: Retire the direct `py_dict_get(dict<str, object>)` lane in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-dict-get-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-dict-get-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01] Retire the direct `py_dict_get(dict<str, object>, ...)` lane.
2. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S1-01] Inventory checked-in call sites of the direct getter.
3. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S1-02] Decide the replacement policy toward `JsonObj` / typed dicts.
4. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S2-01] Move call sites to `JsonObj` accessors.
5. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S2-02] Remove the direct getter from `py_runtime.h`.
6. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S3-01] Update regression / parity / docs.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S1-02] After inventorying checked-in call sites, only the runtime source itself depended on the `dict<str, object>`-specific `py_dict_get(const char*)` / `py_dict_get(const ::std::string&)` in production; generated `std/json.cpp` `JsonObj::get*` had already fallen back to generic `py_dict_get(dict<str, V>, const str&)` with `str` keys. `argparse` and `type_id` were fixed as typed-dict lanes and kept out of scope.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S2-01] Added `_json_obj_require(raw, key)` to `src/pytra/std/json.py`, moved `JsonObj.get/get_obj/get_arr/get_str/get_int/get_float/get_bool` to use that helper after a membership check, regenerated the checked-in runtime `src/runtime/cpp/generated/std/json.{h,cpp}` and the public shim, and removed `py_dict_get(this->raw, key)` from the runtime helper path.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S2-02] Removed `py_dict_get(const dict<str, object>&, const char*)` and `py_dict_get(const dict<str, object>&, const ::std::string&)` from `src/runtime/cpp/native/core/py_runtime.h`. Added removed-inventory guards to `test_cpp_runtime_iterable.py`, and fixed `test_py2cpp_features.py` so `_json_obj_require` and `make_object(_json_obj_require(...))` generation is locked.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-DICTGET-01-S3-01] Representative verification passed `test_py2cpp_features.py -k json`, `test_cpp_runtime_iterable.py`, `test_pylib_json.py`, and fixture parity `cases=3 pass=3 fail=0`. Since the scope of this tranche was limited to JSON runtime helpers and object-dict special getters, least-surface regression plus fixture parity were used as the release gate.

### P0: Retire `py_dict_get_maybe` in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-dict-get-maybe-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-dict-get-maybe-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01] Reduce `py_dict_get_maybe` convenience.
2. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S1-01] Inventory `py_dict_get_maybe` call sites.
3. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S1-02] Fix the migration policy toward `JsonObj` / explicit defaults.
4. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S2-01] Replace representative call sites.
5. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S2-02] Reduce `py_dict_get_maybe` overloads.
6. [x] [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S3-01] Update guard / docs / parity.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S1-02] No checked-in production call sites remained; only `CppEmitter` `DictGetMaybe` lowering and expectations in `test_east3_cpp_bridge.py` were left. The policy was fixed so typed `dict.get(key)` is expanded on the C++ side as `contains/at + optional`, not through runtime convenience.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S2-02] Removed the entire `py_dict_get_maybe` block from `src/runtime/cpp/native/core/py_runtime.h`, and replaced `CppEmitter` `DictGetMaybe` with `([&]() -> ::std::optional<T> { auto&& __dict = ...; auto __key = ...; return contains ? ::std::optional<T>(at) : ::std::nullopt; }())`. `test_cpp_runtime_iterable.py` now also fixes re-entry of generic / `optional<dict<...>>` overloads with `NotIn`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DICTGET-MAYBE-01-S3-01] Representative verification passed `test_east3_cpp_bridge.py`, `test_cpp_runtime_iterable.py`, `test_pylib_json.py`, fixture parity `cases=3 pass=3 fail=0`, and sample parity `cases=18 pass=18 fail=0`.

### P0: Retire the object read bridge in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-read-bridge-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-read-bridge-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01] Retire object read bridges in `py_runtime.h` (`py_at(object)` / `py_slice(object)`) and return to typed / `JsonArr` accessors as the source of truth.
2. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S1-01] Inventory checked-in call sites of `py_at(object)` / `py_slice(object)`.
3. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S1-02] Fix `JsonArr` dependencies and the removal order in the decision log.
4. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S2-01] Replace JSON / runtime call sites with typed / nominal accessors.
5. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S2-02] Update representative regressions.
6. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S3-01] Remove `py_at(object)` / `py_slice(object)`.
7. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S3-02] Update parity / docs / archive and close the plan.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S1-02] After inventorying checked-in call sites, the only production user of `py_at(object)` / `py_slice(object)` was the generated `std/json` `JsonArr::get*` group, with the remainder limited to inventory guards in `test_cpp_runtime_iterable.py`. Before dropping the helpers from `py_runtime.h`, the order was fixed so `JsonArr.raw: object` goes through the typed `list<object>` read helper `_json_array_items`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S2-02] Introduced `_json_array_items` / `_json_new_array` with `@abi(ret="value")` in `src/pytra/std/json.py`, moved `JsonArr.get*` to `_json_array_items(self.raw)[index]`, and moved `_parse_array()` to initialize a typed `list<object>` local from `_json_new_array()` and then `append` to it. In `stmt.py`, type inference was reinforced so plain locals declared from `@abi(ret="value")` call returns receive `list[...]` as `list<...>` signatures.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-READ-01-S3-02] Representative verification passed `test_py2cpp_features.py -k json`, `test_cpp_runtime_iterable.py`, `test_pylib_json.py`, fixture parity `cases=3 pass=3 fail=0`, and sample parity `cases=18 pass=18 fail=0`. `py_at(const object&, int64)` and the two `py_slice(const object&, ...)` overloads are now removed from `py_runtime.h`, and the read fallback is confined to generated JSON helpers.

### P0: Retire the first wave of object collection bridges in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-collection-bridge-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-collection-bridge-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01] Remove the mutation / lookup object-collection bridges in `py_runtime.h` and return typed list/dict/string paths to the source of truth.
2. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S1-01] Inventory checked-in call sites and compatibility dependencies for `py_at/py_slice/py_append/py_set_at/py_extend/py_pop/py_clear/py_reverse/py_sort/py_index`.
3. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S1-02] Fix the removal order and the rule of keeping no compatibility lane in the decision log.
4. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S2-01] Remove the object lanes of `py_append/py_set_at/py_extend/py_pop/py_clear/py_reverse/py_sort`.
5. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S2-02] Remove `py_index(const object&, const object&)` and update the inventory guard.
6. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S3-01] Fix `py_at(const object&, int64)` and `py_slice(const object&, ...)` as deferred, and pass read-bridge dependencies to the next tranche.
7. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S3-02] Update representative regressions / inventory notes after deferring the read bridge.
8. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S4-01] Confirm representative unit / fixture / sample parity and line-count diffs.
9. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S4-02] Sync docs / archive / TODO history and close the plan.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S1-02] After inventorying checked-in call sites, direct dependence on mutation / lookup object-collection bridges was almost limited to object-mutation smoke in `test_cpp_runtime_iterable.py`, with only `py_at(object)` / `py_slice(object, ...)` still remaining because of current `JsonArr.raw: object` dependence. The first wave was fixed to remove only mutation bridges and `py_index(object, ...)`, sending the read bridge to the next tranche.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S2-02] Removed `py_append/py_set_at/py_extend/py_pop/py_clear/py_reverse/py_sort(const object&)` and `py_index(const object&, const object&)` from `py_runtime.h`, updated `test_cpp_runtime_iterable.py` so regressions use the low-level lane `obj_to_list_obj(...)->value` and `py_list_*_mut`, and explicitly declared `list[object]` locals in `iter_ops.cpp` to remove generated-helper dependence on `object out`.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-COLLECTION-01-S4-02] Representative verification passed `test_cpp_runtime_iterable.py`, `test_cpp_runtime_boxing.py`, `check_runtime_std_sot_guard.py`, and fixture parity `cases=3 pass=3 fail=0`. For sample parity, after the full run was green for `17/18`, the failing `18_mini_language_interpreter` was rerun and confirmed `pass=1 fail=0`. The line count of `py_runtime.h` shrank from `2337 -> 2246` compared with the plan start.

### P0: Retire the object lane in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-object-lane-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-object-lane-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01] Reduce JSON accessor convenience and object arithmetic convenience in `py_runtime.h`, returning to `JsonObj` accessors and typed value paths as the source of truth.
2. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S1-01] Inventory call sites and compatibility dependencies of `py_dict_get*` / `dict_get_*` / object arithmetic.
3. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S1-02] Fix the removal order and the minimum compatibility lane to keep in the decision log.
4. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S2-01] Reduce the first slice of `py_dict_get(const object&)` / `py_dict_get_default(const object&, ...)` and shift to `JsonObj` accessors.
5. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S2-02] Reduce the `object` / `optional<dict<str, object>>` lanes of `dict_get_bool/str/int/float/list/node` and update regressions.
6. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S3-01] Inventory call sites of object arithmetic / unary ops / compound assignment and remove or replace them.
7. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S3-02] Add a guard / regression to prevent re-entry of object arithmetic.
8. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S4-01] Confirm representative unit / fixture / sample parity and line-count diffs.
9. [x] [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S4-02] Sync docs / archive / TODO history and close the plan.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S1-01] After inventorying checked-in call sites, production dependence on `py_dict_get*` and `dict_get_*` was limited to the `dict<str, object>` / generic-dict lanes, while `object` receivers and `optional<dict<str, object>>` receivers remained only in runtime convenience. No checked-in call sites for object arithmetic were found, so full removal was made the source of truth.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S2-02] Removed `py_dict_get(const object&)`, `py_dict_get_maybe(const object&)`, `py_dict_get_default(const object&)`, `py_dict_get_default(optional<dict<str, object>>)` and the `object` / `optional<dict<str, object>>` lanes of `dict_get_bool/str/int/float/list/node` from `py_runtime.h`. Kept the `dict<str, object>` and generic-dict lanes and kept `test_cpp_runtime_iterable.py`, `test_cpp_runtime_boxing.py`, and `test_pylib_json.py` green.
- Progress memo: [ID: P0-CPP-PYRUNTIME-OBJECT-LANE-01-S4-02] Removed object arithmetic (`+`, `-`, `*`, `/`, unary `-`, `+=`, `-=`, `*=`, `/=`) from the runtime and blocked re-entry with an inventory guard. Confirmed fixture parity `cases=3 pass=3 fail=0`, sample parity `cases=18 pass=18 fail=0`, and shrank `py_runtime.h` from `2913 -> 2337` lines relative to the plan start.

### P0: Retire the legacy `std::any` compatibility lane for C++ selfhost

Context: [docs/ja/plans/archive/20260308-p0-cpp-selfhost-any-compat-retirement.md](../plans/archive/20260308-p0-cpp-selfhost-any-compat-retirement.md)

1. [x] [ID: P0-CPP-SELFHOST-ANY-COMPAT-01] Remove the old C++ selfhost `std::any` compatibility lane still left in the runtime.
2. [x] [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S1-01] Inventory checked-in call sites of `std::any` runtime helpers.
3. [x] [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S2-01] Remove the first slice of `std::any` helpers from `py_runtime.h` / `str.h`.
4. [x] [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S2-02] Remove `std::any` overloads from `iter_ops.h` and update tests.
5. [x] [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S3-01] Remove `std::any` comparison / iterator compatibility.
6. [x] [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S3-02] Update regression / parity / docs and close the plan.
- Progress memo: [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S1-01] After inventorying checked-in call sites, dependence on the `std::any` lane was limited to the runtime source itself and representative runtime smoke; no direct dependence remained in production codegen or checked-in sample C++.
- Progress memo: [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S3-01] Removed `make_object(any)`, `py_len/py_to_string/py_to_int64/py_to_float64/py_to_bool/py_print(any)`, `py_to<T>(any)`, `py_is_*(any)`, the `std::any` comparison / iterator bridge, `py_slice(any)`, and `py_to_str_list_from_any` from `native/core/py_runtime.h`, and also removed `std::any` overloads from `native/core/str.h` and `native/built_in/iter_ops.h`.
- Progress memo: [ID: P0-CPP-SELFHOST-ANY-COMPAT-01-S3-02] Representative verification passed `test_cpp_runtime_boxing.py`, `test_cpp_runtime_iterable.py`, fixture parity `cases=3 pass=3 fail=0`, and sample parity `cases=18 pass=18 fail=0`. The `std::any` surface is now gone from runtime core, and the only remaining `<any>` dependence is on the out-of-scope `header_builder.py` side.

### P0: Retire dynamic bridges in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-pyruntime-dynamic-bridge-retirement.md](../plans/archive/20260308-p0-cpp-pyruntime-dynamic-bridge-retirement.md)

1. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01] Reduce the remaining `dict_get_*` / compatibility lanes / `std::any` bridges in `py_runtime.h`, returning to decode-first / typed helpers as the source of truth.
2. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S1-01] Inventory call sites and classify debt for the `dict_get_*` / `py_dict_get_default` / `std::any` bridge / `sum(list<object>)` / `optional<dict<str, object>>` lanes.
3. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S1-02] Fix the removal order and the compatibility lane to keep in docs / the decision log.
4. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S2-01] First-slice cleanup of `dict_get_*` / `py_dict_get_default` object / optional / `std::any` overloads, shifting toward `JsonObj.get_*` and typed helpers.
5. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S2-02] Update representative tests and fix the reduced C++ runtime surface after `dict_get_*`.
6. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S3-01] Replace or remove call sites of `sum(const list<object>&)` and add regressions if needed.
7. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S3-02] Remove or minimize the compatibility lane `py_dict_keys/items/values(const ::std::optional<dict<str, object>>& d)` and fix the boundary against the `JsonObj` path.
8. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S4-01] Reduce the `std::any` comparison / arithmetic / `begin/end` bridge, keeping only the subset needed for selfhost.
9. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S4-02] Add regressions / guards to prevent `std::any` bridge re-entry.
10. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S5-01] Confirm representative unit / fixture parity / sample parity / line-count diffs and record them in the decision log.
11. [x] [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S5-02] Sync docs / archive / TODO history and close the plan.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S2-02] Removed `std::any` dict access and compatibility lanes with no checked-in call sites from `py_runtime.h`, and returned `dict_get_*` to typed fallbacks for `dict<str, object>` / `object` / `dict<str, str>` as the source of truth.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S4-02] After inventorying selfhost/generated sources, narrowed the retained `std::any` subset to `== ""`, numeric comparison, and the iterator bridge. Removed any-any comparison, `const char* + any`, arithmetic between `any` values, and arithmetic between `any` and numbers, and blocked re-entry with the inventory guard.
- Progress memo: [ID: P0-CPP-PYRUNTIME-DYNAMIC-BRIDGE-01-S5-02] Representative verification passed `test_cpp_runtime_iterable.py`, `test_cpp_runtime_boxing.py`, fixture parity `cases=3 pass=3 fail=0`, and sample parity `cases=18 pass=18 fail=0`, and shrank `py_runtime.h` from `2995 -> 2913` lines relative to the previous commit.

### P0: Retire the first wave of dynamic helpers in C++ `py_runtime.h`

Context: [docs/ja/plans/archive/20260308-p0-cpp-dynamic-helper-first-wave-retirement.md](../plans/archive/20260308-p0-cpp-dynamic-helper-first-wave-retirement.md)

1. [x] [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01] Remove the first wave of object dynamic helpers from `py_runtime.h` and align the compile-error contract with the runtime surface.
2. [x] [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S1-01] Inventory object-overload call sites / guards / representative tests and fix the first-wave removal targets.
3. [x] [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S1-02] Explicitly state in docs / the decision log that the first wave removes only object overloads, while `list<object>` and `optional<dict<str, object>>` are separate tranches.
4. [x] [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S2-01] Remove `sum(const object&)` and `zip(const object&, const object&)` from `py_runtime.h` and update representative tests.
5. [x] [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S2-02] Remove `py_dict_keys/items/values(const object&)` from `py_runtime.h` and update the typed emitter path and regressions.
6. [x] [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S3-01] Add or strengthen a re-entry guard / inventory test for object dynamic helpers.
7. [x] [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S3-02] Finish representative C++ tests / fixture parity / sample parity / docs archive and close the plan.
- Progress memo: [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S2-02] Removed `sum(const object&)`, `zip(const object&, const object&)`, and `py_dict_keys/items/values(const object&)` from `py_runtime.h`, and updated runtime smoke so it decodes with `py_to<list<object>>(...)` before using typed `zip`, rather than calling helpers on raw `object`.
- Progress memo: [ID: P0-CPP-DYNAMIC-HELPER-FIRSTWAVE-01-S3-02] Inverted `test_cpp_runtime_iterable.py` into an object-overload re-entry guard and narrowed `unknown` out of decode-first rejection so only `object/Any` remain as the dynamic surface. Fixture parity was `cases=3 pass=3 fail=0`, and sample parity confirmed `pass=2 fail=0` after rerunning `14_raymarching_light_cycle` and `16_glass_sculpture_chaos` in addition to the previous `16/18` green.

### P2: Align selfhost JSON boundaries with `JsonValue`

Context: [docs/ja/plans/archive/20260308-p2-jsonvalue-selfhost-decode-alignment.md](../plans/archive/20260308-p2-jsonvalue-selfhost-decode-alignment.md)

1. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01] Use `JsonValue` to align selfhost/host JSON artifact boundaries to a decode-first contract.
2. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S1-01] Inventory places where selfhost/host JSON loaders directly assume `dict[str, object]` / `list[object]`.
3. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S1-02] Fix in the docs the contract that selfhost uses `JsonValue` through decode helpers rather than `match`.
4. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S2-01] Define the minimal selfhost-ready API in `pytra.std.json`: `loads_obj` / `loads_arr` / `JsonObj.get_*`.
5. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S2-02] Move JSON decode in `program_loader` / `program_validator` to `JsonValue` helpers.
6. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S3-01] Align JSON artifact boundaries in `py2x.py` / `py2x-selfhost.py` / `ir2lang.py` to the `JsonValue` decode lane.
7. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S3-02] Return representative selfhost / linked-program / CLI tests to green through the `JsonValue` path.
8. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S4-01] Add guards / regressions to prevent re-entry of raw object-tree dependence.
9. [x] [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S4-02] Finish doc / decision-log / archive sync and close the plan.
- Progress memo: [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S2-02] Moved root decode in `program_loader` / `program_validator` / `link_manifest_io` / `materializer` to `json.loads_obj()` and switched the validator's internal normalized form to `JsonObj` / `JsonArr`. Kept outward-facing return contracts as raw dicts / `Link*Entry` so existing call sites were not broken all at once.
- Progress memo: [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S3-02] Switched link-input detection in `py2x.py`, wrapped/root unwrap in `ir2lang.py` and `toolchain/ir/east_io.py`, to `loads_obj()` / `JsonObj.get_*`, and returned `test_py2x_cli.py`, `test_ir2lang_cli.py`, `test_pylib_json.py`, `test/unit/link/test_*.py`, and `check_noncpp_east3_contract.py --skip-transpile` to green.
- Progress memo: [ID: P2-JSONVALUE-SELFHOST-ALIGN-01-S4-02] Added `tools/check_jsonvalue_decode_boundaries.py` and unit tests, integrated the guard into `tools/run_local_ci.py` / `spec-tools` / `how-to-use`, and made the selfhost/host JSON artifact boundary fail fast if any change tries to return to raw `json.loads(...)`.

### P1: `JsonValue` decode-first contract and retirement of dynamic helpers

Context: [docs/ja/plans/archive/20260308-p1-jsonvalue-decode-first-contract.md](../plans/archive/20260308-p1-jsonvalue-decode-first-contract.md)

1. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01] Confine JSON dynamic boundaries to the `JsonValue` family and retire object-facing dynamic helpers from the user-facing surface.
2. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S1-01] Inventory dependencies on `json.loads()->object` and `sum/zip/sorted/keys/items/values(object)`.
3. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S1-02] Fix the shared `JsonValue` ADT and decode-first contract in `spec-runtime` / `spec-dev`.
4. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S2-01] Decide the public surface of `JsonValue` / `JsonObj` / `JsonArr` and the exact APIs of `loads_obj` / `loads_arr`.
5. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S2-02] Fix the validator/guard policy that direct passing of `object` into built-in / collection helpers is a compile error.
6. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S3-01] Make the carrier policy concrete for C++ / Rust / Swift / Nim and decide the implementation priority order.
7. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S3-02] Add the first implementation slice that moves `std/json` runtime and representative decode paths to the `JsonValue` surface.
8. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S4-01] Fix the guard against dynamic-helper debt and representative parity.
9. [x] [ID: P1-JSONVALUE-DECODE-FIRST-01-S4-02] Finish docs / decision-log / archive sync and close the plan.
- Progress memo: [ID: P1-JSONVALUE-DECODE-FIRST-01-S3-02] The first implementation slice did not fully nominalize `JsonValue` / `JsonArr` at once. Instead, it started from a compatibility lane in `pytra.std.json` that adds `loads_obj(text) -> JsonObj | None` and `JsonObj.get_obj/get_arr/get_str/get_int/get_float/get_bool`. `loads(text) -> object` remains temporarily for compatibility with existing selfhost / host loaders, while checked-in C++ runtime artifacts were aligned with explicit scalar conversion `int(raw) / float(raw) / bool(raw)` and the `_join_strs(parts, sep)` helper.
- Progress memo: [ID: P1-JSONVALUE-DECODE-FIRST-01-S4-02] Added a decode-first guard to `src/toolchain/ir/core.py`, making it fail fast on calls that pass `object` / `Any` / `unknown` directly into `sum/zip/min/max/sorted`, and on paths that call `keys/items/values` on the same dynamic receiver. Re-ran `test_east_core.py`, `test_py2cpp_features.py`, `test_cpp_runtime_iterable.py`, `test_pylib_json.py`, and `runtime_parity_check.py --targets cpp --case-root fixture`, fixing both the runtime-debt snapshot and the representative JSON runtime path.

### P2: `@template` v1 for linked runtime helpers

Context: [docs/ja/plans/archive/20260308-p2-linked-runtime-helper-template-v1.md](../plans/archive/20260308-p2-linked-runtime-helper-template-v1.md)

1. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01] Adopt `@template("T")` as the v1 generic surface for linked runtime helpers, and fix the assumption that it is limited to runtime helpers with no explicit instantiation.
2. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S1-01] Close the comparison between the `TypeVar` and `@template` options and decide `@template("T")` as the v1 canonical syntax.
3. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S1-02] Fix the v1 scope in the spec/plan: runtime-helper only, top-level function only, no explicit instantiation.
4. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S2-01] Design the canonical parser / EAST / linked metadata shape (for example `meta.template_v1`).
5. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S2-02] Design validation rules for placement, parameter names, duplicates, and runtime-helper-only enforcement.
6. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S3-01] Record the surface-extension policy that remains compatible with future `@instantiate(...)`.
7. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S3-02] Organize the connection points to follow-up plans for the specialization collector and monomorphization.
8. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S4-01] Sync docs / TODO / related plans and fix the v1 generic assumptions.
9. [x] [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S4-02] Prepare the decision log and acceptance criteria to the point where the plan can be moved to the archive.
- Progress memo: [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S2-02] Recorded in `spec-template` and `spec-east` the v1 canonical syntax `@template("T", ...)`, `FunctionDef.meta.template_v1`, and the responsibility split between parser/EAST build and the linked-program validator. `TypeVar` remains annotation-only, and runtime-helper provenance is validated fail-closed on the linked-program side.
- Progress memo: [ID: P2-LINKED-RUNTIME-TEMPLATE-01-S4-02] Added the future connection policy for `@instantiate(...)` and the specialization collector to `spec-template`, synchronized related P2 notes (`runtime SoT linked-program integration`, `runtime helper generics`) on the canonical `@template` syntax and `template_v1` metadata, and marked this plan complete as a P2 aimed at documentation fixation.

### P0: Use linked-program builds for pytra-cli C++ max optimization

Context: [docs/ja/plans/archive/20260308-p0-pytra-cli-cpp-maxopt-linked-build.md](../plans/archive/20260308-p0-pytra-cli-cpp-maxopt-linked-build.md)

1. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01] Make `pytra-cli --target cpp --build --codegen-opt 3` use the max C++ route including the linked-program optimizer, and lock non-regression with sample parity.
2. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S1-01] Inventory the optimization-stage correspondence between `pytra-cli --codegen-opt` and `py2x/eastlink/ir2lang/py2cpp`, and fix the target semantics of `codegen-opt=3`.
3. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S1-02] Fix the CLI contract and sample-parity gate for the max-opt C++ route in the spec/plan.
4. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-01] Implement the build route so `pytra-cli --target cpp --build --codegen-opt 3` goes through the linked-program optimizer.
5. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-02] Align the transpile-only route for `pytra-cli --target cpp --codegen-opt 3` so it also uses the linked-program optimizer.
6. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-01] Add representative CLI regressions and fix route selection plus manifest/build/run behavior for `codegen-opt=3`.
7. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-02] Run sample parity and confirm that C++ samples stay green even on the max-opt route.
8. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S4-01] Reflect the max-opt C++ route and sample-parity procedure in `pytra-cli`, how-to-use, and the required docs.
9. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S4-02] Record the completion result, move the plan to the archive, and close it.
- Progress memo: [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-01] Added a `codegen-opt=3`-dedicated linked build route to `src/pytra-cli.py`, using `output_dir/.pytra_linked/` as the intermediate bundle through `py2x.py --link-only -> py2x.py --from-link-output`. `test_pytra_cli.py` now fixes regression coverage through manifest resolution and `make` execution.
- Progress memo: [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-02] Added `--cpp-codegen-opt` to `tools/runtime_parity_check.py`, ran `python3 tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples --cpp-codegen-opt 3 --east3-opt-level 2`, and confirmed `SUMMARY cases=18 pass=18 fail=0`. `how-to-use` and `spec-make` were also synchronized to the multi-file / `--output-dir` contract.

### P0: Simplify `@abi` modes to `value` / `value_mut`

Context: [docs/ja/plans/archive/20260308-p0-runtime-abi-mode-simplify-value-value-mut.md](../plans/archive/20260308-p0-runtime-abi-mode-simplify-value-value-mut.md)

1. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01] Simplify the public `@abi` mode names to `default/value/value_mut`, and make argument-side `value` the canonical surface for read-only value ABI.
2. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S1-01] Inventory the current `default/value/value_readonly` contract and fix the migration rule as `value=value_readonly`, `value_mut=the former mutable value ABI`.
3. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S1-02] Write the canonical naming and migration rules into the spec/plan and decide how to handle `value_readonly`.
4. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-01] Update parser / decorator metadata / validator so they accept `value` / `value_mut` and emit canonical metadata.
5. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-02] Align diagnostics / error messages / target support checks with the new naming.
6. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-01] Migrate annotations on existing helpers (`py_join`, `py_split`, `py_range`, etc.) and expectations on the generated/runtime side to the new naming.
7. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-02] Update representative regressions and confirm non-regression in C++ helpers/codegen.
8. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S4-01] Synchronize docs and how-to-use to the new naming and record migration caveats.
9. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S4-02] Record the completion result, move the plan to the archive, and close it.
- Progress memo: [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-02] Updated `spec-abi` / `spec-east` to the `default/value/value_mut` basis. The parser / validator still accept `value_readonly` as a source alias, but normalize it to `value` in `runtime_abi_v1`. `ret="value_mut"` is rejected fail-closed.
- Progress memo: [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-02] Moved `py_join` in `src/pytra/built_in/string_ops.py` to canonical `@abi(args={"parts": "value"}, ret="value")`, then reran `test_east_core.py -k abi`, `test_global_optimizer.py`, `test_ir2lang_cli.py`, `test_py2cpp_codegen_issues.py -k abi`, and `test_pytra_built_in_string_ops.py` to confirm non-regression.

### P0: Remove runtime-module knowledge from backends

Context: [docs/ja/plans/archive/20260308-p0-backend-runtime-knowledge-leak-retirement.md](../plans/archive/20260308-p0-backend-runtime-knowledge-leak-retirement.md)

1. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01] Remove source-side runtime module names and ad hoc helper ABI knowledge from backends/emitters, and unify on a data-driven contract based on resolved runtime symbols, semantic tags, and adapter kinds.
2. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S1-01] Inventory `math/gif/png/save_gif/write_rgb_png/pyMath*` leakage under `src/backends/**`, classify it by target and violation type, and fix representative cases.
3. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S1-02] Explicitly document backend prohibitions and the canonical resolved-metadata path in `spec-runtime` / `spec-dev`.
4. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S2-01] Extend the runtime symbol index / import-binding API so module imports, function imports, constant imports, and semantic tags can be resolved outside the backend.
5. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S2-02] Normalize helper ABI differences into adapter kinds and remove argument-convention special cases such as `save_gif` from hand-written emitter logic.
6. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-01] Migrate representative backends such as C++ / JS / CS / RS to resolved runtime-symbol / adapter rendering.
7. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-02] Bring Go / Swift / Kotlin / Java / Scala / Ruby / Lua / PHP / Nim into the same contract.
8. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-01] Add representative backend/test/tooling regressions and guards to prevent re-entry of knowledge leakage.
9. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-02] Synchronize docs, run full smoke, and close the plan.
- Progress memo: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-02] Aligned the remaining backends including `Go` / `Java` / `Scala` / `Lua` to the canonical runtime-metadata contract, removing source-side branching for `math` helper routing and `pytra.utils` import completion. Re-ran the targeted regressions for `Go` / `Java` / `Lua` / `Scala` green under the same ID.
- Progress memo: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-01] Narrowed `tools/check_emitter_runtimecall_guardrails.py` to detect only source-side module branches and `pyMath*` helper branches, fixing empty allowlist / zero findings as the baseline. Updated `test_check_emitter_runtimecall_guardrails.py` and `test_runtime_symbol_index.py`, and fixed `gif` module-leaf imports and `save_gif` adapter metadata on the tooling side.
- Progress memo: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-02] As representative full smoke, reran `test_check_emitter_runtimecall_guardrails.py`, `test_runtime_symbol_index.py`, `tools/check_emitter_runtimecall_guardrails.py`, the runtime-metadata cases in `test_code_emitter.py`, and representative runtime-import / math / `save_gif` / fail-closed suites for `CPP` / `JS` / `CS` / `RS` / `Go` / `Kotlin` / `Swift` / `PHP` / `Scala` / `Java` / `Lua` / `Nim` / `Ruby`, confirming `Ran 44 tests in 57.569s`, `OK`.

### P1: Introduce `@abi` decorators for generated/runtime helpers

Context: [docs/ja/plans/archive/20260308-p1-runtime-abi-decorator-for-generated-helpers.md](../plans/archive/20260308-p1-runtime-abi-decorator-for-generated-helpers.md)

1. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01] Introduce an `@abi` decorator that can fix boundary ABI for generated/runtime helpers and make it usable as a boundary policy independent from `@extern`.
2. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S1-01] Document `@abi` syntax / semantics / modes and the responsibility split from `@extern` in `spec-abi`.
3. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S1-02] Decide the EAST / linked metadata format for `@abi` and fix the parser/self-host parser acceptance criteria.
4. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S2-01] Add `abi` to `src/pytra/std/__init__.py` and make the parser / self-host parser / AST build preserve the decorator.
5. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S2-02] Add a validator / lowering guard that detects mutation of `value_readonly`.
6. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-01] Implement minimal lowering of `@abi(args, ret)` in the C++ backend and fix helper signatures to the canonical value-ABI form.
7. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-02] Move representative helpers such as `py_join` onto the `@abi` path and fix regression so `rc<list<str>>` is not exposed.
8. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-01] Add unit tests for combined `@extern` use, unsupported backends, and invalid mutation cases.
9. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-02] Sync docs, record the dependency-resolution note for `P1-CPP-PY-RUNTIME-SLIM-01`, and close the plan.
- Progress memo: [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-02] Added `py_join` with `@abi(args={"parts": "value_readonly"}, ret="value")` to `src/pytra/built_in/string_ops.py`, regenerated the generated C++ runtime `src/runtime/cpp/generated/built_in/string_ops.{h,cpp}` with `const list<str>&` / `list<str>` signatures, updated `test_pytra_built_in_string_ops.py`, `test_cpp_runtime_iterable.py`, and `test_runtime_symbol_index.py`, and fixed regression so generated helpers do not expose `rc<list<str>>`.
- Progress memo: [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-01] Added a target-support guard to `toolchain.frontends.runtime_abi`, making `@abi` fail closed for non-`cpp` targets in both linked-program optimization and raw `ir2lang`. Fixed `@extern + @abi` metadata coexistence in `test_east_core.py`, fixed value-ABI lowering under combined usage in `test_py2cpp_codegen_issues.py`, and fixed unsupported-backend rejection in `test_global_optimizer.py` / `test_ir2lang_cli.py`.
- Progress memo: [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-02] Closed this plan from the active TODO, and added a note to `docs/ja/plans/p1-cpp-py-runtime-core-slimming.md` that `py_join` has already moved onto the fixed-ABI helper lane. This leaves `P1-CPP-PY-RUNTIME-SLIM-01` unblocked by `str.join`.

### P1: Shrink C++ `py_runtime` down to low-level glue

Context: [docs/ja/plans/archive/20260308-p1-cpp-py-runtime-core-slimming.md](../plans/archive/20260308-p1-cpp-py-runtime-core-slimming.md)

1. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01] Separate built-in semantics expressible in pure Python out of `native/core/py_runtime.h`, and slim `core` down so it centers on low-level ABI / object / container / process glue.
2. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S1-01] Inventory functions/classes/helpers in `native/core/py_runtime.h` and classify them into `native/core`, `generated/core`, `generated/built_in`, `native/built_in`, and deferred.
3. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S1-02] Explicitly document the `py_runtime` responsibility boundary and what may remain versus what should move back in `spec-runtime` / `spec-dev`.
4. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S2-01] Decide which candidates should move back to `src/pytra/built_in/*.py` and fix the source-of-truth placement plan.
5. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S2-02] Prepare the generator / layout contracts needed for the `generated/core` or `generated/built_in` emission lane.
6. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-01] Gradually remove pure-Python string/collection built-in semantics from `native/core/py_runtime.h` and move them to the canonical generated lane.
7. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-02] Reorganize `native/core/py_runtime.h` around low-level ABI / object / container / process glue and minimize include aggregation.
8. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-01] Align the runtime symbol index / build graph / representative C++ runtime tests with the new ownership layout.
9. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-02] Finish fixture/sample parity, docs sync, and required guards, then close the plan.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-01] Added `py_split`, `py_splitlines`, and `py_count` to `src/pytra/built_in/string_ops.py`, moved the bodies of `str::split` / `splitlines` / `count` / `join` from `native/core/py_runtime.h` to generated-helper delegates, regenerated `generated/built_in/string_ops.{h,cpp}`, and added `@abi(ret="value")` to `py_range` so the existing `list<int64>` helper ABI remained intact.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-02] Made the C++ emitter add direct built-in headers for modules that use `py_repeat` / `py_range` / `py_any` / `py_all` / `reversed` / `enumerate`, removed transitive `sequence` / `iter_ops` / `predicates` includes from `native/core/py_runtime.h`, regenerated `generated/std/json.cpp` with direct includes, and reorganized `py_runtime.h` around low-level glue plus `str` method delegates.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-01] Aligned the runtime symbol index / build graph / representative C++ integration tests to the ownership change. `test_runtime_symbol_index.py` now fixes the public headers and compile sources for `string_ops` / `sequence`, and `test_cpp_runtime_build_graph.py` fixes direct built-in includes in `generated/std/json.cpp` along with non-reentry of `core/py_runtime.h` seeds.
- Progress memo: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-02] Added a guard to `tools/check_runtime_cpp_layout.py` against re-entry of removed transitive includes (`predicates` / `sequence` / `iter_ops`), synchronized `test_check_runtime_cpp_layout.py` and the spec, and reran `test_cpp_runtime_iterable.py`, `test_pytra_built_in_string_ops.py`, the layout guard, the marker guard, fixture parity (`cases=3 pass=3 fail=0`), and sample parity (`cases=18 pass=18 fail=0`) to close the plan.

### P1: Slim down C++ `py_runtime.h` on an `@template` basis

Context: [docs/ja/plans/archive/20260308-p1-cpp-pyruntime-template-slimming.md](../plans/archive/20260308-p1-cpp-pyruntime-template-slimming.md)

1. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01] Implement `@template` in a runtime-helper-limited way, move generic helpers back to the pure-Python SoT side, and slim down C++ `py_runtime.h`.
2. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S1-01] Inventory generic-helper candidates still left in `py_runtime.h` and classify them into first wave / second wave / deferred.
3. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S1-02] Add the responsibility boundary and specialization contract for helper-limited `@template` to `spec-template` / `spec-runtime` / `spec-east` / `spec-linker`.
4. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S2-01] Make parser / EAST metadata / validator accept `@template("T", ...)` only for runtime helpers.
5. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S2-02] Add a minimum specialization collector and monomorphization implementation on the linked-program side.
6. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S3-01] Move `sum` / `min` / `max` back to the SoT side as pure-Python generic helpers and switch to generated C++ helpers.
7. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S3-02] Move at least one family among `zip` / `sorted` in the same way and remove hand-written helpers from `py_runtime.h`.
8. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S4-01] Align the runtime symbol index / build graph / representative backend/runtime tests to the new contract.
9. [x] [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S4-02] Finish fixture/sample parity, docs sync, and required guards, then close the plan.
- Progress memo: [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S2-02] Ran the runtime-helper template specialization collector at the front of the linked-program optimizer and added a minimum deterministic monomorphization implementation from `meta.template_v1` and call-site concrete-type tuples. Specialized clones carry `meta.template_specialization_v1`, and the program-wide summary is aggregated into `link-output.global.runtime_template_specializations_v1`.
- Progress memo: [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S3-01] Added `src/pytra/built_in/numeric_ops.py`, moved `sum(list[T])`, `py_min(T, T)`, and `py_max(T, T)` back to the SoT side as `@template` helpers, switched C++ runtime emit to the header-only module `generated/built_in/numeric_ops.h`, and removed the hand-written implementations of `sum` and 2-arg `py_min/py_max` from `native/core/py_runtime.h`.
- Progress memo: [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S3-02] Added `src/pytra/built_in/zip_ops.py`, moved `zip(list[A], list[B]) -> list[tuple[A, B]]` to the SoT side as an `@template("A", "B")` helper, made `generated/built_in/zip_ops.h` and the public shim `pytra/built_in/zip_ops.h` header-only modules, and removed the generic `zip(const list<A>&, const list<B>&)` from `native/core/py_runtime.h`.
- Progress memo: [ID: P1-CPP-PYRUNTIME-TEMPLATE-SLIM-01-S4-02] Added a re-entry guard for template-helper bodies of `sum/min/max/zip` to `tools/check_runtime_cpp_layout.py`, synchronized `test_check_runtime_cpp_layout.py` and `spec-runtime` / `how-to-use`, and confirmed `cases=3 pass=3 fail=0` for `tools/runtime_parity_check.py --targets cpp --case-root fixture` and `cases=18 pass=18 fail=0` for `--case-root sample --all-samples` to close the plan.

### P1: Full sample parity across all targets

Context: [docs/ja/plans/archive/20260308-p1-all-target-sample-parity-rollout.md](../plans/archive/20260308-p1-all-target-sample-parity-rollout.md)

1. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01] Make sample parity finish successfully for all parity targets without `toolchain_missing`, and fix the execution baseline for all-target green.
2. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S1-01] Inventory `runner_needs` and current `toolchain_missing` across all parity targets, and build a missing-toolchain matrix by target.
3. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S1-02] Fix the done condition for “all-target parity green,” the disallowed failure categories, and validation commands in the spec/plan.
4. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S2-01] Prepare toolchain bootstrap procedures for compiled targets (`rs/cs/go/java/kotlin/swift/scala`) and eliminate `toolchain_missing`.
5. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S2-02] Prepare toolchain bootstrap procedures for scripting / mixed targets (`ruby/lua/php/nim`) and eliminate `toolchain_missing`.
6. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-01] Reconfirm sample parity for baseline targets (`cpp/js/ts`) and keep `18/18` while other targets are being repaired.
7. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-02] Bring sample parity for compiled targets (`rs/cs/go/java/kotlin/swift/scala`) up to green.
8. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-03] Bring sample parity for scripting / mixed targets (`ruby/lua/php/nim`) up to green.
9. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-01] Prepare scripts / docs / how-to-use for one-shot execution across all targets and fix the rerun procedure.
10. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-02] Record the full parity result, move the plan to the archive, and close it.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-02] After preparing compiled-target toolchains on the current Debian 12 machine, fixed the canonical runtime-import collision in Rust and raised `rs/cs/go/java/kotlin/swift/scala` to `SUMMARY cases=18 pass=18 fail=0` one target at a time. Scala CLI was switched to `/usr/local/bin/scala` to match the `scala run ...` runner contract.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-03] Reran target-specific parity for `ruby/lua/php/nim` as well and confirmed `SUMMARY cases=18 pass=18 fail=0` for all four targets. At this point, both `toolchain_missing` and runtime/backend gaps were cleared on the current machine.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-01] Added `tools/check_all_target_sample_parity.py` and fixed the canonical wrapper that runs the four groups `cpp` / `js_ts` / `compiled` / `scripting_mixed` in order. `spec-tools` and `how-to-use` were also synchronized to the wrapper-based rerun procedure.
- Progress memo: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-02] Recorded the full parity result as the sum of the canonical subgroups. Baseline `cpp/js/ts` recorded `SUMMARY cases=18 pass=18 fail=0 targets=cpp,js,ts`, while the seven compiled targets and four scripting/mixed targets were each confirmed individually at `18/18`, achieving sample-parity green across all 14 targets.

### P4: Repair non-C++ backends after linked-program rollout

Context: [docs/ja/plans/archive/20260308-p4-noncpp-backend-recovery-after-linked-program.md](../plans/archive/20260308-p4-noncpp-backend-recovery-after-linked-program.md)

1. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01] Bring non-C++ backends into a common post-linked-program contract based on `SingleFileProgramWriter`, and resolve the broken state family by family.
2. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S1-01] Build a post-linked-program health matrix for non-C++ backends and classify each target by failure category.
3. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S1-02] Fix the done conditions (handling of static/smoke/transpile/parity/toolchain_missing) and repair order in the spec/plan.
4. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S2-01] Audit the non-C++ compatibility layer in `backend_registry.py` / `py2x.py` / `ir2lang.py` and fill the common-contract gaps required by `SingleFileProgramWriter`.
5. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S2-02] Add or unify a non-C++ backend health checker so broken/green status can be seen per family in a single command.
6. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S3-01] Resolve static-contract / smoke / transpile failures for Wave 1 (`rs/cs/js/ts`) and stabilize the compatibility route.
7. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S3-02] Refresh the Wave 1 parity baseline and separate runtime differences from infrastructure failures.
8. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-01] Resolve static-contract / smoke / transpile failures for Wave 2 (`go/java/kotlin/swift/scala`).
9. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-02] Refresh the Wave 2 parity baseline and fix `toolchain_missing`, execution failures, and artifact differences.
10. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-01] Resolve static-contract / smoke / transpile failures for Wave 3 (`ruby/lua/php/nim`).
11. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-02] Refresh the Wave 3 parity baseline and separate runtime differences from backend bugs.
12. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-01] Integrate the non-C++ backend health check into `run_local_ci.py` or an equivalent regression path.
13. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-02] Update `docs/ja/spec` / `docs/en/spec` / `docs/ja/how-to-use.md`, fix the post-linked-program operating rules for non-C++ backend recovery, and close the plan.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-01] The primary Wave 2 failures had converged on missing source-origin call metadata for bare `Path(...)` construction and `src.*` imports in `kotlin/swift`. Added bare-`Path(...)` constructor fallbacks to `go/java/scala`, then fixed `super` preservation in `java` and import paths in `kotlin/swift`, confirming smoke/transpile green.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-02] For Wave 2 parity, all 18 sample cases of `go/java/kotlin/swift/scala` were still `toolchain_missing`. This fixed the remaining issue as infrastructure baseline only, not backend bugs.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-01] Wave 3 failures came from accidental `src.*` imports in `ruby/php/nim` and stale smoke expectations in `lua`. The former were aligned to `toolchain.*` imports, and the latter were synchronized to `_G.math.max` and a truthiness-preserving if-expression closure, bringing the wave green.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-02] Wave 3 parity also converged on all 18 sample cases being `toolchain_missing` for `ruby/lua/php/nim`. Across Waves 1-3, backend bugs and infrastructure baseline were fully separated.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-01] Added `python3 tools/check_noncpp_backend_health.py --family all --skip-parity` to `tools/run_local_ci.py`, and validated it by running `wave1/wave2/wave3` separately, confirming `status=green` for every family.
- Progress memo: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-02] Synchronized `docs/ja/spec/spec-tools.md`, `docs/en/spec/spec-tools.md`, and `docs/ja/how-to-use.md` to the post-linked-program non-C++ backend workflow and moved the plan to the archive.

# TODO (Open)

> `docs/ja/` is the source of truth. `docs/en/` is its translation.

<a href="../../ja/todo/index.md">
  <img alt="Read in Japanese" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

Last updated: 2026-03-10

## Context Operation Rules

- Every task must include an `ID` and a context file (`docs/ja/plans/*.md`).
- To override priority, issue chat instructions in the format of `docs/ja/plans/instruction-template.md`; do not use `todo2.md`.
- The active target is fixed to the highest-priority unfinished ID (smallest `P<number>`, and the first one from the top when priorities are equal); do not move to lower priorities unless there is an explicit override instruction.
- If even one `P0` remains unfinished, do not start `P1` or lower.
- Before starting, check `Background` / `Out of scope` / `Acceptance criteria` in the context file.
- Progress memos and commit messages must include the same `ID` (example: `[ID: P0-XXX-01] ...`).
- Keep progress memos in `docs/ja/todo/index.md` to a one-line summary only; details (decisions and verification logs) must be recorded in the `Decision log` of the context file (`docs/ja/plans/*.md`).
- If one `ID` is too large, you may split it into child tasks in `-S1` / `-S2` format in the context file (keep the parent checkbox open until the parent `ID` is completed).
- If uncommitted changes remain due to interruptions, do not start a different `ID` until you complete the same `ID` or revert the diff.
- When updating `docs/ja/todo/index.md` or `docs/ja/plans/*.md`, run `python3 tools/check_todo_priority.py` and verify that each progress `ID` added in the diff matches the highest-priority unfinished `ID` (or its child `ID`).
- Append in-progress decisions to the context file `Decision log`.
- For temporary output, use existing `out/` (or `/tmp` only when necessary), and do not add new temporary folders under the repository root.

## Notes

- This file keeps unfinished tasks only.
- Completed tasks are moved to history via `docs/ja/todo/archive/index.md`.
- `docs/ja/todo/archive/index.md` keeps only the index, and the history body is stored by date in `docs/ja/todo/archive/YYYYMMDD.md`.

## Unfinished Tasks

### P2: Move compiler boundaries to typed carriers and retreat internal object-carrier / `make_object` usage

Context: [docs/ja/plans/p2-compiler-typed-boundary.md](../plans/p2-compiler-typed-boundary.md)

1. [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01] Move compiler boundaries to typed carriers and retreat internal object-carrier / `make_object` usage.
2. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01] Inventory remaining `dict[str, object]`, `list[object]`, `make_object`, and `py_to` usage across `transpile_cli`, `backend_registry_static`, selfhost parser paths, and generated compiler runtime, then classify each usage as `compiler_internal`, `json_adapter`, `extern_hook`, or `legacy_bridge`.
3. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02] Lock the typed-boundary contract and non-goals so they stay consistent with `spec-dev`, `spec-runtime`, and `spec-boxing`.
4. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-01] Define typed carrier specs for compiler root payloads (EAST document, backend spec, layer options, emit request/result).
5. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-02] Introduce typed carriers and thin legacy adapters in the Python source of truth.
6. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-03] Introduce typed carrier mirrors or typed wrapper APIs in the C++ selfhost/native compiler interfaces and reduce raw `dict<str, object>` exchange.
7. [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Move selfhost parser / EAST builder node construction onto typed constructors / builder helpers and gradually retire direct `dict<str, object>{{...}}` assembly.
8. [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Retreat remaining `make_object` usage in generated compiler / selfhost runtime down to serialization/export seams only.
9. [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-01] Separate JSON, extern/hooks, and other intentionally dynamic carriers from the compiler typed model behind `JsonValue` or explicit adapters.
10. [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-02] Label every remaining `make_object` / `py_to` / `obj_to_*` usage and add guards that reject uncategorized reintroduction.
11. [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-01] Refresh selfhost build/diff/prepare/bridge regressions and lock non-regression after the typed-boundary changes.
12. [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-02] Update docs / TODO / archive and record whether each remaining `make_object` usage is `user boundary only` or `explicit adapter only`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01] Inventoried object-carrier usage across `transpile_cli`, `backend_registry_static`, and the selfhost/generated parser lanes, then fixed `.json` decode/encode as `json_adapter`, public raw-dict APIs plus selfhost seed helpers as `legacy_bridge`, signature/backend-spec/AST direct assembly as `compiler_internal`, and hook surfaces as the reserved `extern_hook` category.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02] Fixed that P2 is not an `Any/object` removal plan but a typed-compiler-carrier plan, that JSON must converge on the `JsonValue` nominal lane, and that backend/runtime must not reinterpret `type_expr` or `dispatch_mode` semantics during the migration.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-01] Fixed the fields for `CompilerRootDocument`, `BackendSpecCarrier`, `LayerOptionsCarrier`, `EmitRequestCarrier`, `ModuleArtifactCarrier`, and `ProgramArtifactCarrier`; callables stay local implementation detail, while raw EAST/IR docs are treated as migration fields only until S3.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-02] Made `src/toolchain/compiler/typed_boundary.py` the source of truth, moved host/static backend registries plus `ir2lang.py` / `py2x.py` onto typed-carrier-first paths, and shrank the old `dict[str, object]` surfaces to thin `to_legacy_dict()` adapters, a `load_east3_document_typed()` wrapper, and the writer boundary.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-03] Added native C++ `CompilerRootDocument` / `ResolvedBackendSpec` / `LayerOptionsCarrier` wrappers plus `*_typed()` APIs, then updated `selfhost/py2cpp.cpp` and `selfhost/py2cpp_stage2.cpp` to route through those wrappers and fall back to `to_legacy_dict()` only at the remaining legacy seams.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Moved module-root, leading-trivia, and bare-`Expr` assembly in `src/toolchain/ir/core.py` onto `_sh_make_*` builder helpers, and added the typed-carrier support shims (`CompilerRootDocument` re-coercion bypass plus `lower_ir_typed()` / `optimize_ir_typed()` / `emit_source_typed()`) needed to keep stage1 selfhost building.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_name_expr()` / `_sh_make_tuple_expr()` / `_sh_make_assign_stmt()` / `_sh_make_ann_assign_stmt()` and moved `with` bindings, typed bindings, tuple destructuring, class fields, and module top-level assign/annassign assembly through those helpers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_constant_expr()` and moved the leaf `Constant` / `Name` / `Tuple` nodes from `_parse_primary()` / `_parse_comp_target()` through helpers too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_ifexp_expr()` / `_sh_make_boolop_expr()` / `_sh_make_unaryop_expr()` / `_sh_make_compare_expr()` / `_sh_make_binop_expr()` and moved the `IfExp` / `BoolOp` / `UnaryOp` / `Compare` / `BinOp` nodes in both `ExprParser` and the lowered-expression path onto shared helpers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added postfix/collection helpers such as `_sh_make_attribute_expr()` / `_sh_make_call_expr()` / `_sh_make_slice_node()` / `_sh_make_subscript_expr()` / `_sh_make_*_comp_expr()` / `_sh_make_*_expr()` and moved `ExprParser._parse_postfix()`, collection literals, generator/list/dict/set comprehensions, and `range(...)` normalization onto shared builders.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_arg_node()` / `_sh_make_lambda_expr()` / `_sh_make_formatted_value_node()` / `_sh_make_joined_str_expr()` and moved checked-in node assembly for lambda args/body plus f-string fragments onto shared builders.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_import_alias()` / `_sh_make_import*_stmt()` / `_sh_make_if_stmt()` / `_sh_make_for*_stmt()` / `_sh_make_while_stmt()` / `_sh_make_except_handler()` / `_sh_make_try_stmt()` and moved block-parser plus module-root import/control-flow assembly onto shared builders.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_function_def_stmt()` / `_sh_make_class_def_stmt()` and moved checked-in node assembly for nested functions, top-level functions, methods, and class roots onto shared builders.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_arg_node()` / `_sh_make_lambda_expr()` / `_sh_make_formatted_value_node()` / `_sh_make_joined_str_expr()` and moved lambda args, `Lambda`, f-string fragments, and module-level bare `Expr` statements onto shared helpers too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_raise_stmt()` / `_sh_make_pass_stmt()` / `_sh_make_return_stmt()` / `_sh_make_yield_stmt()` / `_sh_make_augassign_stmt()` / `_sh_make_swap_stmt()` and moved simple-statement assembly in the statement-block and class-body parsers onto shared helpers too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Applied `_sh_make_import_alias()` / `_sh_make_import_stmt()` / `_sh_make_import_from_stmt()` / `_sh_make_if_stmt()` / `_sh_make_while_stmt()` / `_sh_make_except_handler()` / `_sh_make_try_stmt()` / `_sh_make_for_stmt()` / `_sh_make_for_range_stmt()` to the real statement-block and module-root assembly paths.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_function_def_stmt()` / `_sh_make_class_def_stmt()` and moved checked-in direct assembly for nested/top-level/method `FunctionDef` nodes plus top-level `ClassDef` nodes onto shared helpers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Moved the remaining string-literal-concatenation `BinOp` and default `ForRange` `Constant` nodes onto existing helpers too, clearing the last checked-in direct `kind` assembly left in `core.py` source-of-truth paths.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Moved concatenated-string `BinOp` assembly and the default `Constant` nodes used by `for ... in range(...)` onto helpers too, so checked-in direct AST node assembly in `src/toolchain/ir/core.py` is now confined to the helper definitions themselves.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Added `_sh_make_def_sig_info()` and routed `_sh_parse_def_sig()` through that helper so the raw signature-carrier dict is no longer assembled inline.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] With checked-in node construction in `src/toolchain/ir/core.py` now aligned on helper-owned builders, S3-01 is closed and the remaining object-carrier retreat work moves to the generated/selfhost runtime lanes in `S3-02`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added module/class-root helpers to the generated C++ selfhost mirror and started routing the large top-level `FunctionDef` / `ClassDef` / `Import` / `ImportFrom`, class-field/method, top-level assignment, and bare-`Expr` `make_object` clusters through those builders. In parallel, the source-of-truth side gained `_sh_make_expr_token()` / `_sh_make_import_binding()` so token/import metadata carriers and mirror regressions use the same helper contract.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_make_expr_token()` and `_sh_make_import_binding()` so the source-of-truth tokenizer/import-metadata carriers now route through helpers instead of open-coded inline dict assembly.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_make_import_symbol_binding()` and `_sh_make_qualified_symbol_ref()` so the module-root import-resolution tail no longer assembles `import_symbols` / `qualified_symbol_refs` carriers inline.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_make_module_source_span()` / `_sh_make_import_resolution_meta()` / `_sh_make_module_meta()` so the remaining module-root carrier assembly inside `_sh_make_module_root()` now also routes through helpers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Applied `_sh_make_import_symbol_binding()` across the pre-scan, import registration, and module parse paths so source-of-truth import-symbol metadata no longer uses raw dict assignment. `test_east_core.py` now guards that regression boundary, and the tracked selfhost test/docs were corrected so they no longer assume ignored mirror edits were part of the tracked slice.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Fixed `_sh_make_import_resolution_binding()` to merge only known resolution fields and aligned the binding-list type hints in `_sh_make_import_resolution_meta()` / `_sh_make_module_root()` with `dict[str, Any]`. The source guard now also rejects the old generic `resolution.items()` merge loop.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_make_lambda_arg_entry()` / `_sh_make_keyword_arg()` / `_sh_make_cast_entry()` so lambda parameter carriers, call keyword carriers, and numeric-promotion cast metadata now route through helper-owned builders too. `test_east_core.py` rejects the old raw `arg_entries.append(...)` / `keywords.append(...)` / `casts.append(...)` dict literals.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added a native-wrapper seam guard in `test_py2x_entrypoints_contract.py` so `make_object(...)` in `transpile_cli.cpp` / `backend_registry_static.cpp` is allowed only on the `to_legacy_dict()` and JSON-export seam lines.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Removed `raw_spec` from native `ResolvedBackendSpec` and stopped `get_backend_spec_typed()` from prebuilding a legacy dict. That pulls `make_object(...)` one step closer to the `to_legacy_dict()` and JSON-export seams, and `test_py2x_entrypoints_contract.py` now fixes the tighter allowlist.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Replaced `json.dumps(make_object(ir))` in native `backend_registry_static.cpp:emit_source_typed()` with the explicit `_dump_json_dict(ir, ...)` serialization seam. That leaves native registry-side `make_object(...)` only in `ResolvedBackendSpec::to_legacy_dict()`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] As an enabler for the next `src/py2x-selfhost.py` typed-boundary slice, fixed both `tools/check_transpiler_version_gate.py` and `tools/run_regen_on_version_bump.py` to read `transpiler_versions.json` from the current `src/toolchain/compiler/` location.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Changed `transpile_cli.cpp:_load_json_root_document()` to build `CompilerRootDocument` directly from `JsonObj`, so the `.json` load path no longer routes through `coerce_compiler_root_document(raw_doc, ...)`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Folded the `.json` load path in `transpile_cli.cpp` into `_load_json_root_document()` so the raw `dict<str, object>` stays inside the helper. `load_east3_document_typed()` now receives a typed `CompilerRootDocument` directly, and `test_py2x_entrypoints_contract.py` guards against the old `_load_json_root_dict` path coming back.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_make_decl_meta()` so top-level function `runtime_abi_v1` / `template_v1` metadata and extern-var `extern_var_v1` metadata now share one helper-owned carrier instead of open-coded dict assembly.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_import_binding_fields()` / `_sh_make_import_resolution_binding()` so the module-root tail no longer chains `binding.get(...)` calls or `dict(binding)` when deriving `import_resolution_bindings`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Extended `test_prepare_selfhost_source.py` with `_sh_make_arg_node()` / `_sh_make_lambda_arg_entry()` / `_sh_make_keyword_arg()` / `_sh_make_cast_entry()` guards so the generated mirror fails fast if lambda args, call keywords, or numeric cast metadata slip back to inline `make_object` dict assembly.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Extended `test_prepare_selfhost_source.py` with `_sh_make_attribute_expr()` / `_sh_make_call_expr()` / `_sh_make_binop_expr()` guards so the generated mirror fails fast if `Attribute` / `Call` / `BinOp` slip back to inline `make_object` dict assembly.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Switched `src/py2x-selfhost.py` to the direct typed path with no legacy shim helpers, so `load/get_backend_spec/resolve_layer_options/lower/optimize/emit/apply_runtime` now all call the `*_typed()` APIs directly. `test_py2x_entrypoints_contract.py` fixes both `selfhost/py2cpp.cpp` and `selfhost/py2cpp_stage2.cpp` to stay free of `to_legacy_dict()` bridges, and the version gate now treats `src/py2x-selfhost.py` as a `cpp`-lane change with `transpiler_versions.json` bumping `cpp` to `0.282.0`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Narrowed the native `LayerOptionsCarrier` internal carrier from `dict<str, object>` to `dict<str, str>`, so `resolve_layer_options_typed()` now preserves raw CLI option strings and legacy boxing stays isolated inside `to_legacy_dict()`. `test_py2x_entrypoints_contract.py` now fixes the corresponding header/impl string-carrier contract as well.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Reworked native `CompilerRootDocument::to_legacy_dict()` so the `kind/source_path/east_stage/schema_version/meta` updates now flow through typed dict conversion instead of explicit `make_object(...)` calls. That drops the explicit boxing calls in `transpile_cli.cpp` to zero and leaves boxing as an internal detail of dict conversion / adapter seams, with coverage locked by `test_py2x_entrypoints_contract.py` and `python3 tools/build_selfhost.py`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Made native `lower_ir_typed()` / `optimize_ir_typed()` stay on the no-op typed lane instead of bouncing through `spec.to_legacy_dict()` plus `lower_options.to_legacy_dict()` / `optimizer_options.to_legacy_dict()`, pushing the legacy adapter one step farther away from the selfhost typed path. `test_py2x_entrypoints_contract.py` now also fixes the absence of the old delegate calls.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Slimmed native `lower_ir_typed()` / `optimize_ir_typed()` into thin adapters that stop rebuilding `spec.to_legacy_dict()` and option carriers; they now return `east.to_legacy_dict()` / `ir` directly, matching the current selfhost direct route. `test_py2x_entrypoints_contract.py` now forbids the old round-trip forms, and `python3 tools/build_selfhost.py` confirms the selfhost build still passes.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Switched the native `transpile_cli.cpp` host-Python direct route away from calling `doc.to_legacy_dict()` inline. It now goes through `typed_boundary.export_compiler_root_document()` as an explicit export seam, while `CompilerRootDocument::to_legacy_dict()` stays as the legacy adapter and `test_py2x_entrypoints_contract.py` fixes that embedded-script contract.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added a one-argument overload for the native `apply_runtime_hook` no-op lane, so the legacy `spec` entry no longer calls `_coerce_backend_spec(...)` before doing nothing. `test_py2x_entrypoints_contract.py` now also fails fast if that old delegate line comes back.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Consolidated native `CompilerRootDocument` export behind `export_compiler_root_document(...)`, so legacy `load_east3_document()` and native `lower_ir_typed()` now share the same explicit export seam instead of calling `to_legacy_dict()` directly.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Native `ResolvedBackendSpec` / `LayerOptionsCarrier` now also export through `export_backend_spec()` / `export_layer_options()`, so legacy `get_backend_spec()` / `resolve_layer_options()` and the remaining `to_legacy_dict()` adapters all share the same explicit export seam.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Switched the host linked-program and writer lane in `src/py2x.py` over to `export_compiler_root_document()` / `export_program_artifact_carrier()` / `export_module_artifact_carrier()`, removing the last inline `to_legacy_dict()` calls from the entrypoint itself. `test_py2x_entrypoints_contract.py` now fixes those helper imports and forbids the old paths.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_make_constant_expr()` / `_sh_make_if_stmt()` / `_sh_make_for_stmt()` / `_sh_make_for_range_stmt()` / `_sh_make_range_expr()` to the generated selfhost core, then routed the `elif` tail, statement-level `if/for/range` fastpath, and list-comprehension range fastpath through those builders instead of open-coded `make_object` dict assembly. `test_prepare_selfhost_source.py` now also rejects the old inline `If` / `For` / `ForRange` / `RangeExpr` clusters.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The generated selfhost core now also uses `_sh_make_formatted_value_node()` / `_sh_make_joined_str_expr()` / `_sh_make_ifexp_expr()` on the f-string and lowered conditional-expression lane, which removes the inline `make_object` dict assembly for `FormattedValue`, `JoinedStr`, and the lowered `IfExp`. `test_prepare_selfhost_source.py` now guards those clusters too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The host/static backend registry typed wrappers now use `export_compiler_root_document()` / `export_layer_options_carrier()` as their regular export seams, which pushes more `to_legacy_dict()` calls out of `lower_ir_typed()` / `optimize_ir_typed()` / `emit_module_typed()`. `test_py2x_entrypoints_contract.py` now fixes those helper-based calls and forbids the old `coerce_compiler_root_document(...).to_legacy_dict()` path from returning.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `ModuleArtifactCarrier` / `ProgramArtifactCarrier` now also export through `export_*_artifact_carrier()` helpers, and the host/static backend registry `emit_module()` / `build_program_artifact()` / `collect_program_modules()` entrypoints no longer call `to_legacy_dict()` inline. `test_py2x_entrypoints_contract.py` now fixes both helper-vs-legacy equivalence and the helper-based source contract in those host/static lanes.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Updated `tools/check_transpiler_version_gate.py` so `src/py2x-selfhost.py` is treated as a direct cpp dependency, and added `test_check_transpiler_version_gate.py` to fail fast when selfhost entrypoint changes are missing a cpp version bump.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The source-of-truth backend spec export now also routes through `export_backend_spec_carrier()` / `export_resolved_backend_spec()`, so host/static `get_backend_spec()` and static spec-cache normalization no longer call `.to_legacy_dict()` inline.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The linked-program build and program-artifact normalization in `src/py2x.py` now also route through `export_compiler_root_document()` / `export_program_artifact_carrier()` / `export_module_artifact_carrier()`, which pushes the remaining inline `.to_legacy_dict()` calls out of the host entrypoint itself.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The host/static legacy `resolve_layer_options()` surface and the host `_normalize_backend_spec()` helper now also export through `export_layer_options_carrier()` / `export_resolved_backend_spec()`, which aligns the remaining inline `.to_legacy_dict()` paths behind helper-owned seams.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The host/static legacy `resolve_layer_options()` surface and the host `_normalize_backend_spec()` helper now also export through `export_layer_options_carrier()` / `export_resolved_backend_spec()`, which aligns the remaining inline `.to_legacy_dict()` paths behind helper-owned seams.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/ir2lang.py` now normalizes writer-facing artifacts through `coerce_module_artifact()` plus `export_*_artifact_carrier()`, which removes both the inline `.to_legacy_dict()` path and the `hasattr(..., "to_legacy_dict")` branch from that host entrypoint.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The writer lane in `src/py2x.py` now also uses `coerce_module_artifact()` directly at the entrypoint, drops the typed-path `hasattr(..., "to_legacy_dict")` branches, and keeps `export_program_artifact_carrier()` / `export_module_artifact_carrier()` as the regular export seam.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] This slice added `coerce_program_artifact()` so the `src/py2x.py` writer lane normalizes through a typed `ProgramArtifactCarrier`, replacing the entrypoint-local dict branch and module-list normalization with helper-owned carrier coercion. `test_py2x_entrypoints_contract.py` and `test_py2x_cli.py` now fix both the dict test-double lane and helper-module flattening behavior.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now guards the generated selfhost core `def_sig` / `expr_token` / `import_binding` lane too, so `_sh_make_def_sig_info()` / `_sh_make_expr_token()` / `_sh_make_import_binding()` must stay present and raw dict return/append paths fail fast if they return to the tracked mirror.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The generated selfhost core module-root tail now uses `_sh_import_binding_fields()` / `_sh_make_import_resolution_binding()` / `_sh_make_import_symbol_binding()` / `_sh_make_qualified_symbol_ref()` / `_sh_make_module_source_span()` / `_sh_make_import_resolution_meta()` / `_sh_make_module_meta()` / `_sh_make_module_root()`, moving the import-resolution and module meta/root assembly out of open-coded dict construction and behind helper-owned carriers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The generated selfhost core `_parse_comp_target()` / `_parse_primary()` lanes now route `Name` / `Tuple` / `Constant` / `List` / `Dict` / `Set` nodes through `_sh_make_name_expr()` / `_sh_make_tuple_expr()` / `_sh_make_constant_expr()` / `_sh_make_list_expr()` / `_sh_make_dict_expr()` / `_sh_make_set_expr()`, shrinking another literal/comprehension-target raw-dict return cluster behind helper-owned builders.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now guards `_sh_make_slice_node()` in the generated selfhost core too, so the postfix slice `Subscript` lane fails fast if the old inline `Slice` literals return.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now also guards the generated selfhost class-field / enum-field / top-level-binding lane, so `_sh_make_assign_stmt()` / `_sh_make_ann_assign_stmt()` / `_sh_make_name_expr()` must remain in place and the old inline `Assign` / `AnnAssign` dicts fail fast if they return.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now also guards the generated selfhost `Name` return / top-level `Expr` helper lane, so `return _sh_make_name_expr(...)` and `_sh_make_expr_stmt(...)` appends must remain in place and the old inline `Expr` dict append fails fast if it returns.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now also guards the generated selfhost comprehension / `range(...)` helper call lane, so `_sh_make_comp_generator(...)` and `_sh_make_range_expr(...)` call sites must remain in place and the old inline generator / `RangeExpr` dicts fail fast if they return.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now also guards the generated selfhost control-flow helper call lane, so `_sh_make_if_stmt()` / `_sh_make_for_stmt()` / `_sh_make_for_range_stmt()` call sites must remain in place and the old inline `If` / `For` / `ForRange` dicts fail fast if they return.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Because the generated mirror under `selfhost/runtime/cpp/pytra-gen/...` is ignored, `test_east_core.py` now also guards the source-of-truth `src/toolchain/ir/core.py` statement lanes for `with/while/try/raise/pass/return/augassign/swap`, locking in helper calls and the absence of the old inline statement dict clusters.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now fixes the residual inline `kind` set in the generated selfhost core, so already-helperized kinds such as `If/While/Try/Raise/Return/AugAssign/Swap` cannot silently fall back to open-coded dicts and new unsynced kinds fail fast if they appear.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now also fixes the residual inline call-site `kind` set in the generated selfhost core, so helper definitions stay excluded while the real open-coded dict call sites cannot expand beyond `AnnAssign/Assign/Expr/Name/Tuple` or silently reintroduce already-helperized statement kinds.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now also fixes the residual inline non-statement call-site `kind` set in the generated selfhost core, so once statement wrappers are excluded the remaining open-coded usage surface cannot expand beyond `Name/Tuple` or silently reintroduce helper-owned decl / expr / call lanes.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The generated-selfhost residual guard helper now exposes explicit `all` / `callsite` / `nonstmt_callsite` scopes, so the duplicated inline call-site regression loop is folded back into one helper-owned implementation.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] To avoid depending only on the ignored generated mirror, `test_east_core.py` now also guards the source-of-truth expr helper lanes for `Attribute/Call/BinOp/Subscript/Lambda/FormattedValue/JoinedStr`, locking in helper call sites and the absence of the old inline dict clusters.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] To avoid depending only on the generated residual `AnnAssign/Assign/Expr/Name/Tuple` call-site set, `test_east_core.py` now also guards the source-of-truth helper lanes for typed bindings, tuple destructuring, the simple list-comp target, and bare `Expr`, including the absence of the old inline clusters.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] To avoid relying only on the ignored generated mirror, `test_east_core.py` now also guards the source-of-truth literal / comprehension-target helper lanes for `Name/Tuple/Constant/List/Dict/Set`, locking in helper call sites and the absence of the old inline returns.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now classifies residual `callsite` lines by actual helper-definition ranges instead of dropping every `return dict<...>` line, fixing the generated selfhost core's real call-site sets as `AnnAssign/Assign/Call/Dict/Expr/Name/Tuple` and `Call/Dict/Name/Tuple` for the non-statement subset.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now adds `_sh_make_dict_entry()` and routes dict-literal entry carriers in `_sh_parse_expr_lowered()` through that helper. `test_east_core.py` also locks in the collection/comprehension lane plus the absence of the old inline entry dicts.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] To avoid leaning too hard on the ignored generated mirror's residual `Call/Dict/Tuple` set, `test_east_core.py` now also guards `_sh_parse_expr_lowered()` directly for the `any/all` normalization and lowered dict/tuple return helpers. That makes the source of truth fail fast if `payload = _sh_make_call_expr(...)`, `return _sh_make_dict_expr(...)`, or `return _sh_make_tuple_expr(...)` slips back to the old inline `Call/Dict/Tuple` dict returns.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_east_core.py` now also fixes the full raw inline `kind` residual set for `src/toolchain/ir/core.py`, so uppercase residuals cannot grow past the helper-definition-only `Expr/Slice` pair and lowercase residuals stay limited to the trivia-only `blank/comment` pair.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now has a shared `_sh_make_kind_carrier()` helper, and the `Expr` / `Slice` / `blank` / `comment` helpers all route through it. `test_east_core.py` now fixes the source-of-truth raw inline `kind` residual set at the empty set.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now routes the top-level function/class and class-method declaration spans through `_sh_block_end_span(...)`, and `test_east_core.py` also locks in the import / declaration helper call sites plus the absence of the old inline `source_span` carriers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now also routes `_sh_block_end_span()` and `_sh_stmt_span()` through `_sh_span(..., end_lineno=...)`, and `test_east_core.py` locks in that the statement-span helpers no longer return raw multi-line `source_span` dict literals directly.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now upgrades the residual guard helper to use `finditer(...)` plus a `functions=` filter, so generated residuals are fixed per function for `_sh_parse_expr_lowered`, `_parse_primary`, and `_sh_parse_stmt_block_mutable`. That isolates the lowered residual `Call/Dict/Name/Tuple` lane from the statement-block non-statement residual `Name/Tuple` lane.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/compiler/transpile_cli.py` now routes `load_east3_document_typed()` straight into the canonical frontend typed API, removing one `load_east3_document()` plus coercion hop from the tracked compiler shim source. The compiler runtime emit still stays header-only, so `test_py2x_entrypoints_contract.py` now fixes both the shim-source contract and that header-only layout.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `CompilerRootMeta.from_legacy_doc()` now preserves legacy `source_path` and `meta.parser_backend`, and `export_compiler_root_document_any()` now follows the canonical `coerce -> export` seam instead of raw-dict short-circuiting. `test_py2x_entrypoints_contract.py` fixes that normalized compiler-root export round-trip.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The host/static legacy artifact surfaces now align on `export_module_artifact_any()` / `export_program_artifact_any()`, extending the typed export-helper contract from compiler-root export to module/program artifacts as well. `test_py2x_entrypoints_contract.py` now fixes the registry imports and return seams.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Legacy backend-spec and layer-option export now align on `export_resolved_backend_spec_any()` / `export_layer_options_any()`, moving host/static `get_backend_spec()` / `resolve_layer_options()` plus the static cache export onto the same typed helper seam. `test_py2x_entrypoints_contract.py` now fixes those helper imports and return contracts too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Native C++ `coerce_compiler_root_document()` now preserves legacy `source_path` and `meta.parser_backend` too, matching the Python-side compiler-root round-trip contract. `test_py2x_entrypoints_contract.py` now fixes those native fallback extractions.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_east_core.py` and `test_prepare_selfhost_source.py` now guard the `any/all` normalization, simple list-comp, and tuple-destructuring residual clusters directly, fixing that the remaining source/generated selfhost `Call/Dict/Name/Tuple` inline nodes do not spread beyond those known branches.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `backend_spec_target()` now owns backend-target extraction in [py2x.py](/workspace/Pytra/src/py2x.py), removing the entrypoint-local `dict` unwrap and `spec.carrier.target_lang if not isinstance(...)` branch. `test_py2x_entrypoints_contract.py` now fixes the import plus helper round-trip too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `typed_boundary.py` now exposes `coerce_ir_document()` / `coerce_module_artifact_or_none()`, and both host/static backend registries route their local dict/module fallback through those helper seams. `test_py2x_entrypoints_contract.py` now fixes the imports, return normalization, and the absence of the old local helper too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `compiler_root_module_id()` now owns fallback module-id extraction in [py2x.py](/workspace/Pytra/src/py2x.py) and [ir2lang.py](/workspace/Pytra/src/ir2lang.py), removing the entrypoint-local `meta.module_id` dict unwrap. `test_py2x_entrypoints_contract.py` now fixes the imports, source patterns, and helper round-trip too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `module_artifact_text()` now owns fallback text extraction in [ir2lang.py](/workspace/Pytra/src/ir2lang.py), and `_identity_ir()` in both host/static registries now routes through `coerce_ir_document()`. `test_py2x_entrypoints_contract.py` now fixes the imports, source patterns, and helper round-trip too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The linked-output lane in [ir2lang.py](/workspace/Pytra/src/ir2lang.py) now uses `LinkedProgramModule` directly, removing the `getattr(..., "module_id"/"source_path"/"east_doc")` unwraps from `_entry_linked_module()`, `_entry_source_path()`, and `_emit_cpp_linked_program()`. `test_py2x_entrypoints_contract.py` now fixes the typed import and forbids the old manual unwraps.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The linked-program validation loops in [py2x.py](/workspace/Pytra/src/py2x.py) and [ir2lang.py](/workspace/Pytra/src/ir2lang.py) now also read `linked_module.east_doc` directly, removing the last `getattr(..., "east_doc", {})` unwraps from the restart lane. `test_py2x_entrypoints_contract.py` now fixes direct typed-field access in both entrypoints.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The host/static registry legacy emit adapters now also route through `normalize_module_artifact_carrier()` and `export_module_artifact_any()`, removing the duplicated `_normalize_module_artifact` helpers and the host-side `_flatten_helper_modules` shim from `backend_registry.py` / `backend_registry_static.py`. `test_py2x_entrypoints_contract.py` now fixes the absence of those local legacy normalizers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The follow-up cleanup also removes the now-dead `_default_module_label()` helper from both host/static registries, leaving artifact label fallback solely to the regular normalizer in `typed_boundary.py`. `test_py2x_entrypoints_contract.py` now fixes the absence of that dead local helper too.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `typed_boundary.py` now also owns `normalize_emitted_module_artifact()`, so the host/static registries no longer keep a local `_normalize_module_artifact_typed()` wrapper around `EmitRequestCarrier`. `test_py2x_entrypoints_contract.py` now fixes the helper import, return path, and the absence of those local request-aware normalizers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `typed_boundary.py` now owns `_legacy_dict_adapter()`, so the remaining open-ended `to_legacy_dict` probing inside `export_layer_options_any()`, `export_resolved_backend_spec_any()`, and `backend_spec_target()` is centralized behind one helper. `test_py2x_entrypoints_contract.py` now also fixes the spec/options round-trip through wrapper objects.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The host/static registry `_legacy_emit_module_adapter()` and the C++ emitter-option read path now both route through `export_layer_options_any(..., layer="emitter")`, removing the remaining `emitter_options if isinstance(..., dict)` branches from tracked source. `test_py2x_entrypoints_contract.py` now fixes both the new helper seam and the absence of the old local dict fallback.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now adds `_sh_make_value_expr()`, so the shared expression envelope (`source_span` / `resolved_type` / `borrow_kind` / `casts` / `repr`) is centralized for the `Name` / `Tuple` / `Call` / `Dict` builders and the surrounding expr-helper family. `test_east_core.py` now fixes the shared helper definition, the per-builder call sites, and the absence of the old inline `"kind": "Name"/"Tuple"/"Call"/"Dict"` carriers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument entry path in `src/toolchain/ir/core.py` now also splits into `_resolve_call_arg_entry_state()` / `_apply_call_arg_entry_state()`, so `_parse_call_arg_entry()` no longer carries the `NAME` save/rewind plus inline keyword builder itself. `test_east_core.py` now fixes both helper definitions, the entry call site, and the absence of the old inline call-arg-entry block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `save_pos` capture for call-argument entries now also routes through `_ShExprParser._resolve_call_arg_entry_save_pos()`, so `_resolve_call_arg_entry_state()` no longer carries the direct `self.pos` read itself. `test_east_core.py` now fixes the save-pos helper definition, the state-helper call site, and the absence of the old inline save-pos block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument loop comma/terminator control now also splits into parser methods `_resolve_call_arg_loop_state()` / `_apply_call_arg_loop_state()`, so `_advance_call_arg_loop()` no longer carries the inline `","` probe plus `self._eat(",")` itself. `test_east_core.py` now fixes both helper definitions, the loop-helper call site, and the absence of the old inline loop-control block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Acceptance of legacy `to_legacy_dict()` adapters now lives in `coerce_compiler_root_document()`, `coerce_layer_options()`, `coerce_backend_spec()`, `coerce_module_artifact()`, and `coerce_program_artifact()`, which lets `export_layer_options_any()`, `export_resolved_backend_spec_any()`, and `backend_spec_target()` shed another layer of open-coded probing. `test_py2x_entrypoints_contract.py` now also fixes compiler-root/module/program coercion through wrapper objects.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The duplicated legacy emit adapter in the host/static registries now lives in `typed_boundary.build_legacy_emit_module_adapter()`, with the only remaining difference expressed as a flag: host still suppresses emit exceptions, while static still propagates errors after the 2-argument fallback. `test_py2x_entrypoints_contract.py` now fixes the helper import/use, the absence of the old local adapter, and both error-policy paths.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `typed_boundary.py` now also owns `normalize_legacy_backend_spec_dict()`, so host/static `_normalize_backend_runtime_spec()` no longer duplicate the raw `default_options` / `option_schema` layer normalization. `test_py2x_entrypoints_contract.py` now fixes the helper import/use and the layer-fill contract.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `tools/check_transpiler_version_gate.py` now treats `src/toolchain/ir/core.py` as a direct `cpp` dependency too, so source-of-truth selfhost-helper changes now land on the same `cpp` version-bump gate as `src/py2x-selfhost.py`. `test_check_transpiler_version_gate.py` now fixes that `core.py` also lights up the `cpp` lane.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now adds `_sh_make_stmt_node()`, centralizing the shared `kind+source_span` envelope for statement helpers including `Expr`, `Assign`, `Try`, `FunctionDef`, and `ClassDef`. `test_east_core.py` now fixes the shared helper definition, representative helper call sites, and the absence of the old inline statement literals.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now also adds `_sh_make_node()`, and `_sh_make_stmt_node()` / `_sh_make_value_expr()` / `_sh_make_slice_node()` / `_sh_make_except_handler()` plus the trivia helpers now share that generic `kind` + field carrier helper instead of rebuilding their own node skeletons. `test_east_core.py` now fixes the generic helper definition, representative statement/value/slice/except call sites, and the absence of the old inline kind carriers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_prepare_selfhost_source.py` now also fixes generated-selfhost residuals at the branch level: the inline `Call+Name` cluster for `any/all` normalization, the inline `Name` cluster for the simple list-comprehension fast path, and the inline `Tuple+Name+Assign` cluster for the tuple-destructuring fallback are all locked independently.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The source-of-truth `core.py` now also routes `_sh_make_arg_node()`, `_sh_make_formatted_value_node()`, and `_sh_make_module_root()` through `_sh_make_node()`, and `test_east_core.py` now catches multiline raw `{\"kind\": ...}` literals too instead of only one-line carriers.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The duplicated host/static backend-spec normalization body now lives in `typed_boundary.build_resolved_backend_spec()`, and `test_py2x_entrypoints_contract.py` fixes the writer/runtime-hook/emit fallback contract plus the new import/source patterns.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The duplicated host/static `collect_program_modules_typed()` / `build_program_artifact_typed()` module-coercion loops now live behind `typed_boundary.collect_program_module_carriers()` / `typed_boundary.build_program_artifact_from_modules()`, so helper-module flattening and program-artifact module normalization are both owned by one typed-boundary seam.

- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `typed_boundary.py` now also exposes `build_resolved_backend_spec()`, removing the duplicated `lower/optimizer/emit/runtime_hook/program_writer` fallback resolution plus legacy emit-adapter assembly from host/static `_normalize_backend_runtime_spec()`. The registry layer now keeps only thin wrappers that pass identity/empty emit/runtime hook/default writer and strictness policy, and `test_py2x_entrypoints_contract.py` fixes the helper import/use, the host/static `default_program_writer` difference, and the absence of the old `normalized = ...` / `emit_module_impl = ...` / `program_writer_impl = ...` / `runtime_hook_impl = ...` boilerplate.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `typed_boundary.py` now also exposes `execute_lower_ir_with_spec()`, `execute_optimize_ir_with_spec()`, and `execute_emit_module_with_spec()`, moving the remaining host/static `lower_ir_typed()` / `optimize_ir_typed()` / `emit_module_typed()` request-normalization, signature-fallback, and error-policy boilerplate behind one helper-owned seam. `test_py2x_entrypoints_contract.py` now fixes both the helper import/use pattern and the fail-soft vs fail-closed contract.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The follow-up helper slice now also adds `emit_source_text_with_spec()`, `export_program_module_artifacts()`, `get_program_writer_with_spec()`, and `apply_runtime_hook_with_spec()` to `typed_boundary.py`, so the remaining host/static text-export, module-list-export, program-writer, and runtime-hook wrappers also route through the same typed helper family.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The next passthrough slice applies that same helper family at the public host/static registry surface too: `collect_program_modules()` / `get_program_writer_typed()` / `emit_source_typed()` / `apply_runtime_hook_typed()` now reduce to direct calls into `export_program_module_artifacts()` / `get_program_writer_with_spec()` / `emit_source_text_with_spec()` / `apply_runtime_hook_with_spec()`. `test_py2x_entrypoints_contract.py` now fixes both the helper import/use and the absence of the older local passthrough forms (`program_writer_impl`, `emit_module_typed(...).text`, and the local runtime-hook variable).
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `test_py2x_entrypoints_contract.py` now also adds a direct helper contract test so `emit_source_text_with_spec()` / `export_program_module_artifacts()` / `get_program_writer_with_spec()` / `apply_runtime_hook_with_spec()` are fixed at the helper level too, including emitted text, helper-module export, program-writer identity, and runtime-hook invocation.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now adds `_sh_make_tuple_destructure_assign_stmt()`, moving the tuple-destructuring `Tuple + Name + Assign` source-of-truth cluster behind one helper call. `test_east_core.py` fixes the new helper definition and call site, while the existing generated-selfhost guard confirms that the current build still keeps the generated residual cluster through inlining.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The simple `[x for x in items]` branch now also routes through `_sh_make_simple_name_list_comp_expr()`, collapsing the source-of-truth `Name + comp_generator + ListComp` cluster into one helper call. `test_east_core.py` fixes that the lowered branch now keeps only the helper call, while the generated selfhost side still stays within the existing residual guard envelope.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `build_program_artifact_from_modules()` now also routes through `collect_program_module_carriers()`, so program-artifact assembly from nested `helper_modules` uses the same typed-boundary helper flattening seam as `collect_program_modules()`. `test_py2x_entrypoints_contract.py` now fixes that nested helper modules expand into the final `modules` list on the host, static, and typed lanes.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `collect_program_modules_from_items()` so `coerce_program_artifact()` and `build_program_artifact_from_modules()` share the same module-coercion and nested-helper flatten loop. A direct contract test now fixes that the program-artifact round-trip lane and the module-list build lane return the same flattened module-id sequence.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The lowered `any/all(<list-comp>)` builtin-call lane now also routes through `_sh_make_builtin_listcomp_call_expr()`, collapsing the `_sh_parse_expr_lowered()` `Call + Name + BuiltinCall metadata` cluster into one helper seam. `test_east_core.py` now fixes the helper definition and call site, while the generated selfhost side stays within the existing `Call+Name` residual guard.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_annotate_runtime_call_expr()` so `_parse_postfix()` now routes `BuiltinCall` / `TypePredicateCall` metadata through one shared helper. The repeated field writes for `print/len/range/...` and method runtime bindings now live behind one helper seam, and `test_east_core.py` fixes both the helper definition and its call sites.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Added `_sh_annotate_resolved_runtime_expr()` so `_parse_postfix()` now routes non-C++ import/module-attr runtime annotations through one shared helper. The `resolved_runtime_call` / `resolved_runtime_source` plus binding/semantic-tag writes now live behind one helper seam, and `test_east_core.py` fixes the absence of the old inline field writes.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] As a follow-up, the direct attribute-access `module_attr` annotation now also routes through `_sh_annotate_resolved_runtime_expr()`, removing the last `_parse_postfix()` `node["resolved_runtime_call"]` / `node["resolved_runtime_source"]` writes from that branch. `test_east_core.py` now also locks that the old `noncpp_module_attr_runtime_call` field writes do not reappear.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `BuiltinAttr` metadata path now also routes through `_sh_annotate_runtime_attr_expr()`, removing the direct `_parse_postfix()` writes for `lowered_kind` / `runtime_call` / `runtime_owner` on the attribute-access branch. `test_east_core.py` now fixes both the helper definition and the absence of the old `node[...]` field writes.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `obj.method(...)` call-metadata path now also routes through `_sh_annotate_runtime_method_call_expr()`, removing the attribute-call branch's direct `owner_method` semantic-tag handling and stdlib method runtime-binding resolution from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old inline `owner_method_semantic_tag` / `rc` handling.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `enumerate(...)` metadata path now also routes through `_sh_annotate_enumerate_call_expr()`, removing the direct `_parse_postfix()` writes for `iterable_trait` / `iter_protocol` / `iter_element_type` / `resolved_type`. `test_east_core.py` now fixes both the helper definition and the absence of the old `elem_t`-driven field writes.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The stdlib function-call metadata path now also routes through `_sh_annotate_stdlib_function_call_expr()`, removing the direct `_parse_postfix()` `lookup_stdlib_function_runtime_binding()` / `lookup_stdlib_function_return_type()` chain and the inline `resolved_type` write. `test_east_core.py` now fixes both the helper definition and the absence of the old `mod_id` / `sig_ret` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The stdlib imported-symbol call metadata path now also routes through `_sh_annotate_stdlib_symbol_call_expr()`, removing the direct `_parse_postfix()` `lookup_stdlib_imported_symbol_runtime_binding()` block. `test_east_core.py` now fixes both the helper definition and the absence of the old `_SH_IMPORT_SYMBOLS` binding block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The non-C++ imported-symbol call metadata path now also routes through `_sh_annotate_noncpp_symbol_call_expr()`, removing the `_SH_IMPORT_SYMBOLS` lookup and binding-semantic-tag block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old `binding_semantic_tag` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The non-C++ module/symbol attribute runtime lookup now also routes through `_sh_lookup_noncpp_attr_runtime_call()`, so both the attribute-access branch and the `Call(Attribute)` branch stop open-coding the same import-module/import-symbol probe inside `_parse_postfix()`. `test_east_core.py` now fixes the helper definition plus the absence of both old inline lookup blocks.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The inside of the simple list-comp helper now also routes through `_sh_make_simple_name_comp_generator()`, collapsing the remaining target `Name` + `comp_generator` cluster inside `_sh_make_simple_name_list_comp_expr()` into one helper seam. `test_east_core.py` now fixes the new helper definition plus the absence of the old `target_node = _sh_make_name_expr(...)` block, while the generated selfhost side stays within the existing `Name` residual guard.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The remaining non-C++ attr-call annotation in `Call(Attribute)` now also routes through `_sh_annotate_noncpp_attr_call_expr()`, removing the inline `resolved_runtime_call`, binding semantic-tag, and `resolved_type` override writes from `_parse_postfix()`. `test_east_core.py` now fixes the helper definition plus the absence of the old `std_module_attr_ret` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `int/float/bool(...)` scalar ctor metadata path now also routes through `_sh_annotate_scalar_ctor_call_expr()`, removing the `static_cast` / `py_to_int64_base` / `py_to_bool` switching and binding selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old `runtime_call` / `runtime_module_id` / `runtime_symbol` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `min/max(...)` metadata path now also routes through `_sh_annotate_minmax_call_expr()`, removing the `py_min/py_max` runtime-call, module, and runtime-symbol selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old inline `py_min/py_max` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `bytes/bytearray/list/set/dict(...)` ctor metadata path now also routes through `_sh_annotate_collection_ctor_call_expr()`, removing the `bytes_ctor` / `bytearray_ctor` / `*_ctor` runtime-call selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old ctor inline blocks.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `any/all(...)` metadata path now also routes through `_sh_annotate_anyall_call_expr()`, removing the `py_any/py_all` runtime-call selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old predicate inline blocks.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `ord/chr(...)` metadata path now also routes through `_sh_annotate_ordchr_call_expr()`, removing the `py_ord/py_chr` runtime-call and runtime-symbol selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old scalar inline blocks.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `iter/next/reversed(...)` metadata path now also routes through `_sh_annotate_iterator_builtin_call_expr()`, removing the `py_iter_or_raise` / `py_next_or_stop` / `py_reversed` runtime-call and module selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old iterator-builtin inline blocks.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `open(...)` metadata path now also routes through `_sh_annotate_open_call_expr()`, removing the `open` runtime-call, module, and runtime-symbol selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old open inline block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Exception/RuntimeError(...)` metadata path now also routes through `_sh_annotate_exception_ctor_call_expr()`, removing the `std::runtime_error` runtime-call, module, and runtime-symbol selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old exception-ctor inline block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `isinstance/issubclass(...)` metadata path now also routes through `_sh_annotate_type_predicate_call_expr()`, removing the `TypePredicateCall` lowered-kind and builtin-name selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old type-predicate inline block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `print/len/range/zip/str(...)` metadata path now also routes through `_sh_annotate_fixed_runtime_builtin_call_expr()`, removing the fixed runtime-call, module, and runtime-symbol selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old fixed-builtin inline block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `print/len/range/zip/str(...)` metadata path now also routes through `_sh_annotate_fixed_runtime_builtin_call_expr()`, removing the fixed runtime-call, module, and runtime-symbol selection block from `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old fixed-builtin inline blocks.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The name-callee return path now also routes through `_sh_infer_known_name_call_return_type()`, moving the open-coded return-type branches for `print/open/int/float/bool/str/len/range/zip/list/set/dict/bytes/bytearray/Exception` plus stdlib imported-symbol returns out of `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old inline `call_ret = ...` branches.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `enumerate(...)` item-type path now also routes through `_sh_infer_enumerate_item_type()`, moving the `args[0]` probe and `elem_t` selection out of `_parse_postfix()`. `test_east_core.py` now fixes both the helper definition and the absence of the old inline `elem_t` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Call-expression return inference now also routes through `_ShExprParser._infer_call_expr_return_type()`, moving the `Name` / `Attribute` / `Lambda` callee `call_ret` branches out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, the absence of the old inline return-inference block, and the fact that `_sh_infer_enumerate_item_type()` is defined exactly once.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute` access lookup cluster now also routes through `_ShExprParser._lookup_attr_expr_metadata()`, moving `lookup_stdlib_attribute_type()`, `lookup_stdlib_method_runtime_binding()`, and `_sh_lookup_noncpp_attr_runtime_call()` out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old attr-lookup inline block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `src/toolchain/ir/core.py` now adds parser method `_annotate_named_call_expr()`, moving the `call_dispatch` field expansion plus the fixed-builtin, scalar-ctor, stdlib, non-C++, exception, iterator, collection, and predicate branches out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline annotation block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `call_dispatch` field unpack now also routes into the helper side, removing the local stdlib/non-C++ runtime-call and builtin semantic-tag unpack from `_annotate_named_call_expr()`. `test_east_core.py` now updates the named-call, builtin-helper, and runtime-helper source guards accordingly.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `_annotate_call_expr()` now also routes its named-call preflight into `_guard_named_call_args()`, removing the local `fn_name` extraction and the decode-first guard for `sum/zip/sorted/min/max` from the call helper body. `test_east_core.py` now fixes the guard-helper definition, the call site, and the absence of the old inline preflight block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `Call(Attribute)` annotations now also route through `_ShExprParser._annotate_attr_call_expr()`, moving the `attr/owner/owner_t` extraction plus the non-C++ and method-metadata writes out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline attr-call annotation block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Call-payload assembly now also routes through parser method `_ShExprParser._annotate_call_expr()`, moving the `fn_name` extraction, the `sum/zip/sorted/min/max` decode-first guard, `_infer_call_expr_return_type()`, `_sh_make_call_expr()`, and the named/attr annotation dispatch out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline call-assembly block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Subscript-suffix parsing now also routes through parser method `_ShExprParser._parse_subscript_suffix()`, moving the `[`-suffix `:` / index / slice branching plus the `_annotate_subscript_expr()` dispatch out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline subscript-suffix block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Attr-suffix parsing now also routes through parser method `_ShExprParser._parse_attr_suffix()`, moving the `.` / `NAME` token consumption, span calculation, and `_annotate_attr_expr()` call out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline attr-suffix block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] `Attribute` access itself now also routes through `_ShExprParser._annotate_attr_expr()`, moving the owner-type guards, metadata lookup, `_sh_make_attribute_expr()`, and runtime / resolved-runtime annotation chain out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline attribute-access block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Subscript / slice construction now also routes through `_ShExprParser._annotate_subscript_expr()`, moving the `_sh_make_subscript_expr()` / `_sh_make_slice_node()` / `_subscript_result_type()` chain out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline subscript block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument loop now also routes through parser method `_ShExprParser._parse_call_args()`, moving the `NAME '='` keyword branch and `save_pos` rewind out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline call-arg loop.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The full `(` suffix now also routes through parser method `_ShExprParser._parse_call_suffix()`, moving the `(` / `)` token consumption, span calculation, and `_annotate_call_expr()` call out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline call-suffix block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The shared postfix `source_span` / `repr` calculation now also routes through `_ShExprParser._resolve_postfix_span_repr()`, so `_parse_call_suffix()`, `_parse_attr_suffix()`, and `_parse_subscript_suffix()` no longer carry the direct `self._node_span()` plus `self._src_slice()` sequence themselves. `test_east_core.py` now fixes the shared-helper definition, each suffix-helper call site, and the absence of the old inline span/repr block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The postfix suffix dispatch itself now also routes through parser method `_ShExprParser._parse_postfix_suffix()`, moving the `.` / `(` / `[` token-kind branching out of `_parse_postfix()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline suffix-dispatch if-chain.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The stdlib / non-C++ side of named-call dispatch now also routes through parser method `_ShExprParser._annotate_runtime_named_call_expr()`, moving the stdlib-function, stdlib-imported-symbol, and non-C++ imported-symbol runtime-call branches out of `_annotate_named_call_expr()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline runtime-dispatch block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Named-call lookup now also routes through `_ShExprParser._annotate_named_call_expr()`, so `_annotate_call_expr()` no longer performs `_sh_lookup_named_call_dispatch(fn_name)`. `test_east_core.py` now fixes that the lookup remains inside the named-call helper and does not re-enter the call helper or `_parse_postfix()`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Call-annotation callee dispatch now also routes through `_ShExprParser._annotate_callee_call_expr()`, so `_annotate_call_expr()` no longer carries the `Name` / `Attribute` branches itself. `test_east_core.py` now fixes that named/attr dispatch stays inside the callee helper while the call helper only delegates to `_annotate_callee_call_expr()`.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The local field unpack for `Attribute` calls now also routes through `_ShExprParser._resolve_attr_callee()`, so `_annotate_attr_call_expr()` no longer performs the inline `attr/value/resolved_type` extraction itself. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline attr-callee unpack block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The local `Attribute`-callee field unpack now also routes through `_ShExprParser._resolve_attr_callee()`, so `_annotate_attr_call_expr()` no longer expands `attr/value/resolved_type` itself. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline attr-callee unpack.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The runtime named-call dispatch field unpack now also routes through `_ShExprParser._resolve_runtime_named_call_dispatch()`, so `_annotate_runtime_named_call_expr()` no longer performs the local stdlib/non-C++ runtime-call and semantic-tag unpack itself. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline runtime-dispatch unpack.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `bool(...)` truthy-runtime probe now also routes through `_ShExprParser._should_use_truthy_runtime_for_bool_ctor()`, so `_annotate_builtin_named_call_expr()` no longer carries the inline object-receiver probe itself. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline bool-constructor probe.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-access metadata field unpack now also routes through `_ShExprParser._resolve_attr_expr_annotation()`, so `_annotate_attr_expr()` no longer performs the local `resolved_type/runtime_call/semantic_tag` unpack itself. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline attr-metadata unpack.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-access metadata lookup plus unpack now also routes through `_ShExprParser._resolve_attr_expr_metadata()`, so `_annotate_attr_expr()` no longer performs the two-step `self._lookup_attr_expr_metadata()` plus `_resolve_attr_expr_annotation()` chain itself. `test_east_core.py` now fixes the combined resolve-helper definition, the call site, and the absence of the old inline attr-metadata resolve block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-access node build now also routes through `_ShExprParser._build_attr_expr_payload()`, so `_annotate_attr_expr()` no longer performs the direct `_sh_make_attribute_expr()` call itself. `test_east_core.py` now fixes the builder-helper definition, the call site, and the absence of the old inline attr-node build.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-access annotation-state return was narrowed too: `_ShExprParser._resolve_attr_expr_annotation_state()` no longer returns `owner_t`, so that owner-type value now stays sealed inside the metadata lane instead of re-entering `_annotate_attr_expr()`. `test_east_core.py` now fixes both the absence of `owner_t` in the state helper return and its absence from the caller-side unpack.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The named-call annotation-state was narrowed too: `_ShExprParser._resolve_named_call_annotation_state()` now returns both the dispatch lookup result and its `builtin/runtime` kind. `_apply_named_call_dispatch()` now performs a single kind-based dispatch instead of the old builtin-then-runtime trial chain, and `test_east_core.py` fixes the state-helper definition, the apply-helper branches, and the absence of the old inline trial sequence.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The named-call annotation state now also returns a dispatch kind, so `_ShExprParser._apply_named_call_dispatch()` no longer performs the builtin/runtime two-step probe. `test_east_core.py` now fixes the dispatch-kind helper definition, the state-helper return shape, and the absence of the old inline fallback chain.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-access owner-type resolution plus preflight guard now also routes through `_ShExprParser._resolve_attr_expr_owner_state()`, so `_annotate_attr_expr()` no longer carries the `keys/items/values` dynamic-helper guard or the forbidden-object-receiver check itself. `test_east_core.py` now fixes the owner-state helper definition, the call site, and the absence of the old inline attr-owner preflight block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-access annotation application now also routes through `_ShExprParser._apply_attr_expr_annotation()`, so `_annotate_attr_expr()` no longer carries the runtime-attr / semantic-tag / non-C++ module-attr branch itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline attr-annotation branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The runtime named-call dispatch application now also routes through `_ShExprParser._apply_runtime_named_call_dispatch()`, so `_annotate_runtime_named_call_expr()` no longer carries the stdlib/non-C++ dispatch if-chain itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline runtime-named-call branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtin side of named-call dispatch now also routes through parser method `_ShExprParser._annotate_builtin_named_call_expr()`, moving the `print/len/...`, scalar-ctor, `min/max`, `enumerate`, iterator/predicate/collection-ctor branches out of `_annotate_named_call_expr()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline builtin-dispatch block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Builtin named-call annotation now also routes through parser method `_ShExprParser._annotate_builtin_named_call_expr()`, moving the `print/len/range/zip/str`, scalar-ctor, `min/max`, `open`, `enumerate`, `iter/next/reversed`, collection-ctor, and type-predicate branches out of `_annotate_named_call_expr()`. `test_east_core.py` now fixes the helper definition, the call site, and the absence of the old inline builtin-dispatch cluster.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtin named-call dispatch application now also routes through `_ShExprParser._apply_builtin_named_call_dispatch()`, so `_annotate_builtin_named_call_expr()` no longer carries the builtin if-chain itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline builtin-named-call branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtin named-call annotation preflight now also routes through `_ShExprParser._resolve_builtin_named_call_annotation_state()`, so the `bool(...)` truthy-runtime probe and `enumerate(...)` item-type inference no longer live inside `_annotate_builtin_named_call_expr()` / `_apply_builtin_named_call_dispatch()`. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline `use_truthy_runtime` / `_sh_infer_enumerate_item_type(args)` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The named-call imported-symbol return lookup and declared fallback now also route through `_ShExprParser._resolve_named_call_return_state()` / `_resolve_named_call_declared_return_type()`, so `_infer_named_call_return_type()` no longer carries `lookup_stdlib_imported_symbol_return_type(...)` plus the `fn_return_types` / `class_method_return_types` fallback chain itself. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline named-return state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The overall named-call annotation application now also routes through `_ShExprParser._apply_named_call_dispatch()`, so `_annotate_named_call_expr()` no longer carries the direct builtin/runtime apply sequence itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline named-call apply branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-call annotation application now also routes through `_ShExprParser._apply_attr_call_expr_annotation()`, so `_annotate_attr_call_expr()` no longer carries the direct non-C++ / runtime-method apply sequence itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline attr-call apply branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-payload assembly now also routes through `_ShExprParser._build_call_expr_payload()`, so `_annotate_call_expr()` no longer carries the direct `_sh_make_call_expr()` block itself. `test_east_core.py` now fixes the builder-helper definition, the call site, and the absence of the old inline call-payload block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The callee-kind call-annotation application now also routes through `_ShExprParser._apply_callee_call_annotation()`, so `_annotate_callee_call_expr()` no longer carries the direct named/attr dispatch sequence itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline callee-dispatch branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-annotation preflight now also routes through `_ShExprParser._resolve_call_expr_annotation_state()`, so `_annotate_call_expr()` no longer carries the direct `_infer_call_expr_return_type()` plus `_guard_named_call_args()` sequence itself. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline pre-annotation block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-annotation payload build and callee apply now also route through `_ShExprParser._apply_call_expr_annotation()`, so `_annotate_call_expr()` no longer carries the direct `_build_call_expr_payload()` plus `_annotate_callee_call_expr()` sequence itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline call-annotation apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The owner-expression `resolved_type` extraction now also routes through `_ShExprParser._owner_expr_resolved_type()`, so `_resolve_attr_callee()`, `_annotate_attr_expr()`, and `_annotate_subscript_expr()` no longer carry that raw field access themselves. `test_east_core.py` now fixes the shared-helper definition, the call sites, and the absence of the old inline owner-type block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtin named-call category resolution now also routes through `_ShExprParser._resolve_builtin_named_call_kind()`, so `_annotate_builtin_named_call_expr()` no longer carries the `print/len/...`, scalar-ctor, `min/max`, `enumerate`, iterator, and collection-ctor classification branch itself. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline builtin-kind block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtins named-call `semantic_tag` / `dispatch_kind` resolution now also routes through `_ShExprParser._resolve_builtin_named_call_dispatch()`, so `_annotate_builtin_named_call_expr()` no longer performs the two-step helper chain itself. `test_east_core.py` now fixes the combined resolve-helper definition, the call site, and the absence of the old inline builtin-dispatch resolve block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The runtime named-call category resolution now also routes through `_ShExprParser._resolve_runtime_named_call_kind()`, so `_annotate_runtime_named_call_expr()` no longer carries the stdlib-function / stdlib-symbol / non-C++-symbol classification branch itself. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline runtime-kind block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The runtime named-call unpack plus `dispatch_kind` resolution now also routes through `_ShExprParser._resolve_runtime_named_call_annotation()`, so `_annotate_runtime_named_call_expr()` no longer performs the two-step helper chain itself. `test_east_core.py` now fixes the combined resolve-helper definition, the call site, and the absence of the old inline runtime-dispatch resolve block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The runtime named-call apply lane now also splits through `_ShExprParser._apply_stdlib_function_named_call_annotation()`, `_apply_stdlib_symbol_named_call_annotation()`, and `_apply_noncpp_symbol_named_call_annotation()`, so `_apply_runtime_named_call_dispatch()` no longer carries the direct `_sh_annotate_*` calls itself. `test_east_core.py` now fixes all three helper definitions, the call sites, and the absence of the old inline runtime-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The named-call dispatch lookup now also routes through `_ShExprParser._resolve_named_call_dispatch()`, so `_annotate_named_call_expr()` no longer calls `_sh_lookup_named_call_dispatch(fn_name)` directly. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline named-call lookup.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-access owner-state resolve plus metadata resolve now also routes through `_ShExprParser._resolve_attr_expr_annotation_state()`, so `_annotate_attr_expr()` no longer carries the two-step `_resolve_attr_expr_owner_state()` plus `_resolve_attr_expr_metadata()` chain. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline attr-annotation preflight/resolve block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-call owner-type resolution now reuses `_ShExprParser._resolve_attr_expr_owner_state()`, so `_resolve_attr_callee()` no longer performs raw `resolved_type` extraction itself. `test_east_core.py` now fixes that the shared owner-state helper remains reused and that the old inline owner-type block stays absent.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-call source-span normalization plus callee resolve now also routes through `_ShExprParser._resolve_attr_call_annotation_state()`, so `_annotate_attr_call_expr()` no longer carries the local `payload.get("source_span")` plus `_resolve_attr_callee()` sequence. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline attr-call state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The payload-derived `source_span` normalization now also routes through `_ShExprParser._payload_source_span()`, so `_resolve_attr_call_annotation_state()` no longer carries the inline `payload.get("source_span")` dict guard itself. `test_east_core.py` now fixes the payload-helper definition, the call site, and the absence of the old inline source-span normalize block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The named-call annotation-state resolve now also routes through `_ShExprParser._resolve_named_call_annotation_state()`, so `_annotate_named_call_expr()` no longer carries the direct `_resolve_named_call_dispatch()` sequence. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline named-call state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The attr-call return lane now also routes owner-type inference through `_ShExprParser._owner_expr_resolved_type()`, so `_infer_attr_call_return_type()` no longer duplicates the `resolved_type/name_types` decode. `test_east_core.py` now fixes the shared owner-type helper call site and the absence of the old inline owner-type block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The attribute-access apply lane now also splits through `_ShExprParser._apply_runtime_attr_expr_annotation()` and `_apply_noncpp_attr_expr_annotation()`, so `_apply_attr_expr_annotation()` no longer carries the runtime/non-C++ inline branches itself. `test_east_core.py` now fixes both helper definitions, the `_apply_attr_expr_annotation()` call sites, and the absence of the old inline apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The runtime named-call apply state now also routes through `_ShExprParser._resolve_runtime_named_call_apply_state()`, so `_annotate_runtime_named_call_expr()` and `_apply_runtime_named_call_dispatch()` now only see `dispatch_kind + the active runtime_call/semantic_tag`. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old wide tuple unpack.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Subscript / Slice` node build now also routes through `_ShExprParser._build_slice_subscript_expr()` / `_build_index_subscript_expr()`, so `_annotate_subscript_expr()` no longer carries the inline slice/index build split itself. `test_east_core.py` now fixes both builder-helper definitions, the call sites, and the absence of the old inline `_sh_make_subscript_expr()` / `_sh_make_slice_node()` block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-call apply lane now also splits through `_ShExprParser._apply_noncpp_attr_call_expr_annotation()` and `_apply_runtime_method_call_expr_annotation()`, so `_apply_attr_call_expr_annotation()` no longer carries the direct helper calls itself. `test_east_core.py` now fixes both helper definitions, the `_apply_attr_call_expr_annotation()` call sites, and the absence of the old inline attr-call apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The named-call apply lane now also splits through `_ShExprParser._apply_builtin_named_call_annotation()` and `_apply_runtime_named_call_annotation()`, so `_apply_named_call_dispatch()` no longer carries the builtin/runtime payload fallback itself. `test_east_core.py` now fixes both helper definitions, the `_apply_named_call_dispatch()` call sites, and the absence of the old inline fallback block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The callee-call apply lane now also splits through `_ShExprParser._apply_named_callee_call_annotation()` and `_apply_attr_callee_call_annotation()`, so `_apply_callee_call_annotation()` no longer carries the direct named/attr apply branch itself. `test_east_core.py` now fixes both helper definitions, the `_apply_callee_call_annotation()` call sites, and the absence of the old inline callee-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The callee-call apply lane now also splits through `_ShExprParser._apply_named_callee_call_annotation()` and `_apply_attr_callee_call_annotation()`, so `_apply_callee_call_annotation()` no longer carries the direct named/attr helper calls itself. `test_east_core.py` now fixes both helper definitions, the `_apply_callee_call_annotation()` call sites, and the absence of the old inline callee-call apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtin named-call lane now also adds `_ShExprParser._resolve_builtin_named_call_annotation_state()`, so the `bool(...)` truthy-runtime probe and the `enumerate(...)` item-type inference move ahead of `_annotate_builtin_named_call_expr()` instead of staying inline there. `test_east_core.py` now fixes the state-helper definition, the apply call site, and the absence of the old inline `use_truthy_runtime` / `iter_element_type` calculations.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Name-callee return inference now also routes through `_ShExprParser._infer_named_call_return_type()`, so `_infer_call_expr_return_type()` no longer carries the direct `_sh_infer_known_name_call_return_type()` / `fn_return_types` / `class_method_return_types` / callable-fallback sequence. `test_east_core.py` now fixes the named-return helper definition, the call site, and the absence of the old inline named-return block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The callee-kind dispatch classification now also routes through `_ShExprParser._resolve_callee_call_annotation_kind()`, so `_apply_callee_call_annotation()` no longer carries the direct `fn_name != ""` plus `callee.get("kind") == "Attribute"` branch. `test_east_core.py` now fixes the resolve-helper definition, the call site, and the absence of the old inline callee-kind branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The callee-call annotation-state resolve now also routes through `_ShExprParser._resolve_callee_call_annotation_state()`, so `_annotate_callee_call_expr()` no longer carries the direct `_resolve_callee_call_annotation_kind()` sequence. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline callee-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Subscript / Slice` owner-type resolve now also routes through `_ShExprParser._resolve_subscript_expr_annotation_state()`, so `_annotate_subscript_expr()` no longer carries the direct `_owner_expr_resolved_type()` call itself. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline subscript owner-type block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Subscript / Slice` build-kind split now also routes through `_ShExprParser._resolve_subscript_expr_build_kind()`, so `_annotate_subscript_expr()` no longer carries the inline `index_expr/lower/upper` branch itself. `test_east_core.py` now fixes the build-kind helper definition, the call site, and the absence of the old inline slice/index branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Subscript / Slice` build dispatch now also routes through `_ShExprParser._apply_subscript_expr_build()`, so `_annotate_subscript_expr()` no longer carries the direct `_build_slice_subscript_expr()` / `_build_index_subscript_expr()` sequence itself. `test_east_core.py` now fixes the apply-helper definition, the call site, and the absence of the old inline build dispatch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The subscript-suffix component parse now also routes through `_ShExprParser._parse_subscript_suffix_components()`, so `_parse_subscript_suffix()` no longer carries the inline `:` / `first` branching itself. `test_east_core.py` now fixes the component-helper definition, the call site, and the absence of the old inline suffix-parse branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute`-suffix token/state resolve now also routes through `_ShExprParser._resolve_attr_suffix_state()`, so `_parse_attr_suffix()` no longer carries the direct `"." + NAME + span/repr` sequence itself. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline attr-suffix state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `call suffix` token/state resolve now also routes through `_ShExprParser._resolve_call_suffix_state()`, so `_parse_call_suffix()` no longer carries the direct `"(" + call-args + ")" + span/repr` sequence itself. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline call-suffix state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The subscript-suffix token/state resolve now also routes through `_ShExprParser._resolve_subscript_suffix_state()`, so `_parse_subscript_suffix()` no longer carries the direct `"[" + component-parse + "]" + span/repr` sequence itself. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline subscript-suffix state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Subscript / Slice` annotation-state resolve now also routes through `_ShExprParser._resolve_subscript_expr_apply_state()`, so `_annotate_subscript_expr()` no longer carries the direct `owner_t + build_kind` resolve itself. `test_east_core.py` now fixes the state-helper definition, the call site, and the absence of the old inline subscript-annotation state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The per-argument positional/keyword split now also routes through `_ShExprParser._parse_call_arg_entry()`, so `_parse_call_args()` no longer carries the inline `NAME` save/rewind plus keyword builder branch itself. `test_east_core.py` now fixes the entry-helper definition, the loop-side call site, and the absence of the old inline call-arg branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtins named-call apply path now also splits into `_ShExprParser._apply_*_builtin_named_call_annotation()` helpers, so `_apply_builtin_named_call_dispatch()` no longer carries direct `_sh_annotate_*` calls itself. `test_east_core.py` now fixes representative helper definitions, the dispatch call sites, and the absence of the old inline builtin-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Subscript / Slice` build-apply path now also splits into `_ShExprParser._apply_slice_subscript_expr_build()` and `_apply_index_subscript_expr_build()`, so `_apply_subscript_expr_build()` no longer carries direct `_build_*` calls itself. `test_east_core.py` now fixes both helper definitions, the dispatch call sites, and the absence of the old inline subscript-build apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `Attribute access` runtime-apply path now also splits into `_ShExprParser._apply_runtime_call_attr_expr_annotation()` and `_ShExprParser._apply_runtime_semantic_attr_expr_annotation()`, so `_apply_runtime_attr_expr_annotation()` no longer carries the inline runtime-call / semantic-tag branch itself. `test_east_core.py` now fixes both helper definitions, the orchestration call site, and the absence of the old inline attr-runtime-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The builtins named-call state now also splits into `_ShExprParser._resolve_builtin_named_call_truthy_state()` and `_ShExprParser._resolve_builtin_named_call_iter_element_type()`, so `_resolve_builtin_named_call_annotation_state()` no longer carries the inline `bool(...)` / `enumerate(...)` special cases itself. `test_east_core.py` now fixes both helper definitions, the state-helper call sites, and the absence of the old inline builtin-state branch.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The single call-argument NAME save/peek path now also splits into `_ShExprParser._resolve_call_arg_entry_state()` and `_apply_call_arg_entry_state()`, so `_parse_call_arg_entry()` no longer carries the inline `save/peek + keyword apply` sequence itself. `test_east_core.py` now fixes both helper definitions, the entry-helper call site, and the absence of the old inline call-arg state/apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The single call-argument apply path now also splits into `_ShExprParser._apply_keyword_call_arg_entry()` and `_apply_positional_call_arg_entry()`, so `_apply_call_arg_entry_state()` no longer carries the direct keyword/positional branch itself. `test_east_core.py` now fixes both helper definitions, the apply-helper call sites, and the absence of the old inline call-arg apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument loop append path now also routes through `_ShExprParser._apply_call_arg_entry()`, so `_parse_call_args()` no longer carries the direct positional/keyword append sequence itself. `test_east_core.py` now fixes the loop-apply helper definition, the call site, and the absence of the old inline call-arg append block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument loop comma/terminator path now also routes through `_ShExprParser._advance_call_arg_loop()`, so `_parse_call_args()` no longer carries the direct `,` / `)` consume sequence itself. `test_east_core.py` now fixes the loop-state helper definition, the call site, and the absence of the old inline call-arg terminator block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument empty-state apply now also routes through `_ShExprParser._apply_call_args_empty_state()`, so `_parse_call_args()` no longer carries the direct `return args, keywords` itself. `test_east_core.py` now fixes the apply-helper definition, the call-args helper call site, and the absence of the old inline empty-return block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-suffix token consume path now also routes through `_ShExprParser._consume_call_suffix_tokens()`, so `_resolve_call_suffix_state()` no longer carries the direct `"(" + call-args + ")"` sequence itself. `test_east_core.py` now fixes the token-helper definition, the state-helper call site, and the absence of the old inline call-suffix token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-suffix `(` open-token consume now also routes through `_ShExprParser._consume_call_suffix_open_token()`, so `_consume_call_suffix_tokens()` no longer carries the direct `self._eat("(")` itself. `test_east_core.py` now fixes the open-token helper definition, the token-helper call site, and the absence of the old inline call-suffix open-token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-suffix `)` close-token consume now also routes through `_ShExprParser._consume_call_suffix_close_token()`, so `_consume_call_suffix_tokens()` no longer carries the direct `self._eat(")")` itself. `test_east_core.py` now fixes the close-token helper definition, the token-helper call site, and the absence of the old inline call-suffix close-token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-suffix open-token state apply now also routes through `_ShExprParser._apply_call_suffix_open_token_state()`, so `_consume_call_suffix_tokens()` no longer carries the direct `args/keywords/rtok` assembly itself. `test_east_core.py` now fixes the apply-helper definition, the token-helper call site, and the absence of the old inline call-open-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` `.` + `NAME` consume path now also routes through `_ShExprParser._resolve_attr_suffix_name_token()`, so `_resolve_attr_suffix_state()` no longer carries the direct token consume sequence itself. `test_east_core.py` now fixes the token-helper definition, the state-helper call site, and the absence of the old inline attr-suffix token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` `.` token consume now also routes through `_ShExprParser._consume_attr_suffix_dot_token()`, so `_resolve_attr_suffix_name_token()` no longer carries the direct `self._eat(".")` call itself. `test_east_core.py` now fixes the dot-token helper definition, the name-token helper call site, and the absence of the old inline attr-suffix dot block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` apply path now also routes through `_ShExprParser._apply_attr_suffix_state()`, so `_parse_attr_suffix()` no longer carries the direct `_annotate_attr_expr(...)` call itself. `test_east_core.py` now fixes the apply-helper definition, the attr-suffix helper call site, and the absence of the old inline attr-suffix apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` postfix span/repr resolve now also routes through `_ShExprParser._resolve_attr_suffix_span_repr()`, so `_resolve_attr_suffix_state()` no longer carries the direct `_resolve_postfix_span_repr(...)` call itself. `test_east_core.py` now fixes the span-helper definition, the state-helper call site, and the absence of the old inline attr-suffix span block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` token-state resolve now also routes through `_ShExprParser._resolve_attr_suffix_token_state()`, so `_resolve_attr_suffix_state()` no longer carries the direct `_resolve_attr_suffix_name_token()` call itself. `test_east_core.py` now fixes the token-state helper definition, the state-helper call site, and the absence of the old inline attr-suffix token-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` token-state apply now also routes through `_ShExprParser._apply_attr_suffix_token_state()`, so `_resolve_attr_suffix_token_state()` no longer carries the direct `return name_tok` itself. `test_east_core.py` now fixes the apply-helper definition, the token-state helper call site, and the absence of the old inline attr-suffix token-state apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` `NAME` token consume now also routes through `_ShExprParser._consume_attr_suffix_name_token()`, so `_resolve_attr_suffix_name_token()` no longer carries the direct `self._eat("NAME")` call itself. `test_east_core.py` now fixes the consume-helper definition, the name-token helper call site, and the absence of the old inline attr-suffix name-consume block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` `[` + component-parse path now also routes through `_ShExprParser._consume_subscript_suffix_tokens()`, so `_resolve_subscript_suffix_state()` no longer carries the direct token consume sequence itself. `test_east_core.py` now fixes the token-helper definition, the state-helper call site, and the absence of the old inline subscript-suffix token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` token-state resolve now also routes through `_ShExprParser._resolve_subscript_suffix_token_state()`, so `_resolve_subscript_suffix_state()` no longer carries the direct `_consume_subscript_suffix_tokens()` call itself. `test_east_core.py` now fixes the token-state helper definition, the state-helper call site, and the absence of the old inline subscript-suffix token-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` component apply now also routes through `_ShExprParser._apply_subscript_suffix_component_state()`, so `_parse_subscript_suffix_components()` no longer carries the direct `if starts_with_slice` branch itself. `test_east_core.py` now fixes the apply-helper definition, the component-helper call site, and the absence of the old inline component-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` first-component kind resolve now also routes through `_ShExprParser._resolve_subscript_suffix_first_component_kind()`, so `_resolve_subscript_suffix_first_component_state()` no longer carries the direct `self._cur()["k"] == ":"` check itself. `test_east_core.py` now fixes the kind-helper definition, the state-helper call site, and the absence of the old inline first-component kind block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` first-component kind assembly now also routes through `_ShExprParser._apply_subscript_suffix_first_component_kind_state()`, so `_resolve_subscript_suffix_first_component_state()` no longer carries the direct `return first, ...` tuple assembly itself. `test_east_core.py` now fixes the apply-helper definition, the state-helper call site, and the absence of the old inline first-component kind-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` `[` open-token consume now also routes through `_ShExprParser._consume_subscript_suffix_open_token()`, so `_consume_subscript_suffix_tokens()` no longer carries the direct `self._eat("[")` call itself. `test_east_core.py` now fixes the open-token helper definition, the token-helper call site, and the absence of the old inline open-token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript slice tail` upper-expr state now also routes through `_ShExprParser._resolve_subscript_slice_upper_expr_state()`, so `_parse_subscript_slice_upper_expr()` no longer carries the direct `"]"` probe itself. `test_east_core.py` now fixes the state-helper definition, the upper-expr helper call site, and the absence of the old inline upper-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The follow-up slice moved `subscript slice tail` upper-expr kind resolution into `_ShExprParser._resolve_subscript_slice_upper_expr_kind()`, so `_resolve_subscript_slice_upper_expr_state()` no longer carries the direct `self._cur()["k"] == "]"` probe itself. `test_east_core.py` now fixes the kind-helper definition, the state-helper call site, and the absence of the old inline upper-kind block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The follow-up slice moved `attr suffix` attribute-name value resolution into `_ShExprParser._resolve_attr_suffix_name_value()`, so `_resolve_attr_suffix_state()` no longer carries the direct `str(name_tok["v"])` unpack itself. `test_east_core.py` now fixes the name-value helper definition, the state-helper call site, and the absence of the old inline attr-name unpack block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript slice tail` upper-expr apply now also routes through `_ShExprParser._apply_subscript_slice_upper_expr_state()`, so `_parse_subscript_slice_upper_expr()` no longer carries the direct `None` / `_parse_ifexp()` branch itself. `test_east_core.py` now fixes the apply-helper definition, the upper-expr helper call site, and the absence of the old inline upper-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript slice tail` `:` token consume now also routes through `_ShExprParser._consume_subscript_slice_tail_colon_token()`, so `_consume_subscript_slice_tail_tokens()` no longer carries the direct `self._eat(":")` itself. `test_east_core.py` now fixes the colon-token helper definition, the tail-token helper call site, and the absence of the old inline colon-token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript slice tail` `]` token consume now also routes through `_ShExprParser._consume_subscript_slice_tail_close_token()`, so `_consume_subscript_slice_tail_tokens()` no longer carries the direct `self._eat("]")` itself. `test_east_core.py` now fixes the close-token helper definition, the tail-token helper call site, and the absence of the old inline close-token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` name token/value assembly now also routes through `_ShExprParser._apply_attr_suffix_name_state()`, so `_resolve_attr_suffix_name_state()` no longer carries the direct `return name_tok, ...` tuple assembly itself. `test_east_core.py` now fixes the apply-helper definition, the name-state helper call site, and the absence of the old inline attr-name-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` token-state apply now also routes through `_ShExprParser._apply_subscript_suffix_token_state()`, so `_resolve_subscript_suffix_state()` no longer carries the direct span/repr assembly itself. `test_east_core.py` now fixes the apply-helper definition, the state-helper call site, and the absence of the old inline subscript-token-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `call suffix` token-state apply now also routes through `_ShExprParser._apply_call_suffix_token_state()`, so `_resolve_call_suffix_state()` no longer carries the direct span/repr assembly itself. `test_east_core.py` now fixes the apply-helper definition, the state-helper call site, and the absence of the old inline call-token-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `attr suffix` name token-state apply now also routes through `_ShExprParser._apply_attr_suffix_name_token_state()`, so `_resolve_attr_suffix_state()` no longer carries the direct span/repr assembly itself. `test_east_core.py` now fixes the apply-helper definition, the state-helper call site, and the absence of the old inline attr-token-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` slice-tail parse now also routes through `_ShExprParser._parse_subscript_slice_tail()`, so `_parse_subscript_suffix_components()` no longer carries the duplicated `":" -> upper -> "]"` branch itself. `test_east_core.py` now fixes the tail-helper definition, the component-helper call sites, and the absence of the old inline slice-tail block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript slice tail` upper-expr parse now also routes through `_ShExprParser._parse_subscript_slice_upper_expr()`, so `_parse_subscript_slice_tail()` no longer carries the direct `"]"` probe plus upper-expr parse sequence itself. `test_east_core.py` now fixes the upper-expr helper definition, the slice-tail helper call site, and the absence of the old inline upper-parse block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript slice tail` token consume path now also routes through `_ShExprParser._consume_subscript_slice_tail_tokens()`, so `_parse_subscript_slice_tail()` no longer carries the direct `":" + upper + "]"` token sequence itself. `test_east_core.py` now fixes the tail-token helper definition, the slice-tail helper call site, and the absence of the old inline tail-token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript slice tail` token/state resolve now also routes through `_ShExprParser._resolve_subscript_slice_tail_state()`, so `_parse_subscript_slice_tail()` no longer carries the direct tail-token helper call itself. `test_east_core.py` now fixes the state-helper definition, the slice-tail helper call site, and the absence of the old inline slice-tail state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` first-expr branch now also routes through `_ShExprParser._parse_subscript_suffix_first_component()`, so `_parse_subscript_suffix_components()` no longer carries the inline `first = _parse_ifexp()` branch itself. `test_east_core.py` now fixes the first-component helper definition, the component-helper call site, and the absence of the old inline first-component block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` first-expr state now also routes through `_ShExprParser._resolve_subscript_suffix_first_component_state()`, so `_parse_subscript_suffix_first_component()` no longer carries the direct `first = _parse_ifexp()` plus `":"` probe sequence itself. `test_east_core.py` now fixes the state-helper definition, the first-component helper call site, and the absence of the old inline first-component state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` first-expr apply path now also routes through `_ShExprParser._apply_subscript_suffix_first_component_state()`, so `_parse_subscript_suffix_first_component()` no longer carries the direct `is_slice` branch plus `"]"` consume sequence itself. `test_east_core.py` now fixes the apply-helper definition, the first-component helper call site, and the absence of the old inline first-component apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The pure-index `subscript suffix` close path now also routes through `_ShExprParser._parse_subscript_index_tail()`, so `_apply_subscript_suffix_first_component_state()` no longer carries the inline `first + ]` close branch itself. `test_east_core.py` now fixes the index-tail helper definition, the first-component apply-helper call site, and the absence of the old inline close block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The pure-index `subscript suffix` `]` close-token consume now also routes through `_ShExprParser._consume_subscript_index_tail_close_token()`, so `_parse_subscript_index_tail()` no longer carries the direct `self._eat("]")` itself. `test_east_core.py` now fixes the close-token helper definition, the index-tail helper call site, and the absence of the old inline index-tail close block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The pure-index `subscript suffix` close-token state now also routes through `_ShExprParser._resolve_subscript_index_tail_state()`, so `_parse_subscript_index_tail()` no longer carries the direct close-token helper call itself. `test_east_core.py` now fixes the state-helper definition, the index-tail helper call site, and the absence of the old inline index-tail state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `call suffix` token-state resolve now also routes through `_ShExprParser._resolve_call_suffix_token_state()`, so `_resolve_call_suffix_state()` no longer carries the direct `_consume_call_suffix_tokens()` call itself. `test_east_core.py` now fixes the state-helper definition, the call-suffix state-helper call site, and the absence of the old inline call-suffix token-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `call suffix` apply path now also routes through `_ShExprParser._apply_call_suffix_state()`, so `_parse_call_suffix()` no longer carries the direct `_annotate_call_expr(...)` call itself. `test_east_core.py` now fixes the apply-helper definition, the call-suffix helper call site, and the absence of the old inline call-suffix apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `call suffix` postfix span/repr resolve now also routes through `_ShExprParser._resolve_call_suffix_span_repr()`, so `_resolve_call_suffix_state()` no longer carries the direct `_resolve_postfix_span_repr()` call itself. `test_east_core.py` now fixes the span-helper definition, the state-helper call site, and the absence of the old inline call-suffix span block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` apply path now also routes through `_ShExprParser._apply_subscript_suffix_state()`, so `_parse_subscript_suffix()` no longer carries the direct `_annotate_subscript_expr(...)` call itself. `test_east_core.py` now fixes the apply-helper definition, the suffix-helper call site, and the absence of the old inline suffix-apply block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` component-head state now also routes through `_ShExprParser._resolve_subscript_suffix_component_state()`, so `_parse_subscript_suffix_components()` no longer carries the direct `":"` probe itself. `test_east_core.py` now fixes the component-state helper definition, the component-helper call site, and the absence of the old inline component-head probe.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` first-expr apply branches now also split into `_ShExprParser._apply_subscript_slice_first_component()` / `_ShExprParser._apply_subscript_index_first_component()`, so `_apply_subscript_suffix_first_component_state()` no longer carries the direct tail-helper calls itself. `test_east_core.py` now fixes both apply-helper definitions, the state-helper call site, and the absence of the old inline apply branches.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` postfix span/repr resolve now also routes through `_ShExprParser._resolve_subscript_suffix_span_repr()`, so `_resolve_subscript_suffix_state()` no longer carries the direct `_resolve_postfix_span_repr()` call itself. `test_east_core.py` now fixes the span-helper definition, the state-helper call site, and the absence of the old inline span-resolve block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `subscript suffix` `[` open-token consume now also routes through `_ShExprParser._consume_subscript_suffix_open_token()`, so `_consume_subscript_suffix_tokens()` no longer carries the direct `self._eat("[")` itself. `test_east_core.py` now fixes the open-token helper definition, the token-helper call site, and the absence of the old inline open-token block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `=` consume for call-argument keywords now also routes through `_ShExprParser._consume_keyword_call_arg_equals_token()`, so `_apply_keyword_call_arg_entry()` no longer carries the direct `self._eat("=")` itself. `test_east_core.py` now fixes the token-helper definition, the keyword-apply helper call site, and the absence of the old inline keyword-equals block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The empty `)` probe for call-argument lists now also routes through `_ShExprParser._resolve_call_args_empty_state()`, so `_parse_call_args()` no longer carries the direct `if self._cur()["k"] == ")"` itself. `test_east_core.py` now fixes the empty-state helper definition, the call-args helper call site, and the absence of the old inline empty-args block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `NAME` start probe for call-argument entries now also routes through `_ShExprParser._resolve_call_arg_entry_has_name()`, so `_resolve_call_arg_entry_state()` no longer carries the direct `if self._cur()["k"] != "NAME"` branch itself. `test_east_core.py` now fixes the has-name helper definition, the state-helper call site, and the absence of the old inline name-start block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The keyword-kind probe for call-argument entries now also routes through `_ShExprParser._resolve_call_arg_entry_kind()`, so `_resolve_call_arg_entry_state()` no longer carries the direct `self._cur()["k"] == "="` itself. `test_east_core.py` now fixes the kind-helper definition, the state-helper call site, and the absence of the old inline keyword-kind block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `,` consume for the call-argument loop now also routes through `_ShExprParser._consume_call_arg_loop_comma_token()`, so `_apply_call_arg_loop_state()` no longer carries the direct `self._eat(",")` itself. `test_east_core.py` now fixes the comma-token helper definition, the loop-apply helper call site, and the absence of the old inline comma-consume block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument loop continue probe now also routes through `_ShExprParser._resolve_call_arg_loop_continue_kind()`, so `_apply_call_arg_loop_state()` no longer carries the direct `self._cur()["k"] != ")"` itself. `test_east_core.py` now fixes the continue-kind helper definition, the loop-apply helper call site, and the absence of the old inline terminator-probe block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `NAME` consume for call-argument entries now also routes through `_ShExprParser._consume_call_arg_entry_name_token()`, so `_resolve_call_arg_entry_state()` no longer carries the direct `self._eat("NAME")` itself. `test_east_core.py` now fixes the name-token helper definition, the state-helper call site, and the absence of the old inline name-consume block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `NAME` value extraction for call-argument entries now also routes through `_ShExprParser._resolve_call_arg_entry_name_value()`, so `_apply_keyword_call_arg_entry()` no longer carries the direct `str(name_tok["v"])` unpack itself. `test_east_core.py` now fixes the name-value helper definition, the keyword-apply helper call site, and the absence of the old inline name-value unpack block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The call-argument keyword name/value state now also routes through `_ShExprParser._resolve_keyword_call_arg_entry_state()` / `_ShExprParser._apply_keyword_call_arg_entry_state()`, so `_apply_keyword_call_arg_entry()` no longer carries the direct `=` consume plus `kw_name/kw_val` assembly itself. `test_east_core.py` now fixes both helper definitions, the keyword-apply helper call site, and the absence of the old inline keyword-state block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The keyword-kind probe for call-argument entries now also routes through `_ShExprParser._resolve_call_arg_entry_is_keyword()`, so `_resolve_call_arg_entry_kind()` no longer carries the direct `self._cur()["k"] == "="` itself. `test_east_core.py` now fixes the keyword-kind helper definition, the kind-helper call site, and the absence of the old inline keyword-kind probe block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `(` tuple branch in `_parse_comp_target()` now also routes through `_ShExprParser._parse_tuple_comp_target()`, so the target-cluster tuple construction is contained inside a parser helper. `test_east_core.py` now fixes the helper definition, the `_parse_comp_target()` call site, and the absence of the old inline tuple-target block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The non-empty call-argument loop now also routes through `_ShExprParser._consume_call_arg_entries()`, so `_parse_call_args()` no longer carries the direct `while True` plus `arg_entry/keyword_entry` sequence itself. `test_east_core.py` now fixes the helper definition, the call-args helper call site, and the absence of the old inline non-empty loop block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The `save_pos` restore for positional call arguments now also routes through `_ShExprParser._apply_call_arg_entry_save_pos()`, so `_apply_positional_call_arg_entry()` no longer carries the direct `self.pos = save_pos` itself. `test_east_core.py` now fixes the helper definition, the positional-apply helper call site, and the absence of the old inline save-pos restore block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The node build for keyword call arguments now also routes through `_ShExprParser._apply_keyword_call_arg_build()`, so `_apply_keyword_call_arg_entry()` no longer carries the direct `_sh_make_keyword_arg(...)` call itself. `test_east_core.py` now fixes the helper definition, the keyword-apply helper call site, and the absence of the old inline keyword-build block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The single-iteration call-argument loop path now also routes through `_ShExprParser._consume_call_arg_loop_entry()`, so `_consume_call_arg_entries()` no longer carries the direct `arg_entry/keyword_entry` handling plus `_advance_call_arg_loop()` call itself. `test_east_core.py` now fixes the loop-entry helper definition, the non-empty loop call site, and the absence of the old inline loop-entry block.
- Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The positional/keyword append in the call-argument loop now also routes through `_ShExprParser._apply_keyword_call_arg_loop_entry()` / `_ShExprParser._apply_positional_call_arg_loop_entry()`, so `_apply_call_arg_entry()` no longer carries the direct `keywords.append(...)` / `args.append(...)` calls itself. `test_east_core.py` now fixes both helper definitions, the loop-apply helper call site, and the absence of the old inline append block.

### P3: Harden compiler contracts and make stage / pass / backend handoffs fail closed

Context: [docs/ja/plans/p3-compiler-contract-hardening.md](../plans/p3-compiler-contract-hardening.md)

1. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01] Harden compiler contracts and make stage / pass / backend handoffs fail closed.
2. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S1-01] Inventory the current `check_east_stage_boundary`, `validate_raw_east3_doc`, and backend-entry guards, then classify blind spots that still go unchecked (`node shape`, `type_expr` / `resolved_type`, `source_span`, helper metadata).
3. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S1-02] Fix the responsibility boundary between schema validators, invariant validators, and backend-input validators so this plan does not overlap with `P1-EAST-TYPEEXPR-01` or `P2-COMPILER-TYPED-BOUNDARY-01`.
4. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S2-01] Extend `spec-dev` or equivalent design docs with required fields, allowed omissions, and diagnostic categories for EAST3 / linked output / backend input.
5. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S2-02] Fix the consistency rules and fail-closed policy for `type_expr` / `resolved_type` mirrors, `dispatch_mode`, `source_span`, and helper metadata.
6. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S3-01] Add central validator primitives around `toolchain/link/program_validator.py` and expand raw EAST3 / linked-output checks from coarse validation into node/meta invariant checks.
7. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S3-02] Add pre/post validation hooks to representative passes, lowering entrypoints, and linker entrypoints so malformed nodes stop propagating.
8. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S4-01] Run compiler-contract validators at representative backend entrypoints (first C++) and replace backend-local crashes or silent fallback with structured diagnostics.
9. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S4-02] Extend `tools/check_east_stage_boundary.py` or its successor guard so it can detect stage semantic-contract drift too.
10. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S5-01] Add representative unit/selfhost regressions so contract violations are reproducible as expected failures.
11. [ ] [ID: P3-COMPILER-CONTRACT-HARDENING-01-S5-02] Refresh docs / TODO / archive / migration notes and fix the rule that validator updates are mandatory when new nodes/meta are introduced.

### P4: Canonicalize backend-registry metadata and strengthen selfhost parity gates

Context: [docs/ja/plans/p4-backend-registry-selfhost-parity-hardening.md](../plans/p4-backend-registry-selfhost-parity-hardening.md)

1. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01] Canonicalize backend-registry metadata and strengthen selfhost parity gates.
2. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S1-01] Inventory duplicated surfaces across `backend_registry.py` and `backend_registry_static.py` (backend spec, runtime copy, writer rules, option schema, direct-route behavior), then classify each difference as intentional or drift-prone.
3. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S1-02] Inventory current gates and blind spots across `build_selfhost`, stage2, direct e2e verification, and multilang selfhost tools, then fix the known-block vs regression classification policy in the decision log.
4. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S2-01] Define the canonical source of truth for backend capability, runtime-copy rules, option schema, and writer metadata so both host and static registries can be derived from it.
5. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S2-02] Fix the boundaries where intentional differences are allowed (for example host-only lazy imports or selfhost-only direct routes) together with their diagnostic contracts.
6. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S3-01] Move host/static registries toward shared metadata, a generator, or equivalent adapters and retire avoidable handwritten duplication.
7. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S3-02] Add a registry-drift guard or diff test so one-sided backend-surface updates fail fast.
8. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S4-01] Reorganize representative stage1 / stage2 / direct e2e / multilang selfhost parity suites so they report a stable shared summary and failure taxonomy.
9. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S4-02] Align unsupported / preview / known-block / regression diagnostics between registry code and parity reports so expected failures are explicitly managed.
10. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S5-01] Refresh docs / plan reports / archive so backend readiness, known blocks, and gate execution flow remain traceable.
11. [ ] [ID: P4-BACKEND-REGISTRY-SELFHOST-PARITY-01-S5-02] Verify that representative internal changes are checked through equivalent contracts on both host and selfhost lanes, then fix reintroduction guards.

### P5: Full rollout of nominal ADTs as a language feature

Context: [docs/ja/plans/p5-nominal-adt-language-rollout.md](../plans/p5-nominal-adt-language-rollout.md)

1. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01] Carry out the full rollout of nominal ADTs as a language feature.
2. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S1-01] Inventory candidate language surfaces for nominal ADT declarations, constructors, variant access, and `match`, then decide on a selfhost-safe staged introduction path.
3. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S1-02] Fix the boundary between type-system base work, narrowing-base work, and full language-feature work so this plan does not overlap with `P1-EAST-TYPEEXPR-01`.
4. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S2-01] Extend `spec-east` / `spec-user` / `spec-dev` with nominal-ADT declaration surface, pattern nodes, match nodes, and diagnostic contracts.
5. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S2-02] Fix the static-check policy and error categories for exhaustiveness, duplicate patterns, and unreachable branches.
6. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S3-01] Update frontend and selfhost parser paths so they can accept representative nominal-ADT syntax.
7. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S3-02] Introduce ADT constructors, variant tests, variant projection, and `match` lowering into EAST/EAST3.
8. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S4-01] Verify through representative tests that built-in `JsonValue` and user-defined nominal ADTs use the same IR category.
9. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S4-02] Implement the minimal constructor / variant-check / destructuring / `match` path in a representative backend (first C++) and forbid silent fallback.
10. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S5-01] Organize rollout order and fail-closed policy for other backends, and fix diagnostics for unsupported targets.
11. [ ] [ID: P5-NOMINAL-ADT-ROLLOUT-01-S5-02] Refresh selfhost / docs / archive / migration notes and close the full nominal-ADT rollout plan.

Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The positional call-argument value/build path now also routes through `_ShExprParser._resolve_positional_call_arg_entry_state()` / `_ShExprParser._apply_positional_call_arg_build_state()`, so `_apply_positional_call_arg_entry()` no longer carries the direct `_parse_call_arg_expr()` call itself. `test/unit/ir/test_east_core.py` now fixes both helper definitions, the positional-apply helper call site, and the absence of the old inline positional-value block.
Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The positional append in the call-argument loop now also routes through `_ShExprParser._apply_positional_call_arg_loop_entry_build()`, so `_apply_positional_call_arg_loop_entry()` no longer carries the direct `args.append(arg_entry)` itself. `test/unit/ir/test_east_core.py` now fixes the helper definition, the positional-loop helper call site, and the absence of the old inline positional-append block.
Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The attribute-suffix `attr_name + source_span + repr_text` merge now also routes through `_ShExprParser._apply_attr_suffix_span_repr_state()`, so `_apply_attr_suffix_name_token_state()` no longer carries the direct `return attr_name, source_span, repr_text` itself. `test/unit/ir/test_east_core.py` now fixes the span/repr apply helper definition, the name-token-state helper call site, and the absence of the old inline attr-suffix span/repr block.
Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The keyword append in the call-argument loop now also routes through `_ShExprParser._apply_keyword_call_arg_loop_entry_build()`, so `_apply_keyword_call_arg_loop_entry()` no longer carries the direct `keywords.append(keyword_entry)` itself. `test/unit/ir/test_east_core.py` now fixes the helper definition, the keyword-loop helper call site, and the absence of the old inline keyword-append block.
Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The dispatch kind for call-argument loop entries now also routes through `_ShExprParser._resolve_call_arg_loop_entry_kind()` / `_apply_call_arg_loop_entry_kind()`, so `_apply_call_arg_entry()` no longer carries the direct `if keyword_entry is not None` branch itself. `test_east_core.py` now fixes both helper definitions, the loop-apply helper call site, and the absence of the old inline loop-dispatch block.
Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The positional append state in the call-argument loop now also routes through `_ShExprParser._resolve_positional_call_arg_loop_entry_state()` / `_ShExprParser._apply_positional_call_arg_loop_entry_state()`, so `_apply_positional_call_arg_loop_entry()` no longer carries the direct `if arg_entry is not None` branch itself. `test/unit/ir/test_east_core.py` now fixes both helper definitions, the positional-loop helper call site, and the absence of the old inline positional-loop state block.
Progress memo: [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] The keyword apply state for call-argument loop entries now also routes through `_ShExprParser._resolve_call_arg_loop_entry_apply_state()` / `_ShExprParser._apply_call_arg_loop_entry_apply_state()`, so `_apply_call_arg_loop_entry_kind()` no longer carries the direct `if is_keyword_entry and keyword_entry is not None` branch itself. `test/unit/ir/test_east_core.py` now fixes both helper definitions, the kind-apply helper call site, and the absence of the old inline loop-entry apply block.

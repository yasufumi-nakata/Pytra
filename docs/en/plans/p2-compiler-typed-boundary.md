# P2: Typed Compiler Boundaries and Retreat of Internal Object Carriers

Last updated: 2026-03-10

Related TODO:
- `ID: P2-COMPILER-TYPED-BOUNDARY-01` in `docs/ja/todo/index.md`

Background:
- Pytra mainly targets typed Python, but compiler/selfhost internal boundaries still widely rely on `dict[str, object]`, `list[object]`, and `make_object(...)`.
- In the current selfhost stage1 path, `transpile_cli`, `backend_registry_static`, and generated selfhost parser artifacts still move compiler documents, backend specs, option payloads, and AST nodes through generic object carriers.
- That was useful as bootstrap scaffolding, but it no longer matches the typed-Python implementation philosophy and blocks retreating `make_object` out of compiler-internal lanes.
- Before removing `make_object` more aggressively, the compiler boundaries themselves must first move to typed carriers; otherwise the selfhost/compiler path breaks wholesale.

Goal:
- Move compiler/selfhost internal boundaries to nominal typed carriers and push `dict[str, object]`, `list[object]`, and `make_object(...)` back into backend/runtime implementation details.
- Restrict `make_object`, `py_to`, and `obj_to_*` to user-facing `Any/object` boundaries or explicit adapter seams, and stop using them for known-schema compiler internals.
- Make the "typed Python is the source of truth" policy consistent inside selfhost/compiler implementation boundaries too.

Scope:
- `src/toolchain/frontends/transpile_cli.py` and its selfhost-expanded artifacts
- `src/runtime/cpp/native/compiler/{transpile_cli,backend_registry_static}.{h,cpp}`
- `src/runtime/cpp/generated/compiler/*` and `selfhost/runtime/cpp/pytra-gen/compiler/*`
- Selfhost parser / EAST builder paths around `src/toolchain/ir/core.py`
- Docs / guards / regression tests for compiler boundaries

Out of scope:
- Removing user-facing `Any/object` functionality itself
- Deleting the `make_object` overload family from `py_runtime.h` in one shot
- Fully removing the stage1 selfhost host-Python bridge in this plan alone
- Redesigning the entire C++ runtime

## Mandatory Rules

These are requirements, not recommendations.

1. Any compiler-internal payload with a known schema must use a nominal typed carrier (class / dataclass / typed record), not `dict[str, object]`.
2. `dict[str, object]` and `list[object]` are allowed only at explicit seams such as JSON decode, extern/hooks, and legacy compatibility adapters. They must not flow through internal logic by default.
3. The selfhost parser / EAST builder must not keep raw `dict<str, object>{{...}}` assembly as the canonical path. Typed node constructors or typed builder helpers must become the source of truth.
4. Dynamic JSON values used inside the compiler must be isolated behind a dedicated nominal type such as `JsonValue`, not by expanding generic object helpers.
5. Compiler-side `make_object`, `py_to`, and `obj_to_*` usage may remain only when it can be classified as `user_boundary`, `json_adapter`, or `legacy_migration_adapter`. Unclassified usage must not remain as hidden debt.
6. Do not add new generic carriers during the migration. If a legacy adapter remains temporarily, its removal step must be recorded in the plan / decision log.
7. Backends/runtimes must not paper over missing typed-boundary work by adding more object fallback helpers. Required type information must be fixed upstream in frontend/lowering/builder lanes.

Acceptance criteria:
- Canonical compiler entrypoints such as `load_east3_document` use a typed root carrier as the source of truth rather than raw `dict[str, object]`.
- `backend_registry_static` passes backend specs, layer options, and IR through typed carriers plus explicit adapters rather than default raw object dict transport.
- In the selfhost parser / generated compiler path, checked-in AST nodes are no longer directly assembled through `dict<str, object>{{... make_object(...) ...}}` paths.
- Remaining compiler-lane `make_object` / `py_to` usage is explicitly classified and limited to user-facing `Any/object` boundaries or adapter seams.
- Guards/tests exist so typed-boundary regressions fail fast.

Planned verification commands:
- `python3 tools/check_todo_priority.py`
- `python3 -m unittest discover -s test/unit/selfhost -p 'test_prepare_selfhost_source.py'`
- `python3 -m unittest discover -s test/unit/selfhost -p 'test_selfhost_virtual_dispatch_regression.py'`
- `python3 -m unittest discover -s test/unit/backends/cpp -p 'test_east3_cpp_bridge.py'`
- `python3 tools/build_selfhost.py`
- `python3 tools/check_selfhost_cpp_diff.py --mode allow-not-implemented`
- `git diff --check`

## Implementation Order

Keep the order fixed: decide the typed contract first, add adapters second, then peel raw object assembly out of selfhost-generated artifacts.

1. Inventory and classification
2. Lock the typed end state
3. Introduce typed carriers in the Python source of truth
4. Mirror them into generated/native compiler interfaces
5. Retire raw object assembly from selfhost parser / EAST builder
6. Isolate JSON / hook / legacy adapters
7. Add guards / regressions / archive updates

## Breakdown

- [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01] Inventory remaining `dict[str, object]`, `list[object]`, `make_object`, and `py_to` usage across `transpile_cli`, `backend_registry_static`, selfhost parser paths, and generated compiler runtime, then classify each usage as `compiler_internal`, `json_adapter`, `extern_hook`, or `legacy_bridge`.
- [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02] Lock the typed-boundary contract and non-goals in the decision log so they stay consistent with `spec-dev`, `spec-runtime`, and `spec-boxing`.
- [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-01] Define typed carrier specs for compiler root payloads (EAST document, backend spec, layer options, emit request/result).
- [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-02] Introduce typed carriers and thin legacy adapters in the Python source of truth (`transpile_cli.py`, registry helpers, builder helpers).
- [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-03] Introduce typed carrier mirrors or typed wrapper APIs in the C++ selfhost/native compiler interfaces and reduce raw `dict<str, object>` exchange.
- [x] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01] Move selfhost parser / EAST builder node construction onto typed constructors / builder helpers and gradually retire direct `dict<str, object>{{...}}` assembly.
- [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02] Retreat remaining `make_object` usage in generated compiler / selfhost runtime down to serialization/export seams only.
- [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-01] Separate JSON, extern/hooks, and other intentionally dynamic carriers from the compiler typed model behind `JsonValue` or explicit adapters.
- [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S4-02] Label every remaining `make_object` / `py_to` / `obj_to_*` usage and add guards that reject uncategorized reintroduction.
- [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-01] Refresh selfhost build/diff/prepare/bridge regressions and lock non-regression after the typed-boundary changes.
- [ ] [ID: P2-COMPILER-TYPED-BOUNDARY-01-S5-02] Update docs / TODO / archive and record whether each remaining `make_object` usage is `user boundary only` or `explicit adapter only`.

## Expected Deliverables

### Deliverables for S1

- A concrete inventory of which files/usages still keep forbidden generic carriers inside compiler internals.
- A written explanation of why this P2 does not mean "delete `make_object` everywhere" and what counts as completion.

### Deliverables for S2

- `transpile_cli` and `backend_registry_static` treat typed payloads as the canonical path.
- Legacy `dict[str, object]` APIs remain only as thin adapters so callers can move gradually.

### Deliverables for S3

- The selfhost parser / EAST builder uses nominal node builders.
- Checked-in compiler paths stop spelling out repeated `make_object("kind")` / `make_object(value)` AST assembly.

### Deliverables for S4

- Only genuinely dynamic paths such as `JsonValue` or extern/hook adapters keep object carriers.
- Remaining compiler-internal generic carriers are all justified and classifiable.

### Deliverables for S5

- Selfhost regressions and audits can detect any collapse back to generic compiler carriers.
- The end state is traceable in docs/TODO/archive.

Decision log:
- 2026-03-09: Added this P2 in response to the user request to prioritize typed compiler boundaries over trying to delete `make_object` directly.
- 2026-03-09: Fixed the policy that user-facing `Any/object` boundaries remain part of the current language/runtime contract; this P2 focuses on compiler/selfhost internal dynamic-carrier cleanup instead.
- 2026-03-09: Fixed the policy that removing the stage1 selfhost host-Python bridge is out of scope here and should be tackled only after typed carriers exist.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01]: Classified `json_adapter` first and kept it limited to explicit JSON root decode/encode seams. Representative sites are the `.json` input lane in `src/toolchain/frontends/transpile_cli.py::load_east_document()` where `json.loads_obj(...).raw` is normalized into the EAST root, `src/runtime/cpp/native/compiler/transpile_cli.cpp::_load_json_root_dict()`, and `src/runtime/cpp/native/compiler/backend_registry_static.cpp::emit_source()` where `ir` is serialized via `json.dumps(make_object(ir))` before calling host Python. These lanes may remain only as the last adapter seam after typed carriers exist.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01]: Fixed `legacy_bridge` to mean "raw object-dict APIs that stay public even though the payload can be typed." Representative sites are `src/toolchain/frontends/transpile_cli.py::load_east_document()` wrapping `east_any` into `{\"east\": east_any}` and forcing it back through `dict_any_get_dict(...)`, the public `load_east3_document(...) -> dict<str, object>` surface in `src/runtime/cpp/native/compiler/transpile_cli.h/.cpp`, and the mirrored `dict_any_get*` helpers in `selfhost/py2cpp.py` that `tools/prepare_selfhost_source.py` exports into the selfhost seed. P2 should push this category into thin adapters and remove it from canonical internal flow.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01]: Fixed `compiler_internal` to cover payloads whose schema is already known but still travel through generic carriers. Representative sites are the `dict[str, dict[str, object]]` signature map returned by `src/toolchain/frontends/transpile_cli.py::extract_function_signatures_from_python_source()`, `get_backend_spec()` / `resolve_layer_options()` / `lower_ir()` / `optimize_ir()` in `src/runtime/cpp/native/compiler/backend_registry_static.cpp`, the source-of-truth node/module builders in `src/toolchain/ir/core.py`, and the selfhost generated parser paths in `selfhost/runtime/cpp/pytra-gen/compiler/east_parts/core.cpp` where `_sh_append_fstring_literal()`, `_sh_parse_def_sig()`, and the module root still assemble AST/meta via `dict<str, object>{{... make_object(...) ...}}`. S2-S3 should replace this category with typed carriers and typed builders.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-01]: Fixed `extern_hook` to mean the explicit dynamic seams used to strip or route `@extern`-driven payloads. Representative sites are `_is_extern_call_expr()`, `_is_extern_function_decl()`, `_is_extern_variable_decl()`, and `_build_cpp_emit_module_without_extern_decls()` in `selfhost/py2cpp.py`, plus `apply_runtime_hook(...)` in `src/runtime/cpp/native/compiler/backend_registry_static.cpp`. These are not canonical typed compiler payload flow and should stay isolated with the other dynamic adapters in S4.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02]: Aligned with `spec-boxing` and fixed the non-goal that P2 does not remove the `Any/object` boundary itself. `make_object(...)`, `obj_to_*`, and `py_to_*` remain part of the user-facing `Any/object` contract; P2 only retreats the compiler/selfhost lanes where the schema is already known but still routed through raw object carriers.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02]: Aligned with `spec-runtime` / `spec-dev` and fixed the rule that JSON dynamics must not justify broader generic-object helpers. Raw `dict[str, object]` / `list[object]` may remain only at explicit JSON root decode/encode seams; the long-term canonical lane is `JsonValue` / `JsonObj` / `JsonArr`. Typed-boundary gaps must not be rescued by adding helper fallbacks such as `sum(object)` or `zip(object, object)`.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02]: Following `spec-dev`, typed-boundary meaning stays frontend/lowering-owned and must not be reinterpreted by backend/hook/native runtime from raw dicts or callee names. `type_expr` remains the source of truth for type meaning, `resolved_type` remains a mirror, and `meta.dispatch_mode` stays a once-fixed compile input. During the migration, backend/runtime silent fallback or re-deciding these semantics is forbidden.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-01]: Split the compiler-root carrier spec into three layers. `CompilerRootMeta` owns `source_path: str`, `east_stage: int`, `schema_version: int`, `dispatch_mode: str`, and `parser_backend: str`. `CompilerRootDocument` owns `meta: CompilerRootMeta`, `module_kind: Literal["Module"]`, and `raw_module_doc` as a nominal wrapper; `raw_module_doc` is explicitly a migration field that may survive only until S3-01. `load_east_document()` / `load_east3_document()` should converge on returning this carrier, while raw dict return surfaces are demoted to `as_legacy_dict()`-style adapters.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-01]: Fixed backend-registry carriers to metadata-only source-of-truth that does not transport callables. `BackendSpecCarrier` must contain `target_lang`, `extension`, `default_options_by_layer`, `option_schema_by_layer`, `emit_strategy`, `lower_strategy`, `optimizer_strategy`, `runtime_hook_key`, and `program_writer_key`. `LayerOptionsCarrier` owns `layer: str` plus `values: dict[str, CompilerOptionScalar]`, where `CompilerOptionScalar = str | int | bool` is a closed scalar union. Host-only callable imports / function pointers remain local implementation detail in host/static registries and are not part of the cross-boundary carrier contract.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-01]: Fixed the emit contract around `EmitRequestCarrier`, `ModuleArtifactCarrier`, and `ProgramArtifactCarrier`. `EmitRequestCarrier` owns `spec`, `ir_document`, `output_path`, `emitter_options`, `module_id`, and `is_entry`; `ir_document` may temporarily stay a raw IR wrapper until S3. `ModuleArtifactCarrier` follows `_normalize_module_artifact()` with `module_id`, `kind`, `label`, `extension`, `text`, `is_entry`, `dependencies`, and `metadata`. `ProgramArtifactCarrier` follows `build_program_artifact()` with `target`, `program_id`, `entry_modules`, `modules`, `layout_mode`, `link_output_schema`, and `writer_options`. Native/selfhost v1 `emit_source()` may remain a thin adapter that returns `ModuleArtifactCarrier.text`, but the canonical contract is the artifact carrier family.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S1-02]: The `ModuleArtifact.metadata` / `ProgramArtifact.writer_options` object slots from `spec-dev` remain allowed only as bounded target-local leaf payloads. They are not justification for keeping whole compiler root payloads on raw `dict[str, object]` transport. P2 types the known-schema lanes first (EAST root, backend spec, layer options, emit request/result), while full nominal-ADT rollout and full stage1 host-Python-bridge removal remain separate non-goals.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-02]: Added `src/toolchain/compiler/typed_boundary.py` as the Python source-of-truth carrier module. `CompilerRootDocument`, `ResolvedBackendSpec`, `LayerOptionsCarrier`, `EmitRequestCarrier`, `ModuleArtifactCarrier`, and `ProgramArtifactCarrier` now live there together with `coerce_*` helpers and `to_legacy_dict()` adapters, so the old raw-dict APIs can stay only as compatibility shims.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-02]: Moved host/static `backend_registry*.py` onto `get_backend_spec_typed()` / `resolve_layer_options_typed()` / `emit_module_typed()` / `build_program_artifact_typed()` as the canonical path, while keeping `get_backend_spec()` / `resolve_layer_options()` / `emit_module()` / `build_program_artifact()` as thin adapters. Updated `src/ir2lang.py` to consume the typed path, and added `load_east3_document_typed()` wrappers to both `toolchain.frontends.transpile_cli` and `toolchain.compiler.transpile_cli` without breaking the existing stage-wrapper contract.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-02]: Switched `src/py2x.py` over to the typed path too, so it now routes through `load_east3_document_typed()`, `get_backend_spec_typed()`, `resolve_layer_options_typed()`, `emit_module_typed()`, `build_program_artifact_typed()`, and `apply_runtime_hook_typed()`. Legacy dict conversion is now confined to the writer seam, and both host/static registries share the `typed_boundary.py` helper-module flattening helpers so helper artifacts keep the `kind=helper` contract.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-03]: Added native C++ `CompilerRootMeta` / `CompilerRootDocument` plus `load_east3_document_typed()` in `transpile_cli.h/.cpp`, and updated the host-Python direct-route script to serialize `load_east3_document_typed().to_legacy_dict()`. That makes the compiler-root wrapper the canonical C++ native path even though the direct route still crosses JSON.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S2-03]: Added native C++ `ResolvedBackendSpec` / `LayerOptionsCarrier` plus `get_backend_spec_typed()`, `resolve_layer_options_typed()`, `emit_source_typed()`, and `apply_runtime_hook_typed()` in `backend_registry_static.h/.cpp`, then switched `selfhost/py2cpp.cpp` and `selfhost/py2cpp_stage2.cpp` to use those wrappers. Raw dicts now reappear only at the still-legacy seams such as `lower_ir()` / `optimize_ir()` entry arguments.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: As the first S3 slice, `src/toolchain/ir/core.py` gained `_sh_make_trivia_blank()`, `_sh_make_trivia_comment()`, `_sh_make_expr_stmt()`, and `_sh_make_module_root()`, moving the checked-in source-of-truth path for leading trivia, bare `Expr` statements, and module-root assembly onto builder helpers. In parallel, `coerce_compiler_root_document()` now returns already-typed `CompilerRootDocument` instances unchanged, host/static registries gained an `emit_source_typed()` alias, and native C++ gained `lower_ir_typed()` / `optimize_ir_typed()` so stage1 selfhost still builds on the typed carrier path.
- 2026-03-09 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: The next slice added `_sh_make_name_expr()`, `_sh_make_tuple_expr()`, `_sh_make_assign_stmt()`, and `_sh_make_ann_assign_stmt()`, then moved checked-in assembly for `with` bindings, typed bindings, tuple destructuring, class fields, and module top-level assign/annassign through those helpers. That removes another dense cluster of direct `Assign` / `AnnAssign` / `Name` construction from the statement/class-body lanes and leaves the broader `ExprParser` envelope for a later slice.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: Added `_sh_make_constant_expr()` and routed the leaf `Constant` / `Name` / `Tuple` nodes built inside `_parse_primary()` and `_parse_comp_target()` through helpers as well. That reduces direct dict assembly in the expression parser itself and leaves the next slice free to focus on broader expression envelopes such as `BinOp`, `Call`, and `Subscript`.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: Added `_sh_make_ifexp_expr()`, `_sh_make_boolop_expr()`, `_sh_make_unaryop_expr()`, `_sh_make_compare_expr()`, and `_sh_make_binop_expr()` for `IfExp` / `BoolOp` / `UnaryOp` / `Compare` / `BinOp`, then moved both the main `ExprParser` path and the lowered-expression path onto those shared builders. That makes helper-owned expression envelopes the canonical form for conditional expressions, boolean chains, unary ops, comparisons, and string-concatenation `BinOp`s, leaving `Call` / `Attribute` / `Subscript` plus collection literal/comprehension families as the main remaining dense clusters.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: The next slice added `_sh_make_attribute_expr()`, `_sh_make_call_expr()`, `_sh_make_slice_node()`, `_sh_make_subscript_expr()`, `_sh_make_comp_generator()`, `_sh_make_list_expr()`, `_sh_make_set_expr()`, `_sh_make_dict_expr()`, `_sh_make_list_comp_expr()`, `_sh_make_dict_comp_expr()`, `_sh_make_set_comp_expr()`, and `_sh_make_range_expr()`, then moved checked-in node assembly for `ExprParser._parse_postfix()`, generator/list/dict/set comprehensions, `range(...)` normalization, and collection literals onto those helpers. That retires direct dict assembly for both postfix and collection/comprehension envelopes, leaving f-string fragments plus smaller lambda/arg and statement-level control/import fragments as the main dense checked-in clusters still to shrink.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: Added `_sh_make_arg_node()`, `_sh_make_lambda_expr()`, `_sh_make_formatted_value_node()`, and `_sh_make_joined_str_expr()`, then moved checked-in lambda arg/body assembly plus f-string fragments onto those shared builders. With that in place, the large expression-side clusters are mostly gone, so the remaining S3-01 work is concentrated in statement-level import/control-flow/def-signature fragments and the generated selfhost mirror shrink.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: The next slice added `_sh_make_import_alias()`, `_sh_make_import_stmt()`, `_sh_make_import_from_stmt()`, `_sh_make_if_stmt()`, `_sh_make_for_stmt()`, `_sh_make_for_range_stmt()`, `_sh_make_while_stmt()`, `_sh_make_except_handler()`, and `_sh_make_try_stmt()`, then moved the remaining import/control-flow node assembly in `_sh_parse_if_tail()`, `_sh_parse_stmt_block_mutable()`, and the module-root import lane onto shared builders. That shrinks the large statement-level checked-in dict cluster too, leaving `FunctionDef` / `ClassDef` / def-signature fragments plus the generated selfhost mirror as the main S3-01 work still left.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: The next slice added `_sh_make_function_def_stmt()` / `_sh_make_class_def_stmt()` and routed nested functions, top-level functions, methods, and class roots through those shared builders. That means most major checked-in source-of-truth node families are now helper-owned, leaving def-signature helpers, a few smaller statement fragments, and the generated selfhost mirror as the main S3-01 work still left.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: A follow-up slice added `_sh_make_arg_node()`, `_sh_make_lambda_expr()`, `_sh_make_formatted_value_node()`, and `_sh_make_joined_str_expr()`, then moved lambda arguments, `Lambda`, f-string placeholder / joined-string nodes, and module-level bare `Expr` statements onto helper-owned construction too. With that, most checked-in direct dict assembly on the expression side is gone, and the remaining S3-01 density is centered on statement-level nodes such as `If`, `Try`, `Import`, `FunctionDef`, and `ClassDef`.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: For simple statements, `_sh_make_raise_stmt()`, `_sh_make_pass_stmt()`, `_sh_make_return_stmt()`, `_sh_make_yield_stmt()`, `_sh_make_augassign_stmt()`, and `_sh_make_swap_stmt()` were added, then the statement-block parser and class-body parser were moved onto those helpers for `Raise` / `Pass` / `Return` / `Yield` / `AugAssign` / `Swap`. That removes most remaining direct dict assembly for terminal statements, leaving the composite builders for `If`, `While`, `Try`, `Import`, `FunctionDef`, and `ClassDef` as the main dense checked-in clusters.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: The next slice moved `_sh_make_import_alias()`, `_sh_make_import_stmt()`, `_sh_make_import_from_stmt()`, `_sh_make_if_stmt()`, `_sh_make_while_stmt()`, `_sh_make_except_handler()`, `_sh_make_try_stmt()`, `_sh_make_for_stmt()`, and `_sh_make_for_range_stmt()` into the actual statement-block and module-root assembly paths, covering `If` / `While` / `Try` / `For` / `ForRange` / `Import` / `ImportFrom` plus the lowered `Try` used for `with`. That shrinks the remaining S3-01 density further toward `FunctionDef` / `ClassDef` / def-signature metadata and the generated selfhost mirror.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: The next slice added `_sh_make_function_def_stmt()` / `_sh_make_class_def_stmt()` and moved checked-in direct assembly for nested functions, top-level functions, class methods, and top-level classes onto shared `FunctionDef` / `ClassDef` helpers. That leaves the remaining S3-01 work concentrated mostly in def-signature metadata fragments and the generated selfhost mirror, while shrinking one more large composite-statement cluster in `core.py`.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: After that, the remaining string-literal-concatenation `BinOp` plus the default `0/1` `Constant` nodes used by `ForRange` normalization were moved onto the existing `_sh_make_binop_expr()` / `_sh_make_constant_expr()` helpers too. That removes the last checked-in direct `kind` assembly from the `core.py` source-of-truth path outside helper definitions, so the remaining S3-01 work now lives more in def-signature helpers and the generated selfhost mirror than in raw node construction.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: Added `_sh_make_def_sig_info()` and moved the raw `name/ret/arg_types/arg_type_exprs/return_type_expr/arg_order/arg_defaults` carrier returned by `_sh_parse_def_sig()` onto that helper too. That narrows the remaining source-of-truth S3-01 work down further: the parsing logic stays, but the bigger residual debt now sits in the generated selfhost mirror and the remaining `make_object`-backed adapter seams rather than inline Python carrier assembly.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: As a cleanup slice, the remaining concatenated-string `BinOp` assembly and the default `Constant` nodes for omitted `range(...)` start/step values were moved onto `_sh_make_binop_expr()` / `_sh_make_constant_expr()` too. That confines checked-in direct AST node assembly inside `src/toolchain/ir/core.py` to the helper definitions themselves, so the remaining S3-01 work has shifted from the source-of-truth parser body to the generated selfhost mirror, def-signature metadata fragments, and other residual `make_object` lanes.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-01]: With checked-in node construction in `src/toolchain/ir/core.py` reduced so that no direct dict AST assembly remains outside the helper definitions, S3-01 is considered complete. The remaining object-carrier retreat work now moves to `S3-02`, focused on generated/selfhost runtime `make_object` usage.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: As the first generated-runtime slice, `selfhost/runtime/cpp/pytra-gen/compiler/east_parts/core.cpp` gained `_sh_make_expr_stmt()`, `_sh_make_name_expr()`, `_sh_make_assign_stmt()`, `_sh_make_ann_assign_stmt()`, `_sh_make_import*_stmt()`, `_sh_make_function_def_stmt()`, and `_sh_make_class_def_stmt()`. Top-level `FunctionDef` / `Import` / `ImportFrom`, class-field `AnnAssign` / enum-field `Assign`, method `FunctionDef`, class-root `ClassDef`, and top-level assign / bare `Expr` assembly now route through those helpers instead of repeating large open-coded `make_object` clusters. In parallel, `src/toolchain/ir/core.py` gained `_sh_make_expr_token()` / `_sh_make_import_binding()` so token/import-metadata carriers and selfhost mirror regressions share the same helper contract on the source-of-truth side too.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next slice added `_sh_make_expr_token()` and `_sh_make_import_binding()` in `src/toolchain/ir/core.py`, then moved `ExprParser._tokenize()` and `_sh_append_import_binding()` onto helper-owned carriers. `test_east_core.py` now guards that source-of-truth boundary too, so direct tokenizer/import-binding dict assembly fails fast if it reappears.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next slice added `_sh_make_import_symbol_binding()` and `_sh_make_qualified_symbol_ref()` for the module-root import-resolution tail, moving `import_symbol_bindings` and `qualified_symbol_refs` off open-coded inline dict assembly. That means the metadata derived from `ImportBinding` now flows through helper-owned carriers too, and the next slice can focus on the `import_resolution` / module-`meta` carrier boundary itself.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: Another slice added `_sh_make_module_source_span()`, `_sh_make_import_resolution_meta()`, and `_sh_make_module_meta()`, moving the empty module `source_span`, `meta.import_resolution`, and module `meta` assembly inside `_sh_make_module_root()` onto helper-owned carriers too. With that, the module-root helper itself is now mostly a composition of smaller carrier builders, so the next slice can focus on `import_resolution_bindings` and the remaining `dict[str, Any]` bridge surfaces.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next source-of-truth slice applied `_sh_make_import_symbol_binding()` to every remaining import-symbol metadata assignment in the pre-scan, `_sh_register_import_symbol()`, and the `dataclasses` / ordinary `from-import` parse paths. That leaves the tracked source-of-truth side with a single helper-owned `module/name` carrier shape, and `test_east_core.py` now rejects raw dict regressions there. In parallel, `test_prepare_selfhost_source.py` and the progress notes were corrected so they no longer imply that ignored generated-mirror edits were part of the tracked slice.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up slice tightened `_sh_make_import_resolution_binding()` so it no longer merges arbitrary `resolution.items()` keys and instead enumerates the known runtime-resolution fields explicitly. The binding-list type hints in `_sh_make_import_resolution_meta()`, `_sh_make_module_meta()`, and `_sh_make_module_root()` were aligned on `dict[str, Any]` too, so this lane is now closer to a fixed carrier contract and `test_east_core.py` can fail fast if the generic merge loop comes back.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next slice added `_sh_make_lambda_arg_entry()`, `_sh_make_keyword_arg()`, and `_sh_make_cast_entry()` so `ExprParser._parse_lambda()` parameter carriers, `_parse_postfix()` call-keyword carriers, and `_make_bin()` numeric-promotion cast metadata no longer assemble open-coded dict literals inline. That pushes another group of expression-side helper carriers onto fixed builders, and `test_east_core.py` now fails fast if the old raw `arg_entries.append(...)`, `keywords.append(...)`, or `casts.append(...)` dicts return.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: Another slice added a native-wrapper seam guard in `test_py2x_entrypoints_contract.py`, fixing the allowed `make_object(...)` lines in `transpile_cli.cpp` and `backend_registry_static.cpp`. That locks the C++ native lane so `make_object` can reappear only inside `CompilerRootDocument::to_legacy_dict()`, `ResolvedBackendSpec::to_legacy_dict()`, `get_backend_spec_typed()`, and the `emit_source_typed()` JSON-export seam; any new native-wrapper leakage fails fast.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next native-carrier slice removed `raw_spec` from `ResolvedBackendSpec` in `src/runtime/cpp/native/compiler/backend_registry_static.h` and stopped `get_backend_spec_typed()` from prebuilding a legacy dict. That pulls the native C++ `make_object` usage closer to `ResolvedBackendSpec::to_legacy_dict()` plus the `emit_source_typed()` JSON-export seam, and the allowlist in `test_py2x_entrypoints_contract.py` shrank with it.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up native export slice removed the `json.dumps(make_object(ir))` path from `backend_registry_static.cpp:emit_source_typed()` and replaced it with the explicit `pytra::std::json::_dump_json_dict(ir, true, ::std::nullopt, ",", ":", 0)` serializer seam. That leaves native registry-side `make_object` only in the legacy export path of `ResolvedBackendSpec::to_legacy_dict()`, and `test_py2x_entrypoints_contract.py` now fixes both the tighter allowlist and the expected serializer helper call.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: To make the next `src/py2x-selfhost.py` typed-boundary slice follow spec-codex again, `tools/check_transpiler_version_gate.py` and `tools/run_regen_on_version_bump.py` were updated so `VERSIONS_FILE` points at the current `src/toolchain/compiler/transpiler_versions.json`. That brings the version gate and sample-regen trigger back in line with the repo and removes the immediate tool-level blocker for future `src/py2*.py` edits.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up native JSON-decode slice removed the `_load_json_root_document()` path that built a `dict<str, object> raw_doc` only to feed it back into `coerce_compiler_root_document(raw_doc, ...)`. `transpile_cli.cpp` now constructs `CompilerRootDocument` directly from `pytra::std::json::JsonObj`, keeping the `.json` load path inside a typed helper and letting `test_py2x_entrypoints_contract.py` fail fast if that legacy coercion reappears inside the helper.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up native-transpile slice folded the `.json` load path in `transpile_cli.cpp` into a `CompilerRootDocument _load_json_root_document(...)` helper, so `load_east3_document_typed()` no longer passes a raw `dict<str, object>` back out of the helper before coercion. `test_py2x_entrypoints_contract.py` now fixes the absence of `_load_json_root_dict` and the presence of `_load_json_root_document`, making that typed-load path fail fast if it regresses.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: Another slice added `_sh_import_binding_fields()` / `_sh_make_import_resolution_binding()`, moving the remaining `binding.get(...)` chain and `dict(binding)` bridge in the module-root tail onto helper-owned carriers too. `convert_source_to_east_self_hosted()` now derives `import_resolution_bindings`, `import_modules`, and `import_symbols` from `ImportBinding` through the same extractor, and `test_east_core.py` forbids raw field extraction outside the helper definitions.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next slice added `_sh_make_decl_meta()`, moving the top-level function `runtime_abi_v1` / `template_v1` metadata and the extern-var `AnnAssign` `extern_var_v1` metadata onto the same helper-owned carrier. That removes the remaining `meta: {}` / `{\"extern_var_v1\": ...}` assembly from this declaration lane and lets `test_east_core.py` fail fast if raw metadata dict assembly comes back.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next generated-mirror guard slice extended `test_prepare_selfhost_source.py` with `_sh_make_arg_node()` / `_sh_make_lambda_arg_entry()` / `_sh_make_keyword_arg()` / `_sh_make_cast_entry()` presence checks plus the absence of the old keyword/cast literals. Even though the generated mirror itself is not tracked, this guard now makes the lambda-arg / call-keyword / numeric-cast cluster fail fast if it falls back to open-coded `make_object` dict assembly.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: Another guard-only slice extended `test_prepare_selfhost_source.py` with `_sh_make_attribute_expr()` / `_sh_make_call_expr()` / `_sh_make_binop_expr()` presence checks and the absence of the old inline `Attribute` / `Call` / `BinOp` literals. The generated mirror itself was already using helper-owned builders there, so this slice hardens the regression boundary and makes that expression-node cluster fail fast if it falls back to open-coded `make_object` dict assembly.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up selfhost-entry slice removed the legacy shim helpers (`_load_east3`, `_get_spec`, `_resolve_opts`, `_lower`, `_optimize`, `_emit`, `_apply_runtime`) from `src/py2x-selfhost.py` and switched main over to direct calls into `load_east3_document_typed()`, `get_backend_spec_typed()`, `resolve_layer_options_typed()`, `lower_ir_typed()`, `optimize_ir_typed()`, `emit_source_typed()`, and `apply_runtime_hook_typed()`. Both `selfhost/py2cpp.cpp` and `selfhost/py2cpp_stage2.cpp` now stay on that direct typed path without `to_legacy_dict()` bridges, and `test_py2x_entrypoints_contract.py` fixes the regression boundary accordingly. In parallel, `tools/check_transpiler_version_gate.py` now treats `src/py2x-selfhost.py` as a `cpp`-lane change and `src/toolchain/compiler/transpiler_versions.json` bumps `cpp` to `0.282.0`.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next native option-carrier slice re-checked the host/static backend registries and confirmed that layer options are currently `str`-typed only. On that basis, `LayerOptionsCarrier.values` in `src/runtime/cpp/native/compiler/backend_registry_static.h` was narrowed from `dict<str, object>` to `dict<str, str>`, `resolve_layer_options_typed()` now preserves the raw CLI option strings directly, and legacy re-entry is coerced back into the string carrier with `dict<str, str>(raw)`. Boxing is therefore confined to `LayerOptionsCarrier::to_legacy_dict()` as the adapter seam, and `test_py2x_entrypoints_contract.py` plus `python3 tools/build_selfhost.py` lock in the non-regression.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up native root-export slice rewired `CompilerRootDocument::to_legacy_dict()` in `src/runtime/cpp/native/compiler/transpile_cli.cpp` around typed dict conversion. The updates for `kind`, `source_path`, `east_stage`, `schema_version`, `meta.dispatch_mode`, `meta.parser_backend`, and the `meta` payload itself now compose through `dict<str, object>(dict<...>{...})`, which reduces the explicit `make_object(...)` calls in that wrapper to zero. `test_py2x_entrypoints_contract.py` now keeps the native transpile-wrapper allowlist empty, and `python3 tools/build_selfhost.py` confirms the selfhost build still passes.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next native no-op-lane slice removed the delegate calls in `src/runtime/cpp/native/compiler/backend_registry_static.cpp:lower_ir_typed()` / `optimize_ir_typed()` that only bounced through `spec.to_legacy_dict()` plus `lower_options.to_legacy_dict()` / `optimizer_options.to_legacy_dict()`. On the selfhost typed path these stages are still no-ops, so returning the typed carrier result directly is sufficient and keeps the legacy dict adapter confined to the old `lower_ir()` / `optimize_ir()` surface. `test_py2x_entrypoints_contract.py` now fixes the absence of the old delegate lines together with the presence of `return east.to_legacy_dict();` / `return ir;`, and `python3 tools/build_selfhost.py` confirmed non-regression.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next native lower/optimizer wrapper slice removed the old `spec.to_legacy_dict()` / `lower_options.to_legacy_dict()` / `optimizer_options.to_legacy_dict()` round-trip from `src/runtime/cpp/native/compiler/backend_registry_static.cpp`. `lower_ir_typed()` / `optimize_ir_typed()` now stay as thin adapters that return `east.to_legacy_dict()` / `ir` directly, which matches the current selfhost direct route and pushes another dict-boxing hop out of the native wrapper. `test_py2x_entrypoints_contract.py` now fails fast if the old return statements come back.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The `src/py2x-selfhost.py` typed-boundary slice is still ahead, but the version gate was not watching that entrypoint as a direct cpp dependency. Another preparatory slice fixed that by adding `src/py2x-selfhost.py` to `tools/check_transpiler_version_gate.py` and by adding `test_check_transpiler_version_gate.py`, so future selfhost entrypoint edits fail fast unless the cpp transpiler version is bumped.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next native transpile-export slice added `export_compiler_root_document()` to `src/toolchain/compiler/typed_boundary.py` and made `CompilerRootDocument::to_legacy_dict()` share that helper. With that in place, the host-Python direct-route script in `src/runtime/cpp/native/compiler/transpile_cli.cpp` no longer calls `json.dumps(doc.to_legacy_dict(), ...)` inline; it now routes through `json.dumps(export_compiler_root_document(doc), ...)` as an explicit export seam. `test_py2x_entrypoints_contract.py` now fixes the helper import and forbids the old inline `doc.to_legacy_dict()` call inside the embedded script, which keeps the typed export seam distinct from the legacy adapter.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next native runtime-hook slice added an `apply_runtime_hook_typed(const Path&)` overload in `src/runtime/cpp/native/compiler/backend_registry_static.h/.cpp`, so the legacy `apply_runtime_hook(spec, output_path)` no-op lane no longer runs `_coerce_backend_spec(spec)`. `apply_runtime_hook_typed(const ResolvedBackendSpec&, ...)` now delegates into that one-argument helper too, and `test_py2x_entrypoints_contract.py` fixes the regression boundary by banning the old delegate line.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up native root-export-helper slice added `export_compiler_root_document(const CompilerRootDocument&)` to `src/runtime/cpp/native/compiler/transpile_cli.h/.cpp` and shrank `CompilerRootDocument::to_legacy_dict()` into a thin adapter around it. Legacy `load_east3_document()` and native `backend_registry_static.cpp:lower_ir_typed()` now share that same helper, and `test_py2x_entrypoints_contract.py` fixes the helper definition, the absence of the old direct `to_legacy_dict()` export path, and the shared export seam in the native lower wrapper.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up native spec/option-export slice added `export_backend_spec(const ResolvedBackendSpec&)` / `export_layer_options(const LayerOptionsCarrier&)` to `src/runtime/cpp/native/compiler/backend_registry_static.h/.cpp`, then shrank `ResolvedBackendSpec::to_legacy_dict()` / `LayerOptionsCarrier::to_legacy_dict()` into thin adapters around those helpers. Legacy `get_backend_spec()` / `resolve_layer_options()` now route through the same helpers too, and `test_py2x_entrypoints_contract.py` fixes the helper definitions plus the absence of the old inline export paths so those native export seams stay explicit.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up host-entrypoint export slice imported `export_compiler_root_document()` / `export_program_artifact_carrier()` / `export_module_artifact_carrier()` into `src/py2x.py` and removed the inline `to_legacy_dict()` calls from both `_load_for_program()` and the writer fallback path. That lines up the host entrypoint's compiler-root and artifact export with the shared `typed_boundary.py` helper seam, and `test_py2x_entrypoints_contract.py` now fixes the helper imports plus the absence of the old inline paths.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next generated-selfhost-core slice added `_sh_make_constant_expr()` / `_sh_make_if_stmt()` / `_sh_make_for_stmt()` / `_sh_make_for_range_stmt()` / `_sh_make_range_expr()` to `selfhost/runtime/cpp/pytra-gen/compiler/east_parts/core.cpp`. The `elif` tail carrier in `_sh_parse_if_tail()`, the statement-level `if` / `for` / `for range(...)` fastpath, and the list-comprehension range fastpath now all use those builders, which pulls the remaining open-coded `If` / `For` / `ForRange` / `RangeExpr` / `Constant(int)` clusters further behind helper-owned builders. `test_prepare_selfhost_source.py` now fixes both the presence of those helpers and the absence of the old inline clusters, so generated-mirror branch/range nodes fail fast if they slip back to raw `make_object` dict assembly.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up generated-selfhost expression slice added `_sh_make_formatted_value_node()` / `_sh_make_joined_str_expr()` to `selfhost/runtime/cpp/pytra-gen/compiler/east_parts/core.cpp`, then moved both the f-string parser and the lowered expression parser onto helper-owned `FormattedValue` / `JoinedStr` builders. In the same slice, the lowered `IfExp` path was switched over to the existing `_sh_make_ifexp_expr()` helper too, and `test_prepare_selfhost_source.py` now fails fast if those inline `FormattedValue` / `JoinedStr` / `IfExp` clusters return.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The host/static backend registry now shares the same export seams by adding `export_layer_options_carrier()` to `src/toolchain/compiler/typed_boundary.py` and making `LayerOptionsCarrier.to_legacy_dict()` use it. With that in place, `src/toolchain/compiler/backend_registry.py` and `src/toolchain/compiler/backend_registry_static.py` switched `lower_ir_typed()` / `optimize_ir_typed()` / `emit_module_typed()` over to `export_compiler_root_document()` plus `export_layer_options_carrier()`, which removes more inline `coerce_compiler_root_document(...).to_legacy_dict()` and `options.to_legacy_dict()` calls from the typed wrappers. `test_py2x_entrypoints_contract.py` now locks in those helper-based calls and the absence of the old paths so the host/static export seam matches the native lane more closely.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up artifact-export slice added `export_module_artifact_carrier()` / `export_program_artifact_carrier()` to `src/toolchain/compiler/typed_boundary.py`, and `ModuleArtifactCarrier.to_legacy_dict()` / `ProgramArtifactCarrier.to_legacy_dict()` now share those helpers. With that in place, `src/toolchain/compiler/backend_registry.py` and `src/toolchain/compiler/backend_registry_static.py` moved `emit_module()` / `build_program_artifact()` / `collect_program_modules()` over to helper-owned exports instead of inline `to_legacy_dict()` calls, so the artifact lane now matches the compiler-root and layer-option seams. `test_py2x_entrypoints_contract.py` now fixes both helper-vs-legacy equivalence and the helper-based host/static source contract for those artifact exports.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The source-of-truth backend-spec export slice added `export_backend_spec_carrier()` / `export_resolved_backend_spec()` to `src/toolchain/compiler/typed_boundary.py` and shrank `BackendSpecCarrier.to_legacy_dict()` / `ResolvedBackendSpec.to_legacy_dict()` into thin adapters. In step with that, `src/toolchain/compiler/backend_registry.py:get_backend_spec()` and the static spec-cache normalization in `src/toolchain/compiler/backend_registry_static.py` now use helper-owned export seams instead of inline `.to_legacy_dict()` calls.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up host-entrypoint export slice switched `_build_linked_program_for_input()` in `src/py2x.py` away from `load_east3_document_typed(...).to_legacy_dict()` and over to `export_compiler_root_document(load_east3_document_typed(...))`. The `program_artifact` normalization path now also routes through `export_program_artifact_carrier()` / `export_module_artifact_carrier()`, which pushes the remaining inline `.to_legacy_dict()` calls out of the host entrypoint itself. `test_py2x_entrypoints_contract.py` was already guarding that host-entry contract; this slice makes the source-of-truth implementation match the guard again.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up host/static legacy-export slice removed `.to_legacy_dict()` from `src/toolchain/compiler/backend_registry.py:_normalize_backend_spec()` and from the host/static `resolve_layer_options()` legacy entries as well, switching them over to `export_resolved_backend_spec()` / `export_layer_options_carrier()` directly. That trims down the remaining inline legacy-adapter calls inside the registry layer, and `test_py2x_entrypoints_contract.py` now fails fast if those older paths come back.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next host/static legacy-surface slice switched `src/toolchain/compiler/backend_registry.py:_normalize_backend_spec()` and `resolve_layer_options()` in both `backend_registry.py` and `backend_registry_static.py` over to `export_resolved_backend_spec()` / `export_layer_options_carrier()`. That pulls the remaining small inline `.to_legacy_dict()` calls in those registries behind helper-owned export seams too, and `test_py2x_entrypoints_contract.py` now fails fast if the old return statements come back.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up `ir2lang.py` export slice removed the writer-path `hasattr(..., "to_legacy_dict")` branch and aligned artifact normalization around `coerce_module_artifact()` / `export_module_artifact_carrier()` / `export_program_artifact_carrier()` as the regular export seam. Using `collect_program_modules_typed()` directly at that entrypoint also trims another host-side legacy-dict flattening hop, and `test_py2x_entrypoints_contract.py` now fails fast if inline `.to_legacy_dict()` returns to `ir2lang.py`.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up `py2x.py` writer-lane slice now coerces the `emit_module_typed()` result through `coerce_module_artifact()` at the entrypoint and feeds `build_program_artifact_typed()` from that typed carrier path. The typed path no longer branches on `hasattr(..., "to_legacy_dict")`; only the dict fallback used by test patches remains, while `export_program_artifact_carrier()` / `export_module_artifact_carrier()` stay as the regular export seam. `test_py2x_entrypoints_contract.py` now also fails fast if those `hasattr(..., "to_legacy_dict")` checks return to `py2x.py`.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: A follow-up `py2x.py` program-carrier slice added `coerce_program_artifact()` to `src/toolchain/compiler/typed_boundary.py` and replaced the entrypoint-local dict branch before the writer with helper-owned carrier coercion. Dict test doubles now get folded into `ProgramArtifactCarrier` with explicit fallback target/program-id values, helper-module flattening also happens inside that coercion step, and `export_program_artifact_carrier()` becomes the single regular export seam used by `py2x.py`. `test_py2x_entrypoints_contract.py` now fixes that source contract plus the helper-flatten behavior, and `test_py2x_cli.py` confirms that the CLI patch lane using dict return values still works.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The generated-selfhost regression guard was extended too: `test_prepare_selfhost_source.py` now locks in the presence and use of `_sh_make_def_sig_info()` / `_sh_make_expr_token()` / `_sh_make_import_binding()` inside `selfhost/runtime/cpp/pytra-gen/compiler/east_parts/core.cpp`. That makes the def-signature, tokenizer, and import-metadata carriers fail fast if they fall back to open-coded raw-dict return/append paths in the tracked generated mirror.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next host-entry/native-wrapper slice added `coerce_program_artifact()` in `src/toolchain/compiler/typed_boundary.py` and switched the `src/py2x.py` writer lane over to `ProgramArtifactCarrier` normalization. That moves the dict fallback behind the helper and leaves `py2x.py` itself on the regular `export_program_artifact_carrier()` seam only. In parallel, the unused `to_legacy_dict()` thin adapters were removed from `src/runtime/cpp/native/compiler/transpile_cli.{h,cpp}` and `src/runtime/cpp/native/compiler/backend_registry_static.{h,cpp}`, so the native C++ compiler-root / backend-spec / layer-option wrappers now stay export-helper-only as well. `test_py2x_entrypoints_contract.py` gained a `coerce_program_artifact()` flatten check and guards for the removed native methods, and regressions were checked with `test_py2x_cli.py` plus `tools/build_selfhost.py`.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The next generated-selfhost module-root slice added `_sh_import_binding_fields()` / `_sh_make_import_resolution_binding()` / `_sh_make_import_symbol_binding()` / `_sh_make_qualified_symbol_ref()` / `_sh_make_module_source_span()` / `_sh_make_import_resolution_meta()` / `_sh_make_module_meta()` / `_sh_make_module_root()` to `selfhost/runtime/cpp/pytra-gen/compiler/east_parts/core.cpp`, then moved the import-resolution / module-meta / module-root tail in `convert_source_to_east_self_hosted()` behind those helper-owned carriers. `test_prepare_selfhost_source.py` now locks in both the helper definitions and their call sites, and it fails fast if the old `sym_binding` / `qref` / `source_span` / `meta` open-coded dict cluster returns.
- 2026-03-10 [ID: P2-COMPILER-TYPED-BOUNDARY-01-S3-02]: The follow-up generated-selfhost postfix guard slice extended `test_prepare_selfhost_source.py` with `_sh_make_slice_node()` presence checks plus the absence of the two old inline `dict<str, object>{{"kind", make_object("Slice")}, ...}` literals left in the postfix parser. That makes the generated selfhost core fail fast if the `Subscript` slice lane slips back out of the helper contract, while `python3 tools/build_selfhost.py` confirmed the surrounding selfhost build still stays intact.

# TODO履歴（2026-03-08）

<a href="../../../en/todo/archive/20260308.md">
  <img alt="Read in English" src="https://img.shields.io/badge/docs-English-2563EB?style=flat-square">
</a>

## 2026-03-08 移管: 完了済みセクション（7件）

以下を `docs/ja/todo/index.md` から `todo/archive` へ移管。

### P0: pytra-cli C++ max最適化で linked-program build を使う

文脈: [docs/ja/plans/archive/20260308-p0-pytra-cli-cpp-maxopt-linked-build.md](../plans/archive/20260308-p0-pytra-cli-cpp-maxopt-linked-build.md)

1. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01] `pytra-cli --target cpp --build --codegen-opt 3` を linked-program optimizer 込みの max C++ route にし、sample parity で非退行を固定する。
2. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S1-01] `pytra-cli` の `--codegen-opt` と `py2x/eastlink/ir2lang/py2cpp` の最適化段対応表を棚卸しし、`codegen-opt=3` の目標 semantics を固定する。
3. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S1-02] `pytra-cli` max-opt C++ route の CLI 契約と sample parity gate を spec/plan に固定する。
4. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-01] `pytra-cli --target cpp --build --codegen-opt 3` が linked-program optimizer を経由する build route を実装する。
5. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-02] `pytra-cli --target cpp --codegen-opt 3` の transpile-only route も linked-program optimizer を使うよう揃える。
6. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-01] representative CLI regression を追加し、`codegen-opt=3` の route 選択と manifest/build/run を固定する。
7. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-02] sample parity を回し、max-opt route でも C++ sample が green であることを確認する。
8. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S4-01] `pytra-cli` / how-to-use / 必要な docs に max-opt C++ route と sample parity 手順を反映する。
9. [x] [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S4-02] 完了結果を記録し、計画を archive へ移して閉じる。
- 進捗メモ: [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S2-01] `src/pytra-cli.py` に `codegen-opt=3` 専用の linked build route を追加し、`py2x.py --link-only -> py2x.py --from-link-output` の 2 段で `output_dir/.pytra_linked/` を中間 bundle として使うようにした。`test_pytra_cli.py` では manifest 解決と `make` 実行までを回帰化した。
- 進捗メモ: [ID: P0-PYTRACLI-CPP-MAXOPT-LINKED-01-S3-02] `tools/runtime_parity_check.py` に `--cpp-codegen-opt` を追加し、`python3 tools/runtime_parity_check.py --targets cpp --case-root sample --all-samples --cpp-codegen-opt 3 --east3-opt-level 2` を実行して `SUMMARY cases=18 pass=18 fail=0` を確認した。`how-to-use` と `spec-make` も multi-file / `--output-dir` 前提へ同期した。

### P0: `@abi` mode を `value` / `value_mut` に整理する

文脈: [docs/ja/plans/archive/20260308-p0-runtime-abi-mode-simplify-value-value-mut.md](../plans/archive/20260308-p0-runtime-abi-mode-simplify-value-value-mut.md)

1. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01] `@abi` の public mode 名を `default/value/value_mut` に整理し、引数側 `value` を read-only value ABI の canonical surface にする。
2. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S1-01] 現行 `default/value/value_readonly` 契約を棚卸しし、`value=value_readonly`, `value_mut=旧 mutable value ABI` の移行方針を固定する。
3. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S1-02] spec/plan に canonical naming と移行ルールを書き、`value_readonly` の扱いを決める。
4. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-01] parser / decorator metadata / validator が `value` / `value_mut` を受理し、新 canonical metadata を出すよう更新する。
5. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-02] diagnostics / error message / target support check を新 naming に合わせる。
6. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-01] 既存 helper（`py_join`, `py_split`, `py_range` など）の注釈と generated/runtime 側期待を新 naming に移行する。
7. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-02] representative regression を更新し、C++ helper/codegen で非退行を確認する。
8. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S4-01] docs と how-to-use を新 naming に同期し、移行注意点を記録する。
9. [x] [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S4-02] 完了結果を記録し、計画を archive へ移して閉じる。
- 進捗メモ: [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S2-02] `spec-abi` / `spec-east` を `default/value/value_mut` 基準へ更新し、parser / validator は `value_readonly` を source alias として受理しつつ `runtime_abi_v1` では `value` へ正規化するようにした。`ret="value_mut"` は fail-closed で reject する。
- 進捗メモ: [ID: P0-RUNTIME-ABI-MODE-SIMPLIFY-01-S3-02] `src/pytra/built_in/string_ops.py` の `py_join` を canonical `@abi(args={"parts": "value"}, ret="value")` へ移し、`test_east_core.py -k abi`、`test_global_optimizer.py`、`test_ir2lang_cli.py`、`test_py2cpp_codegen_issues.py -k abi`、`test_pytra_built_in_string_ops.py` を再実行して非退行を確認した。

### P0: backend から runtime module 知識を撤去する

文脈: [docs/ja/plans/archive/20260308-p0-backend-runtime-knowledge-leak-retirement.md](../plans/archive/20260308-p0-backend-runtime-knowledge-leak-retirement.md)

1. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01] backend / emitter から source-side runtime module 名と ad-hoc helper ABI の知識を撤去し、resolved runtime symbol / semantic tag / adapter kind の data-driven 契約へ統一する。
2. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S1-01] `src/backends/**` の `math/gif/png/save_gif/write_rgb_png/pyMath*` leakage を target 別・類型別に棚卸しし、代表ケースを固定する。
3. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S1-02] `spec-runtime` / `spec-dev` に backend 禁止事項と resolved metadata 正規導線を明文化する。
4. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S2-01] runtime symbol index / import binding API を拡張し、module import / function import / constant import / semantic tag を backend 外で解決できるようにする。
5. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S2-02] helper ABI 差異を adapter kind へ正規化し、`save_gif` などの引数規約を emitter 直書きから外す。
6. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-01] C++ / JS / CS / RS など代表 backend を resolved runtime symbol / adapter 描画へ移行する。
7. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-02] Go / Swift / Kotlin / Java / Scala / Ruby / Lua / PHP / Nim を同じ契約へ追従させる。
8. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-01] representative backend/test/tooling 回帰と guard を追加し、知識漏れの再侵入を防ぐ。
9. [x] [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-02] docs 同期と full smoke を実施し、本計画を閉じる。
- 進捗メモ: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S3-02] `Go` / `Java` / `Scala` / `Lua` を含む残り backend を canonical runtime metadata 契約へ揃え、`math` helper branching と `pytra.utils` import 補完を source-side 分岐から外した。`Go` / `Java` / `Lua` / `Scala` の targeted regression は同一 ID で再実行して green を確認した。
- 進捗メモ: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-01] `tools/check_emitter_runtimecall_guardrails.py` を source-side module branch と `pyMath*` helper branch の検知へ絞り直し、allowlist empty / findings 0 を baseline にした。`test_check_emitter_runtimecall_guardrails.py` と `test_runtime_symbol_index.py` も更新し、`gif` module leaf import と `save_gif` adapter metadata を tooling 側で固定した。
- 進捗メモ: [ID: P0-BACKEND-RUNTIME-KNOWLEDGE-LEAK-01-S4-02] representative full smoke として `test_check_emitter_runtimecall_guardrails.py`、`test_runtime_symbol_index.py`、`tools/check_emitter_runtimecall_guardrails.py`、`test_code_emitter.py` の runtime metadata case、`CPP` / `JS` / `CS` / `RS` / `Go` / `Kotlin` / `Swift` / `PHP` / `Scala` / `Java` / `Lua` / `Nim` / `Ruby` の representative runtime import / math / `save_gif` / fail-closed suite を再実行し、`Ran 44 tests in 57.569s`, `OK` を確認した。

### P1: generated/runtime helper 向け `@abi` decorator 導入

文脈: [docs/ja/plans/archive/20260308-p1-runtime-abi-decorator-for-generated-helpers.md](../plans/archive/20260308-p1-runtime-abi-decorator-for-generated-helpers.md)

1. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01] generated/runtime helper の境界 ABI を固定できる `@abi` decorator を導入し、`@extern` と独立の boundary policy として扱えるようにする。
2. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S1-01] `spec-abi` に `@abi` の syntax / semantics / mode / `@extern` との責務分離を明記する。
3. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S1-02] `@abi` metadata の EAST / linked metadata 形式を決め、parser/selfhost parser の受け入れ基準を固定する。
4. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S2-01] `src/pytra/std/__init__.py` に `abi` を追加し、parser / selfhost parser / AST build が decorator を保持できるようにする。
5. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S2-02] `value_readonly` への mutation を検出する validator / lower guard を追加する。
6. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-01] C++ backend に `@abi(args, ret)` の最小 lowering を実装し、helper signature を value ABI 正規形へ固定する。
7. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-02] `py_join` など代表 helper を `@abi` 前提に移し、`rc<list<str>>` 非露出を回帰で固定する。
8. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-01] `@extern` 併用 case / unsupported backend / invalid mutation case の unit test を追加する。
9. [x] [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-02] docs 同期と `P1-CPP-PY-RUNTIME-SLIM-01` 依存解消メモを記録して本計画を閉じる。
- 進捗メモ: [ID: P1-RUNTIME-ABI-DECORATOR-01-S3-02] `src/pytra/built_in/string_ops.py` に `@abi(args={"parts": "value_readonly"}, ret="value")` 付き `py_join` を追加し、generated C++ runtime `src/runtime/cpp/generated/built_in/string_ops.{h,cpp}` を `const list<str>&` / `list<str>` 署名で再生成した。`test_pytra_built_in_string_ops.py`、`test_cpp_runtime_iterable.py`、`test_runtime_symbol_index.py` を更新し、generated helper に `rc<list<str>>` が露出しないことを固定した。
- 進捗メモ: [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-01] `toolchain.frontends.runtime_abi` に target support guard を追加し、linked-program optimize と raw `ir2lang` の両方で `cpp` 以外の `@abi` を fail-closed にした。`test_east_core.py` では `@extern + @abi` metadata coexistence、`test_py2cpp_codegen_issues.py` では併用時の value ABI lowering、`test_global_optimizer.py` と `test_ir2lang_cli.py` では unsupported backend rejection を固定した。
- 進捗メモ: [ID: P1-RUNTIME-ABI-DECORATOR-01-S4-02] active TODO から本計画を閉じ、`docs/ja/plans/p1-cpp-py-runtime-core-slimming.md` に `py_join` が fixed-ABI helper lane で移行済みであることを追記した。これにより `P1-CPP-PY-RUNTIME-SLIM-01` は `str.join` を blocker とせず進められる状態になった。

### P1: C++ `py_runtime` を低レベル glue へ縮退させる

文脈: [docs/ja/plans/archive/20260308-p1-cpp-py-runtime-core-slimming.md](../plans/archive/20260308-p1-cpp-py-runtime-core-slimming.md)

1. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01] `native/core/py_runtime.h` から pure Python で表現可能な built_in semantics を分離し、`core` を low-level ABI / object / container / process glue 中心へ縮退させる。
2. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S1-01] `native/core/py_runtime.h` の function/class/helper を棚卸しし、`native/core` / `generated/core` / `generated/built_in` / `native/built_in` / 保留へ分類する。
3. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S1-02] `spec-runtime` / `spec-dev` に `py_runtime` の責務境界と「残してよいもの / 戻すべきもの」を明文化する。
4. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S2-01] `src/pytra/built_in/*.py` 側へ戻す候補を決め、SoT 上の配置案を固定する。
5. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S2-02] `generated/core` または `generated/built_in` の emission lane に必要な generator / layout 契約を整備する。
6. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-01] 文字列・collection 系の pure-Python built_in semantics を `native/core/py_runtime.h` から段階的に撤去し、正規の generated lane へ移す。
7. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-02] `native/core/py_runtime.h` を low-level ABI / object / container / process glue 中心へ整理し、include 集約を最小化する。
8. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-01] runtime symbol index / build graph / representative C++ runtime tests を新しい ownership に追従させる。
9. [x] [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-02] fixture/sample parity・docs 同期・必要な guard 追加まで完了し、本計画を閉じる。
- 進捗メモ: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-01] `src/pytra/built_in/string_ops.py` に `py_split` / `py_splitlines` / `py_count` を追加し、`str::split` / `splitlines` / `count` / `join` の実体を `native/core/py_runtime.h` から generated helper delegate へ寄せた。`generated/built_in/string_ops.{h,cpp}` を再生成し、`py_range` には `@abi(ret="value")` を付与して既存 `list<int64>` helper ABI も維持した。
- 進捗メモ: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S3-02] C++ emitter が `py_repeat` / `py_range` / `py_any` / `py_all` / `reversed` / `enumerate` を使う module に direct built_in header を積むようにし、`native/core/py_runtime.h` から `sequence` / `iter_ops` / `predicates` の transitive include を撤去した。`generated/std/json.cpp` も direct include へ再生成し、`py_runtime.h` は low-level glue + `str` method delegate を中心とする集約へ整理した。
- 進捗メモ: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-01] runtime symbol index / build graph / representative C++ integration test を ownership 変更へ追従させた。`test_runtime_symbol_index.py` で `string_ops` / `sequence` の public header・compile source を固定し、`test_cpp_runtime_build_graph.py` で `generated/std/json.cpp` の direct built_in include と `core/py_runtime.h` seed の非再侵入を回帰化した。
- 進捗メモ: [ID: P1-CPP-PY-RUNTIME-SLIM-01-S4-02] `tools/check_runtime_cpp_layout.py` に removed transitive include (`predicates` / `sequence` / `iter_ops`) の再侵入 guard を追加し、`test_check_runtime_cpp_layout.py` と spec を同期した。さらに `test_cpp_runtime_iterable.py` / `test_pytra_built_in_string_ops.py` / layout guard / marker guard / fixture parity (`cases=3 pass=3 fail=0`) / sample parity (`cases=18 pass=18 fail=0`) を再実行して本計画を閉じた。

### P1: 全target sample parity 完了

文脈: [docs/ja/plans/archive/20260308-p1-all-target-sample-parity-rollout.md](../plans/archive/20260308-p1-all-target-sample-parity-rollout.md)

1. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01] parity target 全体について sample parity を `toolchain_missing` なしで完走できる状態にし、全target green の実行基準を固定する。
2. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S1-01] parity target 全体の `runner_needs` と current `toolchain_missing` を棚卸しし、target ごとの不足 toolchain を matrix 化する。
3. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S1-02] 「全target parity green」の done 条件、許容しない failure category、確認コマンドを spec/plan に固定する。
4. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S2-01] compiled target 群（`rs/cs/go/java/kotlin/swift/scala`）の toolchain bootstrap 手順を整備し、`toolchain_missing` を解消する。
5. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S2-02] scripting / mixed target 群（`ruby/lua/php/nim`）の toolchain bootstrap 手順を整備し、`toolchain_missing` を解消する。
6. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-01] baseline target（`cpp/js/ts`）の sample parity を再確認し、他 target 修復中も `18/18` を維持する。
7. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-02] compiled target 群（`rs/cs/go/java/kotlin/swift/scala`）の sample parity を green へ持ち上げる。
8. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-03] scripting / mixed target 群（`ruby/lua/php/nim`）の sample parity を green へ持ち上げる。
9. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-01] 全 target parity 一括実行の scripts / docs / how-to-use を整備し、再実行手順を固定する。
10. [x] [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-02] full parity 実行結果を記録し、計画を archive へ移して閉じる。
- 進捗メモ: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-02] Debian 12 current machine に compiled target toolchain を揃えたあと、Rust の canonical runtime import collision を修正し、`rs/cs/go/java/kotlin/swift/scala` を target 個別 parity でそれぞれ `SUMMARY cases=18 pass=18 fail=0` へ持ち上げた。Scala は `scala run ...` runner 契約に合わせて Scala CLI を `/usr/local/bin/scala` へ切り替えた。
- 進捗メモ: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S3-03] `ruby/lua/php/nim` も target 個別 parity を再実行し、4 target すべて `SUMMARY cases=18 pass=18 fail=0` を確認した。これで `toolchain_missing` と runtime/backend 差分は current machine 上で解消された。
- 進捗メモ: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-01] `tools/check_all_target_sample_parity.py` を追加し、`cpp` / `js_ts` / `compiled` / `scripting_mixed` の 4 group を順に回す canonical wrapper を固定した。`spec-tools` / `how-to-use` も wrapper 前提の再実行手順へ同期した。
- 進捗メモ: [ID: P1-ALLTARGET-SAMPLE-PARITY-01-S4-02] full parity result は canonical subgroup の総和で記録した。baseline `cpp/js/ts` は `SUMMARY cases=18 pass=18 fail=0 targets=cpp,js,ts`、compiled 7 target と scripting/mixed 4 target は各 target 個別実行で `18/18` を確認しており、14 target 全体で sample parity green を達成した。

### P4: linked-program 後の非C++ backend 修復

文脈: [docs/ja/plans/archive/20260308-p4-noncpp-backend-recovery-after-linked-program.md](../plans/archive/20260308-p4-noncpp-backend-recovery-after-linked-program.md)

1. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01] linked-program 導入後の non-C++ backend を `SingleFileProgramWriter` 前提の共通契約へ順次追従させ、broken state を family 単位で解消する。
2. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S1-01] linked-program 後の non-C++ backend health matrix を作成し、各 target を failure category ごとに分類する。
3. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S1-02] done 条件（static/smoke/transpile/parity/toolchain missing の扱い）と修復順序を spec/plan に固定する。
4. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S2-01] `backend_registry.py` / `py2x.py` / `ir2lang.py` の non-C++ 互換層を点検し、`SingleFileProgramWriter` 前提の backend 共通契約不足を埋める。
5. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S2-02] non-C++ backend health checker を追加または既存 checker を統合し、family 単位の broken/green を 1 コマンドで見られるようにする。
6. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S3-01] Wave 1（`rs/cs/js/ts`）の static contract / smoke / transpile failure を解消し、compat route を安定化する。
7. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S3-02] Wave 1 の parity baseline を更新し、runtime 差分と infra failure を分離する。
8. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-01] Wave 2（`go/java/kotlin/swift/scala`）の static contract / smoke / transpile failure を解消する。
9. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-02] Wave 2 の parity baseline を更新し、`toolchain missing` / 実行 failure / artifact 差分を固定化する。
10. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-01] Wave 3（`ruby/lua/php/nim`）の static contract / smoke / transpile failure を解消する。
11. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-02] Wave 3 の parity baseline を更新し、runtime 差分と backend bug を切り分ける。
12. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-01] `run_local_ci.py` または同等の回帰導線へ non-C++ backend health check を統合する。
13. [x] [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-02] `docs/ja/spec` / `docs/en/spec` / `docs/ja/how-to-use.md` を更新し、linked-program 後の non-C++ backend 修復運用を固定して計画を閉じる。
- 進捗メモ: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-01] Wave 2 の primary failure は `Path(...)` ctor の source-origin call metadata 欠落と `kotlin/swift` の `src.*` import に収束していた。`go/java/scala` には bare `Path(...)` ctor fallback を足し、`java` の `super` 保持と `kotlin/swift` の import path 修正まで入れて smoke/transpile green を確認した。
- 進捗メモ: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S4-02] Wave 2 parity は `go/java/kotlin/swift/scala` の sample 18 case 全件が `toolchain_missing` だった。backend bug ではなく infra baseline だけが残っている状態へ切り分けた。
- 進捗メモ: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-01] Wave 3 は `ruby/php/nim` の `src.*` import 事故と、`lua` smoke expectation の古さが原因だった。前者は `toolchain.*` import へ揃え、後者は `_G.math.max` と truthiness-preserving ifexp closure に expectation を同期して green 化した。
- 進捗メモ: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S5-02] Wave 3 parity も `ruby/lua/php/nim` の sample 18 case 全件が `toolchain_missing` で揃った。Wave 1-3 すべてで backend bug と infra baseline を分離できた。
- 進捗メモ: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-01] `tools/run_local_ci.py` に `python3 tools/check_noncpp_backend_health.py --family all --skip-parity` を追加し、検証として `wave1/wave2/wave3` を個別実行して全 family `status=green` を確認した。
- 進捗メモ: [ID: P4-NONCPP-BACKEND-RECOVERY-01-S6-02] `docs/ja/spec/spec-tools.md` / `docs/en/spec/spec-tools.md` / `docs/ja/how-to-use.md` を linked-program 後の non-C++ backend 運用へ同期し、本計画を archive へ移した。
